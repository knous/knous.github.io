<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>LeetCode1475</title>
    <url>/2022/09/01/LeetCode1475/</url>
    <content><![CDATA[<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011043424.png" alt="image-20220901104300494"></p>
<p>这题有多种解法，这里演示的是其中最为简单易懂的那种：双循环</p>
<p>首先我们拿到的是 i 位置的数据，然后直接在 i+1 后面找比 i 小的数据，然后替换掉当前位置的 i ，同时跳出这次循环</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">finalPrices</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; prices)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>;i&lt;prices.<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j = i+<span class="number">1</span>;j&lt;prices.<span class="built_in">size</span>();j++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span>(prices[j]&lt;=prices[i])</span><br><span class="line">                &#123;</span><br><span class="line">                    prices[i] = prices[i]-prices[j];</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> prices;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>时间复杂度：O（N^2）</p>
<p>空间复杂度：O（1）</p>
]]></content>
      <categories>
        <category>每日练习</category>
      </categories>
  </entry>
  <entry>
    <title>LeetCode946</title>
    <url>/2022/09/01/LeetCode946/</url>
    <content><![CDATA[<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011050702.png" alt="image-20220901105042844"></p>
<p>验证栈序列，我们只需要模拟栈就可以了，我们先插入一个数据，然后判断这个数据跟要验证的是否相同，如果相同，我们和把顶端的出栈，然后验证下一个，最后判断栈是不是空的就行。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">validateStackSequences</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; pushed, vector&lt;<span class="type">int</span>&gt;&amp; popped)</span> </span>&#123;</span><br><span class="line">        stack&lt;<span class="type">int</span>&gt; st;</span><br><span class="line">        <span class="type">int</span> v = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>;i&lt;pushed.<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">                st.<span class="built_in">push</span>(pushed[i]);</span><br><span class="line">                <span class="keyword">while</span>(!st.<span class="built_in">empty</span>() &amp;&amp; st.<span class="built_in">top</span>() == popped[v])</span><br><span class="line">                &#123;</span><br><span class="line">                    v++;</span><br><span class="line">                    st.<span class="built_in">pop</span>();</span><br><span class="line">                &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> st.<span class="built_in">empty</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>每日练习</category>
      </categories>
  </entry>
  <entry>
    <title>avl</title>
    <url>/2022/08/02/avl/</url>
    <content><![CDATA[<hr>
<h1 id="AVL树的概念"><a href="#AVL树的概念" class="headerlink" title="AVL树的概念"></a>AVL树的概念</h1><blockquote>
<p>二叉搜索树虽可以缩短查找的效率，但如果数据有序或接近有序二叉搜索树将退化为单支树，查找元素相当于在顺序表中搜索元素，效率低下。因此，两位俄罗斯的数学家G.M.Adelson-Velskii和E.M.Landis在1962年发明了一种解决上述问题的方法：当向二叉搜索树中插入新结点后，如果能保证每个结点的左右子树高度之差的绝对值不超过1(需要对树中的结点进行调整)，即可降低树的高度，从而减少平均搜索长度。</p>
</blockquote>
<p> 简单来说，就是一颗二叉搜索树，这颗树每一个节点的左右子树高度差的绝对值都不能超过1且都是AVL树</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159342.png" alt="img"></p>
<p>这样结构的好处就是，我们搜索起来它的时间复杂度是O（logN）</p>
<h1 id="节点"><a href="#节点" class="headerlink" title="节点"></a>节点</h1><p>AVL树与之前碰见的二叉树不一样，它节点是这样的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line">struct AVLTreeNode</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	AVLTreeNode&lt;T&gt;* _parent; //父亲节点</span><br><span class="line">	AVLTreeNode&lt;T&gt;* _left;	 //左孩子</span><br><span class="line">	AVLTreeNode&lt;T&gt;* _right;	 //右孩子</span><br><span class="line">	T _val;					 //值</span><br><span class="line"></span><br><span class="line">	int bf;					 //平衡因子</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>它多了一个父节点，同时多了一个平衡因子，这个平衡因子就是来帮助我们之后调整二叉树的。</p>
<p>快速写一个构造函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">AVLTreeNode(const T&amp; val = T())</span><br><span class="line">	:_val(val)</span><br><span class="line">	, _parent(nullptr)</span><br><span class="line">	, _left(nullptr)</span><br><span class="line">	, _right(nullptr)</span><br><span class="line">	, _bf(0)</span><br><span class="line">&#123;&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>不需要什么参数，就传过来一个值即可，如果没传，就调用这里的缺省值。</p>
<h1 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h1><p>其实插入跟之前二叉搜索树是一样的</p>
<p><a class="link"   href="https://blog.csdn.net/qq_60306111/article/details/125993690?spm=1001.2014.3001.5501" >【C++】二叉搜索树_Knous的博客-CSDN博客<i class="fas fa-external-link-alt"></i></a></p>
<p>但是不同的地方在于，我们插入之后要更新我们的平衡因子，不是更新插入节点的，而是更新父亲节点的。</p>
<p>这里看图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159492.png" alt="img"></p>
<p> 这里就要提一下，平衡因子是怎么算的：</p>
<p>如果左子树插入一个节点，那父亲的平衡因子就-1</p>
<p>如果右子树插入一个节点，那父亲的平衡因子就+1</p>
<p>按照上面逻辑，现在算一下插入之前的平衡因子：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159065.png" alt="img"></p>
<p>现在插入一个：</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159361.png" alt="img"></p>
<p>此时，这棵树就很平衡，当然，也可以在其他地方插入，那就会不平衡，这里我们先写一个插入，不平衡的情况之后分析。</p>
<p>先写一个模板，这个模板里面只有一个参数，就是根节点</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line">class AVLTree</span><br><span class="line">&#123;</span><br><span class="line">	typedef AVLTreeNode&lt;T&gt; Node;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line">	Node* _root;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h2 id="插入-1"><a href="#插入-1" class="headerlink" title="插入"></a>插入</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool insert(const T&amp; val)</span><br><span class="line">&#123;</span><br><span class="line">	if (_root == nullptr)</span><br><span class="line">	&#123;</span><br><span class="line">		_root = new Node(val);</span><br><span class="line">		return true;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		//插入</span><br><span class="line">		Node* cur = _root;</span><br><span class="line">		Node* parent = cur-&gt;_parent;</span><br><span class="line">		while (cur)</span><br><span class="line">		&#123;</span><br><span class="line">			if (cur-&gt;_val &gt; val)</span><br><span class="line">			&#123;</span><br><span class="line">				parent = cur;</span><br><span class="line">				cur = cur-&gt;_left;</span><br><span class="line">			&#125;</span><br><span class="line">			else if (cur-&gt;_val &lt; val)</span><br><span class="line">			&#123;</span><br><span class="line">				parent = cur;</span><br><span class="line">				cur = cur-&gt;_right;</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">				assert(false);</span><br><span class="line">		&#125;</span><br><span class="line">		cur = new Node(val);</span><br><span class="line">		if (cur == parent-&gt;_left)</span><br><span class="line">			parent-&gt;_left = cur;</span><br><span class="line">		else</span><br><span class="line">			parent-&gt;_right = cur;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h3 id="调整"><a href="#调整" class="headerlink" title="调整"></a>调整</h3><p>我们插入完后，要更新父亲节点的平衡因子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//更新平衡因子</span><br><span class="line">while (parent)</span><br><span class="line">&#123;</span><br><span class="line">	if (cur == parent-&gt;_left)</span><br><span class="line">		parent-&gt;_bf--;</span><br><span class="line">	else</span><br><span class="line">		parent-&gt;_bf++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>更新完了，就该判断是否平衡了</p>
<p>这里简单看几个图分析情况：</p>
<p>第一种情况：</p>
<h3 id="右单旋"><a href="#右单旋" class="headerlink" title="右单旋"></a>右单旋</h3><p>假设此时已经左边高了，而我们又在左边插入了一个元素，此时，就变成了这样的情况，为了方便后续操作，将这个三个节点分别称之为：A&#x2F;B&#x2F;C </p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159118.png" alt="img"></p>
<p>将最高的那个节点拿下来，将其最为第二高的右节点</p>
<p>假设情况在复杂一点：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159798.png" alt="img"></p>
<p>我们看一下插入前后的平衡因子：</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159020.png" alt="img"></p>
<p>当前节点为-1且父亲节点为-2，这就要右旋转了,为了之后方便写代码，这里将节点重新取名</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159023.png" alt="img"></p>
<p>这样，高度不仅没变，还保持了二叉搜索树的条件，为了方便阅读，这里给节点单独取名</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void _RotateR(Node* parent)</span><br><span class="line">&#123;</span><br><span class="line">	Node* subL = parent-&gt;_left;</span><br><span class="line">	Node* subLR = subR-&gt;_right;</span><br><span class="line"></span><br><span class="line">	Node* pparent = parent-&gt;_parent;</span><br><span class="line"></span><br><span class="line">	if (subLR)</span><br><span class="line">		subLR-&gt;_parent = parent;</span><br><span class="line"></span><br><span class="line">	subL-&gt;_right = parent;</span><br><span class="line">	parent-&gt;_left = subLR;</span><br><span class="line"></span><br><span class="line">	if (parent == _root)</span><br><span class="line">	&#123;</span><br><span class="line">		_root = subL;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		if (parent == pparent-&gt;_left)</span><br><span class="line">			pparent-&gt;_left = subL;</span><br><span class="line">		else</span><br><span class="line">			pparent-&gt;_right = subL;</span><br><span class="line">		subL-&gt;_parent = pparent;</span><br><span class="line">	&#125;</span><br><span class="line">	parent-&gt;_bf = subL-&gt;_bf = 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>这里可以对照着图理解一下，因为最后旋转完高度应该是一样的，所以平衡因子应该重置为0</p>
<p>-–</p>
<h3 id="左单旋"><a href="#左单旋" class="headerlink" title="左单旋"></a>左单旋</h3><p>这种完全是跟右单旋反过来，它是这样的图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159208.png" alt="img"></p>
<p>具体思路跟右单旋相反：</p>
<p><img src="https://img-blog.csdnimg.cn/85f0942073bb47558fd03611306c03bf.png" alt="img"><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动">编辑 具体思路差不多，看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void _RotateL(Node* parent)</span><br><span class="line">&#123;</span><br><span class="line">	Node* subR = parent-&gt;_right;</span><br><span class="line">	Node* subRL = subR-&gt;_left;</span><br><span class="line"></span><br><span class="line">	if (subRL)</span><br><span class="line">		subRL-&gt;_parent = parent;</span><br><span class="line"></span><br><span class="line">	Node* pparent = parent-&gt;_parent;</span><br><span class="line">	subR-&gt;_left = parent;</span><br><span class="line">	parent-&gt;_right = subRL;</span><br><span class="line">	parent-&gt;_parent = subR;</span><br><span class="line"></span><br><span class="line">	if (parent == _root)</span><br><span class="line">		_root = subR;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		if (pparent-&gt;_left == parent)</span><br><span class="line">			pparent-&gt;_left = subR;</span><br><span class="line">		else</span><br><span class="line">			pparent-&gt;_right = subR;</span><br><span class="line">	&#125;</span><br><span class="line">	s</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>写完了先来测试一下效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159911.png" alt="img"></p>
<p>这是一个左单旋，2的左边是1，右边是3，调整没问题。</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201159821.png" alt="img"></p>
<p> 右单选，也没问题</p>
<p>-–</p>
<h3 id="左右双旋"><a href="#左右双旋" class="headerlink" title="左右双旋"></a>左右双旋</h3><p>有时，就会出现这种情况，A B C不都在一边，对于A来说，B在它的左边，对于B来说，C在它的右边，此时就不是单纯的左旋转或者右旋转能解决问题的了。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201200940.png" alt="img"></p>
<p> 此时，无论在E、F哪边插入，都会失去平衡，面对这种情况，我们就要考虑双旋，转两次来解决问题</p>
<p>这时，我们就要先左旋，再右旋：</p>
<p>左旋：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201200530.png" alt="img"><img src="/" alt="点击并拖拽以移动"></p>
<p>再右旋；</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201200830.png" alt="img"></p>
<p>此时，就完成了对称调整</p>
<p>这里要注意的一点是，在调整之前C的平衡因子是多少，因为会有以下几种情况：</p>
<p>假设C调整之前是-1，说明在E插入，即上图调整</p>
<p>假设C调整之前是1，说明在F插入，即下图调整</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201200343.png" alt="img"></p>
<p>根据调整之前不同，AB的值也不同</p>
<p>如果是-1：A&#x3D; -1，B&#x3D;C&#x3D;0</p>
<p>如果是1：B&#x3D;1,A&#x3D;C&#x3D;0</p>
<p>这里复用之前写好的旋转逻辑即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void _RotateLR(Node* parent)</span><br><span class="line">&#123;</span><br><span class="line">	Node* subL = parent-&gt;_left;//B</span><br><span class="line">	Node* subLR = subL-&gt;_right;//C</span><br><span class="line">	int bf = subLR-&gt;_bf;</span><br><span class="line">	_RotateL(parent-&gt;_left);</span><br><span class="line">	_RotateR(parent);</span><br><span class="line"></span><br><span class="line">	if (bf == 1)</span><br><span class="line">		subL-&gt;_bf = -1;</span><br><span class="line">	else if (bf == -1)</span><br><span class="line">		parent-&gt;_bf = 1;</span><br><span class="line">	else</span><br><span class="line">		assert(false);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h3 id="右左双旋"><a href="#右左双旋" class="headerlink" title="右左双旋"></a>右左双旋</h3><p>大体可以参考左右双旋，思路也是一样的</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201200257.png" alt="img"></p>
<p> 几乎是镜像左右旋，同样根据C的bf值有不同的结果，最后调整一下即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void _RotateRL(Node* parent)</span><br><span class="line">&#123;</span><br><span class="line">	Node* subR = parent-&gt;_right;</span><br><span class="line">	Node* subRL = subR-&gt;_left;</span><br><span class="line">	int bf = subRL-&gt;_bf;</span><br><span class="line">	_RotateR(parent-&gt;_right);</span><br><span class="line">	_RotateL(parent);</span><br><span class="line"></span><br><span class="line">	if (bf == -1)</span><br><span class="line">		subR-&gt;_bf = 1;</span><br><span class="line">	else if (bf == 1)</span><br><span class="line">		parent-&gt;_bf = -1;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>删除之后也许会更新，了解插入的旋转可以帮助我们理解二叉搜索树的结构了</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>list</title>
    <url>/2022/07/16/list/</url>
    <content><![CDATA[<hr>
<h1 id="list"><a href="#list" class="headerlink" title="list"></a>list</h1><h1 id="list的介绍及使用"><a href="#list的介绍及使用" class="headerlink" title="list的介绍及使用"></a>list的介绍及使用</h1><h2 id="list的介绍"><a href="#list的介绍" class="headerlink" title="list的介绍"></a>list的介绍</h2><p><a class="link"   href="https://m.cplusplus.com/reference/list/list/?kw=list"  title="list - C++ Reference (cplusplus.com)">list - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>list的详细介绍可以看上面的网站，这里只说一下重点地方</p>
<blockquote>
<ol>
<li>list是可以在常数范围内在任意位置进行插入和删除的序列式容器，并且该容器可以前后双向迭代。</li>
<li>list的底层是双向链表结构，双向链表中每个元素存储在互不相关的独立节点中，在节点中通过指针指其前一个元素和后一个元素。</li>
<li>list与forward_list非常相似：最主要的不同在于forward_list是单链表，只能朝前迭代，已让其更简单高效。</li>
<li>与其他的序列式容器相比(array，vector，deque)，list通常在任意位置进行插入、移除元素的执行效率更好。</li>
<li>与其他序列式容器相比，list和forward_list最大的缺陷是不支持任意位置的随机访问，比如：要访问list的第6个元素，必须从已知的位置(比如头部或者尾部)迭代到该位置，在这段位置上迭代需要线性的时间开销；list还需要一些额外的空间，以保存每个节点的相关联信息(对于存储类型较小元素的大list来说这可能是一个重要的因素)</li>
</ol>
</blockquote>
<p>（ 这里如果看的不是很明白，可以去看我之前写过的链表的文章，里面先从单链表介绍，后面就介绍了带头的双向循环链表）</p>
<hr>
<h2 id="list的使用"><a href="#list的使用" class="headerlink" title="list的使用"></a>list的使用</h2><table>
<thead>
<tr>
<th>构造函数（ (constructor)）</th>
<th>接口说明</th>
</tr>
</thead>
<tbody><tr>
<td>list()</td>
<td>构造空的list</td>
</tr>
<tr>
<td>list (size_type n, const value_type&amp; val &#x3D; value_type())</td>
<td>构造的list中包含n个值为val的元素</td>
</tr>
<tr>
<td>list (const list&amp; x)</td>
<td>拷贝构造函数</td>
</tr>
<tr>
<td>list (InputIterator first, InputIterator last)</td>
<td>用[first, last)区间中的元素构造list</td>
</tr>
</tbody></table>
<p>经典的几个构造：</p>
<p>无参</p>
<p>开辟n个空间并初始化为val，如果没有val的值，默认为0</p>
<p>用一个list初始化另一个list</p>
<p>用一个迭代器区间初始化另一个list</p>
<p>具体可以看下面</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201145231.png"></p>
<hr>
<h2 id="list-iterator的使用"><a href="#list-iterator的使用" class="headerlink" title="list iterator的使用"></a>list iterator的使用</h2><table>
<thead>
<tr>
<th>函数声明</th>
<th>接口说明</th>
</tr>
</thead>
<tbody><tr>
<td>begin +</td>
<td></td>
</tr>
<tr>
<td>end</td>
<td>返回第一个元素的迭代器+返回最后一个元素下一个位置的迭代器</td>
</tr>
<tr>
<td>rbegin +</td>
<td></td>
</tr>
<tr>
<td>rend</td>
<td>返回第一个元素的reverse_iterator,即end位置，返回最后一个元素下一个位置的</td>
</tr>
<tr>
<td>reverse_iterator,即begin位置</td>
<td></td>
</tr>
</tbody></table>
<p>这个其实跟vector的差不了多少，暂且可以理解为是一个指针指向list的某一个节点，这里的演示可以看上面的l3，遍历就是用的begin和end，同时范围for（auto）的底层实际上也是调用的迭代器</p>
<h2 id="list-capacity"><a href="#list-capacity" class="headerlink" title="list capacity"></a>list capacity</h2><table>
<thead>
<tr>
<th>函数声明</th>
<th>接口说明</th>
</tr>
</thead>
<tbody><tr>
<td>empty</td>
<td>检测list是否为空，是返回true，否则返回false</td>
</tr>
<tr>
<td>size</td>
<td>返回list中有效节点的个数</td>
</tr>
</tbody></table>
<h2 id="list-element-access"><a href="#list-element-access" class="headerlink" title="list element access"></a>list element access</h2><table>
<thead>
<tr>
<th>函数声明</th>
<th>接口说明</th>
</tr>
</thead>
<tbody><tr>
<td>front</td>
<td>返回list的第一个节点中值的引用</td>
</tr>
<tr>
<td>back</td>
<td>返回list的最后一个节点中值的引用</td>
</tr>
</tbody></table>
<h2 id="list-modifiers"><a href="#list-modifiers" class="headerlink" title="list modifiers"></a>list modifiers</h2><table>
<thead>
<tr>
<th>函数声明</th>
<th>接口说明</th>
</tr>
</thead>
<tbody><tr>
<td>push_front</td>
<td>在list首元素前插入值为val的元素</td>
</tr>
<tr>
<td>pop_front</td>
<td>删除list中第一个元素</td>
</tr>
<tr>
<td>push_back</td>
<td>在list尾部插入值为val的元素</td>
</tr>
<tr>
<td>pop_back</td>
<td>删除list中最后一个元素</td>
</tr>
<tr>
<td>insert</td>
<td>在list position 位置中插入值为val的元素</td>
</tr>
<tr>
<td>erase</td>
<td>删除list position位置的元素</td>
</tr>
<tr>
<td>swap</td>
<td>交换两个list中的元素</td>
</tr>
<tr>
<td>clear</td>
<td>清空list中的有效元素</td>
</tr>
</tbody></table>
<p>基本的函数操作就是这么多，实际上这些如果之前看明白了vector和string的话，这些即使不演示也能想个大概…</p>
<h2 id="list的迭代器失效"><a href="#list的迭代器失效" class="headerlink" title="list的迭代器失效"></a>list的迭代器失效</h2><p>这里说一下，之前的迭代器失效为非是两种情况：</p>
<p>1、迭代器指向错误空间</p>
<p>2、迭代器指向错误数据</p>
<p>但是在list这里迭代器失效只会有一种问题，就是所指向的位置被释放（delete），因为list的底层实际上是一个带头的双向循环链表，所以插入的时候不会引发迭代器失效，只有删除的时候才会，同时失效的只有被删除的那个节点的迭代器，其他迭代器是不会失效的</p>
<hr>
<h1 id="模拟实现list"><a href="#模拟实现list" class="headerlink" title="模拟实现list"></a>模拟实现list</h1><p>首先我们定一个命名空间：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">namespace knous</span><br><span class="line">&#123;</span><br><span class="line">	</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>一开始，这里面什么都没有，这里就快速构造一个list，跟vector一样，这里我们要用模板，因为不确定接收的是什么类似的数据：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line">    	struct list_node</span><br><span class="line">	&#123;</span><br><span class="line"></span><br><span class="line">		list_node* next;</span><br><span class="line">		list_node* prev;</span><br><span class="line">		T _data;</span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	template&lt;class T&gt;</span><br><span class="line">	class list</span><br><span class="line">	&#123;</span><br><span class="line">		typedef list_node Node;</span><br><span class="line">	public:</span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line">		Node* _head;</span><br><span class="line">	&#125;;</span><br></pre></td></tr></table></figure>



<p>首先list里面只有一个Node*的指针，这个Node是被重命名过后的，它是list_node的一个结构体，这个结构体就是我们的节点，里面放着上一个节点和下一个节点的地址，还有数据</p>
<p>因为Node是我们自定义类型，所以这里得写一个默认构造，这个构造也很简单，list里面放着的是我们的头节点，头节点在没有数据的时候它的上一个指针和下一个指针都应该指向自己，里面的数据就用默认的构造即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">list()</span><br><span class="line">		&#123;</span><br><span class="line">			_head = new node;</span><br><span class="line">			_head-&gt;next = _head;</span><br><span class="line">			_head-&gt;prev = _head;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>然后来实现与一个尾插：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void push_back(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			Node* newnode = new Node(x);</span><br><span class="line">			Node* tail = _head-&gt;_prev;</span><br><span class="line">			tail-&gt;_next = newnode;</span><br><span class="line">			newnode-&gt;_prev = tail;</span><br><span class="line">			newnode-&gt;_next = _head;</span><br><span class="line">			_head-&gt;_prev = newnode;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这里用了Node的构造函数，所以这里要写一下Node（list_node）的默认构造：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">list_node(const T&amp; val = T())</span><br><span class="line">			:_next(nullptr)</span><br><span class="line">			, _prev(nullptr)</span><br><span class="line">			, _data(val)</span><br><span class="line">		&#123;&#125;</span><br></pre></td></tr></table></figure>



<p>这里给了一个缺省值，这个是调用T类型的默认构造，假设是int，那int的默认构造就是0</p>
<p>然后我们既然已经可以插入数据了，那我们是不是就要能够打印出来看看？</p>
<p>如何打印？</p>
<p>我们知道vector的打印可以用下标，可以用迭代器，但是list呢？</p>
<p>vector的底层实际上是一块连续的空间，是一块数组，所以它能直接++、–，list的底层是带头的双向循环链表，里面的成员是某一个节点的指针，我们拿到这个指针所指向的地址，可以直接++吗？</p>
<p>不能，但是我们有没有办法可以让它能直接++呢？</p>
<p>可以，我们重新定义一个类出来，这个类的名字就叫iterator</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line">	class __list_iteartor</span><br><span class="line">	&#123;</span><br><span class="line">		typedef list_node&lt;T&gt; Node;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		Node* _node;</span><br><span class="line">	&#125;;</span><br></pre></td></tr></table></figure>



<p>这个类里面也只有一个参数，就是Node*的指针，它的本质其实跟list一样，都是指向某一个节点的指针，但是我们可以通过对它的重载，来让它具备++、–的功能，所以我们先写一个构造出来</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__list_iteartor(Node* Node)</span><br><span class="line">			:_node(Node)</span><br><span class="line">		&#123;&#125;</span><br></pre></td></tr></table></figure>



<p>它的构造需要一个Node*的指针，然后我们直接赋值上去即可</p>
<p>然后我们写一个begin和end函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">iterator begin()</span><br><span class="line">   		&#123;</span><br><span class="line">			return iterator(_head-&gt;_next);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator end()</span><br><span class="line">		&#123;</span><br><span class="line">			return iterator(_head);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>写好了如上迭代器，我们还要解决的一个问题是，迭代器本身是一个指针，我们要通过<em>解引用来获得这里的数据，但是iterator里面也是一个指针，指针指向的位置是一个节点的地址，我们要的不是地址，而是那个地址的数据，所以这里我们要重载一下</em>运算符</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">T operator*()</span><br><span class="line">		&#123;</span><br><span class="line">			return _node-&gt;_data;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>while的判断条件的！&#x3D;，所以重载！&#x3D;运算符</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool operator!=(const self&amp; it)</span><br><span class="line">		&#123;</span><br><span class="line">			return _node != it._node;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>打印了这里的数据完了我们还要让it指向下一个位置，所以重载++运算符</p>
<p>因为每次都写__list_iteartor<T>实在过于麻烦，这里重命名为self</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">self operator++(int)</span><br><span class="line">		&#123;</span><br><span class="line">			_node = _node-&gt;_next;</span><br><span class="line">			return _node;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>因为是后置++，所以在接收里面要加上一个int，这里既然后置++都写了，那就再写一个前置++，前置–和后置–</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">self operator++()</span><br><span class="line">		&#123;</span><br><span class="line">			Node* tmp(*this);</span><br><span class="line">			_node = _node-&gt;_next;</span><br><span class="line">			return tmp;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		self operator--(int)</span><br><span class="line">		&#123;</span><br><span class="line">			_node = _node-&gt;_prev;</span><br><span class="line">			return _node;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		self operator--()</span><br><span class="line">		&#123;</span><br><span class="line">			Node* tmp(*this);</span><br><span class="line">			_node = _node-&gt;_prev;</span><br><span class="line">			return tmp;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>至此，看一下效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201146630.png"></p>
<p>没有问题</p>
<p>既然尾插有了，那我们把剩下几个也写一下：</p>
<p>尾删</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void pop_back()</span><br><span class="line">		&#123;</span><br><span class="line">			Node* tail = _head-&gt;_prev;</span><br><span class="line">			_head-&gt;_prev = tail-&gt;_prev;</span><br><span class="line">			tail-&gt;_prev-&gt;_next = _head;</span><br><span class="line">			delete tail;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>头插</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void push_front(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			Node* newnode = new Node(x);</span><br><span class="line">			Node* next = _head-&gt;_next;</span><br><span class="line">			_head-&gt;_next = newnode;</span><br><span class="line">			newnode-&gt;_next = next;</span><br><span class="line">			next-&gt;_prev = newnode;</span><br></pre></td></tr></table></figure>



<p>头删</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void pop_front()</span><br><span class="line">		&#123;</span><br><span class="line">			Node* next = _head-&gt;_next;</span><br><span class="line">			_head-&gt;_next = next-&gt;_next;</span><br><span class="line">			next-&gt;_next-&gt;_prev = _head;</span><br><span class="line">			delete next;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>至此，再看一遍前面写的效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201146729.png"></p>
<p>写到这里基本也就差不多了，最后再写中间插入删除就可以结束了，至于size和capacity，这里大家可以自己尝试写一下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">iterator insert(iterator pos, const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			Node* newnode = new Node(x);</span><br><span class="line">			Node* cur = pos._node;</span><br><span class="line">			Node* prev = cur-&gt;_prev;</span><br><span class="line"></span><br><span class="line">			prev-&gt;_next = newnode;</span><br><span class="line">			newnode-&gt;_next = cur;</span><br><span class="line">			newnode-&gt;_prev = prev;</span><br><span class="line">			cur-&gt;_prev = newnode;</span><br><span class="line"></span><br><span class="line">			return iterator(newnode);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">iterator erase(iterator pos)</span><br><span class="line">		&#123;</span><br><span class="line">			Node* cur = pos-&gt;_node;</span><br><span class="line">			Node* prev = cur-&gt;_prev;</span><br><span class="line">			Node* next = cur-&gt;_next;</span><br><span class="line"></span><br><span class="line">			prev-&gt;_next = next;</span><br><span class="line">			next-&gt;_prev = prev;</span><br><span class="line">			delete cur;</span><br><span class="line"></span><br><span class="line">			return iterator(next);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>注意这里为什么删除为什么要返回的是下一个节点的地址，是为了防止出现迭代器失效的问题，可以参考上面讲的迭代器的部分，因为失效的只有当前被删除的节点，而之后节点的迭代器是没有问题的，所以返回的是下一个节点的迭代器。</p>
<p>写到这里，我们可以考虑优化一下，头插头删 尾插尾删，是不是都可以复用insert和erase？</p>
<p>然后就写出第二版：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void push_back(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			/*Node* newnode = new Node(x);</span><br><span class="line">			Node* tail = _head-&gt;_prev;</span><br><span class="line">			tail-&gt;_next = newnode;</span><br><span class="line">			newnode-&gt;_prev = tail;</span><br><span class="line">			newnode-&gt;_next = _head;</span><br><span class="line">			_head-&gt;_prev = newnode;*/</span><br><span class="line">			insert(end(), x);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void pop_back()</span><br><span class="line">		&#123;</span><br><span class="line">			/*Node* tail = _head-&gt;_prev;</span><br><span class="line">			_head-&gt;_prev = tail-&gt;_prev;</span><br><span class="line">			tail-&gt;_prev-&gt;_next = _head;</span><br><span class="line">			delete tail;*/</span><br><span class="line">			erase(--end());</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void push_front(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			/*Node* newnode = new Node(x);</span><br><span class="line">			Node* next = _head-&gt;_next;</span><br><span class="line">			_head-&gt;_next = newnode;</span><br><span class="line">			newnode-&gt;_next = next;</span><br><span class="line">			next-&gt;_prev = newnode;*/</span><br><span class="line">			insert(begin(), x);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void pop_front()</span><br><span class="line">		&#123;</span><br><span class="line">			/*Node* next = _head-&gt;_next;</span><br><span class="line">			_head-&gt;_next = next-&gt;_next;</span><br><span class="line">			next-&gt;_next-&gt;_prev = _head;</span><br><span class="line">			delete next;*/</span><br><span class="line"></span><br><span class="line">			erase(begin());</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>结束了吗？</p>
<p>没有，这里还要考虑一个其他问题，就是解引用的部分，因为解引用会有两种情况：</p>
<p>第一种：我只读，打印</p>
<p>第二种：我不仅要读末尾还要修改</p>
<p>所以这里就会出现一个问题，const的迭代器怎么处理？</p>
<p>为了const迭代器专门写一个类也过于繁琐不堪，所以这里就有一个写法是这样的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef __list_iteartor&lt;T,T&amp;,T*&gt; iterator;</span><br><span class="line">		typedef __list_iteartor&lt;T, const T&amp;, const T*&gt; const_iterator;</span><br></pre></td></tr></table></figure>



<p>两个模板，第一个模板是普通类型，传过去的是引用和别名，第二个模板是加了const的，传过去的就是加了const修改的版本</p>
<p>所以最后再将list完善一下功能：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const_iterator begin() const</span><br><span class="line">		&#123;</span><br><span class="line">			return const_iterator(_head-&gt;_next);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const_iterator end() const</span><br><span class="line">		&#123;</span><br><span class="line">			return const_iterator(_head);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>然后构造的时候也有用一个区间构造另一个，&#x3D;号的重载，这里就一起写了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void empty_Init()</span><br><span class="line">		&#123;</span><br><span class="line">			_head = new Node();</span><br><span class="line">			_head-&gt;_next = _head;</span><br><span class="line">			_head-&gt;_prev = _head;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		template&lt;class InputIterator&gt;</span><br><span class="line">		list(InputIterator first, InputIterator last)</span><br><span class="line">		&#123;</span><br><span class="line">			empty_Init();</span><br><span class="line">			while (first != last)</span><br><span class="line">			&#123;</span><br><span class="line">				push_back(*first);</span><br><span class="line">				++first;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>因为迭代器区间是一个不肯定类型，所以要用模板，这里在用区间插入的时候最好先初始化一下，所以这里调用了一个空初始化的函数，跟无参构造的时候是一样的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void swap(list&lt;T&gt; it)</span><br><span class="line">		&#123;</span><br><span class="line">			std::swap(it._head, _head);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		list(const T&amp; it)</span><br><span class="line">		&#123;</span><br><span class="line">			empty_Init();</span><br><span class="line">			list&lt;T&gt; tmp(it.begin(), it.end());</span><br><span class="line">			swap(tmp);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		list&lt;T&gt;&amp; operator=(list&lt;T&gt; it)</span><br><span class="line">		&#123;</span><br><span class="line">			swap(it);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这里用的是现代写法，因为带头链表的交换拷贝构造完了只需要交换头节点，因为形参是实参的临时拷贝，所以这里直接用形参然后交换，然后因为形参出了作用域就销毁，所以它会自动释放</p>
<p>最后放上全部代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#pragma once</span><br><span class="line">#include&lt;iostream&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">using namespace std;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">namespace knous</span><br><span class="line">&#123;</span><br><span class="line">	template&lt;class T&gt;</span><br><span class="line">	struct list_node</span><br><span class="line">	&#123;</span><br><span class="line">		</span><br><span class="line">		list_node(const T&amp; val = T())</span><br><span class="line">			:_next(nullptr)</span><br><span class="line">			, _prev(nullptr)</span><br><span class="line">			, _data(val)</span><br><span class="line">		&#123;&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		list_node* _next;</span><br><span class="line">		list_node* _prev;</span><br><span class="line">		T _data;</span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line">	template&lt;class T, class Ref,class Ptr &gt;</span><br><span class="line">	struct __list_iteartor</span><br><span class="line">	&#123;</span><br><span class="line">		typedef list_node&lt;T&gt; Node;</span><br><span class="line">		typedef __list_iteartor&lt;T, Ref, Ptr &gt; self;</span><br><span class="line"></span><br><span class="line">		__list_iteartor(Node* Node)</span><br><span class="line">			:_node(Node)</span><br><span class="line">		&#123;&#125;</span><br><span class="line"></span><br><span class="line">		Ref operator*()</span><br><span class="line">		&#123;</span><br><span class="line">			return _node-&gt;_data;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		Ptr operator-&gt;()</span><br><span class="line">		&#123;</span><br><span class="line">			//return &amp;(operator*());</span><br><span class="line">			return &amp;_node-&gt;_data;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		bool operator!=(const self&amp; it)</span><br><span class="line">		&#123;</span><br><span class="line">			return _node != it._node;</span><br><span class="line">		&#125;</span><br><span class="line">		</span><br><span class="line">		self operator++(int)</span><br><span class="line">		&#123;</span><br><span class="line">			_node = _node-&gt;_next;</span><br><span class="line">			return _node;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		self operator++()</span><br><span class="line">		&#123;</span><br><span class="line">			Node* tmp(*this);</span><br><span class="line">			_node = _node-&gt;_next;</span><br><span class="line">			return tmp;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		self operator--(int)</span><br><span class="line">		&#123;</span><br><span class="line">			_node = _node-&gt;_prev;</span><br><span class="line">			return _node;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		self operator--()</span><br><span class="line">		&#123;</span><br><span class="line">			_node = _node-&gt;_prev;</span><br><span class="line">			return _node;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		Node* _node;</span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line">	template&lt;class T&gt;</span><br><span class="line">	class list</span><br><span class="line">	&#123;</span><br><span class="line">		typedef list_node&lt;T&gt; Node;</span><br><span class="line">		typedef __list_iteartor&lt;T,T&amp;,T*&gt; iterator;</span><br><span class="line">		typedef __list_iteartor&lt;T, const T&amp;, const T*&gt; const_iterator;</span><br><span class="line"></span><br><span class="line">	public:</span><br><span class="line"></span><br><span class="line">		list()</span><br><span class="line">		&#123;</span><br><span class="line">			_head = new Node();</span><br><span class="line">			_head-&gt;_next = _head;</span><br><span class="line">			_head-&gt;_prev = _head;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void empty_Init()</span><br><span class="line">		&#123;</span><br><span class="line">			_head = new Node();</span><br><span class="line">			_head-&gt;_next = _head;</span><br><span class="line">			_head-&gt;_prev = _head;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		template&lt;class InputIterator&gt;</span><br><span class="line">		list(InputIterator first, InputIterator last)</span><br><span class="line">		&#123;</span><br><span class="line">			empty_Init();</span><br><span class="line">			while (first != last)</span><br><span class="line">			&#123;</span><br><span class="line">				push_back(*first);</span><br><span class="line">				++first;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void swap(list&lt;T&gt; it)</span><br><span class="line">		&#123;</span><br><span class="line">			std::swap(it._head, _head);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		list(const T&amp; it)</span><br><span class="line">		&#123;</span><br><span class="line">			empty_Init();</span><br><span class="line">			list&lt;T&gt; tmp(it.begin(), it.end());</span><br><span class="line">			swap(tmp);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		list&lt;T&gt;&amp; operator=(list&lt;T&gt; it)</span><br><span class="line">		&#123;</span><br><span class="line">			swap(it);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator begin()</span><br><span class="line">		&#123;</span><br><span class="line">			return iterator(_head-&gt;_next);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_iterator begin() const</span><br><span class="line">		&#123;</span><br><span class="line">			return const_iterator(_head-&gt;_next);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator end()</span><br><span class="line">		&#123;</span><br><span class="line">			return iterator(_head);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_iterator end() const</span><br><span class="line">		&#123;</span><br><span class="line">			return const_iterator(_head);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void push_back(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			/*Node* newnode = new Node(x);</span><br><span class="line">			Node* tail = _head-&gt;_prev;</span><br><span class="line">			tail-&gt;_next = newnode;</span><br><span class="line">			newnode-&gt;_prev = tail;</span><br><span class="line">			newnode-&gt;_next = _head;</span><br><span class="line">			_head-&gt;_prev = newnode;*/</span><br><span class="line">			insert(end(), x);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void pop_back()</span><br><span class="line">		&#123;</span><br><span class="line">			/*Node* tail = _head-&gt;_prev;</span><br><span class="line">			_head-&gt;_prev = tail-&gt;_prev;</span><br><span class="line">			tail-&gt;_prev-&gt;_next = _head;</span><br><span class="line">			delete tail;*/</span><br><span class="line">			erase(--end());</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void push_front(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			/*Node* newnode = new Node(x);</span><br><span class="line">			Node* next = _head-&gt;_next;</span><br><span class="line">			_head-&gt;_next = newnode;</span><br><span class="line">			newnode-&gt;_next = next;</span><br><span class="line">			next-&gt;_prev = newnode;*/</span><br><span class="line">			insert(begin(), x);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void pop_front()</span><br><span class="line">		&#123;</span><br><span class="line">			/*Node* next = _head-&gt;_next;</span><br><span class="line">			_head-&gt;_next = next-&gt;_next;</span><br><span class="line">			next-&gt;_next-&gt;_prev = _head;</span><br><span class="line">			delete next;*/</span><br><span class="line"></span><br><span class="line">			erase(begin());</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator insert(iterator pos, const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			Node* newnode = new Node(x);</span><br><span class="line">			Node* cur = pos._node;</span><br><span class="line">			Node* prev = cur-&gt;_prev;</span><br><span class="line"></span><br><span class="line">			prev-&gt;_next = newnode;</span><br><span class="line">			newnode-&gt;_next = cur;</span><br><span class="line">			newnode-&gt;_prev = prev;</span><br><span class="line">			cur-&gt;_prev = newnode;</span><br><span class="line"></span><br><span class="line">			return iterator(newnode);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator erase(iterator pos)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos != end());</span><br><span class="line">			Node* cur = pos._node;</span><br><span class="line">			Node* prev = cur-&gt;_prev;</span><br><span class="line">			Node* next = cur-&gt;_next;</span><br><span class="line"></span><br><span class="line">			prev-&gt;_next = next;</span><br><span class="line">			next-&gt;_prev = prev;</span><br><span class="line">			delete cur;</span><br><span class="line"></span><br><span class="line">			return iterator(next);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line">		Node* _head;</span><br><span class="line">	&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>至此，list大部分功能都实现完了，希望这篇list看完你对list会有更深的理解</p>
<p>Thanks for watching</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>makefile</title>
    <url>/2022/08/07/makefile/</url>
    <content><![CDATA[<hr>
<p> 这里简单用Linux写第一个程序和用这个程序来帮我们理解make&#x2F;Makefile</p>
<p>-–</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">printf(&quot;%d\n&quot;,xxx);</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>简单的第一个小程序，首先我们要了解\r和\n的区别：</p>
<p>\n：直接向下移动一行，并不移动左右</p>
<p>\r：回到当前行的最左边</p>
<p>假设我们要打印9-1</p>
<p>用\n，就会出现：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">9</span><br><span class="line">8</span><br><span class="line">7</span><br><span class="line">6</span><br><span class="line">5</span><br><span class="line">4</span><br><span class="line">3</span><br><span class="line">2</span><br><span class="line">1</span><br><span class="line">0</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>但是如果用\r，就会出现在一个位置一直变化数字，从9变到1，这个不太好演示，但之后可以直接看效果。</p>
<p>另外要说的一个就是缓冲区的概念</p>
<p>假设我们要打印一个变量，它应该是这样的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">printf(&quot;%d\n&quot;,xxx)</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>这里的\n就起到了一个刷新缓冲区的效果，如果没有这个\n，那这个变量会先放到缓冲区，如果在这个过程中有什么sleep之类的暂停，那这个变量就不会被拿出来，直到暂停结束。</p>
<p>假设我们用了sleep（)，但是又想点让屏幕显示</p>
<p>可以用fflush（stdout），直接刷新</p>
<p>-———</p>
<p>这里就正式开始写</p>
<p>首先我们要用到上面提到的函数：</p>
<p>usleep（以微秒为单位暂停一段时间）</p>
<p>fflush（刷新流中的信息）</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203105.png" alt="img"></p>
<p> 编译一下；</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203037.png" alt="img"><img src="/" alt="点击并拖拽以移动"></p>
<p> 运行：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203279.png" alt="img"></p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203611.png" alt="img"></p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203318.png" alt="img"></p>
<p> 可以看到效果不错，但是刚刚我们是用gcc编译的，而这篇文章标题的另一部分是makefile，这个makefile又是什么东西？</p>
<p>-——-</p>
<p>Makefile</p>
<blockquote>
<p>会不会写makefile，从一个侧面说明了一个人是否具备完成大型工程的能力<br> makefile的效果就是，当一个工程的文件很多，有的文件要先编译，有的要后编译，有的要重新编译，而这些如果要手动，是相当麻烦的事</p>
<p>makefile的好处就是，只要写好，它只需要一个make即可完成编译</p>
</blockquote>
<p>就拿我们刚刚写的代码，这里写一个示例：</p>
<p>用makefile之前，我们要先建立一个makefile的文件</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203370.png" alt="img"></p>
<p>随后这么写，这里说一下格式，第一个test是生成的文件名，一个冒号后面跟着的是这个生成的文件需要什么来生成，然后回车，用tab开头，写编译的方式 ，随后我们保存退出</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203454.png" alt="img"></p>
<p>可以看到外面现在是有一个test的，这个时候我们输入make+回车：</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203336.png" alt="img"></p>
<p>它会提示说test已经存在，不需要生成</p>
<p>这个时候我们可以直接删除这个test吗，还可以用.PHONY来让它生成，这里先讲第一个，直接删除test</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203941.png" alt="img"></p>
<p>再进行make</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203273.png" alt="img"></p>
<p>可以看到它自动调用了我们写的执行过程，然后生成了test文件</p>
<p>这里说回第二种方式：.PHONY</p>
<p>这个的意思就是，总是被执行的，可以这么理解，每个文件都有生成时间，这里如果你没有进行修改，那make是不会帮你生成新的，但是如果加上了.PHONY，make就不会管有没有修改更新，它会直接生成新的文件</p>
<p>一般，我们还会写一个析构：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203621.png" alt="img"></p>
<p> 如果调用的话，就这么调用：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203371.png" alt="img"></p>
<p> 然后我们ls看一下文件</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201203033.png" alt="img"></p>
<p>test已经不在了</p>
<p>这里解释一下，make的原理：</p>
<blockquote>
<p>首先它会找名为makefile的文件，然后根据这个文件的内容来生成文件</p>
<p>如果这个生成的文件在目录里面找不到，或者是更新时间比之前生成的新，它会生成一个新的，否则不会生成</p>
</blockquote>
<p>-——-</p>
<p>以上就是本篇文章的全部内容，希望对你有所帮助 </p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>map/set</title>
    <url>/2022/08/01/map_set/</url>
    <content><![CDATA[<hr>
<p>这篇文章主要是讲map、set的接口及应用，模拟实现在之后</p>
<p>-——-</p>
<h1 id="关联式容器"><a href="#关联式容器" class="headerlink" title="关联式容器"></a>关联式容器</h1><p>在之前，我们学过vector、list、deque，这些称之为序列式容器，因为其底层是线性的，里面存储的就是元素本身。</p>
<p>关联式容器的区别就在于，里面存储的不再是单独的一个元素，它存储的是键值对（key，value），在检索的时候效率会更高。</p>
<h1 id="键值对"><a href="#键值对" class="headerlink" title="键值对"></a>键值对</h1><p>那什么是键值对？</p>
<p>可以这么理解，其本身是一个结构体，里面是这样的；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template &lt;class T1, class T2&gt;</span><br><span class="line">struct pair</span><br><span class="line">&#123;</span><br><span class="line">T1 first;</span><br><span class="line">T2 second;</span><br><span class="line">pair()</span><br><span class="line">: first(T1())</span><br><span class="line">, second(T2())</span><br><span class="line">&#123;&#125;</span><br><span class="line">pair(const T1&amp; a, const T2&amp; b)</span><br><span class="line">: first(a)</span><br><span class="line">, second(b)</span><br><span class="line">&#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>在里面有两个元素，这两个元素都是模板类型，里面第一个类型的T1，这个就是键值对里面的键（Key），而T2，就是Key对应的值（Value）。</p>
<p>我们在使用的时候，通过Key来找到对应的Value。</p>
<p>这里面提供了两个默认构造看看就好，不算难理解。</p>
<h2 id="树形结构的关联式容"><a href="#树形结构的关联式容" class="headerlink" title="树形结构的关联式容"></a>树形结构的关联式容</h2><p>根据应用场景不同，有以下几种：</p>
<blockquote>
<p>set</p>
<p>muitiset</p>
<p>map</p>
<p>multimap</p>
</blockquote>
<h2 id="set"><a href="#set" class="headerlink" title="set"></a>set</h2><p><a class="link"   href="https://m.cplusplus.com/reference/set/set/?kw=set" >set - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>模板参数：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201158843.png" alt="img"><img src="/" alt="点击并拖拽以移动"></p>
<p>这里简单概括一下其内容；</p>
<blockquote>
<p>set里面的内容是唯一的</p>
<p>放进去就不能被修改，但可以插入或者删除</p>
<p>set里面是排好序的（迭代器实现的是中序，打印出来是升序）</p>
<p>底层是二叉搜索树（红黑树）实现</p>
<p>跟map不同，map是&lt;Key,Value&gt;,set是&lt;Value,Value&gt;,Value就是对应的Key</p>
<p>查找效率是logN</p>
</blockquote>
<p> 这里直接看它的接口：</p>
<h3 id="构造"><a href="#构造" class="headerlink" title="构造"></a>构造</h3><table>
<thead>
<tr>
<th>函数声明</th>
<th>功能介绍</th>
</tr>
</thead>
<tbody><tr>
<td>set (const Compare&amp; comp &#x3D; Compare(), const Allocator&amp;  		&#x3D; Allocator() );</td>
<td>构造空的set</td>
</tr>
<tr>
<td>set (InputIterator first, InputIterator last, const  		Compare&amp; comp &#x3D; Compare(), const Allocator&amp; &#x3D;  		Allocator() );</td>
<td>用[first, last)区  		间中的元素构造  		set</td>
</tr>
<tr>
<td>set ( const set&lt;Key,Compare,Allocator&gt;&amp; x);</td>
<td>set的拷贝构造</td>
</tr>
</tbody></table>
<p>用的最多的应该是第一个，其次是第二个，拷贝构造不算多 </p>
<h3 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h3><table>
<thead>
<tr>
<th>iterator begin()</th>
<th>返回set中起始位置元素的迭代器</th>
</tr>
</thead>
<tbody><tr>
<td></td>
<td></td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th>iterator end()</th>
<th>返回set中最后一个元素后面的迭代器</th>
</tr>
</thead>
<tbody><tr>
<td>const_iterator cbegin()  		const</td>
<td>返回set中起始位置元素的const迭代器</td>
</tr>
<tr>
<td>const_iterator cend() const</td>
<td>返回set中最后一个元素后面的const迭代器</td>
</tr>
<tr>
<td>reverse_iterator rbegin()</td>
<td>返回set第一个元素的反向迭代器，即end</td>
</tr>
<tr>
<td>reverse_iterator rend()</td>
<td>返回set最后一个元素下一个位置的反向迭代器，  		即rbegin</td>
</tr>
<tr>
<td>const_reverse_iterator  		crbegin() const</td>
<td>返回set第一个元素的反向const迭代器，即cend</td>
</tr>
<tr>
<td>const_reverse_iterator  		crend() const</td>
<td>返回set最后一个元素下一个位置的反向const迭  		代器，即crbegin</td>
</tr>
</tbody></table>
<p> 迭代器用法都一样，这里跟list、vector的都没什么区别</p>
<h3 id="容量"><a href="#容量" class="headerlink" title="容量"></a>容量</h3><table>
<thead>
<tr>
<th>bool empty ( ) const</th>
<th>检测set是否为空，空返回true，否则返回true</th>
</tr>
</thead>
<tbody><tr>
<td>size_type size() const</td>
<td>返回set中有效元素的个数</td>
</tr>
</tbody></table>
<p>用的不多，看看就好</p>
<h3 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h3><table>
<thead>
<tr>
<th>函数声明</th>
<th>功能介绍</th>
</tr>
</thead>
<tbody><tr>
<td>pair&lt;iterator,bool&gt; insert (  		const value_type&amp; x )</td>
<td>在set中插入元素x，实际插入的是&lt;x, x&gt;构成的  		键值对，如果插入成功，返回&lt;该元素在set中的  		位置，true&gt;,如果插入失败，说明x在set中已经  		存在，返回&lt;x在set中的位置，false&gt;</td>
</tr>
<tr>
<td>void erase ( iterator position )</td>
<td>删除set中position位置上的元素</td>
</tr>
<tr>
<td>size_type erase ( const  		key_type&amp; x )</td>
<td>删除set中值为x的元素，返回删除的元素的个数</td>
</tr>
<tr>
<td>void erase ( iterator first,  		iterator last )</td>
<td>删除set中[first, last)区间中的元素</td>
</tr>
<tr>
<td>void swap (  		set&lt;Key,Compare,Allocator&gt;&amp;  		st );</td>
<td>交换set中的元素</td>
</tr>
<tr>
<td>void clear ( )</td>
<td>将set中的元素清空</td>
</tr>
<tr>
<td>iterator find ( const  		key_type&amp; x ) const</td>
<td>返回set中值为x的元素的位置</td>
</tr>
<tr>
<td>size_type count ( const  		key_type&amp; x ) const</td>
<td>返回set中值为x的元素的个数</td>
</tr>
</tbody></table>
<p>怎么说呢，这些其实看看就可以知道其是干什么的，这里就做个简单的例子：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201158118.png" alt="img"></p>
<h2 id="multiset"><a href="#multiset" class="headerlink" title="multiset"></a>multiset</h2><p><a class="link"   href="https://m.cplusplus.com/reference/set/multiset/?kw=multiset" >multiset - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>一句话解决：这个其实就是可以重复的set，除此之外就没什么区别，其他都跟set保持一致</p>
<p>以上面的图举例：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201158529.png" alt="img"></p>
<p>接口都保持了一直，会用了set，mulset也就会了</p>
<p> -–</p>
<h2 id="map"><a href="#map" class="headerlink" title="map"></a>map</h2><p><a class="link"   href="https://m.cplusplus.com/reference/map/map/?kw=map" >map - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>简单概括一下：</p>
<blockquote>
<p>map是关联性容器，跟set一样按照特定次序（比较Key）来存储对应的Value值</p>
<p>map的key是不能重复的</p>
<p>map里面的key和value是关联在一起的，取名pair（参考上面）</p>
<p>支持下标访问（重点）</p>
<p>同样是二叉搜索树（红黑树）</p>
</blockquote>
<p>模板声明：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201158061.png" alt="img"></p>
<p> 这个跟set不同的是有两个参数</p>
<p>key：比较那个</p>
<p>value：key对应的值</p>
<p>Compare：比较办法，默认是比较key</p>
<p>Alloc：暂且不管</p>
<p>-–</p>
<h3 id="构造-1"><a href="#构造-1" class="headerlink" title="构造"></a>构造</h3><table>
<thead>
<tr>
<th>函数声明</th>
<th>功能介绍</th>
</tr>
</thead>
<tbody><tr>
<td>map()</td>
<td>构造一个空的map</td>
</tr>
</tbody></table>
<p>注意，没有用迭代器区间来构造了，因为map是要有key和value的键值对</p>
<h3 id="迭代器-1"><a href="#迭代器-1" class="headerlink" title="迭代器"></a>迭代器</h3><table>
<thead>
<tr>
<th>函数声明</th>
<th>功能介绍</th>
</tr>
</thead>
<tbody><tr>
<td>begin()和end()</td>
<td>begin:首元素的位置，end最后一个元素的下一个位置</td>
</tr>
<tr>
<td>cbegin()和cend()</td>
<td>与begin和end意义相同，但cbegin和cend所指向的元素不  		能修改</td>
</tr>
<tr>
<td>rbegin()和rend()</td>
<td>反向迭代器，rbegin在end位置，rend在begin位置，其  		++和–操作与begin和end操作移动相反</td>
</tr>
<tr>
<td>crbegin()和crend()</td>
<td>与rbegin和rend位置相同，操作相同，但crbegin和crend所  		指向的元素不能修改</td>
</tr>
</tbody></table>
<p>正向反向，const&#x2F;不带const，迭代器都差不多</p>
<h3 id="容量-1"><a href="#容量-1" class="headerlink" title="容量"></a>容量</h3><table>
<thead>
<tr>
<th>函数声明</th>
<th>功能简介</th>
</tr>
</thead>
<tbody><tr>
<td>bool empty ( ) const</td>
<td>检测map中的元素是否为空，是返回  		true，否则返回false</td>
</tr>
<tr>
<td>size_type size() const</td>
<td>返回map中有效元素的个数</td>
</tr>
<tr>
<td>mapped_type&amp; operator[] (const  		key_type&amp; k)</td>
<td>返回去key对应的value</td>
</tr>
</tbody></table>
<h3 id="基本操作-1"><a href="#基本操作-1" class="headerlink" title="基本操作"></a>基本操作</h3><table>
<thead>
<tr>
<th>数声明</th>
<th>功能简介</th>
</tr>
</thead>
<tbody><tr>
<td>pair&lt;iterator,bool&gt; insert (  		const value_type&amp; x )</td>
<td>在map中插入键值对x，注意x是一个键值  		对，返回值也是键值对：iterator代表新插入  		元素的位置，bool代表释放插入成功</td>
</tr>
<tr>
<td>void erase ( iterator position )</td>
<td>删除position位置上的元素</td>
</tr>
<tr>
<td>size_type erase ( const  		key_type&amp; x )</td>
<td>删除键值为x的元素</td>
</tr>
<tr>
<td>void erase ( iterator first,  		iterator last )</td>
<td>删除[first, last)区间中的元素</td>
</tr>
<tr>
<td>void swap (  		map&lt;Key,T,Compare,Allocator&gt;&amp;  		mp )</td>
<td>交换两个map中的元素</td>
</tr>
<tr>
<td>void clear ( )</td>
<td>将map中的元素清空</td>
</tr>
<tr>
<td>iterator find ( const key_type&amp; x  		)</td>
<td>在map中插入key为x的元素，找到返回该元  		素的位置的迭代器，否则返回end</td>
</tr>
<tr>
<td>const_iterator find ( const  		key_type&amp; x ) const</td>
<td>在map中插入key为x的元素，找到返回该元  		素的位置的const迭代器，否则返回cend</td>
</tr>
<tr>
<td>size_type count ( const  		key_type&amp; x ) const</td>
<td>返回key为x的键值在map中的个数，注意  		map中key是唯一的，因此该函数的返回值  		要么为0，要么为1，因此也可以用该函数来  		检测一个key是否在map中</td>
</tr>
</tbody></table>
<p>这里比较重要的是[]的重载，这个比inset\find好用太多</p>
<p>这里简单的示范一下：</p>
<p>插入的几种方式：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201158266.png" alt="img"></p>
<p>这里说一下，find是找到对应的key，返回那个位置迭代器，但是如果没用就返回end位置的迭代器，所以在返回值里面还要判断，比较麻烦</p>
<p>为什么说insert不好用，因为[]兼顾了find和insert的功能</p>
<p>如果[x]，里面的x已经有了，那它返回的就是x对应的value</p>
<p> ———–没用找到x，那它就会插入x，并返回second的别名，就可以直接对second操作，就好像上面我让up对应的secont &#x3D; “上”一样。</p>
<h2 id="multimap"><a href="#multimap" class="headerlink" title="multimap"></a>multimap</h2><p>大体跟map一样，操作接口也都一样，只不过key是可以重复的，这里参考下图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201158851.png" alt="img"></p>
<p>最大的区别是，不支持[]，因为key可以重复，[]要找对应的值，产生冲突</p>
<p>-—–</p>
<p>几道练习题：</p>
<p><a class="link"   href="https://www.nowcoder.com/practice/16f59b169d904f8898d70d81d4a140a0?tpId=94&tqId=31064&rp=1&ru=/activity/oj&qru=/ta/bit-kaoyan/question-ranking&tPage=2" >单词识别_牛客题霸_牛客网 (nowcoder.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201158672.png" alt="img"></p>
<p>非常简单</p>
<p>首先我们接收到字符串，然后遍历，再将其加入到map里面计数即可</p>
<p>参考代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;iostream&gt;</span><br><span class="line">#include&lt;map&gt;</span><br><span class="line">#include&lt;algorithm&gt;</span><br><span class="line"></span><br><span class="line">using namespace std;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    map&lt;string,int&gt; m;</span><br><span class="line">    //接收的字符串</span><br><span class="line">    string s;</span><br><span class="line">    //一次接收一行</span><br><span class="line">    while(getline(cin,s))</span><br><span class="line">    &#123;</span><br><span class="line">        //每次的子串</span><br><span class="line">        string _s;</span><br><span class="line">        //将大写字符串变为小写</span><br><span class="line">        transform(s.begin(),s.end(),s.begin(),::tolower);</span><br><span class="line">        //遍历这个字符串</span><br><span class="line">        for(int i = 0;i&lt;s.size();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            //如果这些，说明_s已经是一个单词了，将其放到map里面，计数++</span><br><span class="line">            if(s[i] == &#x27; &#x27;|| s[i] == &#x27;.&#x27;)</span><br><span class="line">            &#123;</span><br><span class="line">                m[_s]++;</span><br><span class="line">                _s = &quot;&quot;;</span><br><span class="line">            &#125;   </span><br><span class="line">            //说明单词没完，字串++</span><br><span class="line">            else</span><br><span class="line">                _s+=s[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    //遍历这个</span><br><span class="line">    for(auto it : m)</span><br><span class="line">    &#123;</span><br><span class="line">        cout&lt;&lt;it.first&lt;&lt;&quot;:&quot;&lt;&lt;it.second&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p> <a class="link"   href="https://leetcode.cn/problems/top-k-frequent-words/description/" >692. 前K个高频单词 - 力扣（LeetCode）<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201158589.png" alt="img"></p>
<p>首先，我们把出现的单词用map保存起来，随后将其放到vector里面排序 ，排好了就用另一个vector接收即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line"></span><br><span class="line">    //map迭代器</span><br><span class="line">    typedef map&lt;string,int&gt;::iterator Miteartor;</span><br><span class="line"></span><br><span class="line">    //自定义的比较函数</span><br><span class="line">    struct Compare</span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        bool operator()(Miteartor x,Miteartor y)</span><br><span class="line">        &#123;</span><br><span class="line">            return x-&gt;second &gt; y-&gt;second;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    vector&lt;string&gt; topKFrequent(vector&lt;string&gt;&amp; words, int k) &#123;</span><br><span class="line">        map&lt;string,int&gt; m;</span><br><span class="line">        //map接收单词及技术</span><br><span class="line">        for(auto str:words)</span><br><span class="line">        &#123;</span><br><span class="line">            m[str]++;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        // 用来排序的数组</span><br><span class="line">        vector&lt;Miteartor&gt; v;</span><br><span class="line">        vector&lt;string&gt; vs;</span><br><span class="line">        // 将map里面的数据放到vector里面</span><br><span class="line">        auto it = m.begin();</span><br><span class="line">        while(it != m.end())</span><br><span class="line">        &#123;</span><br><span class="line">            v.push_back(it);</span><br><span class="line">            it++;</span><br><span class="line">        &#125;</span><br><span class="line">        //为了稳定，用stable_sort</span><br><span class="line">        stable_sort(v.begin(),v.end(),Compare());</span><br><span class="line"></span><br><span class="line">        //插入</span><br><span class="line">        for(int i = 0;i&lt;k;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            vs.push_back(v[i]-&gt;first);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        return vs;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>感谢您的观看，希望对您学习有所帮助</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>reverse_iterator</title>
    <url>/2022/07/16/reverse_iterator/</url>
    <content><![CDATA[<hr>
<p>这篇文章通用于vector和list的反向迭代器，希望这篇文章看完你能对反向迭代器有更深的理解</p>
<hr>
<h1 id="什么是反向迭代器"><a href="#什么是反向迭代器" class="headerlink" title="什么是反向迭代器"></a>什么是反向迭代器</h1><p>在遍历容器的时候，会有以下几种方式：</p>
<p>1.下标的随机访问，deque、vector和string都支持这种</p>
<p>2.迭代器</p>
<p>3.范围for</p>
<p>从底层来说，范围for其实就是迭代器经过包装之后的样子，这里主要说一下迭代器，这个使用的时候一般是如此使用：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201149502.png"></p>
<p>当然，如果熟悉一点，可以直接上auto</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201149758.png"></p>
<p>在之前，我们模拟实现过vector的迭代器，这里放上代码看看：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">namespace knous</span><br><span class="line"> </span><br><span class="line">&#123;</span><br><span class="line"> </span><br><span class="line">	template&lt;class T&gt;</span><br><span class="line">	class vector</span><br><span class="line">	&#123;</span><br><span class="line">	public:</span><br><span class="line">		typedef T* iterator;</span><br><span class="line">		typedef const T* const_iterator;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        iterator begin()</span><br><span class="line">		&#123;</span><br><span class="line">			return _start;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		iterator end()</span><br><span class="line">		&#123;</span><br><span class="line">			reruen _finish;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		const_iterator cbegin()</span><br><span class="line">		&#123;</span><br><span class="line">			reruen _start;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		const_iterator cend() const</span><br><span class="line">		&#123;</span><br><span class="line">			rerurn _finis;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line">	private:</span><br><span class="line">		iterator _start; // 指向数据块的开始</span><br><span class="line"> </span><br><span class="line">		iterator _finish; // 指向有效数据的尾</span><br><span class="line"> </span><br><span class="line">		iterator _endOfStorage; // 指向存储容量的尾</span><br><span class="line"> </span><br><span class="line">	&#125;;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>可以看到vector的迭代器其实就是指针，分别指向vector的头尾两端，因为其底层空间是连续，所以下标可以直接++，但是list就不一样了</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T, class Ref,class Ptr &gt;</span><br><span class="line">	struct __list_iteartor</span><br><span class="line">	&#123;</span><br><span class="line">		typedef list_node&lt;T&gt; Node;</span><br><span class="line">		typedef __list_iteartor&lt;T, Ref, Ptr &gt; self;</span><br><span class="line"> </span><br><span class="line">		__list_iteartor(Node* Node)</span><br><span class="line">			:_node(Node)</span><br><span class="line">		&#123;&#125;</span><br><span class="line"> </span><br><span class="line">		Ref operator*()</span><br><span class="line">		&#123;</span><br><span class="line">			return _node-&gt;_data;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		Ptr operator-&gt;()</span><br><span class="line">		&#123;</span><br><span class="line">			//return &amp;(operator*());</span><br><span class="line">			return &amp;_node-&gt;_data;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		bool operator!=(const self&amp; it)</span><br><span class="line">		&#123;</span><br><span class="line">			return _node != it._node;</span><br><span class="line">		&#125;</span><br><span class="line">		</span><br><span class="line">		self operator++(int)</span><br><span class="line">		&#123;</span><br><span class="line">			_node = _node-&gt;_next;</span><br><span class="line">			return _node;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		self operator++()</span><br><span class="line">		&#123;</span><br><span class="line">			Node* tmp(*this);</span><br><span class="line">			_node = _node-&gt;_next;</span><br><span class="line">			return tmp;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		self operator--(int)</span><br><span class="line">		&#123;</span><br><span class="line">			_node = _node-&gt;_prev;</span><br><span class="line">			return _node;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		self operator--()</span><br><span class="line">		&#123;</span><br><span class="line">			_node = _node-&gt;_prev;</span><br><span class="line">			return _node;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		Node* _node;</span><br><span class="line">	&#125;;</span><br></pre></td></tr></table></figure>



<p>因为list底层空间并不连续，所以我们常规的一些操作无法直接使用，例如++、–，为了解决这种困难，所以将迭代器单纯封了一个类出来，上面就是list迭代器的代码</p>
<p>那反向迭代器应该是什么样子的，以下面这个举例子：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201149239.png"></p>
<p>原本应该是1 2 3 4，现在变成了 4 3 2 1</p>
<p>这里借用我之前的一张图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201149068.png"></p>
<p>这张图可以很好的帮我们理解反向迭代器，其rbegin跟end指向的是同一块空间，也就是说，我们对这一块空间明面上是++，实际上是–，知道rbegin到rend，也就是begin的位置，迭代器就走完了全部。</p>
<h1 id="模拟实现"><a href="#模拟实现" class="headerlink" title="模拟实现"></a>模拟实现</h1><p>明白了大致逻辑，接下来就正式开写：</p>
<p>这里我用list来写一个反向迭代器，随后复用到vector里面</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class iterator, class Ref, class Ptr &gt;</span><br><span class="line">	struct Reverse_iterator</span><br><span class="line">	&#123;</span><br><span class="line">        iterator _it;</span><br><span class="line">		typedef Reverse_iterator&lt;iterator,Ref, Ptr &gt; self;</span><br><span class="line">	&#125;;</span><br></pre></td></tr></table></figure>



<p>反向迭代器，其本质也是一个迭代器，只不过在实现方式上有所不同，这里就直接复用迭代器部分</p>
<h2 id="构造"><a href="#构造" class="headerlink" title="构造"></a>构造</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Reverse_iterator(iterator it)</span><br><span class="line">			:_it(it)</span><br><span class="line">		&#123;&#125;</span><br></pre></td></tr></table></figure>



<p>借用它传进来的指针来初始化这个迭代器对象</p>
<h2 id="解引用"><a href="#解引用" class="headerlink" title="解引用"></a>解引用</h2><p>这里要注意的一点是，我们的rbegin和rend跟stl库里面保持一致，它的rbegin对应的是end，而迭代器里面的end其实是头节点</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">iterator begin()</span><br><span class="line">		&#123;</span><br><span class="line">			return iterator(_head-&gt;_next);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_iterator begin() const</span><br><span class="line">		&#123;</span><br><span class="line">			return const_iterator(_head-&gt;_next);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>那我们实现的时候就要注意，如果此时rbegin是头节点，那我们对头节点解引用，返回的应该是头节点上一个节点解引用</p>
<p>所以写出如下代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Ref operator*()</span><br><span class="line">		&#123;</span><br><span class="line">			return *(--_it);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h2 id=""><a href="#" class="headerlink" title="++"></a>++</h2><p>注意反向迭代器的++实际上是正向迭代器的–，这里直接复用即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">self operator++()</span><br><span class="line">		&#123;</span><br><span class="line">			--it;</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h2 id="–"><a href="#–" class="headerlink" title="–"></a>–</h2><p>反向迭代器的–，实际上就是正向迭代器的++，复用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">self operator-()</span><br><span class="line">		&#123;</span><br><span class="line">			++it;</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h2 id="！-x3D"><a href="#！-x3D" class="headerlink" title="！&#x3D;"></a>！&#x3D;</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool operator!=(const self&amp; it)</span><br><span class="line">		&#123;</span><br><span class="line">			return _it != it._it;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这里还要写一点，就是在list里面加一个反向迭代器的接口：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">reverse_iterator rbegin()</span><br><span class="line">		&#123;</span><br><span class="line">			return reverse_iterator(end());</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_reverse_iterator rbegin() const</span><br><span class="line">		&#123;</span><br><span class="line">			return const_reverse_iterator(end());</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">reverse_iterator rend()</span><br><span class="line">		&#123;</span><br><span class="line">			return reverse_iterator(begin());</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_reverse_iterator rend() const</span><br><span class="line">		&#123;</span><br><span class="line">			return const_reverse_iterator(begin());</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这里的节点跟迭代器反过来即可</p>
<p>看看效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201149957.png"></p>
<p>对比：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201149058.png"></p>
<p>list可以调用这个反向迭代器，vector呢？</p>
<h1 id="vector的复用"><a href="#vector的复用" class="headerlink" title="vector的复用"></a>vector的复用</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">reverse_iterator rbegin()</span><br><span class="line">		&#123;</span><br><span class="line">			return reverse_iterator(_finish);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		reverse_iterator rend()</span><br><span class="line">		&#123;</span><br><span class="line">			return reverse_iterator(_start);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_reverse_iterator rcbegin()</span><br><span class="line">		&#123;</span><br><span class="line">			reruen const_reverse_iterator(_finish);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_reverse_iterator rcend() const</span><br><span class="line">		&#123;</span><br><span class="line">			rerurn const_reverse_iterator(_start);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>同样新增的几个参数，并且将重命名一下模板的名字</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef Reverse_iterator&lt;iterator, T&amp;, T*&gt; reverse_iterator;</span><br><span class="line">		typedef Reverse_iterator&lt;iterator, const T&amp;, const T*&gt; const_reverse_iterator;</span><br></pre></td></tr></table></figure>



<p>调用：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201149230.png"></p>
<p>对比</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201149201.png"></p>
<p>这里放上全部代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">namespace knous</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	template&lt;class iterator, class Ref, class Ptr &gt;</span><br><span class="line">	struct Reverse_iterator</span><br><span class="line">	&#123;</span><br><span class="line">		iterator _it;</span><br><span class="line"></span><br><span class="line">		typedef Reverse_iterator&lt;iterator, Ref, Ptr &gt; self;</span><br><span class="line"></span><br><span class="line">		Reverse_iterator(iterator&amp; it)</span><br><span class="line">			:_it(it)</span><br><span class="line">		&#123;&#125;</span><br><span class="line"></span><br><span class="line">		Ref operator*()</span><br><span class="line">		&#123;</span><br><span class="line">			iterator tmp = _it;</span><br><span class="line">			return *(--tmp);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		self operator++(int)</span><br><span class="line">		&#123;</span><br><span class="line">			--_it;</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		self operator--(int)</span><br><span class="line">		&#123;</span><br><span class="line">			++_it;</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		bool operator!=(const self&amp; it)</span><br><span class="line">		&#123;</span><br><span class="line">			return _it != it._it;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>注意，要使用这个反向迭代器需要先有正向代码器，同时包上这两个typedef</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef Reverse_iterator&lt;iterator, T&amp;, T*&gt; reverse_iterator;</span><br><span class="line">		typedef Reverse_iterator&lt;iterator, const T&amp;, const T*&gt; const_reverse_iterator;</span><br></pre></td></tr></table></figure>



<p>即可正常使用，谢谢观看</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>stack、queue、priority_queue</title>
    <url>/2022/07/15/stack%E3%80%81queue%E3%80%81priority_queue/</url>
    <content><![CDATA[<hr>
<p>这篇文章主要目标：</p>
<p>stack、queue、priority_queue的介绍和使用</p>
<p>什么是容器适配器</p>
<hr>
<h1 id="stack的介绍及使用"><a href="#stack的介绍及使用" class="headerlink" title="stack的介绍及使用"></a>stack的介绍及使用</h1><p>早在几个月前，我就写过一篇文章来讲解和模拟实现stack，当时用的是C语言的版本，这里放上链接：</p>
<p><a class="link"   href="https://blog.csdn.net/qq_60306111/article/details/123730529?spm=1001.2014.3001.5502"  title="【数据结构】栈_Knous的博客-CSDN博客_数据结构栈">【数据结构】栈_Knous的博客-CSDN博客_数据结构栈<i class="fas fa-external-link-alt"></i></a></p>
<p>因为现在使用的是C++，这里就重新说一下</p>
<h2 id="stack的介绍"><a href="#stack的介绍" class="headerlink" title="stack的介绍"></a>stack的介绍</h2><p><a class="link"   href="https://m.cplusplus.com/reference/stack/stack/?kw=stack"  title="stack - C++ Reference (cplusplus.com)">stack - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>基本可以参考这个网站，但因为是全英文，这里挑一下重点说：</p>
<blockquote>
<ol>
<li><p>stack是一种容器适配器，专门用在具有后进先出操作的上下文环境中，其删除只能从容器的一端进行元素的插入与提取操作。</p>
</li>
<li><p>stack是作为容器适配器被实现的，容器适配器即是对特定类封装作为其底层的容器，并提供一组特定的成员函数来访问其元素，将特定类作为其底层的，元素特定容器的尾部(即栈顶)被压入和弹出。</p>
</li>
<li><p>stack的底层容器可以是任何标准的容器类模板或者一些其他特定的容器类，这些容器类应该支持以下操作：</p>
</li>
</ol>
<p>empty：判空操作</p>
<p>back：获取尾部元素操作</p>
<p>push_back：尾部插入元素操作</p>
<p>pop_back：尾部删除元素操作</p>
<ol start="4">
<li>标准容器vector、deque、list均符合这些需求，默认情况下，如果没有为stack指定特定的底层容器，默认情况下使用deque。</li>
</ol>
</blockquote>
<blockquote>
<p>简单来说，stack是一种容器，这个容器可以用vector是实现，也可以用list实现，只要满足其基本操作需要，如果没有指定容器，就会用deque（双向循环队列）</p>
<p>其基本功能有尾插尾删，获得尾部数据及判空</p>
</blockquote>
<h2 id="stack的使用"><a href="#stack的使用" class="headerlink" title="stack的使用"></a>stack的使用</h2><p>这里说几个常用的函数</p>
<table>
<thead>
<tr>
<th>函数说明</th>
<th>接口说明</th>
</tr>
</thead>
<tbody><tr>
<td>stack()</td>
<td>构造空的栈</td>
</tr>
<tr>
<td>empty()</td>
<td>检测stack是否为空</td>
</tr>
<tr>
<td>size()</td>
<td>返回stack中元素的个数</td>
</tr>
<tr>
<td>top()</td>
<td>返回栈顶元素的引用</td>
</tr>
<tr>
<td>push()</td>
<td>将元素val压入stack中</td>
</tr>
<tr>
<td>pop()</td>
<td>将stack中尾部的元素弹出</td>
</tr>
</tbody></table>
<p>这里借一道题举例：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201147420.png"></p>
<p>让我们实现一个栈，这个栈支持一些基本的操作，其中稍有难度的是最后有获取最小元素，要在常数时间里面，也就是说我们不能采用遍历的办法，这里说一个解法：</p>
<p>我们创建两个栈，第一个来完成插入删除，第二个来完成获取最小值的情况，假设st1（常规操作）要插入值，那就用st2（最小值）来判断</p>
<p>假如st2为空，或者插入的这个值比st2上一个值小，那我们最小值就要更新，这个值就要尾插到st2里面，获取最小值的时候就直接返回st2的顶部元素</p>
<p>这里看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class MinStack &#123;</span><br><span class="line">public:</span><br><span class="line"></span><br><span class="line">    //两个栈，st1完成常规操作，st2来找最小    </span><br><span class="line">    stack&lt;int&gt; st1, st2;</span><br><span class="line"></span><br><span class="line">    MinStack() &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    void push(int val) &#123;</span><br><span class="line">        //先插入st1</span><br><span class="line">        st1.push(val);</span><br><span class="line">        </span><br><span class="line">        //如果走到这一步，st1里面肯定有起码一个值，此时如果st2为空，说明最小值就是那个元素，如果st2不为空，说明里面已有最小值，这个时候判断，如果这个插入值比当前最小值小，那就插入，最小值就要更新</span><br><span class="line">        if(st2.empty() || val &lt;= st2.top())</span><br><span class="line">        &#123;</span><br><span class="line">            st2.push(val);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    //如果删的st1的值刚好是最小值，那st2也要删除，如果不是就直接删除st1即可</span><br><span class="line">    void pop() &#123;</span><br><span class="line">        if(st1.top() == st2.top())</span><br><span class="line">        &#123;</span><br><span class="line">            st1.pop();</span><br><span class="line">            st2.pop();</span><br><span class="line">        &#125;</span><br><span class="line">        else</span><br><span class="line">        st1.pop();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    //直接返回st1即可</span><br><span class="line">    </span><br><span class="line">    int top() &#123;</span><br><span class="line">        return st1.top();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    //最小值都在st2里面，返回st2即可</span><br><span class="line">    int getMin() &#123;</span><br><span class="line">        return st2.top();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<h2 id="模拟实现"><a href="#模拟实现" class="headerlink" title="模拟实现"></a>模拟实现</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T, class con = deque&lt;T&gt;&gt;</span><br><span class="line">	class stack</span><br><span class="line">	&#123;</span><br><span class="line">	public:</span><br><span class="line">	</span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line">		con _con;</span><br><span class="line">	&#125;;</span><br></pre></td></tr></table></figure>



<p>一点一点看，首先这是一个模板，那就用一个T来接收类型，重点是后面的con，这个就是我们的适配器了，因为每次都要造轮子的话也太麻烦了，因为stack的基本操作可以用其他</p>
<p>vector、list实现，那为什么不直接拿来用呢？</p>
<p>所以就出现了这里的，con是一个类模板，这个默认的是用deque，如果不喜欢这个也可以改，不过暂时先用着</p>
<h3 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h3><p>stack的插入也只有尾插一种，所以这里直接调用con的函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void push(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			_con.push_back(x);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>直接调用对应接口，由上面的deque来帮我们解决问题</p>
<h3 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void pop()</span><br><span class="line">		&#123;</span><br><span class="line">			_con.pop_back();</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="返回顶端元素"><a href="#返回顶端元素" class="headerlink" title="返回顶端元素"></a>返回顶端元素</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const T&amp; top()</span><br><span class="line">		&#123;</span><br><span class="line">			return _con.back();</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这里说一下，vector和list都有这个back函数，它的作用是返回最后的数据，这里正好直接用</p>
<p>看一下效果；</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201147110.png"></p>
<p>不是默认用的deque吗，我传个vector行不行？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201147294.png"></p>
<p>当然可以</p>
<p>list：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201147170.png"></p>
<p>都没有问题</p>
<p>只要这里的类模板符合stack的需求，即尾部的插入删除，返回尾部元素等，都可以替换 </p>
<hr>
<h1 id="queue的介绍和使用"><a href="#queue的介绍和使用" class="headerlink" title="queue的介绍和使用"></a>queue的介绍和使用</h1><p>同样我写了一篇queue的c介绍和模拟实现，这里是链接：</p>
<p><a class="link"   href="https://blog.csdn.net/qq_60306111/article/details/123761379?spm=1001.2014.3001.5502"  title="【数据结构】队列_Knous的博客-CSDN博客">【数据结构】队列_Knous的博客-CSDN博客<i class="fas fa-external-link-alt"></i></a></p>
<p>同样，先放上较为官方的文档介绍，再说一下重点</p>
<p><a class="link"   href="https://m.cplusplus.com/reference/queue/queue/"  title="queue - C++ Reference (cplusplus.com)">queue - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<blockquote>
<ol>
<li>队列是一种容器适配器，专门用于在FIFO上下文(先进先出)中操作，其中从容器一端插入元素，另一端提取元素。</li>
<li>队列作为容器适配器实现，容器适配器即将特定容器类封装作为其底层容器类，queue提供一组特定的成员函数来访问其元素。元素从队尾入队列，从队头出队列。</li>
<li>底层容器可以是标准容器类模板之一，也可以是其他专门设计的容器类。该底层容器应至少支持以下操作:</li>
</ol>
<p>empty：检测队列是否为空</p>
<p>size：返回队列中有效元素的个数</p>
<p>front：返回队头元素的引用</p>
<p>back：返回队尾元素的引用</p>
<p>push_back：在队列尾部入队列</p>
<p>pop_front：在队列头部出队列</p>
<ol start="4">
<li>标准容器类deque和list满足了这些要求。默认情况下，如果没有为queue实例化指定容器类，则使用标准容器deque</li>
</ol>
</blockquote>
<p>这里要提一下：</p>
<blockquote>
<p> queue不能使用vector来作为底层，因为queue要支持头删，而vector的头删效率太低，所以不建议使用</p>
</blockquote>
<h2 id="queue的使用"><a href="#queue的使用" class="headerlink" title="queue的使用"></a>queue的使用</h2><p>这里说几个基本函数：</p>
<table>
<thead>
<tr>
<th>函数声明</th>
<th>接口说明</th>
</tr>
</thead>
<tbody><tr>
<td>queue()</td>
<td>构造空的队列</td>
</tr>
<tr>
<td>empty()</td>
<td>检测队列是否为空，是返回true，否则返回false</td>
</tr>
<tr>
<td>size()</td>
<td>返回队列中有效元素的个数</td>
</tr>
<tr>
<td>front()</td>
<td>返回队头元素的引用</td>
</tr>
<tr>
<td>back()</td>
<td>返回队尾元素的引用</td>
</tr>
<tr>
<td>push()</td>
<td>在队尾将元素val入队列</td>
</tr>
<tr>
<td>pop()</td>
<td>将队头元素出队列</td>
</tr>
</tbody></table>
<h2 id="模拟实现-1"><a href="#模拟实现-1" class="headerlink" title="模拟实现"></a>模拟实现</h2><p>因为大体实现思路是跟stack差不多的，所以这里就直接放上代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T,class Con = deque&lt;T&gt;&gt;</span><br><span class="line">	class queue</span><br><span class="line">	&#123;</span><br><span class="line">	public:</span><br><span class="line">		void push(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			_con.push_back(x);</span><br><span class="line">		&#125;</span><br><span class="line">		const T&amp; front()</span><br><span class="line">		&#123;</span><br><span class="line">			return _con.front();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const T&amp; back()</span><br><span class="line">		&#123;</span><br><span class="line">			return _con.back();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		size_t size()</span><br><span class="line">		&#123;</span><br><span class="line">			return _con.size();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		bool empty()</span><br><span class="line">		&#123;</span><br><span class="line">			return _con.empty();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void pop()</span><br><span class="line">		&#123;</span><br><span class="line">			return _con.pop_front();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line">		Con _con;</span><br><span class="line">	&#125;;</span><br></pre></td></tr></table></figure>



<p>演示：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201147143.png"></p>
<hr>
<p>提一个问题，要不要写构造函数？</p>
<p>上面的stack和queue都没有写，这是为什么？</p>
<blockquote>
<p>因为其底层本质是借助容器适配器，在调用的时候容器适配器会完成构造，这里将没写</p>
</blockquote>
<hr>
<h1 id="priority-queue"><a href="#priority-queue" class="headerlink" title="priority_queue"></a>priority_queue</h1><p>这个名看着挺陌生的，但可以这么理解，这个所谓的优先级_队列，其实就是一个二叉堆，为什么这么说，可以看下面的部分。</p>
<hr>
<h2 id="priority-queue的介绍"><a href="#priority-queue的介绍" class="headerlink" title="priority_queue的介绍"></a>priority_queue的介绍</h2><p><a class="link"   href="https://m.cplusplus.com/reference/queue/priority_queue/"  title="priority_queue - C++ Reference (cplusplus.com)">priority_queue - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>翻译一下，就是；</p>
<blockquote>
<ol>
<li>优先队列是一种容器适配器，根据严格的弱排序标准，它的第一个元素总是它所包含的元素中最大的。</li>
<li>此上下文类似于堆，在堆中可以随时插入元素，并且只能检索最大堆元素(优先队列中位于顶部的元素)。</li>
<li>优先队列被实现为容器适配器，容器适配器即将特定容器类封装作为其底层容器类，queue提供一组特定的成员函数来访问其元素。元素从特定容器的“尾部”弹出，其称为优先队列的顶部。</li>
<li>底层容器可以是任何标准容器类模板，也可以是其他特定设计的容器类。容器应该可以通过随机访问迭代器访问，并支持以下操作：</li>
</ol>
<p>empty()：检测容器是否为空</p>
<p>size()：返回容器中有效元素个数</p>
<p>front()：返回容器中第一个元素的引用</p>
<p>push_back()：在容器尾部插入元素</p>
<p>pop_back()：删除容器尾部元素</p>
<ol start="5">
<li>标准容器类vector和deque满足这些需求。默认情况下，如果没有为特定的priority_queue类实例化指定容器类，则使用vector。</li>
<li>需要支持随机访问迭代器，以便始终在内部保持堆结构。容器适配器通过在需要时自动调用算法函数make_heap、push_heap和pop_heap来自动完成此操作</li>
</ol>
</blockquote>
<p>其实看着一大堆，简化一下意思就是：</p>
<blockquote>
<p>这是一个由vector或者deque完成的一个容器，里面有一块连续空间来存放数据，为什么不能用list，因为它要完成调整，平时第一个元素是这块连续空间最大的（或者最小的），所以每插入一个元素都要调整，这么看，像不像之前学的堆？</p>
</blockquote>
<p><a class="link"   href="https://blog.csdn.net/qq_60306111/article/details/123940534?spm=1001.2014.3001.5501"  title="【数据结构】实现堆、堆的应用_Knous的博客-CSDN博客">【数据结构】实现堆、堆的应用_Knous的博客-CSDN博客<i class="fas fa-external-link-alt"></i></a></p>
<p>堆我也写了介绍，这里有兴趣的可以去看看，我们模拟实现其实思路跟那个差不多</p>
<h2 id="priority-queue的使用"><a href="#priority-queue的使用" class="headerlink" title="priority_queue的使用"></a>priority_queue的使用</h2><table>
<thead>
<tr>
<th>函数声明</th>
<th>接口说明</th>
</tr>
</thead>
<tbody><tr>
<td>priority_queue()&#x2F;priority_queue(first,</td>
<td></td>
</tr>
<tr>
<td>last)</td>
<td>构造一个空的优先级队列</td>
</tr>
<tr>
<td>empty( )</td>
<td>检测优先级队列是否为空，是返回true，否则返回</td>
</tr>
<tr>
<td>false</td>
<td></td>
</tr>
<tr>
<td>top( )</td>
<td>返回优先级队列中最大(最小元素)，即堆顶元素</td>
</tr>
<tr>
<td>push(x)</td>
<td>在优先级队列中插入元素x</td>
</tr>
<tr>
<td>pop()</td>
<td>删除优先级队列中最大(最小)元素，即堆顶元素</td>
</tr>
</tbody></table>
<p>这里要注意的不多，只有一个点，默认建立的是大堆，如果是小堆，需要传一个仿函数进去，这里演示的时候多了一个头文件：</p>
<p>functional</p>
<p>这个其实算一个算法的头文件</p>
<p>接下来看一下效果<img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201147499.png"></p>
<p>其实这就是堆排序了，每次都取最大的，依次取到最小的，所以是降序，如果要按照升序，那就要传一个greater过去：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201148508.png"></p>
<p>这个所谓的仿函数是什么？</p>
<p>你看完模拟实现就知道了</p>
<h2 id="priority-queue的模拟实现"><a href="#priority-queue的模拟实现" class="headerlink" title="priority_queue的模拟实现"></a>priority_queue的模拟实现</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T,class Con = deque&lt;T&gt;&gt;</span><br><span class="line">	class priority</span><br><span class="line">	&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		Con _con;</span><br><span class="line">	&#125;;</span><br></pre></td></tr></table></figure>



<p>暂且只有这么多东西，一点一点来，首先写一个尾插看看</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void push(const T&amp; val)</span><br><span class="line">		&#123;</span><br><span class="line">			_con.push_back(val);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这样写，对吗？</p>
<p>如果是这样写的话，那跟stack、queue有什么区别吗？</p>
<blockquote>
<p>我们知道堆最大的特点就是它的最开头的元素是最大的（或者是最小的），为了实现这个特点，我们每插入一个数据的时候都要考虑它是否需要调整，这里就要写一个函数来完成，因为是从最后一个位置向上调，所以这个函数暂且就叫adjustup</p>
</blockquote>
<h3 id="插入-1"><a href="#插入-1" class="headerlink" title="插入"></a>插入</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void push(const T&amp; val)</span><br><span class="line">		&#123;</span><br><span class="line">			_con.push_back(val);</span><br><span class="line">			AdjustUp(_con.size()-1);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void AdjustUp(int child)</span><br><span class="line">		&#123;</span><br><span class="line">			size_t parent = (child - 1) / 2;</span><br><span class="line">			while (child &gt; 0)</span><br><span class="line">			&#123;</span><br><span class="line">				if (_con[parent] &lt; _con[child])</span><br><span class="line">				&#123;</span><br><span class="line">					swap(_con[parent], _con[child]);</span><br><span class="line">					child = parent;</span><br><span class="line">					parent = (child - 1) / 2;</span><br><span class="line">				&#125;</span><br><span class="line">				else</span><br><span class="line">					break;</span><br><span class="line">			&#125;</span><br><span class="line">			return;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="删除-1"><a href="#删除-1" class="headerlink" title="删除"></a>删除</h3><p>所谓的删除，其实就是将开头位置的数据跟最后一个交换，然后再让size–，不过这里还有一个问题就是，最后一个元素其实是叶子节点，让叶子节点到了头节点上面，这就不符合堆的性质了，所以这里还要再写一个调整函数，因为是向下调整，所以这个就取名adjustdown</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void pop()</span><br><span class="line">		&#123;</span><br><span class="line">			if (_con.empty())</span><br><span class="line">				return;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				swap(_con.front(), _con.back());</span><br><span class="line">				_con.pop();</span><br><span class="line">				AdjustDown(0);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void AdjustDown(int praent)</span><br><span class="line">		&#123;</span><br><span class="line">			size_t child = praent * 2 + 1;</span><br><span class="line">			while (child &lt; _con.size())</span><br><span class="line">			&#123;</span><br><span class="line">				if (child + 1 &lt; _con.size() &amp;&amp; _con[child+1] &gt; _con[child])</span><br><span class="line">				&#123;</span><br><span class="line">					child += 1;</span><br><span class="line">				&#125;</span><br><span class="line">				if (_con[praent] &lt; _con[child])</span><br><span class="line">				&#123;</span><br><span class="line">					swap(_con[praent], _con[child]);</span><br><span class="line">					praent = child;</span><br><span class="line">					child = praent * 2 + 1;</span><br><span class="line">				&#125;</span><br><span class="line">				else</span><br><span class="line">					break;</span><br><span class="line">			&#125;</span><br><span class="line">			return ;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="容量、是否为空、顶端元素"><a href="#容量、是否为空、顶端元素" class="headerlink" title="容量、是否为空、顶端元素"></a>容量、是否为空、顶端元素</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">size_t size()</span><br><span class="line">		&#123;</span><br><span class="line">			return _con.size();</span><br><span class="line">		&#125;</span><br><span class="line">bool empty()</span><br><span class="line">		&#123;</span><br><span class="line">			return _con.empty();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">const T&amp; top()</span><br><span class="line">		&#123;</span><br><span class="line">			return _con[0];</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="用迭代器区间来构造"><a href="#用迭代器区间来构造" class="headerlink" title="用迭代器区间来构造"></a>用迭代器区间来构造</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class Inputiterator&gt;</span><br><span class="line">		priority_queue(Inputiterator first, Inputiterator last)</span><br><span class="line">		&#123;</span><br><span class="line">			while (first != last)</span><br><span class="line">			&#123;</span><br><span class="line">				push(*first);</span><br><span class="line">				first++;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>其实跟之前写的迭代器区间没什么区别，先插入，然后调整</p>
<p>测试：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201148390.png"></p>
<p>没什么问题，但是又有点问题</p>
<p>就是，这个调整堆的部分是固定的，也就是说它只能单纯的是大堆或者是小堆，那么有没有办法可以解决这个问题呢？</p>
<p>当然有，这个比较函数不要定死就可以了，既然如此，就有了第三个模板参数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line">	struct less</span><br><span class="line">	&#123;</span><br><span class="line">		bool operatop(T&amp; x,T&amp; y) const</span><br><span class="line">		&#123;</span><br><span class="line">			return x &lt; y ? ;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line">	template&lt;class T,class Con = deque&lt;T&gt;,class cmp = less&lt;T&gt;&gt;</span><br></pre></td></tr></table></figure>



<p>借此，我们写了一个新的类出来</p>
<p>这个类名字就叫less，里面没有参数，只有一个运算符的重载，我们默认传过去的是less，到时候比较的时候我们就可以靠cmp来比较，然后判断交换</p>
<p>经过修改后的比较函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void AdjustUp(int child)</span><br><span class="line">		&#123;</span><br><span class="line">			size_t parent = (child - 1) / 2;</span><br><span class="line">			while (child &gt; 0)</span><br><span class="line">			&#123;</span><br><span class="line">				if (cmp(_con[parent] , _con[child]))</span><br><span class="line">				&#123;</span><br><span class="line">					swap(_con[parent], _con[child]);</span><br><span class="line">					child = parent;</span><br><span class="line">					parent = (child - 1) / 2;</span><br><span class="line">				&#125;</span><br><span class="line">				else</span><br><span class="line">					break;</span><br><span class="line">			&#125;</span><br><span class="line">			return;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void AdjustDown(int praent)</span><br><span class="line">		&#123;</span><br><span class="line">			size_t child = praent * 2 + 1;</span><br><span class="line">			while (child &lt; _con.size())</span><br><span class="line">			&#123;</span><br><span class="line">				if (child + 1 &lt; _con.size() &amp;&amp; cmp(_con[child], _con[child+1]))</span><br><span class="line">				&#123;</span><br><span class="line">					child += 1;</span><br><span class="line">				&#125;</span><br><span class="line">				if (cmp(_con[praent], _con[child]))</span><br><span class="line">				&#123;</span><br><span class="line">					swap(_con[praent], _con[child]);</span><br><span class="line">					praent = child;</span><br><span class="line">					child = praent * 2 + 1;</span><br><span class="line">				&#125;</span><br><span class="line">				else</span><br><span class="line">					break;</span><br><span class="line">			&#125;</span><br><span class="line">			return ;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>注意这里有一个坑，就是less其实就是一个类，作为一个类，它必须先创建一个对象出来，然后才能调用…</p>
<p>既然有了less，那就再写一个greater</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line">	struct greater</span><br><span class="line">	&#123;</span><br><span class="line">		bool operator()(const T&amp; x, const T&amp;  y) const</span><br><span class="line">		&#123;</span><br><span class="line">			return x &gt; y;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;;</span><br></pre></td></tr></table></figure>



<p>既然我们要调用这个greater，那就应该先传过去vector，如图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201148810.png"></p>
<p>那这个greater是什么？</p>
<p>其实这就是一个仿函数，这里就简单说一下，这里当然也可以用函数指针来代替，但却过于繁琐，不如我们直接重载一个括号比较，如果有兴趣可以看这篇博客</p>
<p><a class="link"   href="https://blog.csdn.net/K346K346/article/details/82818801"  title="C++ 仿函数_恋喵大鲤鱼的博客-CSDN博客_c++仿函数">C++ 仿函数_恋喵大鲤鱼的博客-CSDN博客_c++仿函数<i class="fas fa-external-link-alt"></i></a></p>
<p>至此，stack和queue的模拟实现也已经完成，希望这篇文章对你的学习有所帮助</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>string</title>
    <url>/2022/06/05/string/</url>
    <content><![CDATA[<hr>
<h1 id="string"><a href="#string" class="headerlink" title="string"></a>string</h1><h2 id="什么是string，为什么要学习string"><a href="#什么是string，为什么要学习string" class="headerlink" title="什么是string，为什么要学习string"></a>什么是string，为什么要学习string</h2><blockquote>
<p>C语言中，字符串是以’\0’结尾的一些字符的集合，为了操作方便，C标准库中提供了一些str系列的库函数，但是这些库函数与字符串是分离开的，不太符合OOP的思想，而且底层空间需要用户自己管理，稍不留神可能还会越界访问。</p>
</blockquote>
<p>在刷题的时候也经常可以看到一些题目：</p>
<p>类似将字符串交换位置、相加、转化整形等</p>
<p>在这些题目里面，题目基本都是以string类的形式出现：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201108328.png"></p>
<p>string是字符串类型，对于字符串的操作用string来会更加简单、方便且快捷</p>
<hr>
<h2 id="标准库的string"><a href="#标准库的string" class="headerlink" title="标准库的string"></a>标准库的string</h2><p>这里有一个C++的网站可以帮助我们来了解string，缺点是全英文</p>
<p><a class="link"   href="http://m.cplusplus.com/reference/string/string/?kw=string"  title="string - C++ Reference (cplusplus.com)">string - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>这里借用参考的翻译：</p>
<blockquote>
<ol>
<li>字符串是表示字符序列的类</li>
<li>标准的字符串类提供了对此类对象的支持，其接口类似于标准字符容器的接口，但添加了专门用于操作单字节字符字符串的设计特性。</li>
<li>string类是使用char(即作为它的字符类型，使用它的默认char_traits和分配器类型(关于模板的更多信息，请参阅basic_string)。</li>
<li>string类是basic_string模板类的一个实例，它使用char来实例化basic_string模板类，并用char_traits和allocator作为basic_string的默认参数(根于更多的模板信息请参考basic_string)。</li>
<li>注意，这个类独立于所使用的编码来处理字节:如果用来处理多字节或变长字符(如UTF-8)的序列，这个类的所有成员(如长度或大小)以及它的迭代器，将仍然按照字节(而不是实际编码的字符)来操作。</li>
</ol>
</blockquote>
<p>大概也就是看着多，这里可以这么理解：</p>
<blockquote>
<p>string是一个字符串类型</p>
<p>这个类里面的操作办法跟常规的接口相同，然后加了一些专门操作string的函数</p>
<p> string的底层是basic_string模板类的别名</p>
<p>不能操作多字节或者变成字符的序列</p>
<p>要包含头文件<string></p>
</blockquote>
<h3 id="常用的几个接口"><a href="#常用的几个接口" class="headerlink" title="常用的几个接口"></a>常用的几个接口</h3><p>初始构造：</p>
<p>string有以下几个构造的办法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string c1 = &quot;hellow world&quot;; //通过字符串的初始赋值</span><br><span class="line">	string c2(&quot;hello world&quot;);   //同上</span><br><span class="line">	string c3(c1);//拷贝构造</span><br></pre></td></tr></table></figure>



<p>除此之外，还有多种构造：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string c4(&quot;hello worldxxxxxxxx&quot;, 11); //截取长字符串的前n个字符</span><br><span class="line">    string c5(10, &#x27;c&#x27;);	//开十个字节的空间并全部初始化为字符c</span><br><span class="line">	string c6(&quot;xxxhello worldxxxx&quot;, 3, 11); //用一个常量字符串的下标为3的位置到下表为11的位置初始化c6</span><br></pre></td></tr></table></figure>



<p>总之，挺多的，当然，也可以什么都不给，创见一个空的字符串：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string c7;</span><br></pre></td></tr></table></figure>



<p>但是如果直接去打印出来看的话，会发现这里直接空的一排：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109066.png"></p>
<p>如果这个时候调试，我们会看到这样的三个数值</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109107.png"></p>
<p>size、capacity和原始试图，暂且不管最后的原始视图，先看前面两个参数：</p>
<p>size指的是string里面有的字符数量，而capacity则是还可以放多少</p>
<p>这里什么都没打印出来，里面真一个字符都没有吗？</p>
<p>倒也不是</p>
<p>因为它（C++）要兼容C，所以这里其实还有一个’\0’</p>
<hr>
<h3 id="string的容量操作"><a href="#string的容量操作" class="headerlink" title="string的容量操作"></a>string的容量操作</h3><p>这里要介绍几个常用的：</p>
<p>size：返回字符串的有效字符长度</p>
<p>length：返回字符串有效字符长度（跟上面类似，甚至功能重叠，还是C的问题…）</p>
<p>capacity：返回空间总大小</p>
<p>empty：检查字符串是否为空，如果为空就返回true，反之</p>
<p>clear：清空有效字符串</p>
<p>reserve：为字符串保留空间</p>
<p>resize：将有效字符串的个数改成n个，其他的空间会被字符填充</p>
<hr>
<p>一个一个来看：</p>
<p>size和length</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109741.png"></p>
<p>可以看到返回的字符大小里面不包括最后的’\0‘</p>
<hr>
<p>capacity</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109861.png"></p>
<p>上面我们拿一个空字符串来看的时候这里的capacity也是15，所以string的初始字符串容量是15个字节，这里提一下，如果空间不够用的话它会自动扩容，大小大概是自身的1.5倍</p>
<hr>
<p>empty</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109979.png"></p>
<hr>
<p>clear</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109054.png"></p>
<p>可以看到第一次还能打印出hello world，在用过一次clear后就什么也打印不出来了</p>
<hr>
<p>reverse</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109291.png"></p>
<p>这里，c1原本是容量是15，初始化hello world用了11的空间，剩4个，然后我reserve里面填20，让编译器给我保留20个字节的空间，它就扩容11+20 &#x3D; 31个字节的空间</p>
<p>这是往大了扩，加入我要往小了，会发生什么？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109591.png"></p>
<p>可以看倒初始的容量是15个字节，15-11&#x3D;4，这里还剩4个字节的空间，我要保留2个字节：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109691.png"></p>
<p>还是15个字节，也就是说它不会往小了改变容量，如果这里看的不详细，我们把容量变大一点：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109276.png"></p>
<p>首先15个字节的空间，开辟到200，然后再缩减到50，可以看到里面的容量还是200，没有改变。</p>
<p>再看详细一点，打开调试：</p>
<p>一开始的11个字节和15容量没有截图，这里是扩容至200：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109580.png"></p>
<p>这里已经运行到减容至50，但实际容量还是没有改变，size也没有改变：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109527.png"></p>
<hr>
<p>resize</p>
<p>同样的例子，如果用resize，会有什么变化吗？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109853.png"></p>
<p>表面上看是没有的，但如果我们打开调试：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201109839.png"></p>
<p>如果一开始创建，里面有11个字节，容量是15，剩4个，如果往下将大小调整至200：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111795.png"></p>
<p>c1的size（有效个数）来到了200，但是容量还是没有变，我们再将resize调整至50：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111384.png"></p>
<p>可以看到里面的size已经变成了50，但是capacity还是没有改变，也就是说：</p>
<p>resize和reserve都不会改变已经开好的容量，但不同的是，resize在减容的时候会将size修改至要更改的大小并用’\0‘填充</p>
<hr>
<p>这里简单总结一下</p>
<blockquote>
<p>注意：</p>
<ol>
<li>size()与length()方法底层实现原理完全相同，引入size()的原因是为了与其他容器的接口保持一致，一般情况下基本都是用size()。</li>
<li>clear()只是将string中有效字符清空，不改变底层空间大小。</li>
<li>resize(size_t n) 与 resize(size_t n, char c)都是将字符串中有效字符个数改变到n个，不同的是当字符个数增多时：resize(n)用0来填充多出的元素空间，resize(size_t n, char c)用字符c来填充多出的元素空间。注意：resize在改变元素个数时，如果是将元素个数增多，可能会改变底层容量的大小，如果是将元素个数减少，底层空间总大小不变。</li>
<li>reserve(size_t res_arg&#x3D;0)：为string预留空间，不改变有效元素个数，当reserve的参数小于string的底层空间总大小时，reserver不会改变容量大小。</li>
</ol>
</blockquote>
<hr>
<h2 id="string类对象的访问及遍历操作"><a href="#string类对象的访问及遍历操作" class="headerlink" title="string类对象的访问及遍历操作"></a>string类对象的访问及遍历操作</h2><p>先看看访问其普通成员，在C里面可以通过下标来访问，这里就直接看看代码：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111323.png"></p>
<p>普通的没有问题，如果是const：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111932.png"></p>
<p>它就会报错</p>
<p>至于为什么这里可以用方括号访问下标，其实是类里面的一个重载：</p>
<p><a class="link"   href="http://www.cplusplus.com/reference/string/string/operator%5B%5D/"  title="string::operator[] - C++ Reference (cplusplus.com)">string::operator[] - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>这里有兴趣可以看这个网站</p>
<hr>
<p>我们知道，在C语言里面有这么几种遍历字符串的操作：</p>
<p>for循环，下标</p>
<p>while指针读取</p>
<p>在C++里面，这是否也可以用？</p>
<p>直接试试：</p>
<p>for循环</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string c1 = &quot;hello world&quot;; </span><br><span class="line">	for (int i = 0; i &lt; c1.size(); i++)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; c1[i];</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>



<hr>
<p>迭代器</p>
<p>这个迭代器可以说是通用的，它是检查容器元素并遍历的一种数据类型，之后的数组等也可以用这个，这个分为以下几种：</p>
<blockquote>
<ol>
<li>正向迭代器，定义方法如下：</li>
</ol>
<p>容器类名::iterator  迭代器名;</p>
<ol start="2">
<li>常量正向迭代器，定义方法如下：</li>
</ol>
<p>容器类名::const_iterator  迭代器名;</p>
<ol start="3">
<li>反向迭代器，定义方法如下：</li>
</ol>
<p>容器类名::reverse_iterator  迭代器名;</p>
<ol start="4">
<li>常量反向迭代器，定义方法如下：</li>
</ol>
<p>容器类名::const_reverse_iterator  迭代器名;</p>
</blockquote>
<p>知道了string的迭代器，那之后再对数组遍历也差不多：</p>
<p>这里做一个示范：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string c1 = &quot;hello world&quot;; </span><br><span class="line">	string::iterator it = c1.begin();</span><br><span class="line">	while (it != c1.end())</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; *it;</span><br><span class="line">		++it;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>



<p>首先格式是</p>
<p>string::iterator X &#x3D; 数组名.begin()；</p>
<p>这里就可以得到数组的首元素地址，同理，如果将最后的begain变成end，那就可以获得数组最后一个元素的位置，就可以开始遍历；</p>
<p>这个还有另一个版本</p>
<p>rbegin和rend</p>
<p>跟上面操作是一样的，就不多说了</p>
<hr>
<p>范围for</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for (char c : c1)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; c;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>



<p>怎么说呢，这个看上去很好用，实际上还是调用的迭代器，就，看看就好</p>
<hr>
<h2 id="string类对象的修改操作"><a href="#string类对象的修改操作" class="headerlink" title="string类对象的修改操作"></a>string类对象的修改操作</h2><p>这里介绍几个常用的string的操作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">push_back      //在字符串尾插一个字符</span><br><span class="line">append         //尾插一个字符串</span><br><span class="line">operator       //在字符串后面追加字符串 （*用的较多）</span><br><span class="line">c_str          //返回C格式的字符串（*）</span><br><span class="line">find           //这两个都是字符串的查找，但是find是从前往后，rfind是从后往前</span><br><span class="line">rfind          //补充以下，这两个里面都有npos，这个指可以理解为直到字符串结束，常量定义-1</span><br></pre></td></tr></table></figure>



<p>这里看一段代码方便理解：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string str;</span><br><span class="line">	str.push_back(&#x27;x&#x27;); //尾插一个x</span><br><span class="line">	str.append(&quot;hello&quot;);//尾插字符串hello</span><br><span class="line">	str += &quot;k&quot;;			//追加插入一个字符k，‘’也可以</span><br><span class="line">	str += &#x27;nous&#x27;;		//插入字符nous</span><br><span class="line">    cout &lt;&lt; str &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; str.c_str() &lt;&lt; endl; // 以C语言的方式打印字符串</span><br></pre></td></tr></table></figure>



<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111271.png"></p>
<blockquote>
<ol>
<li>在string尾部追加字符时，s.push_back(c) &#x2F; s.append(1, c) &#x2F; s +&#x3D; ‘c’三种的实现方式差不多，一般情况下string类的+&#x3D;操作用的比较多，+&#x3D;操作不仅可以连接单个字符，还可以连接字符串。</li>
<li>对string操作时，如果能够大概预估到放多少字符，可以先通过reserve把空间预留好。</li>
</ol>
</blockquote>
<h2 id="string的类非成员函数"><a href="#string的类非成员函数" class="headerlink" title="string的类非成员函数"></a>string的类非成员函数</h2><blockquote>
<p>operator+                   不好用</p>
<p>opeartor&gt;&gt;                输入的运算符重载</p>
<p>operator&lt;&lt;                输出的运算符重载</p>
<p>getline                        获取一行字符串</p>
<p>relatinoal operator        比较大小</p>
</blockquote>
<p>这里结合几道题来了解如上接口：</p>
<p><a class="link"   href="https://leetcode.cn/problems/first-unique-character-in-a-string/"  title="字符串中的第一个唯一字符">字符串中的第一个唯一字符<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111238.png"></p>
<p>这道题有多种解法，这里说两种：</p>
<p>1、暴力，拿到一个字符就在剩下的字符串里面找，找不到就返回-1，这种方法的时间复杂度直接就是O(N^2)，虽然能写，但比较慢，所以不推荐</p>
<p>2、哈希表，拿一个数组来初始化为0，每出现一个字符就在对应位置+1，最后遍历的时候如果发现位置上有1返回对应下标即可</p>
<p>看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int firstUniqChar(string s) &#123;</span><br><span class="line">        int a[26] = &#123; 0 &#125;;              //初始化这个数组全部为0</span><br><span class="line"></span><br><span class="line">        for(int i = 0;i&lt;s.length();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            ++a[s[i]-&#x27;a&#x27;];               //遍历字符串在对应位置+1，因为这里用的是相对位置，&#x27;a&#x27;-&#x27;a&#x27;=0</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        for(int i = 0;i&lt;s.length();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            if(a[s[i]-&#x27;a&#x27;] == 1)</span><br><span class="line">            &#123;</span><br><span class="line">                   return i;               //最后遍历判断</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return -1;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>



<p>这道题也算是比较简单的那种，再来看下面一道题：</p>
<p><a class="link"   href="https://www.nowcoder.com/practice/8c949ea5f36f422594b306a2300315da?tpId=37&&tqId=21224&rp=5&ru=/activity/oj&qru=/ta/huawei/question-ranking"  title="字符串最后一个单词的长度_牛客题霸_牛客网 (nowcoder.com)">字符串最后一个单词的长度_牛客题霸_牛客网 (nowcoder.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111029.png"></p>
<p>这里就可以用到刚刚说的getline和rfind，因为如果你用cin的话，就会出现如下情况：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111938.png"></p>
<p>很奇怪，为什么这里输出的是5呢？</p>
<p>这里是五字节的字符串只有hello，难道只有hello被算进去了吗？</p>
<p>来到vs里面调试看看</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111098.png"></p>
<p>确实，里面只有一个hello，空格后面的字符串它并没有算进去，这个时候get_line就可以用上，它不管这一句里面有没有空格，它都只会获取一行：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111891.png"></p>
<p>看看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;iostream&gt;</span><br><span class="line">#include&lt;string&gt;</span><br><span class="line">using namespace std;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    string str;            //一个字符串来接收</span><br><span class="line">    getline(cin,str);    //获取输入         </span><br><span class="line">    size_t pos = str.rfind(&#x27; &#x27;);        //找最后一个字符串前面的空格</span><br><span class="line">    cout&lt;&lt;str.length()-1-pos&lt;&lt;endl;     //因为找到的是空格所以要拿总长度-找到空格的位置</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="模拟实现"><a href="#模拟实现" class="headerlink" title="模拟实现"></a>模拟实现</h2><p>从创建开始，拿一个单独的命名空间来写my_string:</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111322.png"></p>
<p>里面有几个参数要注意：</p>
<p>第一个是放字符串的地方，这里是用的char*的指针来完成</p>
<p>第二个是容量，记录最大的有效字符能放多少</p>
<p>第三个是记录已经放了多少</p>
<hr>
<h3 id="string的创建"><a href="#string的创建" class="headerlink" title="string的创建"></a>string的创建</h3><p>常用的string有这么几种创建方式：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111575.png"></p>
<p>1、创建一个空串</p>
<p>2、拿一个字符串去创建</p>
<p>3、拿一个string去赋值给另一个string</p>
<p>这里要注意的是，string的拷贝问题：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111786.png"></p>
<p>现在s1已经指向了一块空间，这里面假设有一大堆数据，如果是浅拷贝，就会出现这样的情况：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201111147.png"></p>
<p>s2指向的也是s1的空间，那么对s2的修改也会影响到s1，到时候析构的时候这块空间会析构两次，如果一块空间析构两次会发生什么？</p>
<p>报错，然后调试半天开始找错….当真痛苦</p>
<p>所以这里我们要用的是深拷贝，让s2指向的是另一块空间，然后将s1的内容拷贝进来：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112449.png"></p>
<p>这里我们就可以开始写了，先写第一种情况，一个空串，不过在写之前，要先看看这个所谓的空空串里面到底是什么东西：</p>
<p>这里打开调试</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112687.png"></p>
<p>可以看到空串就是“”,一个字符串里面什么都可以，但是我们又直到字符串里面会默认自带一个\0表示字符串的结束，所以我们可以理解为所谓的空串就是一个“”</p>
<p>这个空串和后面str2的创建，是不是很像？</p>
<p>只不过一个字符串里面有数据，一个没有，所以这两个我们可以一起写：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string(const char* str = &quot;&quot;)</span><br><span class="line">		&#123;</span><br><span class="line">			_str = new char[strlen(str) + 1];</span><br><span class="line">			strcpy(_str, str);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这串字符我们不会用于修改，所以可以加上const，同时如果没有传进来str，那我们用缺省值来初始化_str</p>
<p>注意这里开辟空间的时候，如果有十个字节，那我们就只开辟十个字节？</p>
<p>别忘了最后还有一个\0，所以我们要多开辟一个字节的空间</p>
<p>用一个string来初始化另一个string，这个思路跟上面差不多，接收参数然后开空间拷贝：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string(const string&amp; s)</span><br><span class="line">		&#123;</span><br><span class="line">			_str = new char[strlen(s._str) + 1];</span><br><span class="line">			strcpy(_str, s._str);</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>注意这里接收的是参数的引用，如果是传形参的话要多拷贝一份，在效率上不入引用，为了保护s不被修改，所以这里加上了const，开空间是往string s里面的参数_str开，这里可不能直接用s开辟空间。</p>
<p>用一个string来赋值另一个string，这里思路就要有点变换，具体先看下面的代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string&amp; operator=(const string &amp;s)</span><br><span class="line">		&#123;</span><br><span class="line">			_str = new char[strlen(s._str) + 1];</span><br><span class="line">			strcpy(_str, s._str);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这里是直接开辟凯空间然后赋值，对吗？</p>
<p>有没有可能，s1 &#x3D; s1，自己给自己赋值？</p>
<p>s2 &#x3D; s1，那s2里面的值应该都是s1的值，s2原本的数据应该被覆盖才对</p>
<p>如果是按照上面写的话，bug就来了</p>
<p>这里看一下标注的写法，这里先判断自己给自己赋值：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if (this == &amp;s)</span><br><span class="line">			&#123;</span><br><span class="line">				return *this;</span><br><span class="line">			&#125;</span><br></pre></td></tr></table></figure>



<p>加一条判断就好了，随后覆盖的问题，我们先将原来的空间释放，再重新开辟一块空间来拷贝</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string&amp; operator=(const string &amp;s)</span><br><span class="line">		&#123;</span><br><span class="line">			if (this == &amp;s)</span><br><span class="line">			&#123;</span><br><span class="line">				return *this;</span><br><span class="line">			&#125;</span><br><span class="line">			delete[] _str;</span><br><span class="line">			_str = new char[strlen(s._str) + 1];</span><br><span class="line">			strcpy(_str, s._str);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>看看效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112815.png"></p>
<h3 id="析构"><a href="#析构" class="headerlink" title="析构"></a>析构</h3><p>析构也算简单，string有三个参数：</p>
<p>char*的指针</p>
<p>capacity</p>
<p>size</p>
<p>指针是new开出来了，所以直接delete释放，剩下两个参可以置零，也可以不管，这里我是直接将其置零：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">~string()</span><br><span class="line">		&#123;</span><br><span class="line">			delete[] _str;</span><br><span class="line">			_size = 0;</span><br><span class="line">			_capacity = 0;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h3 id="移动（操作概述）"><a href="#移动（操作概述）" class="headerlink" title="移动（操作概述）"></a>移动（操作概述）</h3><p>在模拟实现string的时候，还是先来看看对string常用的一些操作：</p>
<blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void push_back(char c);                        //尾插一个字符</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">string&amp; operator+=(char c);                   //+=一个字符，跟尾插差不多</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void append(const char* str);                //在字符串后面链接一个str</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">string&amp; operator+=(const char* str);      //尾插一个字符串</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void clear();                                            //清空</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void swap(string&amp; s);                             //交换</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">const char* c_str()const;                        //将其以变为字符串返回</span><br></pre></td></tr></table></figure>
</blockquote>
<p>先从第一个开始，尾插一个字符</p>
<p>在插入之前，我们要解决一下在初始化的时候未能解决的两个参数，第一个是size，第二个是capacity</p>
<p>size大小可以直接用strlen算出来，capacity也可以直接用size</p>
<p>插入的时候在size的位置插入即可，这里可以看图理解：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112396.png"></p>
<p>strlen求出来的是5，而5个字符串最大的下标是4，所以我们直接在size的位置放上新字符即可，不过在增加之前，我们还是先写一个函数来解决一下扩容问题，如果位置不够，我们就要调整空间，这一部分可以参考顺序表，其实思路都是差不多的。</p>
<h3 id="扩容"><a href="#扩容" class="headerlink" title="扩容"></a>扩容</h3><p>c++的扩容有两种，在上面也有提到过</p>
<p>reserve：为字符串保留空间</p>
<p>resize：将有效字符串的个数改成n个，其他的空间会被字符填充</p>
<p>这里就分别来实现一下</p>
<p>reserve</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void reserve(size_t n)</span><br><span class="line">		&#123;</span><br><span class="line">			if (n &gt; _capacity-_size)</span><br><span class="line">			&#123;</span><br><span class="line">				char* tmp = new char[n + _capacity+1];</span><br><span class="line">				strcpy(tmp, _str);</span><br><span class="line">				delete[] _str;</span><br><span class="line">				_str = tmp;</span><br><span class="line"></span><br><span class="line">				_capacity = n+_capacity;</span><br><span class="line">			&#125;</span><br></pre></td></tr></table></figure>



<p>因为reserve是调整字符串的保留空间，所以只需要判断n大于剩余容量就调整即可</p>
<p>resize就不一样了，它可以将空间扩容也可以减小，这里要两种情况：</p>
<p>第一种，扩大空间，那就要重新开辟空间然后拷贝，再拿字符填充</p>
<p>第二种，缩小空间，那直接在_size的位置放上’\0‘就可以表示字符串缩小，最后调整一下_size和_capacity</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112692.png"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void reserve(size_t n)</span><br><span class="line">		&#123;</span><br><span class="line">			if (n &gt; _capacity-_size)</span><br><span class="line">			&#123;</span><br><span class="line">				char* tmp = new char[n + _capacity+1];</span><br><span class="line">				strcpy(tmp, _str);</span><br><span class="line">				delete[] _str;</span><br><span class="line">				_str = tmp;</span><br><span class="line"></span><br><span class="line">				_capacity = n+_capacity;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void resize(size_t n, char c = &#x27;\0&#x27;)</span><br><span class="line">		&#123;</span><br><span class="line">			if (n &lt; _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				_size = n;</span><br><span class="line">				_str[_size] = &#x27;\0&#x27;;</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">                _size = strlen(_str);</span><br><span class="line">				reserve(n);</span><br><span class="line">				for (int i = _size; i &lt; n; i++)</span><br><span class="line">				&#123;</span><br><span class="line">					_str[i] = c;</span><br><span class="line">				&#125;</span><br><span class="line">				_size = n;</span><br><span class="line">				_str[_size] = &#x27;\0&#x27;;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这里看一下代码，首先reserve只会往大了开辟空间，不会缩小空间，所以进来要先判断，先开空间拷贝，如果上来直接就释放旧空间，有可能发生空间释放了但是新的空间还没有开辟的情况，这点要注意</p>
<p>resize如果往小了开就直接将最后的位置放上’\0’，如果往大了开我们直接调用reserve，然后在加长看的位置填充字符，这里缺省值是’\0’，确保没有传字符串进来字符也有默认参数。</p>
<p>写完了空间的调整，来看一下效果</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112594.png"></p>
<p>reserve调整到了20，随后看看resize</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112541.png"></p>
<p>这里的确把_size调整到了n的大小，但是因为我没有给具体字符所以默认的用的是’ 0‘,这里给个字符试试</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112926.png"></p>
<p>ok，下面就正式的来模拟插入删除操作</p>
<hr>
<h3 id="移动-（模拟）"><a href="#移动-（模拟）" class="headerlink" title="移动 （模拟）"></a>移动 （模拟）</h3><p>尾插一个字符，没什么好说的，直接插入即可，不过要先看看容量够不够</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void push_back(char c)</span><br><span class="line">		&#123;</span><br><span class="line">			if (_size == _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				reserve(_capacity == 0 ? 4 : _capacity * 2);</span><br><span class="line">			&#125;</span><br><span class="line">			_str[_size++] = &#x27;c&#x27;;</span><br><span class="line">			_str[_size] = &#x27;\0&#x27;;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>上来判断空间，然后直接赋值即可</p>
<p>看看效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112512.png"></p>
<p>空间应该是不够的，所以要先开辟空间再赋值，直接下一步；</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112315.png"></p>
<p>没问题，下一步</p>
<p>+&#x3D;</p>
<p>这个思路跟上面的差不多，可以直接复用，注意最后返回的是什么</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string&amp; operator+=(char c)</span><br><span class="line">		&#123;</span><br><span class="line">			push_back(&#x27;c&#x27;);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>注意返回的是string的引用方便连续赋值，this是指向string的指针，我们解引用返回的就是string</p>
<p>append</p>
<p>连接一个字符串</p>
<p>怎么说呢，大体思路跟上面插入一个字符差不多，先算空间大小，再连接进去，这里看看代码方便理解：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void append(const char* str)</span><br><span class="line">		&#123;</span><br><span class="line">			int len = strlen(str);</span><br><span class="line">			if (_size + len &gt; _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				reserve(_size + len);</span><br><span class="line">			&#125;</span><br><span class="line">			strcpy(_str + _size, str);</span><br><span class="line">			_size += len;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>先看空间容量，然后直接拷贝过去即可，最后修改_size</p>
<p>+&#x3D;字符串</p>
<p>既然+&#x3D;可以用push_back来复用，这里也一样，直接用append即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string&amp; operator+=(const char* str)</span><br><span class="line">		&#123;</span><br><span class="line">			append(str);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="insert"><a href="#insert" class="headerlink" title="insert"></a>insert</h3><p>这又分为两种，中间插入一个字符，中间插入一个字符串，先看插入一个字符</p>
<p>先算容量，然后把元素右移，最后放进去即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string&amp; insert(size_t pos, char ch)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos &lt;= _size);</span><br><span class="line">			if (_size == _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				reserve(_capacity == 0 ? 4 : _capacity * 2);</span><br><span class="line">			&#125;</span><br><span class="line">			int end = _size+1;</span><br><span class="line">			while (pos &lt; end)</span><br><span class="line">			&#123;</span><br><span class="line">				_str[end] = _str[end -1];</span><br><span class="line">				end--;</span><br><span class="line">			&#125;</span><br><span class="line">			_str[pos] = ch;</span><br><span class="line">			_size++;</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>因为要插入的是一个字符，所以只+1就可以了</p>
<p>插入一个字符串，同样，算大小，然后移动插入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string&amp; insert(size_t pos, const char* str)</span><br><span class="line">		&#123;</span><br><span class="line">			int len = strlen(str);</span><br><span class="line">			int end = _size + len;</span><br><span class="line">			if (_size == _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				reserve( _capacity+len);</span><br><span class="line">			&#125;</span><br><span class="line">			while (end&gt;pos+len-1)</span><br><span class="line">			&#123;</span><br><span class="line">				_str[end] = _str[end - len];</span><br><span class="line">				end--;</span><br><span class="line">			&#125;</span><br><span class="line">			strncpy(_str + pos, str, len);</span><br><span class="line">			_size += len;</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这里一定要注意的是while的条件，这里是比较难以把握的一个空间分界点，这里我们可以看图理解</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112250.png"></p>
<h3 id="earse删除一个区间的字符串"><a href="#earse删除一个区间的字符串" class="headerlink" title="earse删除一个区间的字符串"></a>earse删除一个区间的字符串</h3><p>这个也算好说的，先分情况：</p>
<p>如果删的长度很长，直接删完，或者是删的位置+长度很长，那可以直接在删的位置放上’\0’表示字符串的结束，再将_size缩小</p>
<p>另一种情况就要考虑将后面的字符往前移，看代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string&amp; earse(size_t pos, size_t len = npos)</span><br><span class="line">		&#123;</span><br><span class="line">			if (len = npos || pos + len &gt; _size)</span><br><span class="line">			&#123;</span><br><span class="line">				_str[pos] = &#x27;\0&#x27;;</span><br><span class="line">				_size = pos;</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				int begin = pos + len;</span><br><span class="line">				while (begin &lt; _size - len)</span><br><span class="line">				&#123;</span><br><span class="line">					_str[begin - len] = _str[begin];</span><br><span class="line">					begin--;</span><br><span class="line">				&#125;</span><br><span class="line">				_size -= len;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="clear"><a href="#clear" class="headerlink" title="clear"></a>clear</h3><p>这个其实用到的不是特别多，但在这里说一下，clear的原理其实就是在字符串的第一个位置将其放上’\n‘来直接换行，所以这里就直接写了</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void clear()</span><br><span class="line">		&#123;</span><br><span class="line">			_str[0] = &#x27;\n&#x27;;</span><br><span class="line">			_size = _capacity = 0;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="swap"><a href="#swap" class="headerlink" title="swap"></a>swap</h3><p>这个其实也没什么好说的，就是交换两个string的每个参数…</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void swap(string&amp; s)</span><br><span class="line">		&#123;</span><br><span class="line">			char* tmp = new char[strlen(s._str)+1];</span><br><span class="line">			int tmp_size = _size,tmp_capacity = _capacity;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">			_str = s._str;</span><br><span class="line">			_size = s._size;</span><br><span class="line">			_capacity = s._capacity;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">			s._str = tmp;</span><br><span class="line">			s._size = tmp_size;</span><br><span class="line">			s._capacity = tmp_capacity;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="c-str"><a href="#c-str" class="headerlink" title="c_str"></a>c_str</h3><p>直接返回_str即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const char* c_str()const</span><br><span class="line">		&#123;</span><br><span class="line">			return _str;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="capacity相关操作"><a href="#capacity相关操作" class="headerlink" title="capacity相关操作"></a>capacity相关操作</h3><p>因为resize和reserve在之前已经写过，所以这里只写几个查看</p>
<blockquote>
<p>size_t size()const  &#x2F;&#x2F;查看size</p>
<p>size_t capacity()const        &#x2F;&#x2F;查看capacity</p>
<p>bool empty()const                &#x2F;&#x2F;看看是否为空</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">size_t size()const</span><br><span class="line">		&#123;</span><br><span class="line">			return _size;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">size_t capacity()const</span><br><span class="line">		&#123;</span><br><span class="line">			return _capacity;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">bool empty()const</span><br><span class="line">		&#123;</span><br><span class="line">			return _size == 0;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="重载"><a href="#重载" class="headerlink" title="重载[]"></a>重载[]</h3><p>这个也算是简单的，我们只需要返回这个位置的字符即可，但是要注意一点，这里返回的类型，因为有的地方只读，有的地方要读还要写，所以最好写成一个重载</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char&amp; operator[](size_t index)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(index &lt; _size);</span><br><span class="line">			return _str[index];</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">const char&amp; operator[](size_t index)const</span><br><span class="line">		&#123;</span><br><span class="line">			assert(index &lt; _size);</span><br><span class="line"></span><br><span class="line">			return _str[index];</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="一些比较操作符"><a href="#一些比较操作符" class="headerlink" title="一些比较操作符"></a>一些比较操作符</h3><p>其实我也不知道这东西会不会用上，但这里还是写上</p>
<blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool operator&lt;(const string&amp; s);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">bool operator&lt;=(const string&amp; s);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">bool operator&gt;(const string&amp; s);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">bool operator&gt;=(const string&amp; s);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">bool operator==(const string&amp; s);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">bool operator!=(const string&amp; s);</span><br></pre></td></tr></table></figure>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool operator&lt;(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return strcmp(s1.c_str, s2.c_str) &lt; 0;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	bool operator==(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return strcmp(s1.c_str, s2.c_str) == 0;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	bool operator&lt;=(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return s1 &lt; s2 || s1 == s2;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	bool operator&gt;(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return !(s1 &lt;= s2);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	bool operator&gt;=(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return s1 &gt; s2 || s1 == s2;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	bool operator!=(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return !(s1 == s2);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>



<p>其实基本上都是代码的复用，只需要写一个小于和等于就好，偏偏这个比较还有函数直接可以使用，算是比较简单的</p>
<h3 id="find"><a href="#find" class="headerlink" title="find"></a>find</h3><p>查找某个字符或者是字符串，直接暴力走起</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 返回c在string中第一次出现的位置</span><br><span class="line">        size_t find(char c, size_t pos = 0) const</span><br><span class="line">		&#123;</span><br><span class="line">			for (int i = pos; i &lt; _size; i++)</span><br><span class="line">			&#123;</span><br><span class="line">				if (_str[i] == c)</span><br><span class="line">				&#123;</span><br><span class="line">					return i;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			return npos;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		// 返回子串s在string中第一次出现的位置</span><br><span class="line"></span><br><span class="line">		size_t find(const char* s, size_t pos = 0) const</span><br><span class="line">		&#123;</span><br><span class="line">			char* tmp = strstr( _str + pos,s);</span><br><span class="line">			if (!tmp)</span><br><span class="line">			&#123;</span><br><span class="line">				return npos;</span><br><span class="line">			&#125;</span><br><span class="line">			return tmp - _str;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h3><p>怎么说呢，这个东西暂时可以理解为是一个char*的指针…</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef char* iterator;</span><br><span class="line">		iterator begin()</span><br><span class="line">		&#123;</span><br><span class="line">			return _str;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator end()</span><br><span class="line">		&#123;</span><br><span class="line">			return _str + _size;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>流插入流提取</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">friend ostream&amp; operator&lt;&lt;(ostream&amp; _cout, const knous::string&amp; s)</span><br><span class="line">		&#123;</span><br><span class="line">			for (auto ch : s)</span><br><span class="line">			&#123;</span><br><span class="line">				out &lt;&lt; ch;</span><br><span class="line">			&#125;</span><br><span class="line">			return out;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		friend ostream&amp; operator&lt;&lt;(ostream&amp; _cout, const knous::string&amp; s)</span><br><span class="line">		&#123;</span><br><span class="line">			char ch;</span><br><span class="line">			in &gt;&gt; ch;</span><br><span class="line">			while (ch != &#x27; &#x27; || ch != &#x27;\0&#x27;)</span><br><span class="line">			&#123;</span><br><span class="line">				s += ch;</span><br><span class="line">				in &gt;&gt; ch;</span><br><span class="line">				ch = in.get();</span><br><span class="line">			&#125;</span><br><span class="line">			return in;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>….</p>
<p>基础演示：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201112079.png"></p>
<p>全部代码；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;iostream&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">using namespace std;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">namespace knous</span><br><span class="line">&#123;</span><br><span class="line">	class string</span><br><span class="line"></span><br><span class="line">	&#123;</span><br><span class="line">	public:</span><br><span class="line">		</span><br><span class="line">		string(const char* str = &quot;&quot;)</span><br><span class="line">		&#123;</span><br><span class="line">			_str = new char[strlen(str) + 1];</span><br><span class="line">			strcpy(_str, str);</span><br><span class="line">			_size = strlen(_str);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		string(const string&amp; s)</span><br><span class="line">		&#123;</span><br><span class="line">			_str = new char[strlen(s._str) + 1];</span><br><span class="line">			strcpy(_str, s._str);</span><br><span class="line">			_size = strlen(_str);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		string&amp; operator=(const string &amp;s)</span><br><span class="line">		&#123;</span><br><span class="line">			if (this == &amp;s)</span><br><span class="line">			&#123;</span><br><span class="line">				return *this;</span><br><span class="line">			&#125;</span><br><span class="line">			delete[] _str;</span><br><span class="line">			_str = new char[strlen(s._str) + 1];</span><br><span class="line">			strcpy(_str, s._str);</span><br><span class="line">			_size = strlen(_str);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		~string()</span><br><span class="line">		&#123;</span><br><span class="line">			delete[] _str;</span><br><span class="line">			_size = 0;</span><br><span class="line">			_capacity = 0;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		void reserve(size_t n)</span><br><span class="line">		&#123;</span><br><span class="line">			if (n &gt; _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				char* tmp = new char[n +1];</span><br><span class="line">				strcpy(tmp, _str);</span><br><span class="line">				delete[] _str;</span><br><span class="line">				_str = tmp;</span><br><span class="line"></span><br><span class="line">				_capacity = n;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void resize(size_t n, char c = &#x27;\0&#x27;)</span><br><span class="line">		&#123;</span><br><span class="line">			if (n &lt; _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				_size = n;</span><br><span class="line">				_str[_size] = &#x27;\0&#x27;;</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				_size = strlen(_str);</span><br><span class="line">				reserve(n);</span><br><span class="line">				for (size_t i = _size; i &lt; n; i++)</span><br><span class="line">				&#123;</span><br><span class="line">					_str[i] = c;</span><br><span class="line">				&#125;</span><br><span class="line">				_size = n;</span><br><span class="line">				_str[_size] = &#x27;\0&#x27;;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void push_back(char c)</span><br><span class="line">		&#123;</span><br><span class="line">			_size = strlen(_str);</span><br><span class="line">			if (_size == _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				reserve(_capacity == 0 ? 4 : _capacity * 2);</span><br><span class="line">			&#125;</span><br><span class="line">			_str[_size++] = &#x27;c&#x27;;</span><br><span class="line">			_str[_size] = &#x27;\0&#x27;;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		string&amp; operator+=(char c)</span><br><span class="line">		&#123;</span><br><span class="line">			push_back(&#x27;c&#x27;);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void append(const char* str)</span><br><span class="line">		&#123;</span><br><span class="line">			int len = strlen(str);</span><br><span class="line">			_size = strlen(_str);</span><br><span class="line">			if (_size + len &gt; _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				reserve(_size + len);</span><br><span class="line">			&#125;</span><br><span class="line">			strcpy(_str + _size, str);</span><br><span class="line">			_size += len;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		string&amp; operator+=(const char* str)</span><br><span class="line">		&#123;</span><br><span class="line">			append(str);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		string&amp; insert(size_t pos, char ch)</span><br><span class="line">		&#123;</span><br><span class="line">			_size = strlen(_str);</span><br><span class="line">			assert(pos &lt;= _size);</span><br><span class="line">			if (_size == _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				reserve(_capacity == 0 ? 4 : _capacity * 2);</span><br><span class="line">			&#125;</span><br><span class="line">			size_t end = _size+1;</span><br><span class="line">			while (pos &lt; end)</span><br><span class="line">			&#123;</span><br><span class="line">				_str[end] = _str[end -1];</span><br><span class="line">				end--;</span><br><span class="line">			&#125;</span><br><span class="line">			_str[pos] = ch;</span><br><span class="line">			_size++;</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		string&amp; insert(size_t pos, const char* str)</span><br><span class="line">		&#123;</span><br><span class="line">			_size = strlen(_str);</span><br><span class="line">			size_t len = strlen(str);</span><br><span class="line">			size_t end = _size + len;</span><br><span class="line">			if (_size == _capacity)</span><br><span class="line">			&#123;</span><br><span class="line">				reserve( _capacity+len);</span><br><span class="line">			&#125;</span><br><span class="line">			while (end &gt; pos + len-1)</span><br><span class="line">			&#123;</span><br><span class="line">				_str[end] = _str[end - len];</span><br><span class="line">				end--;</span><br><span class="line">			&#125;</span><br><span class="line">			strncpy(_str + pos, str, len);</span><br><span class="line">			_size += len;</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		string&amp; earse(size_t pos, size_t len = npos)</span><br><span class="line">		&#123;</span><br><span class="line">			_size = strlen(_str);</span><br><span class="line">			if (len == npos || pos + len &gt; _size)</span><br><span class="line">			&#123;</span><br><span class="line">				_str[pos] = &#x27;\0&#x27;;</span><br><span class="line">				_size = pos;</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				size_t begin = pos + len;</span><br><span class="line">				while (begin &lt;= _size )</span><br><span class="line">				&#123;</span><br><span class="line">					_str[begin - len] = _str[begin];</span><br><span class="line">					begin++;</span><br><span class="line">				&#125;</span><br><span class="line">				_size -= len;</span><br><span class="line">			&#125;</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void clear()</span><br><span class="line">		&#123;</span><br><span class="line">			_str[0] = &#x27;\n&#x27;;</span><br><span class="line">			_size = _capacity = 0;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		void swap(string&amp; s)</span><br><span class="line">		&#123;</span><br><span class="line">			_size = strlen(_str);</span><br><span class="line">			char* tmp = new char[strlen(s._str)+1];</span><br><span class="line">			int tmp_size = _size,tmp_capacity = _capacity;</span><br><span class="line">			_str = s._str;</span><br><span class="line">			_size = s._size;</span><br><span class="line">			_capacity = s._capacity;</span><br><span class="line">			s._str = tmp;</span><br><span class="line">			s._size = tmp_size;</span><br><span class="line">			s._capacity = tmp_capacity;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const char* c_str()const</span><br><span class="line">		&#123;</span><br><span class="line">			return _str;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		size_t size()const</span><br><span class="line">		&#123;</span><br><span class="line">			</span><br><span class="line">			return strlen(_str);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		size_t capacity()const</span><br><span class="line">		&#123;</span><br><span class="line">			return _capacity;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		bool empty()const</span><br><span class="line">		&#123;</span><br><span class="line">			return _size == 0;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		char&amp; operator[](size_t index)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(index &lt; _size);</span><br><span class="line">			return _str[index];</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const char&amp; operator[](size_t index)const</span><br><span class="line">		&#123;</span><br><span class="line">			assert(index &lt; _size);</span><br><span class="line"></span><br><span class="line">			return _str[index];</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		// 返回c在string中第一次出现的位置</span><br><span class="line"></span><br><span class="line">		size_t find(char c, size_t pos = 0) const</span><br><span class="line">		&#123;</span><br><span class="line">			for (size_t i = pos; i &lt; _size; i++)</span><br><span class="line">			&#123;</span><br><span class="line">				if (_str[i] == c)</span><br><span class="line">				&#123;</span><br><span class="line">					return i;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			return npos;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		// 返回子串s在string中第一次出现的位置</span><br><span class="line"></span><br><span class="line">		size_t find(const char* s, size_t pos = 0) const</span><br><span class="line">		&#123;</span><br><span class="line">			char* tmp = strstr( _str + pos,s);</span><br><span class="line">			if (!tmp)</span><br><span class="line">			&#123;</span><br><span class="line">				return npos;</span><br><span class="line">			&#125;</span><br><span class="line">			return tmp - _str;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		typedef char* iterator;</span><br><span class="line">		typedef const char* const_iterator;</span><br><span class="line"></span><br><span class="line">		const_iterator begin() const</span><br><span class="line">		&#123;</span><br><span class="line">			return _str;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_iterator end() const</span><br><span class="line">		&#123;</span><br><span class="line">			return _str + _size;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		friend ostream&amp; operator&lt;&lt;(ostream&amp; out, const knous::string&amp; s)</span><br><span class="line">		&#123;</span><br><span class="line">			for (auto ch : s)</span><br><span class="line">			&#123;</span><br><span class="line">				cout &lt;&lt; ch;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			return cout;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		friend istream&amp; operator&gt;&gt;(istream&amp; in, knous::string&amp; s)</span><br><span class="line">		&#123;</span><br><span class="line">			char ch;</span><br><span class="line">			in &gt;&gt; ch;</span><br><span class="line">			while (ch != &#x27; &#x27; || ch != &#x27;\0&#x27;)</span><br><span class="line">			&#123;</span><br><span class="line">				</span><br><span class="line">				s += ch;</span><br><span class="line">				in &gt;&gt; ch;</span><br><span class="line">				ch = in.get();</span><br><span class="line">			&#125;</span><br><span class="line">			return in;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line">		char* _str;</span><br><span class="line">		size_t _capacity;</span><br><span class="line">		size_t _size;</span><br><span class="line">		const static int  npos;</span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	bool operator&lt;(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return strcmp(s1.c_str(), s2.c_str()) &lt; 0;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	bool operator==(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return strcmp(s1.c_str(), s2.c_str()) == 0;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	bool operator&lt;=(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return s1 &lt; s2 || s1 == s2;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	bool operator&gt;(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return !(s1 &lt;= s2);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	bool operator&gt;=(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return s1 &gt; s2 || s1 == s2;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	bool operator!=(const string&amp; s1, const string&amp; s2)</span><br><span class="line">	&#123;</span><br><span class="line">		return !(s1 == s2);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line">const int knous::string::npos = -1;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>vector</title>
    <url>/2022/07/11/vector/</url>
    <content><![CDATA[<hr>
<h1 id="vector的介绍和使用"><a href="#vector的介绍和使用" class="headerlink" title="vector的介绍和使用"></a>vector的介绍和使用</h1><h2 id="vector的介绍"><a href="#vector的介绍" class="headerlink" title="vector的介绍"></a>vector的介绍</h2><blockquote>
<p>vector是<a class="link"   href="https://baike.baidu.com/item/C%2B%2B"  title="C++">C++<i class="fas fa-external-link-alt"></i></a><a class="link"   href="https://baike.baidu.com/item/%E6%A0%87%E5%87%86%E6%A8%A1%E6%9D%BF%E5%BA%93"  title="标准模板库">标准模板库<i class="fas fa-external-link-alt"></i></a>中的部分内容，中文偶尔译作“容器”，但并不准确。它是一个多功能的，能够操作多种数据结构和算法的模板类和函数库。vector之所以被认为是一个容器，是因为它能够像容器一样存放各种类型的对象，简单地说，vector是一个能够存放任意类型的<a class="link"   href="https://baike.baidu.com/item/%E5%8A%A8%E6%80%81%E6%95%B0%E7%BB%84"  title="动态数组">动态数组<i class="fas fa-external-link-alt"></i></a>，能够增加和压缩数据。 [1]<a href=""> </a></p>
</blockquote>
<p>如何理解这段话呢，可以把它想像成一个数组，这里面可以放任意类型的数据</p>
<p>如果是int，那就是一个整型数组</p>
<p>如果是char，那就有点类似string</p>
<p>它还可以是vecto类似，这就相当于是一个二维数组了…</p>
<h2 id="vector的使用"><a href="#vector的使用" class="headerlink" title="vector的使用"></a>vector的使用</h2><p><a class="link"   href="https://m.cplusplus.com/reference/vector/vector/"  title="vector - C++ Reference (cplusplus.com)">vector - C++ Reference (cplusplus.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>虽然是全英文，但有的游览器会自带翻译，这里说个大概用法：跟string差不多，创建类后用.函数名来使用</p>
<h3 id="vector的定义"><a href="#vector的定义" class="headerlink" title="vector的定义"></a>vector的定义</h3><p>有以下几种：</p>
<table>
<thead>
<tr>
<th>vector()</th>
<th>无参构造</th>
</tr>
</thead>
<tbody><tr>
<td>vector（size_type n, const value_type&amp; val &#x3D; value_type()）</td>
<td>构造并初始化n个val</td>
</tr>
<tr>
<td>vector (const vector&amp; x);</td>
<td>拷贝构造</td>
</tr>
<tr>
<td>vector (InputIterator first, InputIterator last);</td>
<td>使用迭代器进行初始化构造</td>
</tr>
</tbody></table>
<p>一般常用的是无参和拷贝，因为数据的插入和删除也算比较简单的，所以构造并初始化和使用迭代器进行构造的也算比较少</p>
<h3 id="vector-iterator"><a href="#vector-iterator" class="headerlink" title="vector iterator"></a>vector iterator</h3><p>这个在string里面是迭代器，用来遍历string用的，在vector里面使用的办法也一样，用来遍历vector</p>
<p>这里可以看图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201142041.png"></p>
<p>就是一个正向一个反向，其实这里用法跟string一样，不过还是演示一下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vector&lt;int&gt; v;</span><br><span class="line">	v.push_back(1);</span><br><span class="line">	v.push_back(2);</span><br><span class="line">	v.push_back(3);</span><br><span class="line">	v.push_back(4);</span><br><span class="line">	vector&lt;int&gt;::iterator it = v.begin();</span><br><span class="line">	while (it != v.end())</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; *it &lt;&lt;&quot; &quot;;</span><br><span class="line">		++it;</span><br><span class="line">	&#125;</span><br><span class="line">	cout &lt;&lt; endl;</span><br><span class="line">	return 0;</span><br></pre></td></tr></table></figure>



<p>如上代码。创建了一个vector<int>并插入了1234，并用迭代器进行打印，这里要记得++it，不然会死循环，打印效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201142038.png"></p>
<p>这里要提一下，迭代器有两种，第一种是没有const的，这种是可以遍历的时候修改的，另一种带const的就是单纯的遍历</p>
<p>如果对应的vector是带有const修饰的，那编译器会自动调用带const的迭代器</p>
<p>不过这里要注意，如果是带有const的vector，那在迭代前面也要加上const，像这样：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vector&lt;int&gt;::const_iterator it = v.begin();</span><br><span class="line">	while (it != v.end())</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; *it &lt;&lt; &quot; &quot;;</span><br><span class="line">		++it;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>



<p>不然会出现这样的错误；</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201143718.png"></p>
<p>因为普通的it的不带const的，它可以修改对应的值，而const的修饰就是为了不让修改，所以出现了权限放大的问题，这里要注意，带上const就好了，下面是效果的演示</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201143964.png"></p>
<p>vector的空间</p>
<p>这里大致看一下就好了，跟string其实很像，前面有不了解的可以去看前一篇我的string讲解</p>
<table>
<thead>
<tr>
<th>容量空间</th>
<th>接口说明</th>
</tr>
</thead>
<tbody><tr>
<td>size</td>
<td>获取数据个数</td>
</tr>
<tr>
<td>capacity</td>
<td>获取容量大小</td>
</tr>
<tr>
<td>empty</td>
<td>判断是否为空</td>
</tr>
<tr>
<td>resize</td>
<td>改变vector的size</td>
</tr>
<tr>
<td>reserve</td>
<td>改变vector放入capacity</td>
</tr>
</tbody></table>
<p>这里的用法也跟string一样，直接对象名.函数名就可以了</p>
<h3 id="vector的常用操作"><a href="#vector的常用操作" class="headerlink" title="vector的常用操作"></a>vector的常用操作</h3><table>
<thead>
<tr>
<th>push_back</th>
<th>尾插</th>
</tr>
</thead>
<tbody><tr>
<td>pop_back</td>
<td>尾删</td>
</tr>
<tr>
<td>find</td>
<td>查找</td>
</tr>
<tr>
<td>insert</td>
<td>在position之前插入val</td>
</tr>
<tr>
<td>erase</td>
<td>删除position位置的数据</td>
</tr>
<tr>
<td>swap</td>
<td>交换两个vector的数据空间</td>
</tr>
<tr>
<td>operator[]</td>
<td>像数组一样访问</td>
</tr>
</tbody></table>
<p>其实用法也很简单，这里大致示范一下：</p>
<h3 id="vector迭代器失效问题"><a href="#vector迭代器失效问题" class="headerlink" title="vector迭代器失效问题"></a>vector迭代器失效问题</h3><p>这个理解起来也很简单，这里可以先看图理解：</p>
<p>第一种情况：迭代器指向原本被释放的空间</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201144740.png"></p>
<p>一开始开创好了一块空间来放数据 ，假设这个时候我的放的数据又比较多，空间不够了，这里就要reserve调整一块空间，但是在遍历的时候，it还指向的是原空间，导致程序崩溃报错，这里看图</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201144985.png">****</p>
<p>但此时，it还指向的是原空间，这就会导致出错</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//开空间并初始化</span><br><span class="line">vector&lt;int&gt; v&#123;1,2,3,4,5,6&#125;;</span><br><span class="line"></span><br><span class="line">//创建it</span><br><span class="line">auto it = v.begin();</span><br><span class="line"></span><br><span class="line">//调整空间</span><br><span class="line">v.assign(100, 8);</span><br><span class="line"></span><br><span class="line">//打印</span><br><span class="line">while(it != v.end())</span><br><span class="line">&#123;</span><br><span class="line">cout&lt;&lt; *it &lt;&lt; &quot; &quot; ;</span><br><span class="line">++it;</span><br><span class="line">&#125;</span><br><span class="line">cout&lt;&lt;endl;</span><br></pre></td></tr></table></figure>



<p>类似这里就会出现崩溃，因为v的begin和end已经变成了新的空间区域，但it还指向的是原本空间，而原本空间在开辟新空间后已经被释放掉，所以会崩溃报错</p>
<p>解决办法：更新it的值即可</p>
<p>第二种情况：迭代器的意义改变</p>
<p>假设此时有一个vector里面有这样的几个数字：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201144900.png"></p>
<p>此时，我要对这个vector进行如下操作：</p>
<p>在所有偶数前面加上20，其实这样也算简单，因为按照我们以前写的代码逻辑，只需要将偶数的位置由它后一个替换，然后++在判断即可，但是，这样就会出现问题：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201144901.png"></p>
<p>一开始it指向1，1不是偶数，所以不会动，但当it指向2的时候：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201144041.png">****</p>
<p>我们知道it现在指向2，2是偶数，所以要在前面加上20：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201144347.png"></p>
<p>假设it已经成功在2的前面加上20了，从逻辑上来讲，这里应该是没有问题的，但是这里就会出现这样的情况：</p>
<p>原本it指向的是2，但此时2的位置已经变成了20，2的位置已经改变，但it却指向原空间</p>
<p>所以就会出现第二种情况：迭代器所指向的意义变了</p>
<p>解决办法：重新修改迭代器指向即可</p>
<hr>
<h1 id="模拟实现"><a href="#模拟实现" class="headerlink" title="模拟实现"></a>模拟实现</h1><h2 id="目标"><a href="#目标" class="headerlink" title="目标"></a>目标</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">namespace knous</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	template&lt;class T&gt;</span><br><span class="line"></span><br><span class="line">	class vector</span><br><span class="line"></span><br><span class="line">	&#123;</span><br><span class="line"></span><br><span class="line">	public:</span><br><span class="line"></span><br><span class="line">		// Vector的迭代器是一个原生指针</span><br><span class="line"></span><br><span class="line">		typedef T* iterator;</span><br><span class="line"></span><br><span class="line">		typedef const T* const_iterator;</span><br><span class="line"></span><br><span class="line">		iterator begin();</span><br><span class="line"></span><br><span class="line">		iterator end();</span><br><span class="line"></span><br><span class="line">		const_iterator cbegin();</span><br><span class="line"></span><br><span class="line">		const_iterator cend() const;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		// construct and destroy</span><br><span class="line"></span><br><span class="line">		vector();</span><br><span class="line"></span><br><span class="line">		vector(int n, const T&amp; value = T());</span><br><span class="line"></span><br><span class="line">		template&lt;class InputIterator&gt;</span><br><span class="line"></span><br><span class="line">		vector(InputIterator first, InputIterator last);</span><br><span class="line"></span><br><span class="line">		vector&lt;T&gt;&amp; operator= (vector&lt;T&gt; v);</span><br><span class="line"></span><br><span class="line">		~vector();</span><br><span class="line"></span><br><span class="line">		// capacity</span><br><span class="line"></span><br><span class="line">		size_t size() const;</span><br><span class="line"></span><br><span class="line">		size_t capacity() const;</span><br><span class="line"></span><br><span class="line">		void reserve(size_t n);</span><br><span class="line"></span><br><span class="line">		void resize(size_t n, const T&amp; value = T());</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		///////////////access///////////////////////////////</span><br><span class="line"></span><br><span class="line">		T&amp; operator[](size_t pos);</span><br><span class="line"></span><br><span class="line">		const T&amp; operator[](size_t pos)const;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		///////////////modify/////////////////////////////</span><br><span class="line"></span><br><span class="line">		void push_back(const T&amp; x);</span><br><span class="line"></span><br><span class="line">		void pop_back();</span><br><span class="line"></span><br><span class="line">		void swap(vector&lt;T&gt;&amp; v);</span><br><span class="line"></span><br><span class="line">		iterator insert(iterator pos, const T&amp; x);</span><br><span class="line"></span><br><span class="line">		iterator erase(iterator pos);</span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line"></span><br><span class="line">		iterator _start; // 指向数据块的开始</span><br><span class="line"></span><br><span class="line">		iterator _finish; // 指向有效数据的尾</span><br><span class="line"></span><br><span class="line">		iterator _endOfStorage; // 指向存储容量的尾</span><br><span class="line"></span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>上面是之后会实现的东西，我们一点一点来，先将vector的类参数写出来：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">namespace knous</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	template&lt;class T&gt;</span><br><span class="line">	class vector</span><br><span class="line">	&#123;</span><br><span class="line">	public:</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line"></span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>从上往下看，逐步分析：</p>
<p>因为vector是一个容器，它可以放任意类型，所以这里我们单纯的用int、char指针来构造显然是错误的，这里就要用到之前学到的模板，T可以是任意类型的数据，在这里接收到类型之后它会自动替换下面所有T的地方</p>
<h3 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h3><p>这里本来想给大家看一下源代码，但是觉得自己水平不够，所以就不献丑了</p>
<p>我想说的是：vector里面的类型都是迭代器，没有之前在string里面的size、capacity之类的，它会有如下的三种指针：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">iterator _start; // 指向数据块的开始</span><br><span class="line"></span><br><span class="line">		iterator _finish; // 指向有效数据的尾</span><br><span class="line"></span><br><span class="line">		iterator _endOfStorage; // 指向存储容量的尾</span><br></pre></td></tr></table></figure>



<p>既然我们要模拟实现，那这个iterator又是什么呢？</p>
<p>其实是由我们typedef定义的类型，这里就简单定义一下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">namespace knous</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	template&lt;class T&gt;</span><br><span class="line">	class vector</span><br><span class="line">	&#123;</span><br><span class="line">	public:</span><br><span class="line">		typedef T* iterator;</span><br><span class="line">		typedef const T* const_iterator;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line">		iterator _start; // 指向数据块的开始</span><br><span class="line"></span><br><span class="line">		iterator _finish; // 指向有效数据的尾</span><br><span class="line"></span><br><span class="line">		iterator _endOfStorage; // 指向存储容量的尾</span><br><span class="line"></span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这就是我们大致的模板，因为T是任意类型，所以T<em>就是任意类型的指针，让T</em>重定义为iterator方便我们之后好看，然后写一个const版本来应对其他情况</p>
<p>一点一点来</p>
<p>这里先写一个begin和end，因为比较简单：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">iterator begin()</span><br><span class="line">		&#123;</span><br><span class="line">			return _start;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator end()</span><br><span class="line">		&#123;</span><br><span class="line">			reruen _finish;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_iterator cbegin()</span><br><span class="line">		&#123;</span><br><span class="line">			reruen _start;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_iterator cend() const</span><br><span class="line">		&#123;</span><br><span class="line">			rerurn _finis;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h2 id="构造"><a href="#构造" class="headerlink" title="构造"></a>构造</h2><p>我们知道类里面会自动一个构造函数，这里就浅浅的写一个：</p>
<p>无参的，直接让对应的迭代器指向空即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vector()</span><br><span class="line">			:_start(nullptr)</span><br><span class="line">			, _finish(nullptr)</span><br><span class="line">			, _endOfStorage(nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="opeartor-x3D"><a href="#opeartor-x3D" class="headerlink" title="opeartor&#x3D;"></a>opeartor&#x3D;</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vector&lt;T&gt;&amp; operator=(vector&lt;T&gt; v)</span><br><span class="line">		&#123;</span><br><span class="line">			swap(v);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这算一个现代写法，直接在形参那里构造一个，然后跟这个构造的交换空间，这里再写一个交换函数；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void swap(vector&lt;int&gt;&amp; v)</span><br><span class="line">		&#123;</span><br><span class="line">			std::swap(_start, v._start);</span><br><span class="line">			std::swap(_finish, v._finish);</span><br><span class="line">			std::swap(_endofstoage, v._endofstoage);</span><br><span class="line"></span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>这里因为交换函数标准头文件已经有了，就不用自己写了</p>
<h3 id="给一个长度和一个初始值构造"><a href="#给一个长度和一个初始值构造" class="headerlink" title="给一个长度和一个初始值构造"></a>给一个长度和一个初始值构造</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vector(int n, const T&amp; value = T())</span><br><span class="line">			:_start(nullptr)</span><br><span class="line">			, _finish(nullptr)</span><br><span class="line">			, _endOfStorage(nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">			reserve(n);</span><br><span class="line">			for (int i = 0; i &lt; n; i++)</span><br><span class="line">			&#123;</span><br><span class="line">				push_back(value);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>先调整空间，然后插入即可</p>
<h3 id="用一个迭代器区间来初始化另一个"><a href="#用一个迭代器区间来初始化另一个" class="headerlink" title="用一个迭代器区间来初始化另一个"></a>用一个迭代器区间来初始化另一个</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template &lt; class Iterator &gt;</span><br><span class="line">			vector(Iterator first,Iterator last)</span><br><span class="line">			:_start(nullptr)</span><br><span class="line">			, _finish(nullptr)</span><br><span class="line">			, _endOfStorage(nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">			while (first != end)</span><br><span class="line">			&#123;</span><br><span class="line">				push_back(*first);</span><br><span class="line">				++*first;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>因为无法确定这个迭代器区间类型，所以这里还是要用另一个模板来接收然后初始化尾插</p>
<h2 id="基本的操作函数"><a href="#基本的操作函数" class="headerlink" title="基本的操作函数"></a>基本的操作函数</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//重载方括号</span><br><span class="line">     T&amp; operator[](size_t pos)；</span><br><span class="line"></span><br><span class="line">    const T&amp; operator[](size_t pos)const； </span><br><span class="line"></span><br><span class="line">   // capacity</span><br><span class="line"></span><br><span class="line">    size_t size() const ；</span><br><span class="line"></span><br><span class="line">    size_t capacity() const；</span><br><span class="line"></span><br><span class="line">    void reserve(size_t n)；</span><br><span class="line"></span><br><span class="line">    void resize(size_t n, const T&amp; value = T())；</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    void push_back(const T&amp; x);</span><br><span class="line"></span><br><span class="line">    void pop_back();</span><br><span class="line"></span><br><span class="line">    iterator insert(iterator pos, const T&amp; x);</span><br><span class="line"></span><br><span class="line">    iterator erase(Iterator pos);</span><br></pre></td></tr></table></figure>



<p>顺着来：</p>
<h3 id="opeartor"><a href="#opeartor" class="headerlink" title="opeartor[]"></a>opeartor[]</h3><p>两个版本是因为如果只要遍历就可以调用const防止修改，如果要修改就会调用另外一个：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">T&amp; operator[](size_t pos)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos &lt; size());</span><br><span class="line">			return _start[pos];</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const T&amp; operator[](size_t pos)const</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos &lt; size());</span><br><span class="line">			return _start[pos];</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="size、capacity、resize、reserve"><a href="#size、capacity、resize、reserve" class="headerlink" title="size、capacity、resize、reserve"></a>size、capacity、resize、reserve</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">size_t capacity() const</span><br><span class="line">		&#123;</span><br><span class="line">			_endOfStorage - _start;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void reserve(size_t n)</span><br><span class="line">		&#123;</span><br><span class="line">			size_t size = size();</span><br><span class="line"></span><br><span class="line">			if (n &gt; capacity())</span><br><span class="line">			&#123;</span><br><span class="line">				if (_start)</span><br><span class="line">				&#123;</span><br><span class="line">					T* tmp = new T[n];</span><br><span class="line">					for (size_t i = ; i &lt; size(); i++)</span><br><span class="line">					&#123;</span><br><span class="line">						tmp[i] = _start[i];</span><br><span class="line">					&#125;</span><br><span class="line">					delete[] _start;</span><br><span class="line">				&#125;</span><br><span class="line">				_start = tmp;</span><br><span class="line">			&#125;</span><br><span class="line">			_finish = _start + size;</span><br><span class="line">			_endOfStorage = _start + n;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void resize(size_t n, const T&amp; value = T())</span><br><span class="line">		&#123;</span><br><span class="line">			if (n&gt;capacity())</span><br><span class="line">			&#123;</span><br><span class="line">				reserve(n);</span><br><span class="line">			&#125;</span><br><span class="line">			else if (n&gt;size())</span><br><span class="line">			&#123;</span><br><span class="line">					while (_finish &lt; _start + n)</span><br><span class="line">					&#123;</span><br><span class="line">						*_finish = value;</span><br><span class="line">						++_finish;</span><br><span class="line">					&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				_finish = _start + n;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<h3 id="尾插和尾删"><a href="#尾插和尾删" class="headerlink" title="尾插和尾删"></a>尾插和尾删</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void push_back(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			if (capacity() == 0)</span><br><span class="line">			&#123;</span><br><span class="line">				int new_capacity = capacity() == 0 ? 4 : capacity() * 2;</span><br><span class="line">				reserve(new_capacity);</span><br><span class="line">			&#125;</span><br><span class="line">			*_finsh = x;</span><br><span class="line">			_finish++;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void pop_back()</span><br><span class="line">		&#123;</span><br><span class="line">			if (_finish)</span><br><span class="line">				_finish--;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<p>先判断容量，然后直接插入或者删除</p>
<p>随机插入和删除:</p>
<p>这里算是比较重要的地方，插入我们要先记住pos的位置，防止开新空间后pos指向旧空间出现迭代器失效的问题</p>
<p>删除就直接替换就可以</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">iterator insert(iterator pos, const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos &lt;= _finish &amp;&amp; pos &gt;= _start);</span><br><span class="line"></span><br><span class="line">			//看容量</span><br><span class="line">			if (_finish == _endOfStorage)</span><br><span class="line">			&#123;</span><br><span class="line"></span><br><span class="line">				//记录pos</span><br><span class="line">				size_t size = pos - _start;</span><br><span class="line">				size_t new_capacity = capacity() == 0 ? 4 : capacity() * 2;</span><br><span class="line">				reserve(new_capacity);</span><br><span class="line"></span><br><span class="line">				pos = _start + size;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">			//挪数据</span><br><span class="line">			iterator it = _finish;</span><br><span class="line">			while (it != pos)</span><br><span class="line">			&#123;</span><br><span class="line">				*(it) = *(it-1);</span><br><span class="line">				it--;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			//填数据</span><br><span class="line">			*pos = x;</span><br><span class="line">			_finish++;</span><br><span class="line">			return pos;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator erase(iterator pos)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos &lt;= _finish &amp;&amp; pos &gt;= _start);</span><br><span class="line"></span><br><span class="line">			iterator it = pos;</span><br><span class="line">			while (it + 1 != _finish)</span><br><span class="line">			&#123;</span><br><span class="line">				*it = *(it+1);</span><br><span class="line">				it++;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			--_finish;</span><br><span class="line">			return pos;</span><br><span class="line">		&#125;</span><br></pre></td></tr></table></figure>



<hr>
<p>这里放上全部代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">namespace knous</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	template&lt;class T&gt;</span><br><span class="line">	class vector</span><br><span class="line">	&#123;</span><br><span class="line">	public:</span><br><span class="line">		typedef T* iterator;</span><br><span class="line">		typedef const T* const_iterator;</span><br><span class="line"></span><br><span class="line">		iterator begin()</span><br><span class="line">		&#123;</span><br><span class="line">			return _start;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator end()</span><br><span class="line">		&#123;</span><br><span class="line">			return _finish;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_iterator cbegin()</span><br><span class="line">		&#123;</span><br><span class="line">			reruen _start;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const_iterator cend() const</span><br><span class="line">		&#123;</span><br><span class="line">			rerurn _finis;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void swap(vector&lt;int&gt;&amp; v)</span><br><span class="line">		&#123;</span><br><span class="line">			std::swap(_start, v._start);</span><br><span class="line">			std::swap(_finish, v._finish);</span><br><span class="line">			std::swap(_endofstoage, v._endofstoage);</span><br><span class="line"></span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		vector()</span><br><span class="line">			:_start(nullptr)</span><br><span class="line">			, _finish(nullptr)</span><br><span class="line">			, _endOfStorage(nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		vector(int n, const T&amp; value = T())</span><br><span class="line">			:_start(nullptr)</span><br><span class="line">			, _finish(nullptr)</span><br><span class="line">			, _endOfStorage(nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">			reserve(n);</span><br><span class="line">			for (int i = 0; i &lt; n; i++)</span><br><span class="line">			&#123;</span><br><span class="line">				push_back(value);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		template &lt; class Iterator &gt;</span><br><span class="line">			vector(Iterator first,Iterator last)</span><br><span class="line">			:_start(nullptr)</span><br><span class="line">			, _finish(nullptr)</span><br><span class="line">			, _endOfStorage(nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">			while (first != end)</span><br><span class="line">			&#123;</span><br><span class="line">				push_back(*first);</span><br><span class="line">				++*first;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">			~vector()</span><br><span class="line">			&#123;</span><br><span class="line">				delete[] _start;</span><br><span class="line">				_start = _finish = _endOfStorage = nullptr;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">		vector&lt;T&gt;&amp; operator=(vector&lt;T&gt; v)</span><br><span class="line">		&#123;</span><br><span class="line">			swap(v);</span><br><span class="line">			return *this;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		// capacity</span><br><span class="line"></span><br><span class="line">		size_t size() const</span><br><span class="line">		&#123;</span><br><span class="line">			return _finish - _start;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		size_t capacity() const</span><br><span class="line">		&#123;</span><br><span class="line">			return _endOfStorage - _start;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void reserve(size_t n)</span><br><span class="line">		&#123;</span><br><span class="line">			size_t sz = size();</span><br><span class="line"></span><br><span class="line">			if (n &gt; capacity())</span><br><span class="line">			&#123;</span><br><span class="line">				T* tmp = new T[n];</span><br><span class="line">				if (_start)</span><br><span class="line">				&#123;</span><br><span class="line">					for (size_t i = 0; i &lt; size(); i++)</span><br><span class="line">					&#123;</span><br><span class="line">						tmp[i] = _start[i];</span><br><span class="line">					&#125;</span><br><span class="line">					delete[] _start;</span><br><span class="line">				&#125;</span><br><span class="line">				_start = tmp;</span><br><span class="line">			&#125;</span><br><span class="line">			_finish = _start + sz;</span><br><span class="line">			_endOfStorage = _start + n;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void resize(size_t n, const T&amp; value = T())</span><br><span class="line">		&#123;</span><br><span class="line">			if (n&gt;capacity())</span><br><span class="line">			&#123;</span><br><span class="line">				reserve(n);</span><br><span class="line">			&#125;</span><br><span class="line">			else if (n&gt;size())</span><br><span class="line">			&#123;</span><br><span class="line">					while (_finish &lt; _start + n)</span><br><span class="line">					&#123;</span><br><span class="line">						*_finish = value;</span><br><span class="line">						++_finish;</span><br><span class="line">					&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				_finish = _start + n;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		T&amp; operator[](size_t pos)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos &lt; size());</span><br><span class="line">			return _start[pos];</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		const T&amp; operator[](size_t pos)const</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos &lt; size());</span><br><span class="line">			return _start[pos];</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		void push_back(const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			if (_finish == _endOfStorage)</span><br><span class="line">			&#123;</span><br><span class="line">				int new_capacity = capacity() == 0 ? 4 : capacity() * 2;</span><br><span class="line">				reserve(new_capacity);</span><br><span class="line">			&#125;</span><br><span class="line">			*_finish = x;</span><br><span class="line">			_finish++;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		void pop_back()</span><br><span class="line">		&#123;</span><br><span class="line">			if (_finish)</span><br><span class="line">				_finish--;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator insert(iterator pos, const T&amp; x)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos &lt;= _finish &amp;&amp; pos &gt;= _start);</span><br><span class="line"></span><br><span class="line">			//看容量</span><br><span class="line">			if (_finish == _endOfStorage)</span><br><span class="line">			&#123;</span><br><span class="line"></span><br><span class="line">				//记录pos</span><br><span class="line">				size_t size = pos - _start;</span><br><span class="line">				size_t new_capacity = capacity() == 0 ? 4 : capacity() * 2;</span><br><span class="line">				reserve(new_capacity);</span><br><span class="line"></span><br><span class="line">				pos = _start + size;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">			//挪数据</span><br><span class="line">			iterator it = _finish;</span><br><span class="line">			while (it != pos)</span><br><span class="line">			&#123;</span><br><span class="line">				*(it) = *(it-1);</span><br><span class="line">				it--;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			//填数据</span><br><span class="line">			*pos = x;</span><br><span class="line">			_finish++;</span><br><span class="line">			return pos;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		iterator erase(iterator pos)</span><br><span class="line">		&#123;</span><br><span class="line">			assert(pos &lt;= _finish &amp;&amp; pos &gt;= _start);</span><br><span class="line"></span><br><span class="line">			iterator it = pos;</span><br><span class="line">			while (it + 1 != _finish)</span><br><span class="line">			&#123;</span><br><span class="line">				*it = *(it+1);</span><br><span class="line">				it++;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			--_finish;</span><br><span class="line">			return pos;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	private:</span><br><span class="line">		iterator _start; // 指向数据块的开始</span><br><span class="line"></span><br><span class="line">		iterator _finish; // 指向有效数据的尾</span><br><span class="line"></span><br><span class="line">		iterator _endOfStorage; // 指向存储容量的尾</span><br><span class="line"></span><br><span class="line">	&#125;;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>望这篇文章对您有所帮助</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>【剑指 Offer II】和大于等于 target 的最短子数组</title>
    <url>/2022/09/02/%E3%80%90%E5%89%91%E6%8C%87office%E3%80%91%E5%92%8C%E5%A4%A7%E4%BA%8E%E7%AD%89%E4%BA%8E%20target%20%E7%9A%84%E6%9C%80%E7%9F%AD%E5%AD%90%E6%95%B0%E7%BB%84/</url>
    <content><![CDATA[<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209021920011.png" alt="image-20220902192036528"></p>
<hr>
<p>三种办法，这里都说一下。</p>
<h1 id="暴力"><a href="#暴力" class="headerlink" title="暴力"></a>暴力</h1><p>直接每一个字串全部加起来，然后算第一个大于 target 的下标减去刚开始加的下标，得到长度之后开始比较找最小。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">minSubArrayLen</span><span class="params">(<span class="type">int</span> target, vector&lt;<span class="type">int</span>&gt;&amp; nums)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> ans = INT_MAX;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>;i&lt;nums.<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> sum = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j = i;j&lt;nums.<span class="built_in">size</span>();j++)</span><br><span class="line">            &#123;</span><br><span class="line">                sum+=nums[j];</span><br><span class="line">                <span class="keyword">if</span>(sum &gt;= target)</span><br><span class="line">                &#123;</span><br><span class="line">                    ans = <span class="built_in">min</span>(ans,j-i+<span class="number">1</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans == INT_MAX?<span class="number">0</span>:ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>其实你要是跟我说这个不超时的话我是不信的。</p>
<p>每次 LeetCode 针对这种暴力都是直接上很大一串代码的。</p>
<hr>
<h1 id="前缀和-二分"><a href="#前缀和-二分" class="headerlink" title="前缀和+二分"></a>前缀和+二分</h1><p>这里就可以用上前缀和了。</p>
<p>细说前缀和。</p>
<p>看起来好像很厉害的样子，实际上还算简单。</p>
<p>假设有一个数组 a ，这个数组里面是：2，3，1，2，4，3</p>
<p>​								那它的前缀和就是：0，2，5，6，8，12，15</p>
<p>第一个位置是 0 ，因为我们是从 1 开始加的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">V[I] = V[I-1]+a[i-1];</span><br></pre></td></tr></table></figure>

<p>有了这个前缀和，我们就可以继续往后写了。</p>
<p>-找位置-</p>
<p>C++里面有一个函数，叫做：lower_bound</p>
<p>JAVA里面叫做：Arrays.binarySearch</p>
<p>C#称之为：Array.BinarySearch</p>
<p>Python：bisect.bisect_left</p>
<p>其实它们都是一样的，就是找这一串数字里面第一个大于等于要找数字的位置。</p>
<p>返回的是一这个位置的迭代器，这里如果用上，如果熟悉的话会很简单，但是如果不熟悉，建议自己写一个二分。</p>
<p>其实也简单，找一个 &gt;&#x3D; tar 的元素，那就把判断条件改为 &gt;&#x3D; tar，其他的不变即可。</p>
<p>重新说回前缀和。</p>
<p>我们要找第一个元素它大于等于 tar 的区间长度，那我们就在前缀和里面找 &gt;&#x3D; tar 的元素即可。</p>
<p>假设：tar &#x3D; 7</p>
<p>第一个元素是  2 ,它的前缀和是 0 ，那它要找的 tar就是 0+7 &#x3D; 7，tar 就是它的前缀和+它本身， 区间应该是： 2，3，1，2，加起来结果是8，最后 2 的下标是 4 ，所以这段区间的长度就是 4 - 1 + 1。</p>
<p>这里的 -1 是因为是从第一个元素开始找的， +1 是因为别忘了这个元素也是要算在长度里面的。</p>
<p>第二个元素就跟第一个元素不一样了。</p>
<p>它是 3 ，它的前缀和是 2 ，所以它要找的 tar 是 2 + 7 &#x3D; 9，为什么不一样？因为从这里开始算的话他是已经 +2 的结果（加了前面一个值），那它找的结果也要加前面的值，所以它找的是区间是：3，1，2，4，算下来也是 4 .</p>
<p>这里直接跳到 4 的位置。</p>
<p>4 的前缀和是 8 ，也就是说它要找的 tar 值是 8 + 7 &#x3D; 15，那往后找找到结尾的 15，此时长度是 5 - 4 + 1  &#x3D;  2，再将它与前面的比较即可。</p>
<p>思路说完了，来看一下代码怎么写。</p>
<p>第一种，调用 lower_bound：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">minSubArrayLen</span><span class="params">(<span class="type">int</span> target, vector&lt;<span class="type">int</span>&gt;&amp; nums)</span> </span>&#123;</span><br><span class="line">        <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">v</span><span class="params">(nums.size()+<span class="number">1</span>,<span class="number">0</span>)</span></span>;</span><br><span class="line">        <span class="type">int</span> ans = INT_MAX;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">1</span>;i&lt;=nums.<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            v[i] = v[i<span class="number">-1</span>] + nums[i<span class="number">-1</span>];</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">1</span>;i&lt;=nums.<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> tar = v[i<span class="number">-1</span>]+target;</span><br><span class="line">            <span class="keyword">auto</span> pos = <span class="built_in">lower_bound</span>(v.<span class="built_in">begin</span>(),v.<span class="built_in">end</span>(),tar);</span><br><span class="line">            <span class="keyword">if</span>(pos != v.<span class="built_in">end</span>())</span><br><span class="line">            ans = <span class="built_in">min</span>(ans, <span class="built_in">static_cast</span>&lt;<span class="type">int</span>&gt;((pos - v.<span class="built_in">begin</span>()) - (i - <span class="number">1</span>)));</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> ans == INT_MAX?<span class="number">0</span>:ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>static_cast<int> 就是强制类型转换，因为它是两个迭代器相减。</p>
<p>然后我们再自己写一个 lower_bound：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">minSubArrayLen</span><span class="params">(<span class="type">int</span> target, vector&lt;<span class="type">int</span>&gt;&amp; nums)</span> </span>&#123;</span><br><span class="line">        <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">v</span><span class="params">(nums.size()+<span class="number">1</span>,<span class="number">0</span>)</span></span>;</span><br><span class="line">        <span class="type">int</span> ans = INT_MAX;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">1</span>;i&lt;=nums.<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            v[i] = v[i<span class="number">-1</span>] + nums[i<span class="number">-1</span>];</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">1</span>;i&lt;=v.<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> tar = v[i<span class="number">-1</span>]+target;</span><br><span class="line">            <span class="type">int</span> pos = <span class="built_in">my_lower_bound</span>(v,tar);</span><br><span class="line">            <span class="keyword">if</span>(pos != <span class="number">-1</span>)</span><br><span class="line">            ans = <span class="built_in">min</span>(ans,pos-i+<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans == INT_MAX?<span class="number">0</span>:ans;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">my_lower_bound</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; v,<span class="type">int</span> tar)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="type">int</span> left = <span class="number">0</span>,right = v.<span class="built_in">size</span>()<span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(v[right] &lt; tar)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">while</span>(left &lt; right)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> mid = (left+right)&gt;&gt;<span class="number">1</span>;</span><br><span class="line">            <span class="keyword">if</span>(v[mid] &gt;= tar)</span><br><span class="line">            &#123;</span><br><span class="line">                right = mid;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                left = mid+<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> right;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="滑动窗口"><a href="#滑动窗口" class="headerlink" title="滑动窗口"></a>滑动窗口</h1><p>直接上代码吧…这个就是一直 + ，+ 到大于 tar 的时候就可以算区间长度，然后 区间右移，继续 + ，其实思路跟前缀和有那么点像。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">minSubArrayLen</span><span class="params">(<span class="type">int</span> target, vector&lt;<span class="type">int</span>&gt;&amp; nums)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> start = <span class="number">0</span>,end = <span class="number">0</span>;</span><br><span class="line">        <span class="type">int</span> ans = INT_MAX;</span><br><span class="line">        <span class="type">int</span> sum = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>(end &lt; nums.<span class="built_in">size</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            sum+=nums[end];</span><br><span class="line">            <span class="keyword">while</span>(sum &gt;= target)&#123;</span><br><span class="line">                ans = <span class="built_in">min</span>(ans,end-start+<span class="number">1</span>);</span><br><span class="line">                sum -= nums[start];</span><br><span class="line">                start++;</span><br><span class="line">            &#125;</span><br><span class="line">            end++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans == INT_MAX ? <span class="number">0</span> : ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>拿这个举例：2，3，1，2，4，3</p>
<p>start 和 end 都从 0 开始，一直加到 大于 tar（7），区间是：2，3，1，2，end 在 2 的位置，然后end - start +1 注意 start 还在 0 没动，所以它的长度是 4 ，然后我们要做的就是将这个区间重新变为小于 tar 的状态，start - end 区间里面，end 可能很大，就让 start 一直往后移，每一次移动都减去start，直到它小于 tar，然后再继续加…</p>
<p>如果还看不懂也必要紧，这题 LeetCode 考点是 用 N*logN，也就是用第二种办法…</p>
<p>滑动窗口多看几遍，实在看不懂可以走读一下，加强自己的代码阅读能力。</p>
<hr>
]]></content>
      <categories>
        <category>每日练习</category>
      </categories>
  </entry>
  <entry>
    <title>【剑指office】数据流的中位数</title>
    <url>/2022/09/01/%E3%80%90%E5%89%91%E6%8C%87office%E3%80%91%E6%95%B0%E6%8D%AE%E6%B5%81%E7%9A%84%E4%B8%AD%E4%BD%8D%E6%95%B0/</url>
    <content><![CDATA[<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011648962.png" alt="image-20220901164836833"></p>
<p>题目链接：<a class="link"   href="https://leetcode.cn/problems/find-median-from-data-stream/" >295. 数据流的中位数 - 力扣（LeetCode）<i class="fas fa-external-link-alt"></i></a></p>
<hr>
<p>在说这道题思路之前，我们先看看网上的一些解法：</p>
<p>@LeetCode官方</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011649664.png" alt="image-20220901164931755"></p>
<p>@宫水三叶</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011650604.png" alt="image-20220901165030443"></p>
<p>很复杂，也不好理解，然后我看了半天，发现其实很简单，这里我会用小白也可以理解的话来回答这道题。</p>
<hr>
<p><strong>第一种解法，我们用一个容器将里面存起来，然后在找中位数的时候直接排序然后访问中间的元素即可。</strong></p>
<p><strong>省流：超时</strong></p>
<p><strong>不好用别用</strong></p>
<p><strong>这种最简单的办法LeetCode的测试用例巨长一段，看的眼花。</strong></p>
<hr>
<p>第二种，就是我要说的，用两个优先级队列，这个时候你可能就会有疑问，哎呀你这不还是优先级队列吗，你跟他们有什么区别？</p>
<p>用是用，但是我要说的是为什么要这么用。</p>
<p>假设现在我们有两个优先级队列，一个叫做 less ，这是一个大堆，然后我们再创建一个，叫做 greater ，这是一个小堆。</p>
<p>记好，记好，记好。</p>
<p>less是大堆，greater是小堆。</p>
<p>前提就是这些，然后我们开始写。</p>
<p>初始化就可以直接跳过不写，因为这两个会自动生成默认构造，如果这里不懂的话我建议翻一下我之前写过的类和对象。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011659222.png" alt="image-20220901165912317"></p>
<p>为什么queMin中的数要比queMax多一个？</p>
<p>为什么奇数的时候中位数为queMin的对头？</p>
<p>就从这里开始说。</p>
<p>这里我放上图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011703302.png" alt="image-20220901170356177"></p>
<p>我这么说吧，其实上面所谓的Min要比Max多一位之类的话，其实就是，Min就是一段数据的正中间位置，因为Min.top（）是大堆，它下面的元素都比它小，Max是小堆，但是这个小堆最小的也比Min最大的大，可以理解为下面这个图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011707312.png" alt="image-20220901170711185"></p>
<p>这么说，当数据为2N+1的时候，其中一段就会比另一段多 1 ，所以就会出现Min要比Max多1的情况。</p>
<p>我们如何维护的？</p>
<p>一步一步来，首先往大堆插入1。</p>
<p>这里可能就会有人说，为什么要往大堆，能不能往小堆？</p>
<p>等我说完过程，你自己分析一下能不能往小堆。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011712358.png" alt="image-20220901171228177"></p>
<p>ok，已经插入进去了，此时如果要找到中间值，是不是要拿到Min（less）的首位就可以了？</p>
<p>简单。</p>
<p>然后插入2</p>
<p>此时，2要往哪里插？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011713376.png" alt="image-20220901171330664"></p>
<p>我们要维护的是Min.top（）就是这一段数据的中间位置，此时你再往MIn里面插，就会变成这样：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011714584.png" alt="image-20220901171445863"></p>
<p>现在问题就来了，你这两个数字，MIn.top（）已经不是中间位置了，而是数据的末尾：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011715721.png" alt="image-20220901171528032"></p>
<p>我们要确保的是在偶数的情况下这两个优先级队列（Max、Min）应该保持着Min-Max&lt;&#x3D;1的情况。</p>
<p>现在你能明白为什么能长1的吧？</p>
<p>如果&#x3D;1，说明这是数据长度是奇数。</p>
<p>如果&#x3D;0，说明两边一样长。</p>
<p>所以就不要直接插入，我们插入之前比较一下，如果这个数字大于当前的 Min.top（），说明这个数字不应该出现在 Min 里面，因为 Min 里面要确保 Min.top（）是最大的，才能保证它是中间的位置。</p>
<p>所以它就应该插入在 Max里面。</p>
<p>为什么？</p>
<p>因为 Max是一个小堆，它的数字会越来越大，但是这个堆顶的元素一直都保持着：</p>
<p>比 Min.top（）大，但是比其他数字小。</p>
<p>也就是说它是这样的一个位置：<img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011723207.png" alt="image-20220901172334832"></p>
<p>它就是为了数据呈现偶数的时候另一个中间位置。</p>
<p>OK，既然如此，那我们就继续往下：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011725449.png" alt="image-20220901172512286"></p>
<p>现在插入 3 ，3 肯定比 1 大，那它就会被放到 Max 里面：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011725684.png" alt="image-20220901172549534"></p>
<p>现在，中位数就应该是2，我们怎么样拿到这个2呢？</p>
<p>当 Max 比 Min 长的时候，就说明是下面的情况</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011728661.png" alt="image-20220901172807839"></p>
<p>这样如何处理？</p>
<p>很简单，我们让 Min 往右走一步，那怎么往右走呢？</p>
<p>你再想想我们刚刚的，Max.top（） 是不是就是它的下一位？</p>
<p>Max 里面的值都是比 Min.top（） 大的，但是它的值又是 Max 里面最小的，当我们把它放到 Min 里面的时候，它就会替代 Min.top（），成为新的中间位，而我们再把 Max.top（） 直接删掉就可以了。</p>
<p>看看：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209011730136.png" alt="image-20220901173049398"></p>
<p>此时我们再拿 Min.top，是不是又拿到了中间值？</p>
<p>如果反着，当所有值都小于 Min.top（）的时候，我们也只需要将 Min.top（）交给 Max即可。</p>
<p>如果不能理解，这里请自己画一下图，就当加深理解。</p>
<p>如此反复，不管插入多少，我们都保持着这种逻辑即可。</p>
<p>接下来看看代码的实现：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MedianFinder</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MedianFinder</span>() &#123;</span><br><span class="line">        <span class="comment">//priority_queue是自带的类型，所以会调用自带的构造</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">addNum</span><span class="params">(<span class="type">int</span> num)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//先判断如果 Min 为空，说明这是第一次插入，第一次插入放到大堆里面</span></span><br><span class="line">        <span class="comment">//如果不为空，那就判断一下这个值跟 Min.top 的大小相比</span></span><br><span class="line">        <span class="keyword">if</span>(Min.<span class="built_in">empty</span>() || num &lt;= Min.<span class="built_in">top</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            Min.<span class="built_in">push</span>(num);</span><br><span class="line">            <span class="keyword">if</span>(Min.<span class="built_in">size</span>() &gt; Max.<span class="built_in">size</span>() + <span class="number">1</span>) <span class="comment">//说明中间值已经右偏，将 Min 的顶端给 Max，然后自删</span></span><br><span class="line">            &#123;</span><br><span class="line">                Max.<span class="built_in">push</span>(Min.<span class="built_in">top</span>());</span><br><span class="line">                Min.<span class="built_in">pop</span>();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="comment">//插入的这个值比较大，所以放到右边</span></span><br><span class="line">        &#123;</span><br><span class="line">            Max.<span class="built_in">push</span>(num);</span><br><span class="line">            <span class="keyword">if</span>(Max.<span class="built_in">size</span>() &gt; Min.<span class="built_in">size</span>()) <span class="comment">//说明中间值左偏，将 Max 的顶端给 Min ，然后自删</span></span><br><span class="line">            &#123;</span><br><span class="line">                Min.<span class="built_in">push</span>(Max.<span class="built_in">top</span>());</span><br><span class="line">                Max.<span class="built_in">pop</span>();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">double</span> <span class="title">findMedian</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(Min.<span class="built_in">size</span>() &gt; Max.<span class="built_in">size</span>()) <span class="comment">//说明是奇数，Min 位置正是中间值</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> Min.<span class="built_in">top</span>()/<span class="number">1.0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>    <span class="comment">//说明是偶数，此时将两个顶端+起来除2</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">return</span> (Min.<span class="built_in">top</span>() + Max.<span class="built_in">top</span>())/<span class="number">2.0</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    priority_queue&lt;<span class="type">int</span>,vector&lt;<span class="type">int</span>&gt;,less&lt;<span class="type">int</span>&gt;&gt; Min;      <span class="comment">//大堆</span></span><br><span class="line">    priority_queue&lt;<span class="type">int</span>,vector&lt;<span class="type">int</span>&gt;,greater&lt;<span class="type">int</span>&gt;&gt; Max;   <span class="comment">//小堆</span></span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<hr>
<p>我是Knous，每天都会更新题目，希望明天能再见。</p>
]]></content>
      <categories>
        <category>每日练习</category>
      </categories>
  </entry>
  <entry>
    <title>【剑指office】旋转数组的最小数字</title>
    <url>/2022/09/02/%E3%80%90%E5%89%91%E6%8C%87office%E3%80%91%E6%97%8B%E8%BD%AC%E6%95%B0%E7%BB%84%E7%9A%84%E6%9C%80%E5%B0%8F%E6%95%B0%E5%AD%97/</url>
    <content><![CDATA[<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209021908885.png" alt="image-20220902190757333"></p>
<hr>
<p>看着花，说白了，暴力重新排序或者二分查找，还是那种完全不用变的。</p>
<p>这里暴力就直接重新排序找第 0 个就好了，不举例。</p>
<p>举个例子：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209021910358.png" alt="image-20220902191030575"></p>
<p>此时 Left 应该在 3 的位置上，而 Right 应该在 2 的位置上。</p>
<p>那么中间位置就是 5 。</p>
<p>将这个 5 跟最右边判断一下（也可以跟最左边），如果这个位置比 Right 小，就说明最小的那个数肯定不会在右边，同理，如果这个位置比 Right 大，就说明最小的那个数肯定不会在左边。</p>
<p>现在 Left 是 1 （Left &#x3D; mid+1），Right 是 2 ，中间值是 1 。</p>
<p>1 &lt; 2</p>
<p>所以 Right 要变到 1 的位置。</p>
<p>而二分的前提是 Left &lt; Right</p>
<p>我们返回 Left即可。</p>
<p>其他情况也可以根据上面分析。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">minArray</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; numbers)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> left = <span class="number">0</span>;</span><br><span class="line">        <span class="type">int</span> right = numbers.<span class="built_in">size</span>()<span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>(left &lt; right)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> mid = (left+right)/<span class="number">2</span>;</span><br><span class="line">            <span class="keyword">if</span>(numbers[mid] &lt; numbers[right])</span><br><span class="line">                right = mid;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(numbers[mid] &gt; numbers[right])</span><br><span class="line">                left = mid+<span class="number">1</span>;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                right--;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> numbers[left];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>每日练习</category>
      </categories>
  </entry>
  <entry>
    <title>二叉搜索树</title>
    <url>/2022/07/27/%E4%BA%8C%E5%8F%89%E6%90%9C%E7%B4%A2%E6%A0%91/</url>
    <content><![CDATA[<hr>
<p>因为涉及到二叉树的部分知识，建议在有一定的二叉树的基础上再来看这篇文章 </p>
<p>-————-</p>
<h1 id="二叉搜索树"><a href="#二叉搜索树" class="headerlink" title="二叉搜索树"></a>二叉搜索树</h1><h2 id="二叉搜索树的概念"><a href="#二叉搜索树的概念" class="headerlink" title="二叉搜索树的概念"></a>二叉搜索树的概念</h2><blockquote>
<p>若它的左子树不为空，则左子树上所有节点的值都小于根节点的值<br> 若它的右子树不为空，则右子树上所有节点的值都大于根节点的值<br> 它的左右子树也分别为二叉搜索树</p>
</blockquote>
<p>简单来说，每一个节点的左子树都要比根节点小，每一个右子树都要比根节点大</p>
<p>这里看下面的一张图：</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201155919.png" alt="img"><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>这就是一个较为标准的二叉搜索树，左节点&lt;根&lt;右节点，所以按照这样的思路，我们中序遍历的话是可以得到一个从小到大的顺序。</p>
<p>-—–</p>
<h2 id="二叉搜索树的一些操作"><a href="#二叉搜索树的一些操作" class="headerlink" title="二叉搜索树的一些操作"></a>二叉搜索树的一些操作</h2><p>比较简单，插入，删除，查找，下面会提供两个版本：递归、非递归。</p>
<p>先简单看一下功能：</p>
<p>查找：如果查找的值比当前节点的大，就往右走，如果比当前节点的小，往左走，最多走树的高度次，走到空节点的时候那就是没找到，返回false。</p>
<p>（为什么这里不返回当前节点的位置，不是不能返回，是因为二叉搜索树的性质决定了每个值的位置，如果直接传位置的话一定要加上const防止修改）</p>
<p>插入；如果这个值比当前节点的大，往右走，如果比值小，左走，直到空节点，插入</p>
<p>删除是一个特例，这里之后实现部分再讲</p>
<h2 id="模拟实现（非递归）"><a href="#模拟实现（非递归）" class="headerlink" title="模拟实现（非递归）"></a>模拟实现（非递归）</h2><p>首先，定义一个模板，其次，跟模拟实现链表那块一样，这里放着的是根节点的指针，这个指针指向一个节点，节点是自定义的类型，在里面有存放的值，还有左孩子和右孩子</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line">struct Tree_node</span><br><span class="line">&#123;</span><br><span class="line">    T _val;</span><br><span class="line">	Tree_node&lt;T&gt;* _lefe;</span><br><span class="line">	Tree_node&lt;T&gt;* _right;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">template&lt;class T&gt;</span><br><span class="line">class binary_search_tree</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	typedef Tree_node&lt;T&gt; Node;</span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line">	Node* _root = nullptr;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h3 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h3><p><strong>首先，我们拿到一个要插入的值的时候，先判断一下，如果当前根节点为空，说明这是一个空树，要特殊处理一下。</strong></p>
<p><strong>其次，如果这个树不为空，就要找到它合适的位置放进去，再将它接到树里面去，这里就有注意一下，如果要链接起来，我们还需要一个值来找这个合适位置的父节点。</strong></p>
<p><strong>分析好情况，接下来就可以开始写了</strong></p>
<p>在插入之前，先写一个节点的构造函数，方便我们之后开新节点</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Tree_node(const T&amp; val)</span><br><span class="line">	:_val(val)</span><br><span class="line">	, _left(nullptr)</span><br><span class="line">	, _right(nullptr)</span><br><span class="line">&#123;&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>给一个值，然后放进去，将其左右置空</p>
<p>很简单吧？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">	bool insert(const T&amp; val)</span><br><span class="line">&#123;</span><br><span class="line">	//判断根节点为空的空树情况</span><br><span class="line">	if (_root == nullptr)</span><br><span class="line">	&#123;</span><br><span class="line">		_root = new Node(val);</span><br><span class="line">		return true;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		//创建变量，防止修改_root</span><br><span class="line">		Node* cur = _root;</span><br><span class="line">		Node* parent = nullptr;</span><br><span class="line">		//开始遍历往下找</span><br><span class="line">		while (cur)</span><br><span class="line">		&#123;</span><br><span class="line">			//当前值大于要找的，往左走并且将当前地址给父节点</span><br><span class="line">			if (cur-&gt;_val &gt; val)</span><br><span class="line">			&#123;</span><br><span class="line">				parent = cur;</span><br><span class="line">				cur = cur-&gt;_left;</span><br><span class="line">			&#125;</span><br><span class="line">			//当前值小于要找的，往右走并且将当前地址给父节点</span><br><span class="line">			else if (cur-&gt;_val &lt; val)</span><br><span class="line">			&#123;</span><br><span class="line">				parent = cur;</span><br><span class="line">				cur = cur-&gt;_right;</span><br><span class="line">			&#125;</span><br><span class="line">			//不允许有重复值</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				return false;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		//找到了，新创节点并判断父节点大小方便链接</span><br><span class="line">		cur = new Node(val);</span><br><span class="line">		if (parent-&gt;_val &gt; val)</span><br><span class="line">		&#123;</span><br><span class="line">			parent-&gt;_lefe = cur;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			parent-&gt;_right = cur;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	return true;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h3 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h3><p>跟插入思路一样，比找的值小，右走，找的值大，左走，为空，找不到</p>
<p>返回值就不要返回当前节点的指针了，不让修改防止破坏树的结构</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool find(const T&amp; val)</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = _root;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		if (cur-&gt;_val &gt; val)</span><br><span class="line">		&#123;</span><br><span class="line">			cur = cur-&gt;_lefe;</span><br><span class="line">		&#125;</span><br><span class="line">		else if (cur-&gt;_val &lt; val)</span><br><span class="line">		&#123;</span><br><span class="line">			cur = cur-&gt;_right;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			return true;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	return false;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>因其思路过于简单就没有写注释，我相信各位也是可以看得懂的。</p>
<h3 id="遍历"><a href="#遍历" class="headerlink" title="遍历"></a>遍历</h3><p>嗯，按照二叉搜索树的结构的话，如果中序遍历，我们得到的应该是一个有序数组，这里就简单写一个中序，肯定要用到递归，但是我们可以稍微封装一下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">    void InOrder()</span><br><span class="line">	&#123;</span><br><span class="line">		_InOrder(_root);</span><br><span class="line">	&#125;	</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void _InOrder(Node* root)</span><br><span class="line">	&#123;</span><br><span class="line">		if (root == nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">			return;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			_InOrder(root-&gt;_lefe);</span><br><span class="line">			cout &lt;&lt; root-&gt;_val &lt;&lt; &quot; &quot;;</span><br><span class="line">			_InOrder(root-&gt;_right);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>上面是我们提供的接口，下面是接口的实现，下面的部分会放到隐私部分</p>
<p>这里简单的测试一下：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201156116.png" alt="img"></p>
<h3 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h3><p>删除应该是二叉搜索树最难的一个部分，因为要理清思路：</p>
<p>1、这个节点有没有左右孩子</p>
<p>2、这个节点只有左孩子</p>
<p>3、这个节点只有右孩子</p>
<p>4、这个节点有两个孩子</p>
<p>1可以放到2 3里面去解决，这里分别说一下解决思路：</p>
<p>如果这个节点只有左孩子，那就要让父节点的指针指向被删节点的左子节点</p>
<p>如果这个节点只有右节点，那就要让父节点的指针指向本山节点的右子节点</p>
<p>如果这个节点有两个孩子，那就要用到替换法，我们在它的左子树里面找到最大的那个值，或者在右字数里面找最小的那个值，然后用那个值放进去，再将替换那个值的节点删掉</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201156678.png" alt="img"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool Erase(const T&amp; val)</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = _root;</span><br><span class="line">	Node* parent = nullptr;</span><br><span class="line"></span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		//先找到要删除的节点</span><br><span class="line">		if (cur-&gt;_val &lt; val)</span><br><span class="line">		&#123;</span><br><span class="line">			parent = cur;</span><br><span class="line">			cur = cur-&gt;_right;</span><br><span class="line">		&#125;</span><br><span class="line">		else if (cur-&gt;_val &gt; val)</span><br><span class="line">		&#123;</span><br><span class="line">			parent = cur;</span><br><span class="line">			cur = cur-&gt;_left;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			//如果这个只有右孩子</span><br><span class="line">			if (cur-&gt;_left == nullptr)</span><br><span class="line">			&#123;</span><br><span class="line">				//判断是不是根，要不要特殊处理</span><br><span class="line">				if (cur == _root)</span><br><span class="line">					_root = cur-&gt;_right;</span><br><span class="line">				else</span><br><span class="line">				&#123;</span><br><span class="line">					if (cur == parent-&gt;_left)</span><br><span class="line">						parent-&gt;_left = cur-&gt;_right;</span><br><span class="line">					else</span><br><span class="line">						parent-&gt;_right = cur-&gt;_right;</span><br><span class="line">				&#125;</span><br><span class="line">				delete cur;</span><br><span class="line">			&#125;</span><br><span class="line">			//如果只有左孩子</span><br><span class="line">			else if (cur-&gt;_right == nullptr)</span><br><span class="line">			&#123;</span><br><span class="line">				if (cur == _root)</span><br><span class="line">				_root = cur-&gt;_left;</span><br><span class="line">				else</span><br><span class="line">				&#123;</span><br><span class="line">					if (cur == parent-&gt;_right)</span><br><span class="line">						parent-&gt;_left = cur-&gt;_left;</span><br><span class="line">					else</span><br><span class="line">						parent-&gt;_right = cur-&gt;_left;</span><br><span class="line">				&#125;</span><br><span class="line">				delete cur;</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				//找左边节点最大的</span><br><span class="line">				Node* maxParent = cur;</span><br><span class="line">				Node* maxleft = cur-&gt;_left;</span><br><span class="line">				while (maxleft-&gt;_right != nullptr)</span><br><span class="line">				&#123;</span><br><span class="line">					maxParent = maxleft;</span><br><span class="line">					maxleft = maxleft-&gt;_right;</span><br><span class="line">				&#125;</span><br><span class="line">				cur-&gt;_val = maxleft-&gt;_val;</span><br><span class="line">				//链接起来</span><br><span class="line">				if (maxParent-&gt;_left == maxleft)</span><br><span class="line">				&#123;</span><br><span class="line">					maxParent-&gt;_left = maxleft-&gt;_left;</span><br><span class="line">				&#125;</span><br><span class="line">				else</span><br><span class="line">				&#123;</span><br><span class="line">					maxParent-&gt;_right = maxleft-&gt;_left;</span><br><span class="line">				&#125;</span><br><span class="line">				//别忘了删除这个左边最大的节点</span><br><span class="line">				delete maxleft;</span><br><span class="line">			&#125;</span><br><span class="line">			//删完了返回真</span><br><span class="line">			return true;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	//没找到就返回假</span><br><span class="line">	return false;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>看看效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201156360.png" alt="img"></p>
<h2 id="模拟实现（递归）"><a href="#模拟实现（递归）" class="headerlink" title="模拟实现（递归）"></a>模拟实现（递归）</h2><p>递归大体思路跟非递归没有什么区别，唯一要注意的是细节方便，这里看代码理解会更好</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line">class binary_search_tree_R</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	typedef Tree_node&lt;T&gt; Node;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	Node* _root = nullptr;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>为了好看，我新创建一个类出来，_R代表这是一个递归版本</p>
<h3 id="插入-1"><a href="#插入-1" class="headerlink" title="插入"></a>插入</h3><p>思路参考非递归，找空节点插入，根节点特殊处理</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">	bool Insert(const T&amp; val)</span><br><span class="line">	&#123;</span><br><span class="line">		return _Insert(_root, val);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line"></span><br><span class="line">	bool _Insert(Node*&amp; root, T val)</span><br><span class="line">	&#123;</span><br><span class="line">		if (root == nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">			root = new Node(val);</span><br><span class="line">			return true;</span><br><span class="line">		&#125;</span><br><span class="line">		if (root-&gt;_val &gt; val)</span><br><span class="line">			return _Insert(root-&gt;_left,val);</span><br><span class="line">		else if (root-&gt;_val &lt; val)</span><br><span class="line">			return  _Insert(root-&gt;_right, val);</span><br><span class="line">		else</span><br><span class="line">			return false;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>将根节点传进来，然后判断是否为空，为空就直接构建节点并赋值，不为空就返回自己下一个要找的节点的插入，直到找不到或者是插入完成。</p>
<p>这里为什么是Node*&amp;，因为单纯用parent来记录当前节点的父节点会使得参数变多。</p>
<p>这里看图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201156393.png" alt="img"></p>
<p><strong>如果是按照之前的逻辑，那我们应该用一个节点来记录当前root的位置，让其链接上，但现在不一样的是我们传过去的是root所指向节点的别名，也就是说它其实是将root所指向的节点的别名传了过去，因为是别名，所以我们可以直接修改里面的值。</strong></p>
<p> <strong>可以这么理解，我们传过去的是不是root-&gt;right的地址拷贝，而是直接将root-&gt;right（或者是left）传了过去，对传过去的节点的修改是直接可以反应到树上，就不需要父节点了，当然也可以用，这里随自己喜欢。</strong></p>
<h3 id="查找-1"><a href="#查找-1" class="headerlink" title="查找"></a>查找</h3><p>思路就不说了，直接看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool find(const T&amp; val)</span><br><span class="line">&#123;</span><br><span class="line">	return _find_R(_root, val);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">bool _find_R(Node* root, const T&amp; val)</span><br><span class="line">&#123;</span><br><span class="line">	if (root == nullptr)</span><br><span class="line">		return false;</span><br><span class="line">	if (root-&gt;_val &gt; val)</span><br><span class="line">	&#123;</span><br><span class="line">		_find_R(root-&gt;_left, val);</span><br><span class="line">	&#125;</span><br><span class="line">	else if (root-&gt;_val &lt; val)</span><br><span class="line">	&#123;</span><br><span class="line">		_find_R(root-&gt;-right, val);</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		return true;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>递归其实跟非递归实现的是一样的逻辑，如果懂了非递归，递归也很好理解</p>
<h3 id="打印"><a href="#打印" class="headerlink" title="打印"></a>打印</h3><p>其实非递归的打印也用了递归，这里就直接拿过来用即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">   void InOrder()</span><br><span class="line">&#123;</span><br><span class="line">	_InOrder(_root);</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void _InOrder(Node* root)</span><br><span class="line">	&#123;</span><br><span class="line">		if (root == nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">			return;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			_InOrder(root-&gt;_left);</span><br><span class="line">			cout &lt;&lt; root-&gt;_val &lt;&lt; &quot; &quot;;</span><br><span class="line">			_InOrder(root-&gt;_right);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>效果；</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201156511.png" alt="img"></p>
<h3 id="删除-1"><a href="#删除-1" class="headerlink" title="删除"></a>删除</h3><p>思路跟非递归一样，判断节点，然后根据节点条件来删除</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool _Earse(Node*&amp; root, const T&amp; val)</span><br><span class="line">&#123;</span><br><span class="line">	//当前节点为空，没找到，返回失败</span><br><span class="line">	if (root == nullptr)</span><br><span class="line">		return false;</span><br><span class="line">	//大于查找值，递归往左</span><br><span class="line">	if (root-&gt;_val &gt; val)</span><br><span class="line">	&#123;</span><br><span class="line">		return _Earse(root-&gt;_left, val);</span><br><span class="line">	&#125;</span><br><span class="line">	//小于查找值，递归往右</span><br><span class="line">	else if (root-&gt;_val &lt; val)</span><br><span class="line">	&#123;</span><br><span class="line">		return _Earse(root-&gt;_right, val);</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		//先拿一个值记录等会要删的节点</span><br><span class="line">		Node* del = root;</span><br><span class="line"></span><br><span class="line">		//只有右节点</span><br><span class="line">		if (root-&gt;_left == nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">			root = root-&gt;_right;</span><br><span class="line">		&#125;</span><br><span class="line">		//只有左节点</span><br><span class="line">		else if (root-&gt;_right == nullptr)</span><br><span class="line">		&#123;</span><br><span class="line">			root = root-&gt;_left;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			//先找到左边最大值</span><br><span class="line">			Node* maxLeft = root-&gt;_left;</span><br><span class="line">			while (maxLeft-&gt;_right != nullptr)</span><br><span class="line">			&#123;</span><br><span class="line">				maxLeft = maxLeft-&gt;_right;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			//交换左边最大值</span><br><span class="line">			swap(root-&gt;_val, maxLeft-&gt;_val);</span><br><span class="line"></span><br><span class="line">			//因为交换了之后也满足二叉搜索树的条件，且这个位置一定是叶子节点，可以递归调用自己</span><br><span class="line">			return _Earse(root-&gt;_left, val);</span><br><span class="line">		&#125;</span><br><span class="line">		delete del;</span><br><span class="line">		return true;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>其实可以看到，递归跟非递归，它们的思路大体都是一样的，只不过实现方式上有所不同，看着递归的代码少，但理解起来却比非递归麻烦的多。 </p>
<p>后续会有更新</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>二叉树的练习题</title>
    <url>/2022/04/11/%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    <content><![CDATA[<hr>
<p> 做的时候总感觉自己掌握的不够深，所以在这里写一篇博客来加深理解。</p>
<hr>
<h2 id="单值二叉树"><a href="#单值二叉树" class="headerlink" title="单值二叉树"></a>单值二叉树</h2><p><a class="link"   href="https://leetcode-cn.com/problems/univalued-binary-tree/"  title="965. 单值二叉树 - 力扣（LeetCode） (leetcode-cn.com)">965. 单值二叉树 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200929934.png"></p>
<p>二叉树肯定是递归的，但原函数给我们的却是：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200929496.png"></p>
<p>这个函数的参数却只有一个root，我们判断是否为单值还需要其中一个节点的值，如果这个值跟其他值不一样，那我们就返回false，如果节点为空、或者是值一样，我们返回真</p>
<p>那我们就自己建一个函数，然后递归这个创建的函数即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//递归判断</span><br><span class="line">bool fun(struct TreeNode* root,size_t val)</span><br><span class="line">&#123;</span><br><span class="line">    if(root == NULL)</span><br><span class="line">    &#123;</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line">    if(root-&gt;val != val)</span><br><span class="line">    &#123;</span><br><span class="line">        return false;</span><br><span class="line">    &#125;</span><br><span class="line">    return fun(root-&gt;left,val)&amp;&amp;fun(root-&gt;right,val);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">bool isUnivalTree(struct TreeNode* root)&#123;</span><br><span class="line">    if(root == NULL)</span><br><span class="line">    &#123;</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line">    size_t val = root-&gt;val;</span><br><span class="line"></span><br><span class="line">    return fun(root,val);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>二叉树的最大深度</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200929205.png"></p>
<p><a class="link"   href="https://leetcode-cn.com/problems/maximum-depth-of-binary-tree/"  title="104. 二叉树的最大深度 - 力扣（LeetCode） (leetcode-cn.com)">104. 二叉树的最大深度 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>我们先看题目给我们的条件：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200929855.png"></p>
<p>已知根节点，求最大深度</p>
<p>我们拿两个值，一个记录左节点深度，一个记录右节点深度，然后我们比较返回大的那个</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int maxDepth(struct TreeNode* root)&#123;</span><br><span class="line">    if(root == NULL)</span><br><span class="line">    &#123;</span><br><span class="line">        return 0;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /如果不为空，这里就要返回1</span><br><span class="line">    size_t left = maxDepth(root-&gt;left)+1;</span><br><span class="line">    size_t right = maxDepth(root-&gt;right)+1;</span><br><span class="line"></span><br><span class="line">    return left&gt;right?left:right;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="二叉树的前序构造："><a href="#二叉树的前序构造：" class="headerlink" title="二叉树的前序构造："></a>二叉树的前序构造：</h2><p>我们先来创建一个二叉树的结构体，再定义里面的数据类型：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef char BTDataType;</span><br><span class="line"> </span><br><span class="line">typedef struct BinaryTreeNode</span><br><span class="line">&#123;</span><br><span class="line">	BTDataType _data;</span><br><span class="line">	struct BinaryTreeNode* _left;</span><br><span class="line">	struct BinaryTreeNode* _right;</span><br><span class="line">&#125;BTNode;</span><br></pre></td></tr></table></figure>



<p>前序，我们知道是：根 左节点、右节点，我们拿到一串数据，就可以按照这样的顺序递归下去</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//一个参数是我们要放进去的数据，另一个指针记录放了几个，每放一个就++</span><br><span class="line">BTNode* BinaryTreeCreate(BTDataType* a, int* n)</span><br><span class="line">&#123;</span><br><span class="line">    //如果这个地方是&#x27;#&#x27;，就说明这里是空指针，我们让指针++，返回空</span><br><span class="line">	if (a[*n] == &#x27;#&#x27;)</span><br><span class="line">	&#123;</span><br><span class="line">		++(*n);</span><br><span class="line">		return NULL;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">        //先开辟节点，这个就是根</span><br><span class="line">		BTNode* root = (BTNode*)malloc(sizeof(BTNode));</span><br><span class="line">        //放数据</span><br><span class="line">		root-&gt;data = a[*n];</span><br><span class="line">        让指针++</span><br><span class="line">		++(*n);</span><br><span class="line">        先左节点</span><br><span class="line">		root-&gt;left = BinaryTreeCreate(a, n);</span><br><span class="line">        //右节点</span><br><span class="line">		root-&gt;right = BinaryTreeCreate(a, n);</span><br><span class="line">        </span><br><span class="line">        //返回这个的地址</span><br><span class="line">		return root;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="销毁二叉树"><a href="#销毁二叉树" class="headerlink" title="销毁二叉树"></a>销毁二叉树</h2><p>如果理解了如何构造，那这个应该不难</p>
<p>我们拿到一个节点，开始判断这个节点是不是空，如果是空，那我们就直接返回，如果不是空，那我们就继续找这个节点的子节点，直到我们找到叶子，然后free</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 二叉树销毁</span><br><span class="line">void BinaryTreeDestory(BTNode* root)</span><br><span class="line">&#123;</span><br><span class="line">    //判断节点</span><br><span class="line">	if (root == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">        //往下找</span><br><span class="line">		BinaryTreeDestory(root-&gt;left);</span><br><span class="line">		BinaryTreeDestory(root-&gt;right);</span><br><span class="line">        //释放</span><br><span class="line">		free(root);</span><br><span class="line">	&#125;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="二叉树的节点（叶子）个数"><a href="#二叉树的节点（叶子）个数" class="headerlink" title="二叉树的节点（叶子）个数"></a>二叉树的节点（叶子）个数</h2><p>思路跟销毁二叉树差不多，我们拿到根节点，然后判断它是否为空，为空就返回0，不为空就继续往下找，找到根节点后开始返回1</p>
<p>如果是判断叶子节点，那我们就把判断条件改一下，如果这个节点的左右节点指针都为空，返回1，然后我们返回值调用这个函数，把根的左指针和右指针给它即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 二叉树节点个数</span><br><span class="line">int BinaryTreeSize(BTNode* root)</span><br><span class="line">&#123;</span><br><span class="line">	if (root == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		return 0;</span><br><span class="line">	&#125;</span><br><span class="line">	return 1 + BinaryTreeSize(root-&gt;left)</span><br><span class="line">			 + BinaryTreeSize(root-&gt;right);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 二叉树叶子节点个数</span><br><span class="line">int BinaryTreeLeafSize(BTNode* root)</span><br><span class="line">&#123;</span><br><span class="line">	if (root-&gt;left == NULL &amp;&amp; root-&gt;right == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		return 1;</span><br><span class="line">	&#125;</span><br><span class="line">	return BinaryTreeLeafSize(root-&gt;left) + BinaryTreeLeafSize(root-&gt;right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="二叉树第K层的节点个数"><a href="#二叉树第K层的节点个数" class="headerlink" title="二叉树第K层的节点个数"></a><strong>二叉树第K层的节点个数</strong></h2><p>我们找K层，实际是找K-1层，因为只有它可以拿到K层的地址，如果我们找第3层，那实际上我们找的是第2层，我们拿到第二层根的节点，，然后把它的左右指针传进去，如果 k &#x3D;&#x3D; 1（我们每次传进去k都-1），如果传进去的节点为空，就返回0，如K&#x3D;&#x3D;1，那就说明是这一层，我们返回1，把两个值加起来</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 二叉树第k层节点个数</span><br><span class="line">int BinaryTreeLevelKSize(BTNode* root, int k)</span><br><span class="line">&#123;</span><br><span class="line">	if (root == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		return 0;</span><br><span class="line">	&#125;</span><br><span class="line">	if (k == 1)</span><br><span class="line">	&#123;</span><br><span class="line">		return 1;</span><br><span class="line">	&#125;</span><br><span class="line">	return BinaryTreeLevelKSize(root-&gt;left,k-1) + BinaryTreeLevelKSize(root-&gt;right,k-1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="查找二叉树里面值为X的节点"><a href="#查找二叉树里面值为X的节点" class="headerlink" title="查找二叉树里面值为X的节点"></a>查找二叉树里面值为X的节点</h2><p>结束两件有两个：</p>
<p>1、找到了，返回这个节点</p>
<p>2、没找到，返回空</p>
<p>其他情况下继续递归即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//二叉树查找值为x的节点</span><br><span class="line"></span><br><span class="line">BTNode* BinaryTreeFind(BTNode* root, BTDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	if (root == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		return ;</span><br><span class="line">	&#125;</span><br><span class="line">	if (root-&gt;data == x)</span><br><span class="line">	&#123;</span><br><span class="line">		return root;</span><br><span class="line">	&#125;</span><br><span class="line">    //创建临时指针开始找左边</span><br><span class="line">	BTNode* tmp = BinaryTreeFind(root-&gt;left, x);</span><br><span class="line">    //为空就是没找到，找右边</span><br><span class="line">	if (tmp == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		tmp = BinaryTreeFind(root-&gt;right, x);</span><br><span class="line">	&#125;</span><br><span class="line">    //不为空就是找到了，返回</span><br><span class="line">	if (tmp != NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		return tmp;</span><br><span class="line">	&#125;</span><br><span class="line">    //右边找完也为空那就返回空</span><br><span class="line">	return NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="二叉树的前、中、后序遍历"><a href="#二叉树的前、中、后序遍历" class="headerlink" title="二叉树的前、中、后序遍历"></a>二叉树的前、中、后序遍历</h2><p>如果这个节点是空，我们可以打印出来，或者是直接返回</p>
<p>一开始传进来的是根节点，然后我们打印根节点，再打印左节点，同时左节点又作为子树的根，所以用递归，左边递归完再递归右边：</p>
<p>改变顺序即可改变遍历方式</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 二叉树前序遍历 </span><br><span class="line">void BinaryTreePrevOrder(BTNode* root)</span><br><span class="line">&#123;</span><br><span class="line">	if (root == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;null &quot;);</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line">	printf(&quot;%c &quot;, root-&gt;data);</span><br><span class="line">	BinaryTreePrevOrder(root-&gt;left);</span><br><span class="line">	BinaryTreePrevOrder(root-&gt;right);</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 二叉树中序遍历</span><br><span class="line">void BinaryTreeInOrder(BTNode* root)</span><br><span class="line">&#123;</span><br><span class="line">	if (root == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;null &quot;);</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line">	BinaryTreeInOrder(root-&gt;left);</span><br><span class="line">	printf(&quot;%c &quot;, root-&gt;data);</span><br><span class="line">	BinaryTreeInOrder(root-&gt;right);</span><br><span class="line">&#125;</span><br><span class="line">// 二叉树后序遍历</span><br><span class="line">void BinaryTreePostOrder(BTNode* root)</span><br><span class="line">&#123;</span><br><span class="line">	if (root == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;null &quot;);</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line">	BinaryTreeInOrder(root-&gt;left);</span><br><span class="line">	BinaryTreeInOrder(root-&gt;right);</span><br><span class="line">	printf(&quot;%c &quot;, root-&gt;data);</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="层序遍历"><a href="#层序遍历" class="headerlink" title="层序遍历"></a>层序遍历</h2><p>这可能是最难的一部分，我们需要用到之前的队列</p>
<p>具体操作如下：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200930774.png"></p>
<p>假设我们现在的队列是这样的，那我们层序打印的时候就应该是：</p>
<p>A B C D E F G</p>
<p>如何用队列实现？</p>
<p>我们创建一个队列并初始化：</p>
<p>这里有两种实现队列的方式</p>
<p>第一种用数组，这种方式简单、好理解，但缺点是不能合适的开辟空间</p>
<p>第二种用链表，之后会更新实现</p>
<p>之后我们就要考虑如何在队列里面插入数据了：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200930977.png"></p>
<p>我们先将A插进去并打印出来，之后判断：如果队列不为空（此时队列应该有A），我们就继续插入数据，插入的是A的子节点（B、C）</p>
<p>我们再删除A的数据，因为删除了A的数据，而如果刚刚插入成功的话，那下一个到的就是B，再将B的子节点放进去，打印B的节点，再删除，循环</p>
<p>这是我们会用到的关于队列的函数（如果看不懂的话可以点开专栏看看我以前写的顺序表和队列）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.h</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">#include&lt;stdbool.h&gt;</span><br><span class="line"></span><br><span class="line">//二叉树里面的类型</span><br><span class="line">typedef char BTDataType;</span><br><span class="line"></span><br><span class="line">//二叉树节点个数</span><br><span class="line">#define MaxSize 100</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//二叉树的结构体</span><br><span class="line">typedef struct BinaryTreeNode</span><br><span class="line">&#123;</span><br><span class="line">	BTDataType data;</span><br><span class="line">	struct BinaryTreeNode* left;</span><br><span class="line">	struct BinaryTreeNode* right;</span><br><span class="line">&#125;BTNode;</span><br><span class="line"></span><br><span class="line">//队列的结构体</span><br><span class="line">typedef struct Queue</span><br><span class="line">&#123;</span><br><span class="line">	struct BinaryTreeNode* arr[MaxSize];</span><br><span class="line">	int front;</span><br><span class="line">	int end;</span><br><span class="line">&#125;Queue;</span><br><span class="line"></span><br><span class="line">// 初始化队列</span><br><span class="line">void QueueInit(Queue* q);</span><br><span class="line">// 队尾入队列</span><br><span class="line">void QueuePush(Queue* q, struct BinaryTreeNode* data);</span><br><span class="line">// 队头出队列</span><br><span class="line">void QueuePop(Queue* q);</span><br><span class="line">// 获取队列头部元素</span><br><span class="line">struct BinaryTreeNode* QueueFront(Queue* q);</span><br><span class="line">// 检测队列是否为空，如果为空返回非零结果，如果非空返回0 </span><br><span class="line">bool QueueEmpty(Queue* q);</span><br><span class="line"></span><br><span class="line">// 层序遍历</span><br><span class="line">void BinaryTreeLevelOrder(BTNode* root);</span><br><span class="line">// 判断二叉树是否是完全二叉树</span><br><span class="line">bool BinaryTreeComplete(BTNode* root);</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.c</span><br><span class="line"></span><br><span class="line">//初始化</span><br><span class="line">void QueueInit(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	q-&gt;end = q-&gt;head = 0;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//插入元素</span><br><span class="line">void QueuePush(Queue* q, struct BinaryTreeNode* x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	q-&gt;arr[q-&gt;end++] = x;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//删除元素并返回元素</span><br><span class="line">struct BinaryTreeNode* QueuePop(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	struct BinaryTreeNode* tmp = q-&gt;arr[q-&gt;head];</span><br><span class="line">	q-&gt;head++;</span><br><span class="line">	return tmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">//判断队列是否为空</span><br><span class="line">bool QueueEmpty(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	return q-&gt;end == q-&gt;head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>写完了队列的操作，那我们就可以正式开始写层序了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void BinaryTreeLevelOrder(BTNode* root)</span><br><span class="line">&#123;</span><br><span class="line">    //创建并初始化</span><br><span class="line">	Queue q;</span><br><span class="line">	QueueInit(&amp;q);</span><br><span class="line">    //插入</span><br><span class="line">	QueuePush(&amp;q, root);</span><br><span class="line">    //开始走</span><br><span class="line">	while (!QueueEmpty(&amp;q))</span><br><span class="line">	&#123;</span><br><span class="line">        //当前节点数据方便我们使用</span><br><span class="line">		BTNode* node = QueueFront(&amp;q);</span><br><span class="line">        //打印</span><br><span class="line">		printf(&quot;%c &quot;, node-&gt;data);</span><br><span class="line">        //如果它有子节点就插入</span><br><span class="line">		if (node-&gt;left)</span><br><span class="line">		&#123;</span><br><span class="line">			QueuePush(&amp;q, node-&gt;left);</span><br><span class="line">		&#125;</span><br><span class="line">		if (node-&gt;right)</span><br><span class="line">		&#123;</span><br><span class="line">			QueuePush(&amp;q, node-&gt;right);</span><br><span class="line">		&#125;</span><br><span class="line">        //记得删除</span><br><span class="line">		QueuePop(&amp;q);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="是否为完全二叉树"><a href="#是否为完全二叉树" class="headerlink" title="是否为完全二叉树"></a>是否为完全二叉树</h2><p>如果知道如何层序遍历，那判断完全，就很简单了</p>
<p>我们还是一样遍历插入，但是如果我们插入过程中遇到了空节点，那就跳出循环，不再插入，这个时候我们就开始判断</p>
<p>如果这个空节点后面还有数据，那它将不是完全二叉树</p>
<p>如果这个空节点之后都是空，那它就是完全二叉树</p>
<p>我们只需要将层序的改一下就好</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 判断二叉树是否是完全二叉树</span><br><span class="line">bool BinaryTreeComplete(BTNode* root)</span><br><span class="line">&#123;</span><br><span class="line">	Queue q;</span><br><span class="line">	QueueInit(&amp;q);</span><br><span class="line">	QueuePush(&amp;q, root);</span><br><span class="line">    //同样插入遍历</span><br><span class="line">	while (!QueueEmpty(&amp;q))</span><br><span class="line">	&#123;</span><br><span class="line">		BTNode* node = QueueFront(&amp;q);</span><br><span class="line">        //如果这个节点为空就跳出</span><br><span class="line">		if (node == NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			break;</span><br><span class="line">		&#125;</span><br><span class="line">        //不为空就插入</span><br><span class="line">		QueuePush(&amp;q, node-&gt;left);</span><br><span class="line">		QueuePush(&amp;q, node-&gt;right);</span><br><span class="line">		QueuePop(&amp;q);</span><br><span class="line">	&#125;</span><br><span class="line">    //开始判断队列里剩下元素</span><br><span class="line">	while (!QueueEmpty(&amp;q))</span><br><span class="line">	&#123;</span><br><span class="line">		BTNode* node = QueueFront(&amp;q);</span><br><span class="line">		QueuePop(&amp;q);</span><br><span class="line">        //如果有元素，那就是false</span><br><span class="line">		if (node)</span><br><span class="line">		&#123;</span><br><span class="line">			return false;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">    //走到这里队列就应该走完了，那就返回真</span><br><span class="line">	return true;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="相同的树"><a href="#相同的树" class="headerlink" title="相同的树"></a>相同的树</h2><p><a class="link"   href="https://leetcode-cn.com/problems/same-tree/"  title="100. 相同的树 - 力扣（LeetCode） (leetcode-cn.com)">100. 相同的树 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200930336.png"></p>
<p>我们拿到两棵树的根，开始比较，如果都是空、或者值一样，就递归下去到左节点，左节点递归完再右节点</p>
<p>过程中如果有不对的地方直接返回false</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool isSameTree(struct TreeNode* p, struct TreeNode* q)&#123;</span><br><span class="line">    //都是空，为真</span><br><span class="line">    if(q == NULL &amp;&amp; p == NULL)</span><br><span class="line">    return true;</span><br><span class="line">    //其中一个为空或者值不一样，为假</span><br><span class="line">    if(q == NULL || p == NULL || p-&gt;val != q-&gt;val )</span><br><span class="line">    return false;</span><br><span class="line">    return isSameTree(q-&gt;left,p-&gt;left) &amp;&amp; isSameTree(q-&gt;right,p-&gt;right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="二叉树是否对称"><a href="#二叉树是否对称" class="headerlink" title="二叉树是否对称"></a>二叉树是否对称</h2><p><a class="link"   href="https://leetcode-cn.com/problems/symmetric-tree/"  title="101. 对称二叉树 - 力扣（LeetCode） (leetcode-cn.com)">101. 对称二叉树 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200931287.png"></p>
<p>确实是有点简单了，根节点不需要比较，我们把左边看成一棵树，右边看成一棵树，然后比较的左边的左子节点和右边的右子节点即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool isSameTree(struct TreeNode* p, struct TreeNode* q)&#123;</span><br><span class="line">    if(q == NULL &amp;&amp; p == NULL)</span><br><span class="line">    return true;</span><br><span class="line">    if(q == NULL || p == NULL || q-&gt;val != p-&gt;val)</span><br><span class="line">    return false;</span><br><span class="line">    //注意传过去的是左、右，不要都传一样的</span><br><span class="line">    return isSameTree(q-&gt;left,p-&gt;right) &amp;&amp; isSameTree(q-&gt;right,p-&gt;left);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bool isSymmetric(struct TreeNode* root)&#123;</span><br><span class="line">    //空肯定对称</span><br><span class="line">    if(root == NULL)</span><br><span class="line">    return true;</span><br><span class="line">    //递归即可</span><br><span class="line">    return isSameTree(root-&gt;left,root-&gt;right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="另一颗树的子树"><a href="#另一颗树的子树" class="headerlink" title="另一颗树的子树"></a>另一颗树的子树</h2><p><a class="link"   href="https://leetcode-cn.com/problems/subtree-of-another-tree/"  title="572. 另一棵树的子树 - 力扣（LeetCode） (leetcode-cn.com)">572. 另一棵树的子树 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200931111.png"></p>
<p>我们拿到节点之后，每一个节点都对比一次，就用上面的是否相同函数，如果不同，我们就递归下去，直到左边的全部节点都走完一遍</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool isSameTree(struct TreeNode* p, struct TreeNode* q)&#123;</span><br><span class="line">    if(q == NULL &amp;&amp; p == NULL)</span><br><span class="line">    return true;</span><br><span class="line">    if(q == NULL || p == NULL || p-&gt;val != q-&gt;val )</span><br><span class="line">    return false;</span><br><span class="line">    return isSameTree(q-&gt;left,p-&gt;left) &amp;&amp; isSameTree(q-&gt;right,p-&gt;right);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">bool isSubtree(struct TreeNode* root, struct TreeNode* subRoot)&#123;</span><br><span class="line">    if(root == NULL)</span><br><span class="line">    return false;</span><br><span class="line">            //当前节点开始判断、左子树判断、右子树判断</span><br><span class="line">    return isSameTree(root,subRoot)</span><br><span class="line">        || isSubtree(root-&gt;left,subRoot)</span><br><span class="line">        || isSubtree(root-&gt;right,subRoot);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h2 id="翻转二叉树"><a href="#翻转二叉树" class="headerlink" title="翻转二叉树"></a>翻转二叉树</h2><p><a class="link"   href="https://leetcode-cn.com/problems/invert-binary-tree/"  title="226. 翻转二叉树 - 力扣（LeetCode） (leetcode-cn.com)">226. 翻转二叉树 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200931707.png"></p>
<p>拿到节点开始判断，如果这个节点为空，我们就返回，如果不为空，我们就直接把它左右子树交换，然后继续递归下去</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct TreeNode* invertTree(struct TreeNode* root)&#123;</span><br><span class="line">    if(root == NULL)</span><br><span class="line">    &#123;</span><br><span class="line">        return NULL;</span><br><span class="line">    &#125;</span><br><span class="line">    //交换</span><br><span class="line">    struct TreeNode* tmp = root-&gt;left;</span><br><span class="line">    root-&gt;left = root-&gt;right;</span><br><span class="line">    root-&gt;right = tmp;</span><br><span class="line">    //递归下去，返回值就不用接收了</span><br><span class="line">    invertTree(root-&gt;left);</span><br><span class="line">    invertTree(root-&gt;right);</span><br><span class="line">    return root;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<p>感谢观看</p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>内存管理</title>
    <url>/2022/05/28/%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<hr>
<p> 在开始阅读之前，先做几道题，来看看自己基础怎么样：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int globalVar = 1;</span><br><span class="line">static int staticGlobalVar = 1;</span><br><span class="line">void Test()</span><br><span class="line">&#123;</span><br><span class="line"> static int staticVar = 1;</span><br><span class="line"> int localVar = 1;</span><br><span class="line"> </span><br><span class="line"> int num1[10] = &#123;1, 2, 3, 4&#125;;</span><br><span class="line"> char char2[] = &quot;abcd&quot;;</span><br><span class="line"> char* pChar3 = &quot;abcd&quot;;</span><br><span class="line"> int* ptr1 = (int*)malloc(sizeof (int)*4);</span><br><span class="line"> int* ptr2 = (int*)calloc(4, sizeof(int));</span><br><span class="line"> int* ptr3 = (int*)realloc(ptr2, sizeof(int)*4);</span><br><span class="line"> free (ptr1);</span><br><span class="line"> free (ptr3);</span><br><span class="line">&#125;</span><br><span class="line">1. 选择题：</span><br><span class="line"> 选项: A.栈 B.堆 C.数据段 D.代码段</span><br><span class="line"> globalVar在哪里？____ staticGlobalVar在哪里？____</span><br><span class="line"> staticVar在哪里？____ localVar在哪里？____</span><br><span class="line"> num1 在哪里？____</span><br><span class="line"> </span><br><span class="line"> char2在哪里？____ *char2在哪里？___</span><br><span class="line"> pChar3在哪里？____ *pChar3在哪里？____</span><br><span class="line"> ptr1在哪里？____ *ptr1在哪里？____</span><br><span class="line"></span><br><span class="line">2. 填空题：</span><br><span class="line"> sizeof(num1) = ____; sizeof(char2) = ____; </span><br><span class="line"> strlen(char2) = ____;sizeof(pChar3) = ____; </span><br><span class="line"> strlen(pChar3) = ____;sizeof(ptr1) = ____;</span><br></pre></td></tr></table></figure>



<p>先看选择题的第一题：</p>
<p>globaVal：可以看到这是全局的变量，全局的变量都在数据段（静态区）。</p>
<p>staticGlobalVar：这同样是一个全局变量，和上面一样选C</p>
<p>staticticVar：全局变量，不多说</p>
<p>lovalVar：整形变量，开辟在栈上</p>
<p>num1：整形数组，还是在栈上</p>
<p>char2：字符数组，还是在栈上</p>
<p>*char2：char2是数组char2首元素地址，对这个地址解引用就可以放到到里面的字符串，字符串是储存在代码段里面的</p>
<p>pchar3：在栈上，因为这是一个指向字符串的指针，指针都开辟在栈上</p>
<p>*pchar3：数据段，对pchar3解引用就放到了这一串字符，字符都是在代码段里面的</p>
<p>prt1：栈上，因为它只是一个int*的指针</p>
<p>*ptr1：堆上，malloc开辟的空间都是在堆上开辟出来的</p>
<p>sizeof(num1) ：可以看到这里是开辟了十个整形的空间，虽然里面只初始化了四个元素，但空间还是开辟了的，所以是40字节</p>
<p>sizeof(char2) ：求char2所占内存，虽然这里看上去只有四个字符，但别忘了字符串结尾的\0，所以这里是5</p>
<p>strlen(char2) ：strlen是碰到\0就停止，所以这里是四个字节</p>
<p>sizeof(pChar3) ：这里就判断是是一个指针的大小了，所以是4\8字节</p>
<p>strlen(pChar3) ：这里求的是pchar3对应的字符串长度，所以是4个字节</p>
<p>sizeof(ptr1) ：这就只是单纯的一个指针了，指着那句是4\8个字节</p>
<hr>
<p>这里就总结一下栈\堆\数据段\代码段的区别：</p>
<blockquote>
<ol>
<li>栈又叫堆栈，非静态局部变量&#x2F;函数参数&#x2F;返回值等等，栈是向下增长的。</li>
<li>内存映射段是高效的I&#x2F;O映射方式，用于装载一个共享的动态内存库。用户可使用系统接口创建共享共享内存，做进程间通信。（Linux课程如果没学到这块，现在只需要了解一下）</li>
<li>堆用于程序运行时动态内存分配，堆是可以上增长的。</li>
<li>数据段–存储全局数据和静态数据。</li>
<li>代码段–可执行的代码&#x2F;只读常量。</li>
</ol>
</blockquote>
<hr>
<h2 id="动态内存管理方式"><a href="#动态内存管理方式" class="headerlink" title="动态内存管理方式"></a>动态内存管理方式</h2><p>我们知道动态内存管理有三种方式：</p>
<p>malloc：在堆上开辟一块空间并返回开辟空间的地址，如果没有开辟成功则返回空指针</p>
<p>calloc：在堆上开辟一块空间并初始化，随后返回开辟空间地址，如果开辟失败则返回空指针</p>
<p>realloc：重新调整给定空间大小，如果给定空间是空指针，则相当了malloc</p>
<p>free：没什么好说的，开辟的空间用完都要还回去，释放掉一块空间</p>
<hr>
<h2 id="C-内存管理方式"><a href="#C-内存管理方式" class="headerlink" title="C++内存管理方式"></a>C++内存管理方式</h2><p>我们知道在C语言里面用malloc后都需要判断一下，否则编译器会报错，用起来就比较麻烦，所以在C++里面，就引出了新的办法：</p>
<p>new\delete</p>
<p>用法也很简单：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201105612.png"></p>
<p>就跟用mallc一样，用完要delete，但用完不需要if判断返回值，因为如果开辟失败它会直接报异常错误。</p>
<p>用法是在这样的：</p>
<p>类型指针 对象名 &#x3D; new 类型</p>
<p>如果类型后面有圆括号就是初始化，如果是方括号就是开辟括号里面个数的空间</p>
<p>如果是开辟了一个对象，就用delete 对象名</p>
<p>如果开辟了多个对象空间，就用delete[] 对象名</p>
<p>[]要对上</p>
<hr>
<h3 id="new一个自定义类型"><a href="#new一个自定义类型" class="headerlink" title="new一个自定义类型"></a>new一个自定义类型</h3><p>这也是没问题的，这里我写一个简单的Date来测试：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Date</span><br><span class="line">&#123;</span><br><span class="line">	Date()</span><br><span class="line">		:_year(2022),</span><br><span class="line">		_month(5),</span><br><span class="line">		_day(28)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;Date()&quot; &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	~Date()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;~Date()&quot; &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line">	int _year;</span><br><span class="line">	int _month;</span><br><span class="line">	int _day;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>这是一个简单的日期类，里面只有构造函数和默认构造，如果我们是用mallc写，应该是这样的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void Test1()</span><br><span class="line">&#123;</span><br><span class="line">	Date* ptr1 = (Date*)malloc(sizeof(Date));</span><br><span class="line">	Date* ptr2 = (Date*)malloc(sizeof(Date) * 10);</span><br><span class="line">	free(ptr1);</span><br><span class="line">	free(ptr2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>我这里是vs2013，malloc下面还没有用assert检查，好像bs2019就必须要用assert检查了。</p>
<p>但如果是new，那就应该这么写：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void Test2()</span><br><span class="line">&#123;</span><br><span class="line">	Date* ptr3 = new Date;</span><br><span class="line">	Date* prt4 = new Date[10];</span><br><span class="line"></span><br><span class="line">	delete ptr3;</span><br><span class="line">	delete[] prt4;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>为什么要这么写，我们分别测试一下Test1和Test2</p>
<p>当我们调用Tets1的时候：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201105623.png"></p>
<p>可以看到里面就是很简单的开辟了一块空间，但是如果我们用Test2，里面的new来开辟空间，则是：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201105083.png"></p>
<p>可以看到里面开辟好的空间已经全部有了初始值，这个初始值就是类里面写的默认构造的值，当我要delete这个对象的时候，delete也会去调用这个类的析构函数：</p>
<p><img src="https://img-blog.csdnimg.cn/3371f1b1c69345af9ccb901b22715647.png">编辑</p>
<p>同时，我也没有用sizeof去求这个类的大小，编译器也自动算出来开辟好了。</p>
<p>这里的重点是：</p>
<p>在申请自定义类型的空间时，new会调用构造函数，delete会调用析构函数，而malloc与free不会</p>
<hr>
<h2 id="operator-new与operator-delete函数"><a href="#operator-new与operator-delete函数" class="headerlink" title="operator new与operator delete函数"></a>operator new与operator delete函数</h2><p>光是看这个名字就知道这两个肯定跟new和delete有关，事实也确是如此</p>
<p>先看一下概念：</p>
<blockquote>
<p>new和delete是用户进行动态内存申请和释放的操作符，operator new 和operator delete是系统提供的全局函数，new在底层调用operator new全局函数来申请空间，delete在底层通过operator delete全局函数来释放空间。</p>
</blockquote>
<p>不好整，直接看源代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void *__CRTDECL operator new(size_t size) _THROW1(_STD bad_alloc) &#123;</span><br><span class="line"> void *p;</span><br><span class="line"> while ((p = malloc(size)) == 0)</span><br><span class="line"> if (_callnewh(size) == 0)</span><br><span class="line"> &#123;</span><br><span class="line"> static const std::bad_alloc nomem;</span><br><span class="line"> _RAISE(nomem);</span><br><span class="line"> &#125;</span><br><span class="line"> return (p);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这里就可以看到其本质是通过malloc开辟的一块空间，然后进行判断，如果开辟失败就会报出一个bad_alloc的异常，所以我们才不需要跟malloc一样用assert判断</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void operator delete(void *pUserData) &#123;</span><br><span class="line"> _CrtMemBlockHeader * pHead;</span><br><span class="line"> RTCCALLBACK(_RTC_Free_hook, (pUserData, 0));</span><br><span class="line"> if (pUserData == NULL)</span><br><span class="line"> return;</span><br><span class="line"> _mlock(_HEAP_LOCK); </span><br><span class="line"> __TRY</span><br><span class="line"> pHead = pHdr(pUserData);</span><br><span class="line"> _ASSERTE(_BLOCK_TYPE_IS_VALID(pHead-&gt;nBlockUse));</span><br><span class="line"> _free_dbg( pUserData, pHead-&gt;nBlockUse );</span><br><span class="line"> __FINALLY</span><br><span class="line"> _munlock(_HEAP_LOCK); </span><br><span class="line"> __END_TRY_FINALLY</span><br><span class="line"> return; &#125;</span><br></pre></td></tr></table></figure>



<p>看着一大串，其实本质还是调用free来清理空间</p>
<p>顺带看一下free的实现：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define free(p) _free_dbg(p, _NORMAL_BLOCK)</span><br></pre></td></tr></table></figure>



<h2 id="new、delete的实现原理"><a href="#new、delete的实现原理" class="headerlink" title="new、delete的实现原理"></a>new、delete的实现原理</h2><h3 id="内置类型"><a href="#内置类型" class="headerlink" title="内置类型"></a>内置类型</h3><p>如果是内置类型，new和malloc，delete和free其实是很像的，不同的是new、delete是申请单个元素的空间，new[]和delete[]申请的一块连续空间，new失败的时候会直接抛异常</p>
<h3 id="自定义类型"><a href="#自定义类型" class="headerlink" title="自定义类型"></a>自定义类型</h3><p>new的原理：调用operator new函数来申请空间，再在申请的空间上执行构造函数</p>
<p>delete的原理：在空间上执行析构函数，完成对象里面的清理，在调用free来释放空间、</p>
<p>new name[N]的原理：调用operator new[]函数，在operator[]里面完成N个对象的空间申请，再在申请的空间里面执行N次构造函数</p>
<p>delete name[N]的原理：在申请的空间里面执行N次析构函数，完成N个对象的资源清理，随后再调用operator[]释放空间，再在operator delete[]里面调用operator delate释放空间</p>
<p>总之，就套起来了。</p>
<hr>
<h2 id="定位new表达式"><a href="#定位new表达式" class="headerlink" title="定位new表达式"></a>定位new表达式</h2><p>这个，知道就好了，他的作用是在已经开辟好的空间调用初始化函数初始化一个对象。</p>
<p>这里直接看代码理解就好了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Date</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	Date()</span><br><span class="line">		:_year(2022),</span><br><span class="line">		_month(5),</span><br><span class="line">		_day(28)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;Date()&quot; &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	~Date()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;~Date()&quot; &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line">	int _year;</span><br><span class="line">	int _month;</span><br><span class="line">	int _day;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	Date* ptr1 = (Date*)malloc(sizeof(Date));</span><br><span class="line">	new(ptr1) Date;</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>一般来说用不到，看看就好了。</p>
<p>这里还有一个要注意的，就是如果构造函数里面要参数</p>
<hr>
<p>这里有几到很常见的题，了解就好了：</p>
<p>malloc&#x2F;free和new&#x2F;delete的区别 </p>
<blockquote>
<p>malloc&#x2F;free和new&#x2F;delete的共同点是：都是从堆上申请空间，并且需要用户手动释放。不同的地方是：</p>
<ol>
<li>malloc和free是函数，new和delete是操作符</li>
<li>malloc申请的空间不会初始化，new可以初始化</li>
<li>malloc申请空间时，需要手动计算空间大小并传递，new只需在其后跟上空间的类型即可</li>
<li>malloc的返回值为void*, 在使用时必须强转，new不需要，因为new后跟的是空间的类型</li>
<li>malloc申请空间失败时，返回的是NULL，因此使用时必须判空，new不需要，但是new需要捕获异常</li>
<li>申请自定义类型对象时，malloc&#x2F;free只会开辟空间，不会调用构造函数与析构函数，而new在申请空间后会调用构造函数完成对象的初始化，delete在释放空间前会调用析构函数完成空间中资源的清理</li>
</ol>
</blockquote>
<hr>
<h2 id="内存泄漏"><a href="#内存泄漏" class="headerlink" title="内存泄漏"></a>内存泄漏</h2><p>这里就直接看看定义和危害就好了：</p>
<blockquote>
<p>什么是内存泄漏：内存泄漏指因为疏忽或错误造成程序未能释放已经不再使用的内存的情况。内存泄漏并不是指内存在物理上的消失，而是应用程序分配某段内存后，因为设计错误，失去了对该段内存的控制，因而造成了内存的浪费。</p>
<p>内存泄漏的危害：长期运行的程序出现内存泄漏，影响很大，如操作系统、后台服务等等，出现内存泄漏会导致响应越来越慢，最终卡死。</p>
</blockquote>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>基础知识</title>
    <url>/2022/05/12/%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/</url>
    <content><![CDATA[<h1 id="test"><a href="#test" class="headerlink" title="test"></a>test</h1><hr>
<h2 id="C-是什么："><a href="#C-是什么：" class="headerlink" title="C++是什么："></a>C++是什么：</h2><p>用较为简单易懂的话来解释，就是在C的基础上改进的新语言，+的意思就是PLUS，因为是基于C语言的改进，所以引入了一些新的概念、语法，同时它也兼容C。</p>
<h2 id="关键字"><a href="#关键字" class="headerlink" title="关键字"></a>关键字</h2><p>跟C语言一样，C++同样有一些平时常用的单词，例如int char之类的，这里我放一张表，记不住也没有关系，之后学了才会有更深的理解。</p>
<p>下面是表：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201045410.png"></p>
<h2 id="命名空间"><a href="#命名空间" class="headerlink" title="命名空间"></a>命名空间</h2><p>这里要提到的一点是命名空间，相较于其他关键字，namespace出现的频率会高一些，在C++程序里的前几排，总有这么一行代码：</p>
<p>using namespace std；</p>
<p>要想理解这句话，就应该从什么是namespace说起，先来看一下官方的解释：</p>
<blockquote>
<p>在C&#x2F;C++中，变量、函数和后面要学到的类都是大量存在的，这些变量、函数和类的名称将都存在于全局作用域中，可能会导致很多冲突。使用命名空间的目的是对标识符的名称进行本地化，以避免命名冲突或名字污染，namespace关键字的出现就是针对这种问题的。</p>
</blockquote>
<p>如何理解？</p>
<p>举个例子，现在，我有两个变量都叫A,那我运行程序的时候程序肯定会报错，因为它不知道要用的是哪个A。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201045949.png"></p>
<p>这种情况下，就可以用到namespace。</p>
<p>以下面情况举个例子。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">namespace N1</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    int a;</span><br><span class="line">        </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">namespace N2</span><br><span class="line">&#123;</span><br><span class="line">    int a1 = 10;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">namespace N3</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    int c;</span><br><span class="line">namespace N2</span><br><span class="line">&#123;</span><br><span class="line">    int a1 = 10;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>两种都是可以的，namespace的作用就是将花括号里面的内容单独开辟一块空间出来，这块空间是不会与主程序产生冲突。</p>
<p>函数、结构体…这些都是可以放在命名空间里面的。</p>
<p>命名空间是可以嵌套使用的，但如果命名空间的名字相同，比如说这块空间是N1,另一块空间也是N1，那最后处理的时候它们就会合并到一起去。</p>
<p>命名空间就像新开辟了一块空间，命名空间里面的内容都局限于这一块空间中</p>
<p>如果要调用这块命名空间的内容，那就有两种办法，第一，是指定一块空间让系统在那里面去找。</p>
<p>举个例子</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046359.png"></p>
<p>这里就是很典型的例子，可以看到主程序里面并没有a1，但是我在它的左边加了两个双引号，并在双引号左边加了一个N2。</p>
<p>这行代码就是让程序在N2这块空间里面找一个叫a1的数字。</p>
<p>第二种办法是直接释放这块命名空间，第一种办法每次都只是去找其中一个元素，使用的次数一多，每次都加双引号和命名空间名是件很麻烦的事，所以为了解决这个问题可以一次将这里面的元素释放出来，这就需要在前面加一句</p>
<p>using namespace xxx；</p>
<p>这里又有两种用法，第一种是：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">using N2::a1</span><br></pre></td></tr></table></figure>

<p><img src="/%22%E7%82%B9%E5%87%BB%E5%B9%B6%E6%8B%96%E6%8B%BD%E4%BB%A5%E7%A7%BB%E5%8A%A8%22"></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046360.png"></p>
<p>意思是将N2里面的a1元素释放出来，如果嵌套的很多，那就要加更多的引号。</p>
<p>第二种：一次释放完全</p>
<p>使用办法：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046087.png"></p>
<p>如果是一次释放完全的话，那就应该在前面加上namespace，表明释放的是一块命名空间而不是单个元素。</p>
<p>那之前提到的：</p>
<p>using namespace std；</p>
<p>的意思就是将std里面的全部东西释放出来，经常使用的函数等…</p>
<h2 id="C-输入-amp-输出"><a href="#C-输入-amp-输出" class="headerlink" title="C++输入&amp;输出"></a>C++输入&amp;输出</h2><p>就好像新学C语言是先在屏幕上打印一个“hello world”一样，C++也先从这里开始比较好。</p>
<p>先输出吧，C++的输出跟C的区别这里就能看出来，假设要打印一句：hello world</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046784.png"></p>
<p>cout表示要输出东西，随后用两个左箭头，再用双引号将文本括起来，最后再用两个左箭头隔开，endl是换行的意思。</p>
<p>这里有几点要注意：</p>
<blockquote>
<ol>
<li>使用cout标准输出(控制台)和cin标准输入(键盘)时，必须包含&lt; iostream &gt;头文件以及std标准命名空间。</li>
</ol>
<p>注意：早期标准库将所有功能在全局域中实现，声明在.h后缀的头文件中，使用时只需包含对应头文件即可，后来将其实现在std命名空间下，为了和C头文件区分，也为了正确使用命名空间，规定C++头文件不带.h；旧编译器(vc 6.0)中还支持&lt;iostream.h&gt;格式，后续编译器已不支持，因此推荐使用 <iostream>+std的方式。</p>
<ol start="2">
<li>使用C++输入输出更方便，不需增加数据格式控制，比如：整形–%d，字符–%c</li>
</ol>
</blockquote>
<p>头文件肯定是要包含的，std命名空间要用记得释放。</p>
<p>这里要说的是第二点，C++输出的时候不要增加类型，编译器会自动识别，这里将输入也连起来：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046983.png"></p>
<p>创建三个变量，输入有要用cin，随后顺序输入,中间用向右的箭头连接，表示下一个输入的是它。</p>
<p>输入的时候跟刚刚一样，只不过中间的空格需要手打比较麻烦，不过这里可以看到没有提示类型它也可以直接输出，相较C舒服多了。</p>
<h2 id="缺省参数"><a href="#缺省参数" class="headerlink" title="缺省参数"></a>缺省参数</h2><p>这里是跟C差异较大的地方，在C里面，函数声明有几个参数，那你传过去就要几个参数，但是在C++里面，就不是这样的。</p>
<p>先从概念说起：</p>
<blockquote>
<p>缺省参数是声明或定义函数时为函数的参数指定一个默认值。在调用该函数时，如果没有指定实参则采用该默认值，否则使用指定的实参。</p>
</blockquote>
<p>举个例子：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046175.png"></p>
<p>这里就是调用了缺省参数，我参数里面接受类型里面定义了一个变量a，但是我传参数的时候缺没有传a的值过去，那C++就默认用函数参数里面定义的那个值，这里是 10。</p>
<p>但是如果我传来一个参数过去：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046909.png"></p>
<p>这里如果我传了一个2作为参数过去，那函数调用就会优先使用我传过去的参数。</p>
<h2 id="函数重载"><a href="#函数重载" class="headerlink" title="函数重载"></a>函数重载</h2><p>在C里面，函数名是不可以冲突的，但是在C++里面，就不会有这个问题，举个例子：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046095.png"></p>
<p>这里就是重载函数，它函数名一样，但是函数的参数不一样，根据我传过去的参数不同，系统会调用对应的函数。</p>
<p>然后，再来看一下对于重载的说明：</p>
<blockquote>
<p>函数重载:是函数的一种特殊情况，C++允许在同一作用域中声明几个功能类似的同名函数，这些同名函数的形参列表(参数个数 或 类型 或 顺序)必须不同，常用来处理实现功能类似数据类型不同的问题。</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">short Add(short left, short right) </span><br><span class="line">&#123;</span><br><span class="line"> return left+right; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int Add(short left, short right) </span><br><span class="line">&#123;</span><br><span class="line"> return left+right; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这算吗？</p>
<p>它的函数名不同吗？</p>
<p>它的参数个数不同吗？</p>
<p>它的参数类型不一样吗？</p>
<p>但是它只有返回值不一样，所以它并不算重载参数。</p>
<h2 id="重载参数的实现"><a href="#重载参数的实现" class="headerlink" title="重载参数的实现"></a>重载参数的实现</h2><p>这里就要从底层实现开始说，当程序开始运行的时候，它会经过几个阶段：</p>
<p>1、预处理：头文件展开、宏的替换、条件编译、去掉注释</p>
<p>这里会生成 .I文件</p>
<p>2、编译：检查语法，生成汇编代码</p>
<p>这里生成 .s 文件</p>
<p>3、汇编：将汇编代码转换为二进制的机器码</p>
<p>这里生成 .o文件</p>
<p>4、连接：开始找要调用的函数地址，然后将其连接在一起</p>
<p>这里生成  .out文件</p>
<p>那函数重载，C&#x2F;C++就是在连接部分不一样，因为我还不会Linux，所以这里先说一下结论，之后再将详细过程写出来。</p>
<p>总之，C在连接过程中会很简单的将函数名对应起来，如果相同那它就会产生冲突</p>
<p>但是C++不是，它的函数名对应是这样的：_Z+函数名+参数首字母</p>
<p>假设是这样的一个函数：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046864.png"></p>
<p>C里面应该是两个fun函数，但是C++是这样的：_Z3i 和 _Z3d</p>
<p>因为函数名的参数不一样，它的函数名就不一样，所以在找的时候就不会有冲突问题。</p>
<h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><p>这个我个人认为是指针的改版，举个例子：</p>
<p>我现在要交换两个数，如果是指针，那应该这么写：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046133.png"></p>
<p>如果是用引用，那它应该是这样的</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201046909.png"></p>
<p>同样可以达成想要的效果，那再来看看引用的解释：</p>
<p>引用不是新定义一个变量，而是给已存在变量取了一个别名，编译器不会为引用变量开辟内存空间，它和它引用的变量共用同一块内存空间。</p>
<p>类型&amp; 引用变量名(对象名) &#x3D; 引用实体；</p>
<p>这里要注意两点，引用类型应该和引用实体是一个类型（但是如果不是一个类型，加一个const也是可以的）。</p>
<h3 id="引用特性"><a href="#引用特性" class="headerlink" title="引用特性"></a>引用特性</h3><blockquote>
<ol>
<li>引用在定义时必须初始化 </li>
<li>一个变量可以有多个引用</li>
<li>引用一旦引用一个实体，再不能引用其他实体</li>
</ol>
</blockquote>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047303.png"></p>
<p>这是可行的，但是如果引用没有初始化，那就会报错：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047689.png"></p>
<p>同时，还可以：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047829.png"></p>
<p>引用引用（套娃）</p>
<p>但是引用只有一个对象，不能再引用其他对象，这里就要注意下。</p>
<p>同时，这里还要注意一点：</p>
<p>引用只能将权限缩而不能放大：</p>
<p>举个例子：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047856.png"></p>
<p>在这里就是一个很明显的例子，const修饰的对象是不能被修改的，而你引用的权限是可以修改，所以就会报错。</p>
<p>如果要引用，那在引用的时候就应该也带上const<img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047918.png"></p>
<p>引用还可以作为返回值，作为返回值返回的是那一块空间的地址。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047256.png"></p>
<p>但是这种是错误的示范，因为函数返回的是n的别名（本质还是地址），但是n是一个局部变量，出了函数就会销毁，在销毁之后再去访问就会出现问题——越界。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047474.png"></p>
<p>再看这样一个代码，这里拿ret来接收了它的返回值，但是之后又调用了这个函数，在之后打印结果是？</p>
<p>本意是 3.但是引用返回的是c的地址，在1+2后又对这一块空间进行修改：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047881.png"></p>
<p>所以它就出错了。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047081.png"></p>
<p>如果再次看结果，这就是一个随机值了，因为这一块空间已经归还给了系统，再去看就被刷新了。</p>
<h3 id="引用的效率问题"><a href="#引用的效率问题" class="headerlink" title="引用的效率问题"></a>引用的效率问题</h3><p>值和引用的作为返回值类型的性能比较</p>
<p>如果是这样的一个程序：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047853.png"></p>
<p>明明c的寿命周期已经到了，那为什么还能返回？</p>
<p>因为系统会将这个值暂存下来，可以理解为临时拷贝一份，那既然是临时拷贝，这效率就可以直说。</p>
<p>它没有引用块，假设这个数据很大，有一百万个数据，一千万个数据 ，那拷贝就要耗时间，而引用是直接返回地址，所以会更快。</p>
<p>相较于指针，引用和指针应该是一个级别。</p>
<h2 id="引用和指针的区别"><a href="#引用和指针的区别" class="headerlink" title="引用和指针的区别"></a>引用和指针的区别</h2><blockquote>
<p>在概念上，指针是一块地址，引用是变量的别名，它们占用空间就有区别</p>
<p>但底层实际方面，引用跟指针实现方式很像，也是要占用一块空间的。</p>
</blockquote>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047391.png"></p>
<blockquote>
<ol>
<li>引用在定义时必须初始化，指针没有要求</li>
<li>引用在初始化时引用一个实体后，就不能再引用其他实体，而指针可以在任何时候指向任何一个同类型</li>
</ol>
<p>实体</p>
<ol start="3">
<li>没有NULL引用，但有NULL指针</li>
<li>在sizeof中含义不同：引用结果为引用类型的大小，但指针始终是地址空间所占字节个数(32位平台下占</li>
</ol>
<p>4个字节)</p>
<ol start="5">
<li>引用自加即引用的实体增加1，指针自加即指针向后偏移一个类型的大小</li>
<li>有多级指针，但是没有多级引用 </li>
<li>访问实体方式不同，指针需要显式解引用，引用编译器自己处理 </li>
<li>引用比指针使用起来相对更安全</li>
</ol>
</blockquote>
<h2 id="内联函数"><a href="#内联函数" class="headerlink" title="内联函数"></a>内联函数</h2><p>当使用函数的时候，会有这么一种情况，就是：如果这个函数很短，但是需要用的地方很多，比方说实现一个A+B的函数，有没有什么办法可以解决这个问题？</p>
<p>当然，宏，那宏是怎么写的呢？</p>
<p>这里给你十秒，想一个宏实现A+B。</p>
<p>想出来了吗？</p>
<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047389.png"></p>
<p>因为宏直接替换，不好理解而且不方便查看过程，很容易出错，那这里就引入一个概念叫内联函数。</p>
<p>这里先看一下内联函数的定义：</p>
<blockquote>
<p>以inline修饰的函数叫做内联函数，编译时C++编译器会在调用内联函数的地方展开，没有函数压栈的开销，内联函数提升程序运行的效率。</p>
</blockquote>
<p>要理解这句话，要先理解什么是函数栈帧，每调用一个函数，都会在内存中开辟一块空间，然后压栈。</p>
<p>但如果是使用内联函数，那么程序运行就会直接在函数调用地方展开，不压栈，这样效率会更高。</p>
<p>但是如果这个函数很长，再用内联函数的话，每次调用都展开未免也有点太长了，所以编译器在执行的时候会先判断这个函数内容多不多，如果这个函数内容很多，它会不执行内联，如果很短，它会先替换。</p>
<p>那么多少代码算长的？</p>
<p>10行左右。</p>
<p>如果你想，所有函数前面都可以加一个inline，让编译器自己判断。</p>
<hr>
<h3 id="内联函数的特性"><a href="#内联函数的特性" class="headerlink" title="内联函数的特性"></a>内联函数的特性</h3><blockquote>
<ol>
<li>inline是一种以空间换时间的做法，省去调用函数额开销。所以代码很长或者有循环&#x2F;递归的函数不适宜使用作为内联函数。</li>
<li>inline对于编译器而言只是一个建议，编译器会自动优化，如果定义为inline的函数体内有循环&#x2F;递归等等，编译器优化时会忽略掉内联。</li>
<li>inline不建议声明和定义分离，分离会导致链接错误。因为inline被展开，就没有函数地址了，链接就会找不到。</li>
</ol>
</blockquote>
<p>这里重点要了解第三条，如果分离，展开的时候就找不到函数本体，这里看一个例子：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047299.png"></p>
<p>在Code.h里面我声明了一个内联函数，但是我没有定义，而是在：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047669.png"></p>
<p>Code.h里面定义了这个函数，如果现在要用这个函数，会发生什么？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047740.png"></p>
<p>无法解析的外部符号，这里就可以直接理解为程序运行的时候找不到这个函数，但是我明明声明了这个函数，为什么还是找不到？</p>
<p>因为inline被展开之后它没有定义，只是一个声明，既然只有一个声明那它怎么可能调用呢？</p>
<p>这里要注意一下。</p>
<p>这里把宏的优缺点说一下：</p>
<blockquote>
<p>优点：</p>
<p>1.增强代码的复用性。</p>
<p>2.提高性能。</p>
<p>缺点：</p>
<p>1.不方便调试宏。（因为预编译阶段进行了替换）</p>
<p>2.导致代码可读性差，可维护性差，容易误用。</p>
<p>3.没有类型安全的检查 。</p>
</blockquote>
<blockquote>
<p>C++有哪些技术替代宏？</p>
<ol>
<li>常量定义 换用const</li>
<li>函数定义 换用内联函数</li>
</ol>
</blockquote>
<p>这里第一条有在《EffectiveC++》的条款里面提到：</p>
<blockquote>
<p>EffectiveC++ 条款02：尽量以const,enum,inline替换#define</p>
</blockquote>
<p>这里看图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201047838.png"></p>
<p>可以看到，maxa已经有值了，但是无法识别出MAX。</p>
<p>为了解决这种类似问题，就可以用const——无法修改的变量和enum枚举常量来代替。</p>
<h2 id="auto关键字"><a href="#auto关键字" class="headerlink" title="auto关键字"></a>auto关键字</h2><p>当定义一个变量的时候，应该是这样定义的：</p>
<p>int i &#x3D; 1；</p>
<p>float j &#x3D; 1.1；</p>
<p>char k &#x3D; ‘A’;</p>
<p>auto的作用就是，它会根据后面变量的数值来给类型：</p>
<blockquote>
<p>在早期C&#x2F;C++中auto的含义是：使用auto修饰的变量，是具有自动存储器的局部变量，但遗憾的是一直没有人去使用它，大家可思考下为什么？</p>
<p>C++11中，标准委员会赋予了auto全新的含义即：auto不再是一个存储类型指示符，而是作为一个新的类型指示符来指示编译器，auto声明的变量必须由编译器在编译时期推导而得。</p>
</blockquote>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048339.png"></p>
<p>如果用auto，是怎么样的？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048011.png"></p>
<p>可以看到基本是一样的。</p>
<p>但如果开始并未赋值，就会报错：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048628.png"></p>
<p>因为auto是根据后面的值来判断类型，如果不给值那它就无法判断。</p>
<p>如果用auto来定义指针，会发生什么？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048721.png"></p>
<p>可以看到，如果是auto + &amp;，那auto就会默认是int*</p>
<p>如果是第二种，auto后面已经带<em>了，那auto还是int</em>。</p>
<p>如果是引用，那就应该是这样：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048440.png"></p>
<p>还要注意，如果一行定义多个变量，那这些变量都应该是一个类型：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048581.png"></p>
<p>同时，它不能作为函数参数：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048107.png"></p>
<p>还是那句，它是根据后面的值来给类型的，并且必须初始化。</p>
<p>同时，不能声明数组：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048494.png"></p>
<h3 id="auto在for循环内部"><a href="#auto在for循环内部" class="headerlink" title="auto在for循环内部"></a>auto在for循环内部</h3><p>以往，遍历数组是这样的</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048909.png"></p>
<p>现在，可以用auto：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048969.png"></p>
<p>它会自己遍历每个数打印，那能不能直接修改呢？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048916.png"></p>
<p>可以看到，第一个遍历已经修改了里面的值，但是到了第二个遍历可以看到还是原来的值。</p>
<p>这是因为这里面的e是一份临时拷贝而不是对应的那个值，如果通过这个修改值，那有什么方法？</p>
<p>别名。</p>
<p>可以看下图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048887.png"></p>
<p>这样就可以了。</p>
<h2 id="指针空值nullptr"><a href="#指针空值nullptr" class="headerlink" title="指针空值nullptr"></a>指针空值nullptr</h2><p>这个是后来的规定，大致意思：</p>
<p>C语言有以下两种办法指定空指针：</p>
<p>int*p &#x3D; 0；</p>
<p>int* pp &#x3D; NULL；</p>
<p>这两种都是制定为空指针，但是在C++（11）的标准里面，不算规范写法，是因为这两种写法的等价的：</p>
<blockquote>
<p>#ifndef NULL</p>
<p>#ifdef __cplusplus</p>
<p>#define NULL 0</p>
<p>#else</p>
<p>#define NULL ((void *)0)</p>
<p>#endif</p>
<p>#endif</p>
</blockquote>
<p>这是网上找到的C头文件，可以看到这里是直接讲 NULL替换为 0，或者是void类型的指针。</p>
<p>这里有办法可以看看：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048000.png"></p>
<p>这是一个重载函数，但区别是一个是指针一个是整形，前面两个传参是 0 和NULL，看右边可以看出来它们调用的都是整形的函数，这里就可以看出来NULL跟0是一样的，而如果在传参时将NULL强转为int*，那它才会调用第二个函数。</p>
<p>那规范的空指针写法是什么样的？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201048699.png"></p>
<p>如图，nullptr是规范的空指针写法，传过去的也是一个指针。</p>
<p>这里有几点注意：</p>
<blockquote>
<p>nullptr是关键字，不需要头文件</p>
<p>C++11中，sizeof(nullptr) 与sizeof((void*)0所占的字节数相同</p>
<p>能用nullptr，就用nullptr，这样比较规范</p>
</blockquote>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>堆</title>
    <url>/2022/04/03/%E5%A0%86/</url>
    <content><![CDATA[<hr>
<p><strong>建议先明白二叉树的基本概念之后来看这个，二叉树我会单独另写一篇专栏。</strong></p>
<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200927615.png"></p>
<p>这个看着比较麻烦，不好理解，不过可以直接解释为：</p>
<p><strong>用数组的形式存储一个完全二叉树，这个二叉树是大根或者小根</strong></p>
<hr>
<p>如果明白顺序表的增删查改，那这个会很好理解，对顺序表有兴趣可以点开我的另一篇专栏</p>
<p>我们先从Heap.h文件开始：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//包含头文件</span><br><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">#include&lt;stdbool.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//创建结构体和自定义类型，方便之后如果存储类型有变化</span><br><span class="line">typedef int HeapDataType;</span><br><span class="line"></span><br><span class="line">typedef struct Heap</span><br><span class="line">&#123;</span><br><span class="line">	HeapDataType* arr;</span><br><span class="line">	int size;</span><br><span class="line">	int capacity;</span><br><span class="line">&#125;Heap;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//之后会实现的函数</span><br><span class="line"></span><br><span class="line">//交换</span><br><span class="line">void swap(HeapDataType* pa, HeapDataType* pb);</span><br><span class="line"></span><br><span class="line">//打印</span><br><span class="line">void HeapPrintf(Heap* hp);</span><br><span class="line"></span><br><span class="line">//向上调整</span><br><span class="line">void AdjustUp(HeapDataType* a, size_t child);</span><br><span class="line"></span><br><span class="line">//向下调整</span><br><span class="line">void AdjustDown(Heap* hp);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 堆的初始化</span><br><span class="line">void HeapInit(Heap* hp);</span><br><span class="line"></span><br><span class="line">// 堆的销毁</span><br><span class="line">void HeapDestory(Heap* hp);</span><br><span class="line"></span><br><span class="line">// 堆的插入</span><br><span class="line">void HeapPush(Heap* hp, HeapDataType x);</span><br><span class="line"></span><br><span class="line">// 堆的删除</span><br><span class="line">void HeapPop(Heap* hp);</span><br><span class="line"></span><br><span class="line">// 取堆顶的数据</span><br><span class="line">HeapDataType HeapTop(Heap* hp);</span><br><span class="line"></span><br><span class="line">// 堆的数据个数</span><br><span class="line">int HeapSize(Heap* hp);</span><br><span class="line"></span><br><span class="line">// 堆的判空</span><br><span class="line">bool HeapEmpty(Heap* hp);</span><br></pre></td></tr></table></figure>



<p>Heap.c：</p>
<h2 id="堆的初始化"><a href="#堆的初始化" class="headerlink" title="堆的初始化"></a>堆的初始化</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 堆的构建</span><br><span class="line">void HeapInit(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">    //判断传进来的指针</span><br><span class="line">	assert(hp);</span><br><span class="line"></span><br><span class="line">    //一些初始的赋值</span><br><span class="line">	hp-&gt;arr = NULL;</span><br><span class="line">	hp-&gt;capacity = 0;</span><br><span class="line">	hp-&gt;size = 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="堆的销毁"><a href="#堆的销毁" class="headerlink" title="堆的销毁"></a>堆的销毁</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 堆的销毁</span><br><span class="line">void HeapDestory(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line"></span><br><span class="line">     //跟初始化差不多</span><br><span class="line">	free(hp-&gt;arr);</span><br><span class="line">	hp-&gt;arr = NULL;</span><br><span class="line">	hp-&gt;capacity = 0;</span><br><span class="line">	hp-&gt;size = 0;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="堆的插入"><a href="#堆的插入" class="headerlink" title="堆的插入"></a>堆的插入</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 堆的插入</span><br><span class="line">void HeapPush(Heap* hp, HeapDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line"></span><br><span class="line">    //扩容</span><br><span class="line">	if (hp-&gt;capacity == hp-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		int newcapacity = hp-&gt;capacity == 0 ? 4 : hp-&gt;capacity * 2;</span><br><span class="line">		hp-&gt;arr = (HeapDataType*)realloc(hp-&gt;arr, sizeof(HeapDataType)* newcapacity);</span><br><span class="line">		if (hp-&gt;arr == NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			printf(&quot;Realloc Fail\n&quot;);</span><br><span class="line">			exit(-1);</span><br><span class="line">		&#125;</span><br><span class="line">		hp-&gt;capacity = newcapacity;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">    //插入</span><br><span class="line">	hp-&gt;arr[hp-&gt;size] = x;	</span><br><span class="line">	hp-&gt;size++;</span><br><span class="line"></span><br><span class="line">    //调整</span><br><span class="line">	AdjustUp(hp-&gt;arr, hp-&gt;size-1);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这里注意的是我们要调整，假设我们这是一个小根，比如是</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200927284.png"></p>
<p>这是我们目前的数组的逻辑结构，假设我们现在要插入一个0，0肯定比这上面的所有数字都小，那我们就要调整，调整到它是根位置。</p>
<p>这是它的物理结构</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200927444.png"></p>
<p>我们知道二叉树的父子节点的关系是：</p>
<p>假设父节点是0，那它的左子节点就是 0*2+1 &#x3D; 1，就是下标为1的元素</p>
<p>它的右子节点就是0*2+2 &#x3D; 2。</p>
<p>…</p>
<p>我们插入的时候是在尾部插入，那这就是子节点，我们要拿它跟父节点比较，如果它比父节点小，那我们就交换</p>
<p>既然插入要调整，那删除肯定也要调整，所以我们可以写一个交换函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//交换</span><br><span class="line">void swap(HeapDataType* pa, HeapDataType* pb)</span><br><span class="line">&#123;</span><br><span class="line">	HeapDataType data = *pa;</span><br><span class="line">	*pa = *pb;</span><br><span class="line">	*pb = data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//向上调整</span><br><span class="line">void AdjustUp(HeapDataType* arr, size_t child)</span><br><span class="line">&#123;</span><br><span class="line">	assert(arr);</span><br><span class="line"></span><br><span class="line">    //算父节点大小</span><br><span class="line">	size_t parents = (child - 1) / 2;</span><br><span class="line">	while (child &gt; 0)</span><br><span class="line">	&#123;</span><br><span class="line">        //比较</span><br><span class="line">		if (arr[parents] &gt; arr[child])</span><br><span class="line">		&#123;</span><br><span class="line">			swap(&amp;arr[parents], &amp;arr[child]);</span><br><span class="line">            //别忘了重置父子节点继续往下走</span><br><span class="line">			child = parents;</span><br><span class="line">			parents = (child - 1) / 2;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">            //不大我们就可以退出了。</span><br><span class="line">			break;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>插入我们明白了，那我们写一个删除：</p>
<h2 id="堆的删除"><a href="#堆的删除" class="headerlink" title="堆的删除"></a>堆的删除</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 堆的删除</span><br><span class="line">void HeapPop(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line">	assert(hp-&gt;size &gt; 0);</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">    //将第一个元素跟最后一个换位置，然后将size（元素个数-1）</span><br><span class="line">	hp-&gt;arr[0] = hp-&gt;arr[hp-&gt;size - 1];</span><br><span class="line">	hp-&gt;size--;</span><br><span class="line">    //向下调整</span><br><span class="line">	AdjustDown(hp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200927122.png"></p>
<p>我们让8跟1，也就是根换位置，然后让size–，那就好像：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200927317.png"></p>
<p>然后让8跟2 3比较 ，找到它们之中小的那个交换位置，如果它子节点还是比它大，那就再换：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//向下调整</span><br><span class="line">void AdjustDown(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line">	size_t parents = 0;</span><br><span class="line">	size_t child = parents * 2 + 1;</span><br><span class="line">	while (child &lt; hp-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		if (hp-&gt;arr[child + 1] &lt; hp-&gt;arr[child])</span><br><span class="line">		&#123;</span><br><span class="line">			child++;</span><br><span class="line">		&#125;</span><br><span class="line">		if (hp-&gt;arr[parents]&gt;hp-&gt;arr[child])</span><br><span class="line">		&#123;</span><br><span class="line">			swap(&amp;hp-&gt;arr[parents], &amp;hp-&gt;arr[child]);</span><br><span class="line">			parents = child;</span><br><span class="line">			child = parents * 2 + 1;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			break;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="获取堆顶数据"><a href="#获取堆顶数据" class="headerlink" title="获取堆顶数据"></a>获取堆顶数据</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 取堆顶的数据</span><br><span class="line">HeapDataType HeapTop(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line">	assert(hp-&gt;size &gt; 0);</span><br><span class="line"></span><br><span class="line">	return hp-&gt;arr[0];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="判断堆是否为空"><a href="#判断堆是否为空" class="headerlink" title="判断堆是否为空"></a>判断堆是否为空</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 堆的判空</span><br><span class="line">bool HeapEmpty(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line"></span><br><span class="line">	return hp-&gt;size == 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="查看元素个数"><a href="#查看元素个数" class="headerlink" title="查看元素个数"></a>查看元素个数</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int HeapSize(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line"></span><br><span class="line">	return hp-&gt;size;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="这里放上全部代码方便复制："><a href="#这里放上全部代码方便复制：" class="headerlink" title="这里放上全部代码方便复制："></a>这里放上全部代码方便复制：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Heap.h</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">#include&lt;stdbool.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">typedef int HeapDataType;</span><br><span class="line"></span><br><span class="line">typedef struct Heap</span><br><span class="line">&#123;</span><br><span class="line">	HeapDataType* arr;</span><br><span class="line">	int size;</span><br><span class="line">	int capacity;</span><br><span class="line">&#125;Heap;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//交换</span><br><span class="line">void swap(HeapDataType* pa, HeapDataType* pb);</span><br><span class="line"></span><br><span class="line">//打印</span><br><span class="line">void HeapPrintf(Heap* hp);</span><br><span class="line"></span><br><span class="line">//向上调整</span><br><span class="line">void AdjustUp(HeapDataType* a, size_t child);</span><br><span class="line"></span><br><span class="line">//向下调整</span><br><span class="line">void AdjustDown(Heap* hp);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 堆的构建</span><br><span class="line">void HeapInit(Heap* hp);</span><br><span class="line">// 堆的销毁</span><br><span class="line">void HeapDestory(Heap* hp);</span><br><span class="line">// 堆的插入</span><br><span class="line">void HeapPush(Heap* hp, HeapDataType x);</span><br><span class="line">// 堆的删除</span><br><span class="line">void HeapPop(Heap* hp);</span><br><span class="line">// 取堆顶的数据</span><br><span class="line">HeapDataType HeapTop(Heap* hp);</span><br><span class="line">// 堆的数据个数</span><br><span class="line">int HeapSize(Heap* hp);</span><br><span class="line">// 堆的判空</span><br><span class="line">bool HeapEmpty(Heap* hp);</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Heap.c</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">#include&quot;Heap.h&quot;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//交换</span><br><span class="line">void swap(HeapDataType* pa, HeapDataType* pb)</span><br><span class="line">&#123;</span><br><span class="line">	HeapDataType data = *pa;</span><br><span class="line">	*pa = *pb;</span><br><span class="line">	*pb = data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">//打印</span><br><span class="line">void HeapPrintf(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line">	for (int i = 0; i &lt; hp-&gt;size; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d &quot;, hp-&gt;arr[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	printf(&quot;\n&quot;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//向上调整</span><br><span class="line">void AdjustUp(HeapDataType* arr, size_t child)</span><br><span class="line">&#123;</span><br><span class="line">	assert(arr);</span><br><span class="line"></span><br><span class="line">	size_t parents = (child - 1) / 2;</span><br><span class="line">	while (child &gt; 0)</span><br><span class="line">	&#123;</span><br><span class="line">		if (arr[parents] &gt; arr[child])</span><br><span class="line">		&#123;</span><br><span class="line">			swap(&amp;arr[parents], &amp;arr[child]);</span><br><span class="line">			child = parents;</span><br><span class="line">			parents = (child - 1) / 2;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			break;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 堆的构建</span><br><span class="line">void HeapInit(Heap* hp, HeapDataType* a, int n)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line">	hp-&gt;arr = NULL;</span><br><span class="line">	hp-&gt;capacity = 0;</span><br><span class="line">	hp-&gt;size = 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 堆的销毁</span><br><span class="line">void HeapDestory(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line">	free(hp-&gt;arr);</span><br><span class="line">	hp-&gt;arr = NULL;</span><br><span class="line">	hp-&gt;capacity = 0;</span><br><span class="line">	hp-&gt;size = 0;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">// 堆的插入</span><br><span class="line">void HeapPush(Heap* hp, HeapDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line"></span><br><span class="line">	if (hp-&gt;capacity == hp-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		int newcapacity = hp-&gt;capacity == 0 ? 4 : hp-&gt;capacity * 2;</span><br><span class="line">		hp-&gt;arr = (HeapDataType*)realloc(hp-&gt;arr, sizeof(HeapDataType)* newcapacity);</span><br><span class="line">		if (hp-&gt;arr == NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			printf(&quot;Realloc Fail\n&quot;);</span><br><span class="line">			exit(-1);</span><br><span class="line">		&#125;</span><br><span class="line">		hp-&gt;capacity = newcapacity;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	hp-&gt;arr[hp-&gt;size] = x;	</span><br><span class="line">	hp-&gt;size++;</span><br><span class="line">	AdjustUp(hp-&gt;arr, hp-&gt;size-1);</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//向下调整</span><br><span class="line">void AdjustDown(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line">	size_t parents = 0;</span><br><span class="line">	size_t child = parents * 2 + 1;</span><br><span class="line">	while (child &lt; hp-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		if (hp-&gt;arr[child + 1] &lt; hp-&gt;arr[child])</span><br><span class="line">		&#123;</span><br><span class="line">			child++;</span><br><span class="line">		&#125;</span><br><span class="line">		if (hp-&gt;arr[parents]&gt;hp-&gt;arr[child])</span><br><span class="line">		&#123;</span><br><span class="line">			swap(&amp;hp-&gt;arr[parents], &amp;hp-&gt;arr[child]);</span><br><span class="line">			parents = child;</span><br><span class="line">			child = parents * 2 + 1;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			break;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 堆的删除</span><br><span class="line">void HeapPop(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line">	assert(hp-&gt;size &gt; 0);</span><br><span class="line"></span><br><span class="line">	hp-&gt;arr[0] = hp-&gt;arr[hp-&gt;size - 1];</span><br><span class="line">	hp-&gt;size--;</span><br><span class="line">	AdjustDown(hp);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 取堆顶的数据</span><br><span class="line">HeapDataType HeapTop(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line">	assert(hp-&gt;size &gt; 0);</span><br><span class="line"></span><br><span class="line">	return hp-&gt;arr[0];</span><br><span class="line">&#125;</span><br><span class="line">// 堆的数据个数</span><br><span class="line">int HeapSize(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line"></span><br><span class="line">	return hp-&gt;size;</span><br><span class="line">&#125;</span><br><span class="line">// 堆的判空</span><br><span class="line">bool HeapEmpty(Heap* hp)</span><br><span class="line">&#123;</span><br><span class="line">	assert(hp);</span><br><span class="line"></span><br><span class="line">	return hp-&gt;size == 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<p>堆的应用：</p>
<h2 id="堆排序"><a href="#堆排序" class="headerlink" title="堆排序"></a>堆排序</h2><p>我们已知这是一个小根，每次堆顶的元素都是最小的元素，那每次都拿出堆顶的元素，然后再删除，再拿出，再删除，一直到堆没有即可：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200927162.png"></p>
<hr>
<h2 id="TOP-K"><a href="#TOP-K" class="headerlink" title="TOP K"></a>TOP K</h2><p>假设现在要从100w个数据中选最大的前K个，三种办法：</p>
<p>1、排序：时间复杂度:O（N*logN）,空间复杂度：O（1），如果优化可能会更快</p>
<p>2、建堆：然后每次取堆顶的再删除，时间复杂度:O（N<em>logN</em>K），空间复杂度：O（1）</p>
<p>3、用前K个建小堆，如果后面的数字比堆顶大，我们就调整，这样的好处是时间复杂度为：O（K+logK*（N-K）），看上去很多，但实际上因为K很小，而N很大，所以几乎可以越等为O（N），但空间复杂度就是O（K）</p>
<p>这里主要说的就是第三种</p>
<p>先说思路，我们拿到数据，然后根据需求建堆，再遍历调整，因为代码都是之前写过的，所以这里只在代码内部标上注释方便阅读</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line"></span><br><span class="line">//最大前top个</span><br><span class="line">#define top 5</span><br><span class="line">//交换</span><br><span class="line">void swap(int* a, int* b)</span><br><span class="line">&#123;</span><br><span class="line">	int tmp = *a;</span><br><span class="line">	*a = *b;</span><br><span class="line">	*b = tmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">//向下调整</span><br><span class="line">void AdjustDown(int* arr, int root, int end)</span><br><span class="line">&#123;</span><br><span class="line">	int parent = root;</span><br><span class="line">	int child = parent * 2+1;</span><br><span class="line">	while (child &lt; end)</span><br><span class="line">	&#123;</span><br><span class="line"></span><br><span class="line">		if (arr[child + 1] &lt; arr[child] &amp;&amp; (child + 1) &lt; end)</span><br><span class="line">		&#123;</span><br><span class="line">			child++;</span><br><span class="line">		&#125;</span><br><span class="line">		if (arr[parent] &gt; arr[child])</span><br><span class="line">		&#123;</span><br><span class="line">			swap(&amp;(arr[parent]), &amp;(arr[child]));</span><br><span class="line">			parent = child;</span><br><span class="line">			child = parent * 2+1;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			break;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int* topk(int* brr,int end , int len)</span><br><span class="line">&#123;</span><br><span class="line">	int* arr = (int*)malloc(sizeof(int)*len);</span><br><span class="line">	//初始化</span><br><span class="line">	for (int i = 0; i &lt; len; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		arr[i] = brr[i];</span><br><span class="line">	&#125;</span><br><span class="line">	//建小堆</span><br><span class="line">	for (int i = (len - 2)/2; i &gt;= 0; i--)</span><br><span class="line">	&#123;</span><br><span class="line">		AdjustDown(arr, i, top);</span><br><span class="line">	&#125;</span><br><span class="line">	//如果比根大就交换，再调整</span><br><span class="line">	for (int i = len; i &lt; end; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		if (brr[i] &gt; arr[0])</span><br><span class="line">		&#123;</span><br><span class="line">			arr[0] = brr[i];</span><br><span class="line">			AdjustDown(arr, 0, top);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	return arr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">//主函数测试用</span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	int arr[10000];</span><br><span class="line">	for (int i = 0; i &lt; 10000; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		arr[i] = 100;</span><br><span class="line">	&#125;</span><br><span class="line">	arr[10] = 10001;</span><br><span class="line">	arr[100] = 10002;</span><br><span class="line">	arr[1000] = 10003;</span><br><span class="line">	arr[2000] = 10004;</span><br><span class="line">	arr[3000] = 10005;</span><br><span class="line">	/*int arr[] = &#123; 6, 5, 4, 3, 2, 1, 9, 8, 7 &#125;;*/</span><br><span class="line">	int len = sizeof(arr) / sizeof(arr[0]);</span><br><span class="line">	int* a = topk(&amp;arr,len, top);</span><br><span class="line">	for (int i = 0; i &lt; top; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d &quot;, a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	system(&quot;pause&quot;);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<p>感谢您的观看，我们下次再见</p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>复杂度</title>
    <url>/2022/03/06/%E5%A4%8D%E6%9D%82%E5%BA%A6/</url>
    <content><![CDATA[<h2 id="为什么要明白复杂度"><a href="#为什么要明白复杂度" class="headerlink" title="为什么要明白复杂度"></a>为什么要明白复杂度</h2><p>当我们写出一个算法的时候，我们如何去看这个算法的好坏？</p>
<p>这里拿经典的斐波那契的递归算法举例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">long long Fib(int N) </span><br><span class="line">&#123;</span><br><span class="line"> if(N &lt; 3)</span><br><span class="line"> return 1;</span><br><span class="line"> </span><br><span class="line"> return Fib(N-1) + Fib(N-2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到这里用递归实现看上去相当简洁，感觉上是个很好的算法，但实际上呢，它到底好不好呢？</p>
<p>这里就要引出一个概念：</p>
<h2 id="算法复杂度"><a href="#算法复杂度" class="headerlink" title="算法复杂度"></a>算法复杂度</h2><p>算法在编写成可执行程序后，运行时需要耗费时间资源和空间(内存)资源 。因此衡量一个算法的好坏，一般是从时间和空间两个维度来衡量的，即时间复杂度和空间复杂度。</p>
<p>时间复杂度主要衡量一个算法的运行快慢，而空间复杂度主要衡量一个算法运行所需要的额外空间。在计算 机发展的早期，计算机的存储容量很小。所以对空间复杂度很是在乎。但是经过计算机行业的迅速发展，计 算机的存储容量已经达到了很高的程度。所以我们如今已经不需要再特别关注一个算法的空间复杂度。</p>
<h3 id="时间复杂度"><a href="#时间复杂度" class="headerlink" title="时间复杂度"></a>时间复杂度</h3><p>时间复杂度的定义：在计算机科学中，算法的时间复杂度是一个函数，它定量描述了该算法的运行时间。一 个算法执行所耗费的时间，从理论上说，是不能算出来的，只有你把你的程序放在机器上跑起来，才能知 道。但是我们需要每个算法都上机测试吗？是可以都上机测试，但是这很麻烦，所以才有了时间复杂度这个 分析方式。一个算法所花费的时间与其中语句的执行次数成正比例，算法中的基本操作的执行次数，为算法的时间复杂度。</p>
<p>即：找到某条基本语句与问题规模N之间的数学表达式，就是算出了该算法的时间复杂度。</p>
<p>这样看上去感觉很复杂，那为了方便理解，我们举个例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void Func1(int N) </span><br><span class="line">&#123;</span><br><span class="line">int count = 0;</span><br><span class="line">for (int i = 0; i &lt; N ; ++ i) </span><br><span class="line">&#123;</span><br><span class="line"> for (int j = 0; j &lt; N ; ++ j)</span><br><span class="line"> &#123;</span><br><span class="line"> ++count;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">for (int k = 0; k &lt; 2 * N ; ++ k) </span><br><span class="line">&#123;</span><br><span class="line"> ++count; </span><br><span class="line">&#125;</span><br><span class="line">int M = 10;</span><br><span class="line">while (M--)</span><br><span class="line"> &#123;</span><br><span class="line"> ++count; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们看看这个代码运行了多少次：第一个for循环运行n次，在这n次里面又运行n次，所以是n^2，而后下面的for循环运行了2*n次，最后while循环运行了m（10）次，所以全部加起来就是：</p>
<p>F（N） &#x3D; N<em>N+2</em>N+10</p>
<p>但我们在编程题目里面常见的却是 O（N^2）,这是为什么？</p>
<p>实际中我们计算时间复杂度时，我们其实并不一定要计算精确的执行次数，而只需要大概执行次数，那么这里使用的就是大O的渐进表示法.</p>
<p>可能这里就会有疑惑，为什么上面一串表达式最后就变成了N^2，这里我们将N变成一个具体的值举例：</p>
<p>当N &#x3D; 10；执行次数为 10<em>10+10</em>2+10 &#x3D; 130</p>
<p>当N &#x3D; 100；执行次数为 100<em>100+100</em>2+10 &#x3D; 10210</p>
<p>当N &#x3D; 1000,执行次数为 1000*1000 <em>1000</em>2+10 &#x3D; 1002010</p>
<p>可以看到，当N越来越大的时候后面的2*N对结果的影响就越小，当N无限大的时候它就可以忽略不计，所以我们可以将其省略。</p>
<hr>
<p>大O的渐进表示法</p>
<p>大O符号（Big O notation）：是用于描述函数渐进行为的数学符号。</p>
<p>推导大O阶方法：</p>
<p>1、用常数1取代运行时间中的所有加法常数。</p>
<p>2、在修改后的运行次数函数中，只保留最高阶项。</p>
<p>3、如果最高阶项存在且不是1，则去除与这个项目相乘的常数。得到的结果就是大O阶。</p>
<hr>
<p>另外有些算法的时间复杂度存在最好、平均和最坏情况：</p>
<p>最坏情况：任意输入规模的最大运行次数(上界)</p>
<p>平均情况：任意输入规模的期望运行次数</p>
<p>最好情况：任意输入规模的最小运行次数(下界)</p>
<p>例如：在一个长度为N数组中搜索一个数据x</p>
<p>最好情况：1次找到</p>
<p>最坏情况：N次找到</p>
<p>平均情况：N&#x2F;2次找到</p>
<p>在实际中一般情况关注的是算法的最坏运行情况，所以数组中搜索数据时间复杂度为O(N)</p>
<hr>
<p>我们直接看代码理解：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void Func4(int N) </span><br><span class="line">&#123;</span><br><span class="line"> int count = 0;</span><br><span class="line"> for (int k = 0; k &lt; 100; ++ k)</span><br><span class="line"> &#123;</span><br><span class="line"> ++count;</span><br><span class="line"> &#125;</span><br><span class="line"> printf(&quot;%d\n&quot;, count);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里，很直观的看到不管N是多少，这个程序都只会运行100次，这种常数表达的我们都统认为是</p>
<p>O(1);</p>
<hr>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void Func2(int N) &#123;</span><br><span class="line"> int count = 0;</span><br><span class="line"> for (int k = 0; k &lt; 2 * N ; ++ k)</span><br><span class="line"> &#123;</span><br><span class="line"> ++count;</span><br><span class="line"> &#125;</span><br><span class="line"> int M = 10;</span><br><span class="line"> while (M--)</span><br><span class="line"> &#123;</span><br><span class="line"> ++count;</span><br><span class="line"> &#125;</span><br><span class="line"> printf(&quot;%d\n&quot;, count);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>再来看这个，我们可以看到第一个for循环运行N*2次，而下面的while循环运行10次，加起来就是：</p>
<p>2*N+10；</p>
<p>10我们肯定是可以直接丢掉的，而N前面这个2，当N趋近无限大的时候，2*N和N已经没什么区别了，所以也是可以丢掉的，所以这个我们认为它是O(N);</p>
<hr>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">const char * strchr ( const char * str, int character );</span><br></pre></td></tr></table></figure>

<p>这个strchr函数是在字符串里面找一个字符，我们都知道这种查找就会分情况：</p>
<p>第一种情况：我们第一个就找到了；</p>
<p>第二种情况：我们在中间找到了；</p>
<p>第三种情况：我们找完整个字符串或者是没有找到这个字符。</p>
<p>当出现多种情况的时候，我们一律按最坏的情况考虑，那就是查找整个字符串，所以运行次数就等于整个数组长度，就是O(N)；</p>
<hr>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void BubbleSort(int* a, int n) &#123;</span><br><span class="line"> assert(a);</span><br><span class="line"> for (size_t end = n; end &gt; 0; --end)</span><br><span class="line"> &#123;</span><br><span class="line"> int exchange = 0;</span><br><span class="line"> for (size_t i = 1; i &lt; end; ++i)</span><br><span class="line"> &#123;</span><br><span class="line"> if (a[i-1] &gt; a[i])</span><br><span class="line"> &#123;</span><br><span class="line"> Swap(&amp;a[i-1], &amp;a[i]);</span><br><span class="line"> exchange = 1;</span><br><span class="line"> &#125;</span><br><span class="line"> &#125;</span><br><span class="line"> if (exchange == 0)</span><br><span class="line"> break;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个函数大家肯定不会陌生，冒泡排序，老朋友了，我们大体上看一眼这个函数的时间复杂度，估算出来是 N<em>N,但是实际上，大家要注意一点是，我第一个数字要移到后面去，所以是 N-1次，而第二个数字移到最后面去要N-2次，而第三个就是N-3次，这样看下来就会发现这里并不是N</em>N，所以我们分情况讨论：</p>
<p>比较好的情况是我们循环整个数组没有需要变的，所以时间复杂度是O（N）;</p>
<p>另外就是最差的情况，我们每一个都需要交换位置，根据上面的式子，我们可以找出其中规律，从N-1一直加到N，这个等差数列我们都学过，所以表达式为：（N-1）*N&#x2F;2，我们把这个式子展开再化简，最后得到的是O(N^2).</p>
<hr>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int BinarySearch(int* a, int n, int x) &#123;</span><br><span class="line"> assert(a);</span><br><span class="line"> int begin = 0;</span><br><span class="line"> int end = n-1;</span><br><span class="line"> while (begin &lt; end)</span><br><span class="line"> &#123;</span><br><span class="line"> int mid = begin + ((end-begin)&gt;&gt;1);</span><br><span class="line"> if (a[mid] &lt; x)</span><br><span class="line"> begin = mid+1;</span><br><span class="line"> else if (a[mid] &gt; x)</span><br><span class="line"> end = mid;</span><br><span class="line"> else</span><br><span class="line"> return mid;</span><br><span class="line"> &#125;</span><br><span class="line"> return -1; &#125;</span><br></pre></td></tr></table></figure>

<p>这个相信大家也不陌生，二分查找，这里面的时间复杂度是多少呢？</p>
<p>首先我们看二分查找是如何实现的，每次都拿中间值和要找的数字比较，如果中间值比它小就左边就移位，反之亦然。</p>
<p>每次都会去掉一部分的查找范围，我们就可以排除N和N*N，最多运行多少次呢？</p>
<p>当我的begin大于我的end就该结束了，结束了就意味着整个数组我们都找了一边，我们写一个式子： 2^X &#x3D; N,这里大家应该都看的出来，这是一个对数表达式，我们可以写成以2为底的log N， 这里说一个小知识，就是log以2为底不是很好打出来，所以通常都认为log是以2为底，但如果有的写成lg，那就该注意一下，这一般默认是以10为底 。</p>
<hr>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">long long Fac(size_t N) &#123;</span><br><span class="line"> if(0 == N)</span><br><span class="line"> return 1;</span><br><span class="line"> </span><br><span class="line"> return Fac(N-1)*N; &#125;</span><br></pre></td></tr></table></figure>

<p>我们再看这个，这个函数我们可以看到它是一个递归，每次都递归（N-1）次，这样就很好解决了，我们拿一个数字带入进去，当N &#x3D; 10的时候，它运行10次，当N&#x3D;20的时候，它运行20次，所以它的时间复杂度应该是O（N）。</p>
<hr>
<p>既然我们对时间复杂度有一个初略的了解，我们再反过头来看最开始的那个斐波那契递归函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">long long Fib(size_t N) &#123;</span><br><span class="line"> if(N &lt; 3)</span><br><span class="line"> return 1;</span><br><span class="line"> </span><br><span class="line"> return Fib(N-1) + Fib(N-2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200643083.png"></p>
<p>我们看这张图，假设每一层都是满的，那第一次运行只有一个，第二次是两个，第三层是四个，</p>
<p>1  2  4…</p>
<p>这不是很简单的一道等比数列吗，找到规律我们就可以直接写：</p>
<p>O（2^N）。</p>
<hr>
<h2 id="空间复杂度"><a href="#空间复杂度" class="headerlink" title="空间复杂度"></a>空间复杂度</h2><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200643809.png"></p>
<p>这是leetcode的一道题，看这个题目及要求，是个很简单的题目，为什么要看这样一道题呢？</p>
<p>因为这里有一个进阶条件：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200646781.png"></p>
<p>至少三种不同的办法，和空间复杂度为O（1），我们就从这里看，什么是空间复杂度。</p>
<hr>
<p>空间复杂度</p>
<p>空间复杂度也是一个数学表达式，是对一个算法在运行过程中 临时占用存储空间大小的量度 。</p>
<p>空间复杂度不是程序占用了多少bytes的空间，因为这个也没太大意义，所以空间复杂度算的是变量的个数。</p>
<p>空间复杂度计算规则基本跟实践复杂度类似，也使用大O*渐进表示法 。</p>
<p>注意： 函数运行时所需要的栈空间  (  存储参数、局部变量、一些寄存器信息等  )在编译期间已经确定好了，因 此空间复杂度主要通过函数在运行时候显式申请的额外空间来确定。</p>
<hr>
<p>我们在上篇已经明白了什么是大O的渐进表示法，在空间复杂度里面，它也同样适用。</p>
<p>我们同样是看代码来了解：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void BubbleSort(int* a, int n) &#123;</span><br><span class="line"> assert(a);</span><br><span class="line"> for (size_t end = n; end &gt; 0; --end)</span><br><span class="line"> &#123;</span><br><span class="line"> int exchange = 0;</span><br><span class="line"> for (size_t i = 1; i &lt; end; ++i)</span><br><span class="line"> &#123;</span><br><span class="line"> if (a[i-1] &gt; a[i])</span><br><span class="line"> &#123;</span><br><span class="line"> Swap(&amp;a[i-1], &amp;a[i]);</span><br><span class="line"> exchange = 1;</span><br><span class="line"> &#125;</span><br><span class="line"> &#125;</span><br><span class="line"> if (exchange == 0)</span><br><span class="line"> break;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是一个冒泡排序，我们从上往下阅读代码，发现这里一共创建了三个变量：</p>
<p>size_t end;</p>
<p>size_t i;</p>
<p>int exchange;</p>
<p>既然是常数个，那它的空间复杂度毫无疑问是O（1）.</p>
<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200644767.png"></p>
<p>如果我们写出如上代码，可以看到这里开辟空间的大小完全是根据numsize大小而开辟的，也就是说它会因为numsize变大而变大，所以这里它的空间复杂度就是O（N）。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200644600.png"></p>
<p>同样，这里开辟的空间也是跟numssize有关，所以同样是O（N）。</p>
<hr>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">long long* Fibonacci(size_t n) &#123;</span><br><span class="line"> if(n==0)</span><br><span class="line"> return NULL;</span><br><span class="line"> </span><br><span class="line"> long long * fibArray = (long long *)malloc((n+1) * sizeof(long long));</span><br><span class="line"> fibArray[0] = 0;</span><br><span class="line"> fibArray[1] = 1;</span><br><span class="line"> for (int i = 2; i &lt;= n ; ++i)</span><br><span class="line"> &#123;</span><br><span class="line"> fibArray[i] = fibArray[i - 1] + fibArray [i - 2];</span><br><span class="line"> &#125;</span><br><span class="line"> return fibArray; &#125;</span><br></pre></td></tr></table></figure>

<p><img src="/%22%E7%82%B9%E5%87%BB%E5%B9%B6%E6%8B%96%E6%8B%BD%E4%BB%A5%E7%A7%BB%E5%8A%A8%22"></p>
<p>我们再看这一串，这是一个递归写法的斐波那契数，我们知道它的时间复杂度是2^n,那它的看见复杂度是多少呢？</p>
<p>n*n？</p>
<p>2^n?</p>
<p>这里要注意的是，空间复杂度不是时间复杂度，它们最主要的区别是时间是用了就用了的，但空间不是，这里可能有点难以理解，当我们fib[i - 1]这边递归完之后，它会将这一块的空间返回，而fib[i - 2]用的也是这一块空间，也就是说它只开辟了N个空间，所以它的空间复杂度是O（N）。</p>
<hr>
<p>这里有一张图，可以看一下：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200644114.png"></p>
<hr>
<p>到这里，我们就了解了什么是空间复杂度，再回过头来看看最开始的那道题，题目说的三种办法，这里我们分别实现一下：</p>
<p>第一种，我们一次右旋一个，右旋n次。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void rotate(int* nums, int numsSize, int k)</span><br><span class="line">&#123;</span><br><span class="line">    int i;</span><br><span class="line">    k %= numsSize;</span><br><span class="line">        for(i = 0;i&lt;k;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            int tmp = nums[numsSize-1];</span><br><span class="line">            for(int j = 1;j&lt;numsSize;j++)</span><br><span class="line">            &#123;</span><br><span class="line">                nums[numsSize-j] = nums[numsSize-1-j];</span><br><span class="line">            &#125;</span><br><span class="line">            nums[0] = tmp;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第二种，我们再开一个数组，让后面的先放进去，再放前面的进去：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void rotate(int* nums, int numsSize, int k)</span><br><span class="line">&#123;</span><br><span class="line">    int i;</span><br><span class="line">    int* arr = (int*)malloc(4*numsSize);</span><br><span class="line">    k %=numsSize;</span><br><span class="line">    if(numsSize == 1)</span><br><span class="line">    &#123;</span><br><span class="line">        return ;</span><br><span class="line">    &#125;</span><br><span class="line">   for(i = 0;i&lt;k;i++)</span><br><span class="line">   &#123;</span><br><span class="line">       arr[i] = nums[numsSize-k+i];</span><br><span class="line">   &#125;</span><br><span class="line">   for(i = 0;i&lt;numsSize-k;i++)</span><br><span class="line">   &#123;</span><br><span class="line">       arr[k+i] = nums[i];</span><br><span class="line">   &#125;</span><br><span class="line">   for(i = 0;i&lt;numsSize;i++)</span><br><span class="line">   &#123;</span><br><span class="line">       nums[i] = arr[i];</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第三种，我们先将前面几个数字交换，再将后面几个数字交换，最后整体交换：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200644925.png"></p>
<p>看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void reverser(int *arr,int left,int right)</span><br><span class="line">&#123;</span><br><span class="line">    while(left&lt;right)</span><br><span class="line">    &#123;</span><br><span class="line">        int tmp = arr[left];</span><br><span class="line">        arr[left] = arr[right];</span><br><span class="line">        arr[right] = tmp;</span><br><span class="line">        left++;</span><br><span class="line">        right--;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void rotate(int* nums, int numsSize, int k)</span><br><span class="line">&#123;</span><br><span class="line">    k %= numsSize;</span><br><span class="line">    reverser(nums,0,numsSize-k-1);</span><br><span class="line">    reverser(nums,numsSize-k,numsSize-1);</span><br><span class="line">    reverser(nums,0,numsSize-1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>C</category>
      </categories>
  </entry>
  <entry>
    <title>多态</title>
    <url>/2022/07/23/%E5%A4%9A%E6%80%81/</url>
    <content><![CDATA[<hr>
<h1 id="多态的概念"><a href="#多态的概念" class="headerlink" title="多态的概念"></a>多态的概念</h1><p>简单来说，就是去执行某个函数，但是不同对象去执行有不同的结果。</p>
<p>就好比买票，我拿纸币买票要六块，但是用支付宝可以九折，不同的方式去买票要付的钱不一样，这句是一种多态、</p>
<h1 id="多态的定义和实现"><a href="#多态的定义和实现" class="headerlink" title="多态的定义和实现"></a>多态的定义和实现</h1><h2 id="多态的构成条件"><a href="#多态的构成条件" class="headerlink" title="多态的构成条件"></a>多态的构成条件</h2><p>1、必须用基类的指针或者引用调用虚函数</p>
<p>2、被调用的函数必须是虚函数且必须对基类的虚函数进行过重写</p>
<p>这里可以参考下图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201154178.png" alt="img"></p>
<p>第一个对象调用Func函数的时候，函数参数接收类型的ali（父类），但是传过去的参数是子类，所以调用的是子类的虚函数</p>
<p>第二个参数就是父类调用它父类的虚函数 </p>
<h2 id="虚函数"><a href="#虚函数" class="headerlink" title="虚函数"></a>虚函数</h2><p>被virtual修饰的称之为虚拟函数，如上图，在函数返回值前面加一个virtual（虚拟）。</p>
<h2 id="虚函数的重写"><a href="#虚函数的重写" class="headerlink" title="虚函数的重写"></a>虚函数的重写</h2><p> 虚函数的充血（也叫覆盖）：指在派生类里面有一个跟基类完全相同（返回值、函数名、参数）的虚函数，称之为子类的虚函数重写了基类的虚函数。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class ali</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	virtual void BuyTicker()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;九折&quot; &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">class me :public ali</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	virtual void BuyTicker()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;原价&quot; &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h2 id="虚函数重写的例外"><a href="#虚函数重写的例外" class="headerlink" title="虚函数重写的例外"></a>虚函数重写的例外</h2><h3 id="1、协变（基类和反派生的返回值类型不同）"><a href="#1、协变（基类和反派生的返回值类型不同）" class="headerlink" title="1、协变（基类和反派生的返回值类型不同）"></a>1、协变（基类和反派生的返回值类型不同）</h3><blockquote>
<p>派生类重写基类虚函数时，与基类虚函数返回值类型不同。即基类虚函数返回基类对象的指<br> 针或者引用，派生类虚函数返回派生类对象的指针或者引用时，称为协变。</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class a&#123;&#125;;</span><br><span class="line"></span><br><span class="line">class b :public a&#123;&#125;;</span><br><span class="line"></span><br><span class="line">class ali</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	virtual a* BuyTicker()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;九折&quot; &lt;&lt; endl;</span><br><span class="line">		return new a;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">class me :public ali</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	virtual b* BuyTicker()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;原价&quot; &lt;&lt; endl;</span><br><span class="line">		return new b;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>就像这样，了解一下就好了。</p>
<h3 id="2-析构函数的重写-基类与派生类析构函数的名字不同"><a href="#2-析构函数的重写-基类与派生类析构函数的名字不同" class="headerlink" title="2. 析构函数的重写(基类与派生类析构函数的名字不同)"></a>2. 析构函数的重写(基类与派生类析构函数的名字不同)</h3><blockquote>
<p><strong>如果基类的析构函数为虚函数，此时派生类析构函数只要定义，无论是否加virtual关键字，都与基类的析构函数构成重写，虽然基类与派生类析构函数名字不同。虽然函数名不相同，看起来违背了重写的规则，其实不然，这里可以理解为编译器对析构函数的名称做了特殊处理，编译后析构函数的名称统一处理成destructor。</strong></p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class ali</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	virtual ~ali()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;~ali()&quot; &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">class me :public ali</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	virtual ~me()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;~me()&quot; &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">//只有派生类的析构函数重写了父类的析构函数，才能构成多态，确保p1、p2指向的对象正确调用了虚构函数</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	ali* p1 = new ali;</span><br><span class="line">	ali* p2 = new me;</span><br><span class="line"></span><br><span class="line">	delete p1;</span><br><span class="line">	delete p2;</span><br><span class="line"></span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h2 id="override-和-final"><a href="#override-和-final" class="headerlink" title="override 和 final"></a>override 和 final</h2><p>从虚函数的重写条件来看是比较严格的，但是有的时候就会出现函数名写错一点无法构成重载，这种时候因为不会报错，所以很难发现，慢慢调试过于折磨，就出现了这个个关键字：</p>
<h3 id="final"><a href="#final" class="headerlink" title="final"></a>final</h3><p>修饰虚函数，表示这个虚函数不能重写</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201155224.png" alt="img"></p>
<h3 id="override"><a href="#override" class="headerlink" title="override"></a>override</h3><p>检查派生类是否重写，如果没有重写就会报错</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201155834.png" alt="img"></p>
<h2 id="重载、覆盖、隐藏（重定义）的对比"><a href="#重载、覆盖、隐藏（重定义）的对比" class="headerlink" title="重载、覆盖、隐藏（重定义）的对比"></a>重载、覆盖、隐藏（重定义）的对比</h2><p> <strong>重载：两个函数在同一个作用域，函数名相同但是参数略有不同</strong></p>
<p><strong>重写：两个函数分别在基类和子类，返回类型、函数名、参数都必须相同且是虚函数</strong></p>
<p><strong>重定义：两个函数分别在基类和派生类，函数名相同但不构成重写</strong></p>
<h1 id="抽象类"><a href="#抽象类" class="headerlink" title="抽象类"></a>抽象类</h1><h2 id="抽象类的概念"><a href="#抽象类的概念" class="headerlink" title="抽象类的概念"></a>抽象类的概念</h2><p>在了解抽象类之前，要先理解什么是纯虚函数</p>
<blockquote>
<p>在基类中仅仅给出声明，不对虚函数实现定义，而是在派生类中实现。这个虚函数称为纯虚函数。普通函数如果仅仅给出它的声明而没有实现它的函数体，这是编译不过的。纯虚函数没有函数体。</p>
<p>纯虚函数需要在声明之后加个&#x3D;0。</p>
</blockquote>
<blockquote>
<p>含有纯虚函数的类被称为抽象类。抽象类只能作为派生类的基类，不能定义对象，但可以定义指针。在派生类实现该纯虚函数后，定义抽象类对象的指针，并指向或引用子类对象。</p>
</blockquote>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201155537.png" alt="img"></p>
<p>在父类Buy里面，我声明了一个Ticket的纯虚函数，同时在继承的两个子类里面重写，然后调用即可得到对应的结果。</p>
<h3 id="接口继承和实现继承"><a href="#接口继承和实现继承" class="headerlink" title="接口继承和实现继承"></a>接口继承和实现继承</h3><p>普通函数的继承是一种实现继承，可以直接使用函数。</p>
<p>虚函数的继承是接口继承，只是继承了对应的接口，实际内容需要重写。</p>
<p>如果不实现多态，不要把函数定义为虚函数。</p>
<h1 id="多态的原理"><a href="#多态的原理" class="headerlink" title="多态的原理"></a>多态的原理</h1><h2 id="虚函数表"><a href="#虚函数表" class="headerlink" title="虚函数表"></a>虚函数表</h2><p>存在虚函数的类都有一个虚函数表，称之为虚表。</p>
<p>虚表由类对象里面指向虚表的指针所指，这个指针称之为虚指针（vptr，一般是类里面的第一个成员）。</p>
<p>虚表里面放着的是类里面成员函数指针。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201155501.png" alt="img"></p>
<p>上图，一般来说Base类里面只有一个int，而函数会放到公共区不算大小，应该是4，但是这里是8，可见Fun1占了四个字节的大小，为了看的清晰一点，创建一个对象出来</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201155559.png" alt="img"></p>
<p>在监视里面可以看到正如刚刚所说多了应该vptr，这就是虚表指针，指向的是虚表的地址，而虚表里面放着的正是虚函数的地址。</p>
<p>这里就直接说结论吧，证明过程网上都传烂了。</p>
<blockquote>
<p><strong>1、派生类也有一个虚表，如果虚函数完成了重写，那么虚表里面函数的地址是重写后的地址，也就是说两个虚表里面如果完成了重写，那么同名函数的函数指针地址是不一样的，所以重写也叫做覆盖。</strong></p>
<p><strong>2、如果继承下来的函数是虚函数，就会被放进虚表，如果不是，则不会进入虚表，但是会放在后面。</strong></p>
<p><strong>3、虚表的本质是一个存放虚函数指针的数组，由最后的nullptr结尾。</strong></p>
<p><strong>4、虚函数的生成：派生类先拷贝父类的虚表，如果派生类自己重写了虚函数，则在对应位置覆盖，如果派生类有新增的虚函数，会被放到最后。</strong></p>
<p><strong>5、虚函数存在虚表里面，虚表存在对象里面。</strong></p>
<p><strong>6、虚表存的是指针，不是函数。</strong></p>
</blockquote>
<h2 id="多态的原理-1"><a href="#多态的原理-1" class="headerlink" title="多态的原理"></a>多态的原理</h2><p>当指针指向某一个对象的时候，这个指针会在对象的虚表里面找到对应的虚函数。</p>
<p>这样就可以解释为什么要完成虚函数的覆盖和为什么要用指针和引用调用。</p>
<h2 id="动态绑定与静态绑定"><a href="#动态绑定与静态绑定" class="headerlink" title="动态绑定与静态绑定"></a>动态绑定与静态绑定</h2><p>静态绑定（前期绑定）可以理解为在编译的时候就确定了调用哪个函数，比如说函数重载，虽然看上去调用是函数名一样，但是底层（函数名）是不一样的。</p>
<p>动态绑定（后期绑定）是程序在运行的时候，根据拿到的类型来确定行为的，比如上面的多态，先要在虚表里面找到确切的虚函数地址再进行调用。</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>堆</title>
    <url>/2022/04/23/%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<hr>
<p><strong>1.插入排序</strong></p>
<p><strong>2.希尔排序</strong></p>
<p><strong>3.选择排序</strong></p>
<p><strong>4.堆排序</strong></p>
<p><strong>5.冒泡排序</strong></p>
<p><strong>6.快速排序</strong></p>
<p><strong>7.归并排序</strong></p>
<p><strong>8.计数排序</strong></p>
<hr>
<h2 id="插入排序"><a href="#插入排序" class="headerlink" title="插入排序"></a>插入排序</h2><p>这个排序原来很简单，如图演示：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200932305.png"></p>
<p>我们先把 9 看作有序数组插入，它比8大，那就交换位置</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200932128.png"></p>
<p>此时，8前面没有数字了，所以它就停下，再把8 9 看作有序数组：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200932900.png"></p>
<p>再把 7 插入，因为9比7大，交换，8比7大，交换，就是这样：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200932118.png"></p>
<p>重复这样的步骤，直到数组结&#x2F;或者已经交换完全</p>
<p>实现代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 插入排序</span><br><span class="line">void InsertSort(int* a, int n)</span><br><span class="line">&#123;</span><br><span class="line">    //因为tmp是当前下标的后一个元素比较，所以最后一个元素的位置是n-1</span><br><span class="line">	for (int i = 0; i &lt; n-1; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		int n = i;</span><br><span class="line">		int tmp = a[1 + n];</span><br><span class="line">        //假设每次都要比到最后</span><br><span class="line">		while (n &gt;= 0)</span><br><span class="line">		&#123;</span><br><span class="line">            //如果小于，就让这个位置元素被替代，然后再跟前一个比较</span><br><span class="line">			if (tmp &lt; a[n])</span><br><span class="line">			&#123;</span><br><span class="line">				a[n + 1] = a[n];</span><br><span class="line">				n -= 1;</span><br><span class="line">			&#125;</span><br><span class="line">            //如果不小，那就退出</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				break;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">        //它停下的位置就是前面已经没有比它小的元素了，但是因为上面替换之前-1，所以这里要+1</span><br><span class="line">		a[n + 1] = tmp;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<p><strong>时间复杂度：最好O（N^2）,最坏O（N^2）,因为它总要比较判断</strong></p>
<p><strong>空间复杂度：O（1），没有额外开辟空间</strong></p>
<p>时间太慢了，不是很推荐</p>
<hr>
<h2 id="希尔排序"><a href="#希尔排序" class="headerlink" title="希尔排序"></a><strong>希尔排序</strong></h2><p>插入排序属实太慢了，假设一个很小的元素在最后面，那它要移动N次才能来到前面的位置（1在最后面，它要来到9的位置就要换8次）</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200932406.png"></p>
<p>所以它的改进版本，希尔排序就出来了，希尔排序是这么想的，有一个间隔数gap，这个数可以很大，也可以很小，它先将数组分为几个小组：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200932115.png"></p>
<p>红色一组为：9 6 3</p>
<p>蓝色一组为：8 5 2</p>
<p>绿色一组为：7 4 1</p>
<p>我这里设置的gap是3，应该可以看的出来，然后分别对这几个小组排序：</p>
<p>9 6 3 变成 3 6 9</p>
<p>8 5 2 变成 2 5 8</p>
<p>7 4 1 变成 1 4 7</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200932778.png"></p>
<p>就变成这样，这里只是举例子，因为降序方便看就设置为这样，实际要混乱一点，然后再对这降序的再排一次（这里已经有序了就没画）</p>
<p><strong>每一次排序都会让数组更加有序</strong></p>
<p><strong>如果这个gap很大，那小的数组会更快的来到前面，因为它一次可以跳gap格，大的也会更快的去到后面</strong></p>
<p><strong>如过这个gap很小，那它每次移动都会给更加有序，如果是1，那就跟插入排序一样了</strong></p>
<p><strong>这个gap肯定不能太小，并且不能是固定的数字，如果是固定的数字，那它每次移动就会很慢，所以最好跟N扯上关系，如果看懂了插入排序，那希尔就很好理解，它不过是把 n+1 变成了 n+gap，然后再插入</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 希尔排序</span><br><span class="line">void ShellSort(int* a, int n)</span><br><span class="line">&#123;</span><br><span class="line">    //定义一个gap</span><br><span class="line">	int gap = n;</span><br><span class="line">	while (gap &gt; 1)</span><br><span class="line">	&#123;</span><br><span class="line">        //这个值就是间隔</span><br><span class="line">		gap = gap / 3 + 1;</span><br><span class="line">        //每次都是跟 n+gap比较，所以最后一个比较元素的位置是n-gap</span><br><span class="line">		for (int i = 0; i &lt; n - gap; i++)</span><br><span class="line">		&#123;</span><br><span class="line">            //从这个下标开始，如果直接拿i的话会死循环</span><br><span class="line">			int n = i;</span><br><span class="line">			int tmp = a[n + gap];</span><br><span class="line">            //如果这个n&gt;0,那它前面还有没比较的值</span><br><span class="line">			while (n &gt;= 0)</span><br><span class="line">			&#123;</span><br><span class="line">                //比较</span><br><span class="line">				if (tmp &lt; a[n])</span><br><span class="line">				&#123;</span><br><span class="line">                    //交换，并且让n-gap，找到下一个间隔为gap的比较</span><br><span class="line">					a[n + gap] = a[n];</span><br><span class="line">					n -= gap;</span><br><span class="line">				&#125;</span><br><span class="line">				else</span><br><span class="line">				&#123;</span><br><span class="line">					break;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">            //同理，最后一次这个n肯定被-gap，所以加上</span><br><span class="line">			a[n+gap] = tmp;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>*<em>时间复杂度： O (n <em>logn)</em></em></p>
<p><strong>空间复杂度：O（1）</strong></p>
<p>这个算法非常快，建议使用</p>
<hr>
<h2 id="选择排序"><a href="#选择排序" class="headerlink" title="选择排序"></a><strong>选择排序</strong></h2><p><strong>最好理解的排序，每次找最大的（或者最小的），也可以一起找出来，然后跟第一个元素和最后一个元素换位置，换完就排完了</strong></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933104.png"></p>
<p>然后交换，找出次大的，次小的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933050.png"></p>
<p>循环即可 ，但是一定要注意，因为这里一次交换两个值，所以可能出现：</p>
<p><strong>原本最大值的下标是0，然后最小值的下标是3，最小值先跟0的位置换，这个最大值就被交换到3的位置去了，再去交换下标0就会找到最小值，所以先判断交换位置有没有冲突，有就调整</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 选择排序</span><br><span class="line">void SelectSort(int* a, int n)</span><br><span class="line">&#123;</span><br><span class="line">    //要交换的左右下标</span><br><span class="line">	int left = 0, right = n - 1;</span><br><span class="line">	while (left &lt; right)</span><br><span class="line">	&#123;</span><br><span class="line">        //找出数组里面最大值和最小值的下标</span><br><span class="line">		int max, min;</span><br><span class="line">		max = min = left;</span><br><span class="line">		for (int i = left+1; i &lt;= right; i++)</span><br><span class="line">		&#123;</span><br><span class="line">			if (a[i] &gt; a[max])</span><br><span class="line">			&#123;</span><br><span class="line">				max = i;</span><br><span class="line">			&#125;</span><br><span class="line">			if (a[i] &lt; a[min])</span><br><span class="line">			&#123;</span><br><span class="line">				min = i;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">        //交换</span><br><span class="line">		swap(&amp;a[left], &amp;a[min]);</span><br><span class="line">        //如果最大值的下标是上面的left，那它就会被交换到min的位置，所以让max指向被交换后的位置</span><br><span class="line">		if (max == left)</span><br><span class="line">		&#123;</span><br><span class="line">			max = min;</span><br><span class="line">		&#125;</span><br><span class="line">        //再次交换</span><br><span class="line">		swap(&amp;a[right], &amp;a[max]);</span><br><span class="line">        //再换下一组</span><br><span class="line">		left++;</span><br><span class="line">		right--;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><strong>时间复杂度：最好O（N^2）,最坏O（N^2），因为不管怎么样它都要查找然后交换</strong></p>
<p><strong>空间复杂度：O（1），没有额外开辟空间</strong></p>
<p><strong>不是很快，不推荐使用</strong></p>
<p><strong>———–</strong></p>
<h2 id="堆排序"><a href="#堆排序" class="headerlink" title="堆排序"></a>堆排序</h2><p>这个在我以前专栏里面有详细说过，如果不了解堆的性质可以去看看，这里简单描述一下：</p>
<p><strong>一个堆的堆顶是数组里面的最大值，那它就是大堆</strong></p>
<p><strong>一个堆的堆顶是数组里面的最小值，那它就是小堆</strong></p>
<p><strong>假设我们是升序，那我们就要建立大堆（降序建小堆），然后把堆顶的元素（也就是下标为0的元素）跟最后一个元素换，然后再调整一下堆即可</strong></p>
<p>这里我就直接上代码实现了：</p>
<p>先是建立大堆的调整函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void AdjustDown(int* arr, int* root, int* end)</span><br><span class="line">&#123;</span><br><span class="line">    //父亲节点</span><br><span class="line">	int parent = root;</span><br><span class="line">    //孩子节点</span><br><span class="line">	int child = parent * 2+1;</span><br><span class="line">	while (child &lt; end)</span><br><span class="line">	&#123;</span><br><span class="line">        //选两个孩子里面大的那个</span><br><span class="line">		if (arr[child + 1] &gt; arr[child] &amp;&amp; (child+1) &lt;end)</span><br><span class="line">		&#123;</span><br><span class="line">			child++;</span><br><span class="line">		&#125;</span><br><span class="line">        //如果父亲比孩子小，交换</span><br><span class="line">		if (arr[parent] &lt; arr[child])</span><br><span class="line">		&#123;</span><br><span class="line">			swap(&amp;arr[parent], &amp;arr[child]);</span><br><span class="line">			parent = child;</span><br><span class="line">			child = parent * 2 + 1;</span><br><span class="line">		&#125;</span><br><span class="line">        //不小就跳出</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			break;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>然后是排序：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 堆排序</span><br><span class="line">void HeapSort(int* a, int n)</span><br><span class="line">&#123;</span><br><span class="line">        //先建立一个大堆</span><br><span class="line">		for (int i = (n - 2)/2; i &gt;= 0; --i)</span><br><span class="line">		&#123;</span><br><span class="line">			AdjustDown(a, i, n);</span><br><span class="line">		&#125;</span><br><span class="line">        //最后一个元素的下标，交换使用</span><br><span class="line">		int end = n - 1;</span><br><span class="line">		while (end)</span><br><span class="line">		&#123;</span><br><span class="line">            //将最大的放到最后</span><br><span class="line">			swap(&amp;a[end], &amp;a[0]);</span><br><span class="line">            //重新调整堆</span><br><span class="line">			AdjustDown(a, 0, end);</span><br><span class="line">            //最后位置就不用排了</span><br><span class="line">            end--;</span><br><span class="line">		&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>*<em>时间复杂度： O (n <em>logn)</em></em></p>
<p><strong>空间复杂度：O（1）</strong></p>
<p>这个算法跟希尔排序一个级别，除了代码比较多以外，还是很推荐的</p>
<hr>
<h2 id="冒泡排序"><a href="#冒泡排序" class="headerlink" title="冒泡排序"></a>冒泡排序</h2><p>老朋友了，我相信这个我不说大家也明白，具体思路是：</p>
<p>如果前面一个大于后面一个，交换，然后遍历数组</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 冒泡排序</span><br><span class="line">void BubbleSort(int* a, int n)</span><br><span class="line">&#123;</span><br><span class="line">	for (int i = 0; i &lt; n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		for (int j = 0; j &lt; n; j++)</span><br><span class="line">		&#123;</span><br><span class="line">			if (a[j - 1] &gt; a[j])</span><br><span class="line">			&#123;</span><br><span class="line">				swap(&amp;a[j - 1], &amp;a[j]);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>就不写注释了，实在太简单了</p>
<p><strong>时间复杂度：O（N^2）</strong></p>
<p><strong>空间复杂度：O（N）</strong></p>
<p>不推荐使用，太慢了，太慢了，太慢了！！！</p>
<p>如果代码没问题但时间超时，多半是它的锅，别用</p>
<hr>
<h2 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h2><p><strong>这个不是那个qsort，但也挺快的</strong></p>
<p>有好几种思路，先说第一种：</p>
<h3 id="1、hoare版："><a href="#1、hoare版：" class="headerlink" title="1、hoare版："></a><strong>1、hoare版：</strong></h3><p><strong>使用递归，看图演示：</strong></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933318.png"></p>
<p>假设这是要排序的数组，第一步，我们选一个数字作为Key，这里我选 6（第一个元素的位置） ，那我们就从6开始走，先走Key的另一边，如果我选左边，那就先走右边，选右边就先走左边：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933613.png"></p>
<p>右边要找到比Key小的值，因为Key是6，这里2比6小，所以停下</p>
<p>然后找左边：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933362.png"></p>
<p>左边找比Key大的值，5 4 都不符合条件，所以跳过，9比6大，所以停下，然后我们交换位置：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933842.png"></p>
<p>然后右边再继续先走，找到比6小的：，左边找比6大的</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933565.png"></p>
<p>右边停在1，左边停在8，停下再次交换：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933891.png"></p>
<p>此时右边再开始走：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933302.png"></p>
<p>它们相遇了，这个时候我们就把这个位置的值跟Key（第一个元素的位置交换）：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933764.png"></p>
<p>到这里，第一次就走完了，我们发现比Key小的都在左边，比Key大的都在右边，而6也来到了正确的位置，它的左边都比它小，它的右边都比它大</p>
<p>然后我们将数组分割：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933328.png"></p>
<p>在这两个数组里面重复上面的步骤，直到它们不可再分割为止：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933429.png"></p>
<p>第一次分割将数组分为两半，第二次将左边长的一段又变成两段…</p>
<p>看看代码理解：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 单趟快排</span><br><span class="line">int PartSort(int* a, int left, int right)</span><br><span class="line">&#123;</span><br><span class="line">    //先将left现在的值保留下来（一般是0）</span><br><span class="line">	int keyi = left;</span><br><span class="line">    //如果左边大于右边那肯定相遇了</span><br><span class="line">	while (left &lt; right)</span><br><span class="line">	&#123;</span><br><span class="line">		// 找小</span><br><span class="line">		while (left &lt; right &amp;&amp; a[right] &gt;= a[keyi])</span><br><span class="line">			--right;</span><br><span class="line"></span><br><span class="line">		// 找大</span><br><span class="line">		while (left &lt; right &amp;&amp; a[left] &lt;= a[keyi])</span><br><span class="line">			++left;</span><br><span class="line">        </span><br><span class="line">        //交换</span><br><span class="line">		swap(&amp;a[left], &amp;a[right]);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    //出来了就是相遇，这里的元素跟Key交换</span><br><span class="line">	swap(&amp;a[keyi], &amp;a[left]);</span><br><span class="line"></span><br><span class="line">    //返回中间的节点，将其分割为两段</span><br><span class="line">	return left;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这是一趟，我们递归调用：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void QuickSort(int* a, int begin, int end)</span><br><span class="line">&#123;</span><br><span class="line">	// 如果起始位址等于（或者大于）终点为止，那就可以返回了</span><br><span class="line">	if (begin &gt;= end)</span><br><span class="line">		return;</span><br><span class="line"></span><br><span class="line">    //这个值就是中点</span><br><span class="line">	int keyi = PartSort(a, begin, end);</span><br><span class="line">	</span><br><span class="line">    //左半边，因为Key已经到了正确的为止，所以不用排了，left通常是0</span><br><span class="line">	QuickSort(a, begin, keyi - 1);</span><br><span class="line">       </span><br><span class="line">    //右边，Key不排，结束为止就是end（数组长度）</span><br><span class="line">	QuickSort(a, keyi + 1, end);</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="2、挖坑法"><a href="#2、挖坑法" class="headerlink" title="2、挖坑法"></a>2、挖坑法</h3><p>这种思路是这么的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200933759.png"></p>
<p>假设现在有这么一组数据，将最左边这个 6 保存下来，并让一个值指向这里，我们称之为pit（坑位）。</p>
<p>程序开始走，因为坑位在左边，所以要从右边开始走，right 从 7 开始，它的目标是找到比 6 小的数字：</p>
<p>7 &lt; 6 ?</p>
<p>8 &lt; 6 ?</p>
<p>9 &lt; 6 ?</p>
<p>1 &lt; 6 ?</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934990.png"></p>
<p>它就停下来了 ，此时它指向 1 ，1 &lt; 6</p>
<p>找到了小，我们就让 1 放到坑位里面，坑位就是这里的 6 ：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934583.png"></p>
<p>此时，因为坑位到了右边，下一次就要从左边开始走，left 从 5 开始，找比 6 大的：</p>
<p>5 &lt; 6 ?</p>
<p>4 &lt; 6 ?</p>
<p>3 &lt; 6 ?</p>
<p>2 &lt;6 ?</p>
<p>1 &lt; 6 ?</p>
<p>此时 left 来到 right 的位置，它们重叠了，而这里正好是 1 的位置，将一开始保存的 6 放到这里：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934325.png"></p>
<p>一趟排序就完成了，左边都是比 tmp（6） 小的，右边都是比它大的，随后我们递归调用即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 快速排序挖坑法</span><br><span class="line">int PartSort2(int* a, int left, int right)</span><br><span class="line">&#123;</span><br><span class="line">    //坑位</span><br><span class="line">	int pit = left;</span><br><span class="line">    //key</span><br><span class="line">	int key = a[left];</span><br><span class="line">    //一趟走N次</span><br><span class="line">	while (left &lt; right)</span><br><span class="line">	&#123;</span><br><span class="line"></span><br><span class="line">        //找小</span><br><span class="line">		while (a[right] &gt; key &amp;&amp; left &lt; right)</span><br><span class="line">		&#123;</span><br><span class="line">			right--;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        //将找到的小放到坑里面，然后将这里变成新的坑</span><br><span class="line">		a[pit] = a[right];</span><br><span class="line">		pit = right;</span><br><span class="line"></span><br><span class="line">        //找大</span><br><span class="line">		while (a[left] &lt; key &amp;&amp; left &lt; right)</span><br><span class="line">		&#123;</span><br><span class="line">			left++;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">       //将找到的大放到坑里面，然后将这里变成新的坑</span><br><span class="line">		a[pit] = a[left];</span><br><span class="line">		pit = left;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    //它们相遇的位置，就是key的位置</span><br><span class="line">	a[left] = key;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h3 id="3、前后指针法"><a href="#3、前后指针法" class="headerlink" title="3、前后指针法"></a>3、前后指针法</h3><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934791.png"></p>
<p>第一个指针指向 6</p>
<p>第二个指针指向 5</p>
<p>key 是第一个元素 6</p>
<p>如果 cur 位置的元素大于 key 并且  ++prev 的位置不等于 cur ，那就交换：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934838.png"></p>
<p>5 &lt; 6 ? cur++;</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934906.png"></p>
<p>4 &lt; 5 ?   ++prev &#x3D;&#x3D; cur ?</p>
<p>交换它们的值：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934121.png"></p>
<p>cur++;</p>
<p>3 &lt; 6 ? ++prev &#x3D;&#x3D; cur?</p>
<p>再次交换：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934925.png"></p>
<p>cur++；</p>
<p>…..</p>
<p>直到：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934668.png"></p>
<p>此时，cur处元素大于key，所以不执行交换，但是cur++</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934521.png"></p>
<p>cur 大于 key</p>
<p>cur++</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934209.png"></p>
<p>cur 大于 key</p>
<p>cur++</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934137.png"></p>
<p>此时cur越界，所以循环停下，将prev的值跟key的值交换：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934065.png"></p>
<p>第一趟结束</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 快速排序前后指针法</span><br><span class="line">int PartSort3(int* a, int left, int right)</span><br><span class="line">&#123;</span><br><span class="line">    //前、后指针</span><br><span class="line">	int prev = left;</span><br><span class="line">	int cur = prev + 1;</span><br><span class="line">    //key的值</span><br><span class="line">	int key = left;</span><br><span class="line">	while (cur &lt;= right)</span><br><span class="line">	&#123;</span><br><span class="line"></span><br><span class="line">        //开始走</span><br><span class="line">		if (a[cur] &lt; a[key] &amp;&amp; a[++prev] != a[cur])</span><br><span class="line">		&#123;</span><br><span class="line">			swap(&amp;a[prev], &amp;a[cur]);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        //每次cur都++</span><br><span class="line">		cur++;</span><br><span class="line">	&#125;</span><br><span class="line">    </span><br><span class="line">    //交换</span><br><span class="line">	swap(&amp;a[prev], &amp;a[key]);</span><br><span class="line"></span><br><span class="line">	return prev;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<hr>
<h3 id="如何优化快排"><a href="#如何优化快排" class="headerlink" title="如何优化快排"></a>如何优化快排</h3><p><strong>小区间优化：</strong></p>
<p>但看快排的代码，它是靠递归完成的，这样的一个好处是可以把一个很麻烦的问题分成小问题解决</p>
<p>但是缺点同样很明显，假设这一次只要排 4 个数据，你还分成好几个小块，就有点太麻烦了，所以我们可以在快排里面调用一下 插入排序，当要排序的小于一定个数时，就不递归了，直接排序</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void QuickSort(int* a, int begin, int end)</span><br><span class="line">&#123;</span><br><span class="line">	// 如果起始位址等于（或者大于）终点为止，那就可以返回了</span><br><span class="line">	if (begin &gt;= end)</span><br><span class="line">		return;</span><br><span class="line"></span><br><span class="line">	//小于13个就插入，这个区间可以自己定</span><br><span class="line">	if (end - begin + 1 &lt; 13)</span><br><span class="line">	&#123;</span><br><span class="line">		InsertSort(a, end - begin + 1);</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	//这个值就是中点</span><br><span class="line">	int keyi = PartSort3(a, begin, end);</span><br><span class="line"></span><br><span class="line">	//左半边，因为Key已经到了正确的为止，所以不用排了，left通常是0</span><br><span class="line">	QuickSort(a, begin, keyi - 1);</span><br><span class="line"></span><br><span class="line">	//右边，Key不排，结束为止就是end（数组长度）</span><br><span class="line">	QuickSort(a, keyi + 1, end);</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><strong>选择优化</strong></p>
<p>另外，还有一个可以优化的地方，如果每次快排的 key 都是数组中间的数，那它的时间复杂度是</p>
<p>N*logN，那它的最坏情况呢？</p>
<p>如果每次都选到两边的数据，那它的时间复杂度毫无疑问的是 N^2,针对这种情况，我们还可以优化一个函数，确保key不是数组两边的数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int FindKey(int* a, int left,int right)</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">	int key = left + (left + right) / 2;</span><br><span class="line">	if (a[left] &lt; a[key])</span><br><span class="line">	&#123;</span><br><span class="line">		if (a[right] &gt; a[key])</span><br><span class="line">		&#123;</span><br><span class="line">			return key;</span><br><span class="line">		&#125;</span><br><span class="line">		else if (a[left] &gt; a[right])</span><br><span class="line">		&#123;</span><br><span class="line">			return left;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			return right;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		if (a[key] &gt; a[right])</span><br><span class="line">		&#123;</span><br><span class="line">			return key;</span><br><span class="line">		&#125;</span><br><span class="line">		else if (a[left] &lt; a[right])</span><br><span class="line">		&#123;</span><br><span class="line">			return left;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			return right;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>看着复杂，不过不算难，再将用到Key的地方全部换成这个函数优化key的选择，快排还能还进一步…</p>
<h3 id=""><a href="#" class="headerlink" title=""></a></h3><p>非递归实现</p>
<p>如何实现非递归？</p>
<p>那我们就要好好想想递归是来做什么的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934196.png"></p>
<p>这是要排序的数组，快排递归实现就是如下图所示：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934441.png"></p>
<p>它会将选定的key排到它应该有的位置，而 左边都比它小，右边都比它大，随后，它将key左边传入函数，再递归，再将右边传入函数递归：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934799.png"></p>
<p>每一次，它都至少可以保证一个数字来到应该有的位置上，如此反复</p>
<p>…</p>
<p>如果要非递归，那我们就要实现的是每一次都传入一个区间，直到每个区间都覆盖到，这里就要用到之前的 队列&#x2F;栈</p>
<p>我这里用的是栈， 在用之前，要先把栈的函数拷贝一份：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void StackInit(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	ps-&gt;arr = NULL;</span><br><span class="line">	ps-&gt;capacity = 0;</span><br><span class="line">	ps-&gt;top = 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void StackPush(Stack* ps, STDataType data)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	if (ps-&gt;top == ps-&gt;capacity)</span><br><span class="line">	&#123;</span><br><span class="line">		int newcapacity = ps-&gt;capacity == 0 ? 4 : (ps-&gt;capacity) * 2;</span><br><span class="line">		ps-&gt;arr = (STDataType*)realloc(ps-&gt;arr, sizeof(Stack)* newcapacity);</span><br><span class="line">		if (ps-&gt;arr == NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			printf(&quot;realloc fail\n&quot;);</span><br><span class="line">			exit(-1);</span><br><span class="line">		&#125;</span><br><span class="line">		ps-&gt;capacity = newcapacity;</span><br><span class="line">	&#125;</span><br><span class="line">	ps-&gt;arr[ps-&gt;top++] = data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void StackPop(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	assert(ps-&gt;top &gt; 0);</span><br><span class="line">	ps-&gt;top--;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">STDataType StackTop(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	return ps-&gt;arr[ps-&gt;top-1];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int StackSize(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	return ps-&gt;top;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int StackEmpty(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	return ps-&gt;top == 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void StackDestroy(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	free(ps-&gt;arr);</span><br><span class="line">	ps-&gt;arr = NULL;</span><br><span class="line">	ps-&gt;top = 0;</span><br><span class="line">	ps-&gt;capacity = 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>都是之前写过也用过好几次的代码了，这里就不重复说明了</p>
<p>思路是这样的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200934542.png"></p>
<p>已有一个数组，将它最左边的和最右边的下标压下去</p>
<p>这里压的是 6 和 8</p>
<p>循环判断，如果栈不为空，就进去</p>
<p>随后拿两个值来接收栈里面的值，一个是左下标，一个是右下标，对这一块空间排序</p>
<p>然后对它进行一次排序，随后它应该会被分成两块：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200935482.png"></p>
<p>可以看到key左边是一块待排序的空间，右边也是一块待排序的空间</p>
<p>将它的左边压栈，它左边的空间是 0 - key-1</p>
<p>将它的右边压栈，它右边的空间是 key+1 - end（数组末尾）</p>
<p>不过在压栈之前，最好先判断一下它们中间有没有数据</p>
<p>如果此时数组还没排完，那栈里面应该还压着其他区间，随后再次读取数据</p>
<p>直到栈为空</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void QuickSortNonR(int* a, int left, int right)</span><br><span class="line">&#123;</span><br><span class="line">    //创建栈并初始化</span><br><span class="line">	Stack st;</span><br><span class="line">	StackInit(&amp;st);</span><br><span class="line">    </span><br><span class="line">    //插入头尾</span><br><span class="line">	StackPush(&amp;st, left);</span><br><span class="line">	StackPush(&amp;st, right);</span><br><span class="line"></span><br><span class="line">    //循环代替递归</span><br><span class="line">	while (!StackEmpty(&amp;st))</span><br><span class="line">	&#123;</span><br><span class="line"></span><br><span class="line">        //因为后压的右下标，所以右下标先出来</span><br><span class="line">		int right1 = StackTop(&amp;st);</span><br><span class="line">		StackPop(&amp;st);</span><br><span class="line">		int left1 = StackTop(&amp;st);</span><br><span class="line">		StackPop(&amp;st);</span><br><span class="line"></span><br><span class="line">        //对这一块空间排序</span><br><span class="line">		int mid = PartSort2(a, left1, right1);</span><br><span class="line"></span><br><span class="line">        //如果左边还有就压左边</span><br><span class="line">		if (mid-1-left1&gt;0)</span><br><span class="line">		&#123;</span><br><span class="line">			StackPush(&amp;st, left1);</span><br><span class="line">			StackPush(&amp;st, mid - 1);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">        //右边还有就压右边</span><br><span class="line">		if (right1-mid-1&gt;0)</span><br><span class="line">		&#123;</span><br><span class="line">			StackPush(&amp;st, mid + 1);</span><br><span class="line">			StackPush(&amp;st, right1);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    //排序完销毁栈</span><br><span class="line">	StackDestroy(&amp;st);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这里用的单趟快排上面可以复制，这里将不写了.</p>
<p><strong>时间复杂度：O（N*logN）</strong></p>
<p><strong>空间复杂度：O（1）</strong></p>
<p><strong>挺快的，但代码很长…</strong></p>
<hr>
<h2 id="归并"><a href="#归并" class="headerlink" title="归并"></a>归并</h2><p>如果做过这样的一道题，那归并的思路就很好理解了：</p>
<p>请合并两个有序数组，并让返回的数组依旧有序</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200935351.png"></p>
<p>先比较第一个数据</p>
<p>1 &gt;  2  ?</p>
<p>新创建的数组的第一个元素就放小的那个，然后让 3 跟  2  比</p>
<p>2 小于 3</p>
<p>所以新数组第二个元素就放 2</p>
<p>然后 4 跟  3  比</p>
<p>…….</p>
<p>直到其中一个没有或者两个都比完：</p>
<p>这就叫归并</p>
<p>那归并排序是什么样的呢？</p>
<p>看图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200935212.png"></p>
<p>这是已有的数组</p>
<p>对其中每一块区域分别归并：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200935488.png"></p>
<p>以左半边举例</p>
<p>9  3  8  6 显然无序</p>
<p>将它分为9  3   一组 8   6</p>
<p>9  3 也无序，所以将其分为   9  一组，3一组</p>
<p>然后归并就变成了</p>
<p>3  9</p>
<p>8  6归并就成了6  8</p>
<p>再将3  9 和6  8归并</p>
<p>3  6  8  9</p>
<p>递归的条件，即有多个元素，每次都分一半，直到没得分，这个时候就可以归并了</p>
<p>切记，归并不能在原数组归并，不然会丢失数据，所以要新建立一个临时数组，然后将这个数组的值拷贝进去</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void _MergeSort(int*a, int begin, int end, int* tmp)</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    //如果begin &gt;= end  说明这个区间是无效区间，直接返回</span><br><span class="line">	if (begin &gt;= end)</span><br><span class="line">		return;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    //每次都取一半</span><br><span class="line">	int mid = (begin + end) / 2;</span><br><span class="line"></span><br><span class="line">    //左半边</span><br><span class="line">	_MergeSort(a, 0, mid, tmp);</span><br><span class="line"></span><br><span class="line">    //右半边</span><br><span class="line">	_MergeSort(a, mid+1, end, tmp);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    //单趟（归并没什么好说的吧？）</span><br><span class="line">	int begin1 = begin ,  end1 = mid;</span><br><span class="line">	int begin2 = mid + 1, end2 = end;</span><br><span class="line">	int pos = begin;</span><br><span class="line"></span><br><span class="line">	while (begin1 &lt;= end1 &amp;&amp; begin2 &lt;= end2)</span><br><span class="line">	&#123;</span><br><span class="line">		if (a[begin1] &lt; a[begin2])</span><br><span class="line">			tmp[pos++] = a[begin1++];</span><br><span class="line">		else</span><br><span class="line">			tmp[pos++] = a[begin2++];</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    //也许是其中一个数组放完了，另一个还没有，所以判断一下</span><br><span class="line">	while (begin1 &lt;= end1)</span><br><span class="line">		tmp[pos++] = a[begin1++];</span><br><span class="line"></span><br><span class="line">	while (begin2 &lt;= end2)</span><br><span class="line">		tmp[pos++] = a[begin2++];</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    //别忘了拷贝过去</span><br><span class="line">	memcpy(a + begin, tmp + begin, (end - begin + 1)*sizeof(int));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">// 归并排序递归实现</span><br><span class="line">void MergeSort(int* a, int n)</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    //新建立的临时数组，用于存放归并数组的数据</span><br><span class="line">	int* tmp = (int*)malloc(sizeof(int)* n);</span><br><span class="line">	assert(tmp);</span><br><span class="line"></span><br><span class="line">    //调用归并函数</span><br><span class="line">	_MergeSort(a, 0, n, tmp);</span><br><span class="line"></span><br><span class="line">	free(tmp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><strong>时间复杂度：O（N*logN）</strong></p>
<p><strong>空间复杂度：O（N）</strong></p>
<p><strong>不好用，别用</strong></p>
<hr>
<h3 id="非递归实现归并"><a href="#非递归实现归并" class="headerlink" title="非递归实现归并"></a>非递归实现归并</h3><p>非递归，就是模拟递归的一个过程</p>
<p>在递归实现里面，先将其中元素一个一个归并，然后再两个两个归并，再四个四个归并…</p>
<p>那是不是可以拿一个循环解决？</p>
<p>所以，非递归版本就这么来了：</p>
<p>首先要定义一个gap，这个gap是每次递归的区间</p>
<p>如果是1，每次就归并一个</p>
<p>如果是2，每次就归并两个</p>
<p>如果是4，每次就归并四个</p>
<p>…</p>
<p>每次都归并一个数组的长度，可以嵌套一个for循环</p>
<p>在定义两个数组的头尾指针</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200935691.png"></p>
<p>如果gap是1，预期的效果是每次比较1个，那头尾指针就该这么定义：</p>
<p>begin1 &#x3D; 0，end1 &#x3D; begin1+gap-1；</p>
<p>begin1是从0开始的，如果循环比较，那这里应该赋值为i（循环）</p>
<p>end1 则是 begin1 加个gap（间隔），但是别忘了-1才是下标</p>
<p>begin2 则是 begin+gap，这样就跳过了begin1的数组</p>
<p>end2 则是 begin2+gap-1，同end1原理</p>
<p>…</p>
<p>在走过一遍之后，要将gap*2，这样就可以一次归并两个长度为2的数组</p>
<p>…</p>
<p><strong>切记</strong></p>
<p><strong>可能越界，因为gap每次乘2，及有发生以下情况：</strong></p>
<p><strong>end1越界了，那直接修改end1 &#x3D; n（长度）-1</strong></p>
<p><strong>begin2 越界，那此时就要修改begin2和end2，因为判断条件是begin2 &lt;&#x3D; end2，所以只需要让begin2&gt;end2即可</strong></p>
<p><strong>end2越界，此时需要将end2修改为 n（长度）-1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void MergeSortNonR(int* a, int n)</span><br><span class="line">&#123;</span><br><span class="line">	</span><br><span class="line">	//间隔 gap</span><br><span class="line">	int gap = 1;</span><br><span class="line">	int* tmp = (int*)malloc(sizeof(int)*n);</span><br><span class="line"></span><br><span class="line">    //如果间隔大于长度，就不用比了</span><br><span class="line">	while (gap &lt; n)</span><br><span class="line">	&#123;</span><br><span class="line"></span><br><span class="line">        //每次都遍历一遍数组</span><br><span class="line">		for (int i = 0; i &lt; n; i += 2 * gap)</span><br><span class="line">		&#123;</span><br><span class="line">			int begin1 = i, end1 = i + gap - 1;</span><br><span class="line">			int begin2 = i + gap, end2 =i+2*gap - 1;</span><br><span class="line"></span><br><span class="line">        </span><br><span class="line"></span><br><span class="line">            //判断可能越界的值</span><br><span class="line">			if (end1 &gt;= n)</span><br><span class="line">				end1 = n - 1;</span><br><span class="line"></span><br><span class="line">			// begin2 越界，第二个区间不存在</span><br><span class="line">			if (begin2 &gt;= n)</span><br><span class="line">			&#123;</span><br><span class="line">				begin2 = n;</span><br><span class="line">				end2 = n - 1;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			// begin2 ok， end2越界，修正end2即可</span><br><span class="line">			if (begin2 &lt; n &amp;&amp; end2 &gt;= n)</span><br><span class="line">				end2 = n - 1;</span><br><span class="line"></span><br><span class="line">            //这里完全是为了好看，如果报错这里可以看到每次的区间</span><br><span class="line">			printf(&quot;%d %d %d %d\n&quot;, begin1, end1, begin2, end2);</span><br><span class="line"></span><br><span class="line">            //归并</span><br><span class="line">			int pos = begin1;</span><br><span class="line">			while (begin1 &lt;= end1 &amp;&amp; begin2 &lt;= end2)</span><br><span class="line">			&#123;</span><br><span class="line">				if (a[begin1] &lt; a[begin2])</span><br><span class="line">					tmp[pos++] = a[begin1++];</span><br><span class="line">				else</span><br><span class="line">					tmp[pos++] = a[begin2++];</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			//也许是其中一个数组放完了，另一个还没有，所以判断一下</span><br><span class="line">			while (begin1 &lt;= end1)</span><br><span class="line">				tmp[pos++] = a[begin1++];</span><br><span class="line"></span><br><span class="line">			while (begin2 &lt;= end2)</span><br><span class="line">				tmp[pos++] = a[begin2++];</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">			//别忘了拷贝过去</span><br><span class="line">			memcpy(a, tmp, sizeof(int)*end2);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		gap = gap * 2;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    //用完了释放，别忘记了</span><br><span class="line">	free(tmp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><strong>时间复杂度：O（N*logN）</strong></p>
<p><strong>空间复杂度：O（N）</strong></p>
<p><strong>不好用，别用</strong></p>
<hr>
<h2 id="计数排序"><a href="#计数排序" class="headerlink" title="计数排序"></a>计数排序</h2><p>一个比较简单的排序，如果理解的话，应该是这样理解的</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200935506.png"></p>
<p>这是要排序的数组，先建立一个数组，这个数组的大小应该是要排序里面数字的最大值</p>
<p>举个例子，这里最大的是9，所以就要开辟 10 个整型的空间（因为只有10个空间最后的下标才是9）</p>
<p>将待排序的数组元素作为下标，放到开辟空间对应的下标处，每放一个，就+1（初始化0）</p>
<p>打印的时候再将开辟数组里面不为0的地方打印出来</p>
<p>比方说这里有 2个3，那新开辟数组下标为3的位置就有 2 个，到时候返回就多返回一个</p>
<hr>
<p>但是这样做有一个坏处，就是，如果用最大数字来开辟空间的话，那假设是这样的一个数组</p>
<p>10000 5000  9999   8888</p>
<p>最大值10000，也就是说要开辟10000个整型的空间，但前面4999个都用不到</p>
<p>于是就有了优化版本</p>
<p>以最小的为参考值</p>
<p>这里是5000</p>
<p>开辟空间时开辟 最大值-最小值+1的空间</p>
<p>放数据的时候再-一个最小值</p>
<p>再之后打印的时候再+最小值</p>
<p>比方说5000，它放进去的时候-5000，就会放到 0 的位置</p>
<p>打印的时候 0+ 5000，所以打印出来的就是5000</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 计数排序</span><br><span class="line">void CountSort(int* a, int n)</span><br><span class="line">&#123;</span><br><span class="line">    //找最大最小值</span><br><span class="line">	int max, min;</span><br><span class="line">	max = min = a[0];</span><br><span class="line">	int i;</span><br><span class="line">	for (i = 1; i &lt; n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		if (a[i] &gt; max)</span><br><span class="line">		&#123;</span><br><span class="line">			max = a[i];</span><br><span class="line">		&#125;</span><br><span class="line">		if (a[i] &lt; min)</span><br><span class="line">		&#123;</span><br><span class="line">			min = a[i];</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	//开辟空间</span><br><span class="line">	int range = max - min + 1;</span><br><span class="line">	int* arr = (int*)calloc(range, sizeof(int));</span><br><span class="line">	assert(arr);</span><br><span class="line"></span><br><span class="line">	//开始计数</span><br><span class="line">	for (i = 0; i &lt; n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		arr[a[i]-min] +=1;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	//放回原数组</span><br><span class="line">	int j = 0;</span><br><span class="line">	for (i = 0; i &lt; range; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		while (arr[i]--)</span><br><span class="line">		&#123;</span><br><span class="line">			a[j++] = i+min;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">		</span><br><span class="line">	//释放空间</span><br><span class="line">	free(arr);</span><br><span class="line">	arr = NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>时间复杂度：O（N+Range（数据范围））</p>
<p>空间复杂度：O（N+Range（数据范围））</p>
<p><strong>适用于范围比较集中、数据量大的情况，如果类似 10000 1</strong></p>
<p><strong>还是不要用这种了</strong></p>
<hr>
<h2 id="稳定性："><a href="#稳定性：" class="headerlink" title="稳定性："></a>稳定性：</h2><p>排序除了时间复杂度和空间复杂度以外，还有一个值得比较的是它的稳定性</p>
<p>稳定性可以用下图理解：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200935009.png"></p>
<p>原本红色的99在黑的前面，现在排完序它们之间的相对位置还是没变，红的还是在黑的前面</p>
<p>上面的八个排序中：</p>
<p><strong>1.插入排序：稳</strong></p>
<p><strong>每次移动一个，原本在前面的在移动后面的时候也不会变位</strong></p>
<p><strong>2.希尔排序：不稳</strong></p>
<p><strong>如果有两个同样的数据但被分到不一样的组，那之后调整就可以出问题</strong></p>
<p><strong>3.选择排序：不稳</strong></p>
<p><strong>3 3  1  1</strong></p>
<p><strong>假设原本是这样的，将1调整到第一位的时候，原本第一位的3会来到第三位，这样就会出问题</strong></p>
<p><strong>4.堆排序：不稳</strong></p>
<p><strong>堆排序建大堆小堆直接就乱了</strong></p>
<p><strong>5.冒泡排序：稳</strong></p>
<p><strong>每次都只是交换，前后位置还是不变的</strong></p>
<p><strong>6.快速排序：不稳</strong></p>
<p><strong>每次分割一半，兴许哪一次就出现问题</strong></p>
<p><strong>7.归并排序：稳（不稳）</strong></p>
<p><strong>如果两数一样时都放第一个数组元素，那它就是稳的，如果放第二个数组，那就是不稳的</strong></p>
<p><strong>8.计数排序：不稳</strong></p>
<p><strong>它只管计数，然后放回去，不会管先放的是哪个</strong></p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>日期类</title>
    <url>/2022/05/19/%E6%97%A5%E6%9C%9F%E7%B1%BB/</url>
    <content><![CDATA[<hr>
<p>既然决定写一个 日期 类，那就需要三个参数：</p>
<p>年，月，日</p>
<p>所以先定义出来：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Data</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">public：</span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line">	int _year;</span><br><span class="line">	int _month;</span><br><span class="line">	int _day;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>里面的三个对象就不用公开，有需要可以直接在类里面操作。</p>
<hr>
<h2 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h2><p>我们知道，有三种构造函数：</p>
<p>1、无参</p>
<p>2、缺省</p>
<p>3、编译器自带</p>
<p>这里就不适合用编译器自带的，因为编译器自带的对于内置类型（int、double…）不会做处理，所以这里用的是缺省参数。</p>
<blockquote>
<p>Data(int year,int month, int day)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> this-&gt;_year = year;</span><br><span class="line"></span><br><span class="line"> this-&gt;_month = month;</span><br><span class="line"></span><br><span class="line"> this-&gt;_day = day;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>为了方便阅读，所以我在这里会用this指针，可以简单理解为就是要改变的参数，假设这是d1要初始化，那this就是d1，this-&gt;_year &#x3D; d1-&gt;year;</p>
<p>当写出这样的代码的时候，有没有一种可能，就是用户使用的时候他传来一个相对错误的日期，比方说：</p>
<p>不是闰年的时候传进来2 29号</p>
<p>一个月的第32天</p>
<p>公元300年</p>
<p>…</p>
<p>显然，这些是我们没有考虑到的，所以为了更合理的使用这个日期类，我们最好加一个判断条件：</p>
<p>如果年份太离谱，报错</p>
<p>如果月份日期不对，报错</p>
<p>但是，要如何来判断这个月份对不对呢？</p>
<p>可以再套一个函数：</p>
<blockquote>
<p>Data(int year,int month, int day)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> if (year &lt; 1900 ||</span><br><span class="line"></span><br><span class="line">     month &lt; 1 ||</span><br><span class="line"></span><br><span class="line">     month&gt;12 ||</span><br><span class="line"></span><br><span class="line">     day&gt;GetMonthDay(year, month))</span><br><span class="line"></span><br><span class="line"> &#123;</span><br><span class="line"></span><br><span class="line">     cout &lt;&lt; &quot;error&quot; &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> this-&gt;_year = year;</span><br><span class="line"></span><br><span class="line"> this-&gt;_month = month;</span><br><span class="line"></span><br><span class="line"> this-&gt;_day = day;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>稍微改进一下，如果年份小于1900，那就报错，如果月份小于1，如果月份大于12，或者是</p>
<p>day&gt;GetMonthDay ；</p>
<p>这个GetMonthDay ，是之后要实现的一个函数，来判断这个月有多少天，具体实现也挺简单的，首先要开辟一个数组，这个数组里面放上每个月的具体天数，然后根据传过去的参数来返回这个月有多少天。</p>
<p>这里要注意一点，别忘了判断闰年：</p>
<blockquote>
<p>int GetMonthDay(int year, int month)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> int MonthDayArray[13] = &#123; 0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31 &#125;; </span><br><span class="line"></span><br><span class="line"> if (year % 4 == 0 &amp;&amp; year % 100 != 0 ||</span><br><span class="line"></span><br><span class="line">     year % 400 == 0)</span><br><span class="line"></span><br><span class="line">     if (month == 2)</span><br><span class="line"></span><br><span class="line">         return 29;    </span><br><span class="line"></span><br><span class="line"> return MonthDayArray[month];</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>这里要注意的一点是，最后返回日期的时候，如果创建的数组的大小是12，那返还的的应该是MonthDayArray[month-1]</p>
<p>连起来：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201050305.png"></p>
<p>既然写好了构造函数，那就来用一下：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201050974.png"></p>
<p>报错，因为2022不是闰年，所以没有29天</p>
<p>既然创建好了一个初始对象，那我们正好写一个打印函数，来方便我们查看里面的内容：</p>
<blockquote>
<p>void printf()</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> cout &lt;&lt; this-&gt;_year &lt;&lt; &quot; &quot; &lt;&lt; this-&gt;_month &lt;&lt; &quot; &quot; &lt;&lt; this-&gt;_day &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>很简单的函数，就不细说了。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201050576.png"></p>
<p>可以看到，如果是之前的代码来跑这个的话，因为当时写的时候是直接打印error然后返回，所以d1里面的值都是随机值，如果我们写一个正确的值的话：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201050006.png"></p>
<p>还是没问题的。</p>
<hr>
<h2 id="析构函数"><a href="#析构函数" class="headerlink" title="析构函数"></a>析构函数</h2><p>写不写都可以，这里为了说明写一个简单一点的析构函数：</p>
<blockquote>
<p>~Data()</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> this-&gt;_day = this-&gt;_month = this-&gt;_year = 0;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>类名前面加上一个波浪号表示这是一个析构函数，实现方式就是直接讲三个参数重制为零就好了。</p>
<hr>
<h2 id="拷贝构造函数"><a href="#拷贝构造函数" class="headerlink" title="拷贝构造函数"></a>拷贝构造函数</h2><p>初始化的时候，我们不止可以用单纯的的类型来初始化，还可以用一个对象来初始化另一个对象，这个操作称之为拷贝构造。</p>
<p>拷贝分为两种：</p>
<p>浅拷贝，也称之为值拷贝，它是很简单的一个字节一个字节的拷贝</p>
<p>对于那些int double 来说它是可以用的，但是假若开辟了一块空间，那浅拷贝的时候它会把这个指针的地址也拷贝过去，也就是说两个对象的指针指向的是同一块空间，就会出现析构两次，或者是操作一个对象另一个对象也出现对应现象的错误。</p>
<p>为了解决这种错误，出现了另一个，叫做：深拷贝</p>
<p>深拷贝就不会出现这种问题，但对于我们现在写的日期来说，浅拷贝也已经够用了。</p>
<p>同样，这里也是可以不写的，这里是为了示范：</p>
<blockquote>
<p>Data(const Data&amp; d)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> this-&gt;_year = d._year;</span><br><span class="line"></span><br><span class="line"> this-&gt;_month = d._month;</span><br><span class="line"></span><br><span class="line"> this-&gt;_day = d._day;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>这里要说一点，当我们传参数的时候，假若是要讲d2拷贝为d1的内容，那我们调用类函数的时候是这样的：</p>
<p>d2（d1）；</p>
<p>我们传过去的参数只要一个d1，d2它是会被默认传过去的，如果在传一个d2过去就会引发错误，而我们想找d2的时候直接用this就可以了。</p>
<p>所以这里直接就是this-&gt;_year &#x3D; d._year</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051430.png"></p>
<hr>
<h2 id="赋值运算符重载"><a href="#赋值运算符重载" class="headerlink" title="赋值运算符重载"></a>赋值运算符重载</h2><p>这个就好像那些很简单的，用一个对象来赋值给另一个类型的对象，注意以下几点：</p>
<p>注意传参的时候只用传要赋值的那个就可以了</p>
<p>注意被赋值的对象跟要赋值的对象应该不一样，如果是自己赋值自己，这种行为还是报错比较好</p>
<p>注意我们最后要返回这个值本身，因为语言本身支持这样：a &#x3D; b &#x3D; c，这种连续赋值，如果我们不返回值那这种情况就会报错</p>
<p>注意用引用，如果返回的单纯是一个值，返回中会有拷贝过程，所以这里用引用效率，或者说是速度会更快一点。</p>
<p>注意this是一个对象指针，指向的是要赋值的那个对象，所以要返回对象的时候应该先解引用。</p>
<blockquote>
<p>Data&amp; operator&#x3D;(const Data&amp; d)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> if (this == &amp;d)</span><br><span class="line"></span><br><span class="line">     cout &lt;&lt; &quot;error&quot; &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line"> else</span><br><span class="line"></span><br><span class="line"> &#123;</span><br><span class="line"></span><br><span class="line">     this-&gt;_year = d._year;</span><br><span class="line"></span><br><span class="line">     this-&gt;_month = d._month;</span><br><span class="line"></span><br><span class="line">     this-&gt;_day = d._day;</span><br><span class="line"></span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> return *this;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051320.png"></p>
<h2 id="日期-x3D-天数"><a href="#日期-x3D-天数" class="headerlink" title="日期+&#x3D;天数"></a>日期+&#x3D;天数</h2><p>这个其实也很好解决，我们直接将天数加上去，然后进行判断，如果这个天数大于这个月该有的天数，那我们的月份就+1，然后天数-这个月的天数，如果过程中月份到了13，那就应该把月份重制为1.</p>
<p>简单写一个：</p>
<blockquote>
<p>Data&amp; operator+&#x3D;(int day)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> this-&gt;_day += day;</span><br><span class="line"></span><br><span class="line"> while (this-&gt;_day &gt; GetMonthDay(this-&gt;_year, this-&gt;_month))</span><br><span class="line"></span><br><span class="line"> &#123;</span><br><span class="line"></span><br><span class="line">     this-&gt;_day -= GetMonthDay(this-&gt;_year, this-&gt;_month);</span><br><span class="line"></span><br><span class="line">     this-&gt;_month += 1;</span><br><span class="line"></span><br><span class="line">     if (this-&gt;_month == 13)</span><br><span class="line"></span><br><span class="line">     &#123;</span><br><span class="line"></span><br><span class="line">         this-&gt;_year += 1;</span><br><span class="line"></span><br><span class="line">         this-&gt;_month = 1;</span><br><span class="line"></span><br><span class="line">     &#125;</span><br><span class="line"></span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> return *this;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>目前效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051949.png"></p>
<h2 id="日期-天数"><a href="#日期-天数" class="headerlink" title="日期+天数"></a>日期+天数</h2><p>既然有+&#x3D;，那就有+，不过这里有一点要注意：</p>
<p>+不一定是真的+上去，也许是在表达式里面进行的一个判断过程，所以我们不能直接用+&#x3D;的思想。</p>
<p>这里也简单写一个：</p>
<blockquote>
<p>Data&amp; operator+(int day)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> Data tmp(*this);</span><br><span class="line"></span><br><span class="line"> tmp += day;</span><br><span class="line"></span><br><span class="line"> return tmp;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>先初始画一个拷贝的值，然后对这个拷贝的值进行操作，再返回，不要对*this操作，注意注意注意。</p>
<hr>
<h2 id="日期-x3D-天数-1"><a href="#日期-x3D-天数-1" class="headerlink" title="日期-&#x3D;天数"></a>日期-&#x3D;天数</h2><p>有加就有减，大体思路其实跟日期+差不多，同样是先减，然后再循环判断：</p>
<p>注意，先减一月，因为在上来的this-&gt;_day -&#x3D; day的时候这个月已经减完了，所以要拿下个月的出来。</p>
<blockquote>
<p>Data&amp; operator-&#x3D;(int day)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> this-&gt;_day -= day;</span><br><span class="line"></span><br><span class="line"> while (this-&gt;_day &lt; 0)</span><br><span class="line"></span><br><span class="line"> &#123;    </span><br><span class="line"></span><br><span class="line">     this-&gt;_month -= 1;</span><br><span class="line"></span><br><span class="line">     if (this-&gt;_month == 0)</span><br><span class="line"></span><br><span class="line">     &#123;</span><br><span class="line"></span><br><span class="line">         this-&gt;_year--;</span><br><span class="line"></span><br><span class="line">     &#125;</span><br><span class="line"></span><br><span class="line">     this-&gt;_day += GetMonthDay(this-&gt;_year, this-&gt;_month);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> return *this;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<hr>
<h2 id="日期-天数-1"><a href="#日期-天数-1" class="headerlink" title="日期-天数"></a>日期-天数</h2><p>这个，就没什么好说的，如果明白了+，那-应该也明白了。</p>
<blockquote>
<p>Data&amp; operator-(int day)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> Data tmp(*this);</span><br><span class="line"></span><br><span class="line"> tmp -= (day);</span><br><span class="line"></span><br><span class="line"> return tmp;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<hr>
<h2 id="gt-运算符重载"><a href="#gt-运算符重载" class="headerlink" title="&gt;运算符重载"></a>&gt;运算符重载</h2><p>作为一个日期类，比较大小还是很简单的，首先比较年，如果年比较不出来大小，那就比较月，如果月比较不出来，那就比较天。</p>
<p>简单写一下：</p>
<blockquote>
<p>bool operator&gt;(const Data&amp; d)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> if (this-&gt;_year &lt; d._year ||</span><br><span class="line"></span><br><span class="line">     this-&gt;_year &lt;= d._year&amp;&amp;this-&gt;_month &lt; d._month ||</span><br><span class="line"></span><br><span class="line">     this-&gt;_year &lt;= d._year&amp;&amp;this-&gt;_month &lt;= d._month&amp;&amp;this-&gt;_day &lt;= d._day)</span><br><span class="line"></span><br><span class="line">     return false;</span><br><span class="line"></span><br><span class="line"> return true;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051280.png"></p>
<hr>
<h2 id="x3D-x3D-运算符"><a href="#x3D-x3D-运算符" class="headerlink" title="&#x3D;&#x3D;运算符"></a>&#x3D;&#x3D;运算符</h2><p>这个，算非常简单的那一种，我们直接比较对应的年月日即可：</p>
<blockquote>
<p>bool operator&#x3D;&#x3D;(const Data&amp; d)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> return this-&gt;_year == d._year&amp;&amp;</span><br><span class="line"></span><br><span class="line">        this-&gt;_month == d._month&amp;&amp;</span><br><span class="line"></span><br><span class="line">        this-&gt;_day == d._day;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051807.png"></p>
<hr>
<h2 id="gt-x3D-运算符重载"><a href="#gt-x3D-运算符重载" class="headerlink" title="&gt;&#x3D;运算符重载"></a>&gt;&#x3D;运算符重载</h2><p>这里，&gt;&#x3D;的结果，是不是就是把我们之前写的大于和等于结合起来。</p>
<p>所以我们直接复用代码：</p>
<blockquote>
<p>bool operator&gt;&#x3D;(const Data&amp; d)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> return *this &gt; d||*this == d;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051989.png"></p>
<hr>
<h2 id="lt-运算符重载"><a href="#lt-运算符重载" class="headerlink" title="&lt;运算符重载"></a>&lt;运算符重载</h2><p>小于，是不是大于等于的结果取反？</p>
<p>所以还是直接复用代码：</p>
<blockquote>
<p>bool operator&lt;(const Data&amp; d)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> return !(*this &gt;= d);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051701.png"></p>
<hr>
<h2 id="lt-x3D-运算符重载"><a href="#lt-x3D-运算符重载" class="headerlink" title="&lt;&#x3D;运算符重载"></a>&lt;&#x3D;运算符重载</h2><p>小于等于，那不就是大于的取反吗</p>
<p>继续复用：</p>
<blockquote>
<p>bool operator&lt;&#x3D;(const Data&amp; d)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> return !(*this&gt;d);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051557.png"></p>
<hr>
<h2 id="x3D-运算符重载"><a href="#x3D-运算符重载" class="headerlink" title="!&#x3D;运算符重载"></a>!&#x3D;运算符重载</h2><p>这还用说吗，等于取反：</p>
<blockquote>
<p>bool operator!&#x3D;(const Data&amp;d)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> return !(*this == d);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051542.png">编辑</p>
<hr>
<h2 id="日期-日期-返回天数"><a href="#日期-日期-返回天数" class="headerlink" title="日期-日期 返回天数"></a>日期-日期 返回天数</h2><p>这里有好几种方法实现，比如说：</p>
<p>1、年减年，月减月，日减日</p>
<p>2、一直加那个小的直到它们两个相等</p>
<p>第一种方法是很容易想到但是不容易实现的，因为要考虑到有的年月天数不一样，所以会比较麻烦，这里推荐使用第二种：</p>
<p>因为不能改变原有数据，所以我们先创建一个对象，让它来表示小的那个，然后循环：</p>
<blockquote>
<p>int operator-(const Data&amp; d)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> Data min(*this);</span><br><span class="line"></span><br><span class="line"> Data max(d);</span><br><span class="line"></span><br><span class="line"> int count = 0;</span><br><span class="line"></span><br><span class="line"> if (*this &gt; d)</span><br><span class="line"></span><br><span class="line"> &#123;</span><br><span class="line"></span><br><span class="line">     min = d;</span><br><span class="line"></span><br><span class="line">     max = *this;</span><br><span class="line"></span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> while (max != min)</span><br><span class="line"></span><br><span class="line"> &#123;</span><br><span class="line"></span><br><span class="line">     count++;</span><br><span class="line"></span><br><span class="line">     min+=1;</span><br><span class="line"></span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> return count;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>默认小的那个是*this，大的那个是d，然后判断如果取反了就交换，再累加计算，效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051527.png"></p>
<hr>
<h2 id="前置"><a href="#前置" class="headerlink" title="前置++"></a>前置++</h2><p>这里要注意的一点是，前置++是先++，然后再算。</p>
<blockquote>
<p>Data operator++()</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> *this += 1;</span><br><span class="line"></span><br><span class="line"> return *this;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051281.png"></p>
<hr>
<h2 id="后置"><a href="#后置" class="headerlink" title="后置++"></a>后置++</h2><p>这里要注意一点，因为是后置，所以为了区分前置，它会在参数里面默认传过去一个int，不需要接收，只是为了表明这是后置++。</p>
<p>注意一点，后置++是先运行完再++，所以不能直接用<em>this，而是用一个对象保存当前</em>this的值饭后，然后再++*this。</p>
<blockquote>
<p>Data operator++(int)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> Data tmp(*this);</span><br><span class="line"></span><br><span class="line"> *this += 1;</span><br><span class="line"></span><br><span class="line"> return tmp;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051951.png"></p>
<p>可以看到第一次的时候还不相等，++完就相等了。</p>
<hr>
<h2 id="前置–"><a href="#前置–" class="headerlink" title="前置–"></a>前置–</h2><p>跟前置++一样，直接复制过来修改一下就好：</p>
<blockquote>
<p>Data operator–()</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> *this -= 1;</span><br><span class="line"></span><br><span class="line"> return *this;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<h2 id="后置–"><a href="#后置–" class="headerlink" title="后置–"></a>后置–</h2><p>同理：</p>
<blockquote>
<p>Data operator–(int)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"></span><br><span class="line"> Data tmp(*this);</span><br><span class="line"></span><br><span class="line"> *this -= 1;</span><br><span class="line"></span><br><span class="line"> return tmp;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201051007.png"></p>
<hr>
<p>支持，日期类就告一段落，希望这篇文章对您有所帮助，下篇文章再见</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>栈</title>
    <url>/2022/03/25/%E6%A0%88/</url>
    <content><![CDATA[<h1 id="栈的概念及结构"><a href="#栈的概念及结构" class="headerlink" title="栈的概念及结构"></a>栈的概念及结构</h1><p>在学习栈前，我们先看一下栈的正式解释：</p>
<p>栈：一种特殊的线性表，其只允许在固定的一端进行插入和删除元素操作。进行数据插入和删除操作的一端称为栈顶，另一端称为栈底。栈中的数据元素遵守后进先出LIFO（Last In First Out）的原则。</p>
<p>压栈：栈的插入操作叫做进栈&#x2F;压栈&#x2F;入栈，入数据在栈顶。</p>
<p>出栈：栈的删除操作叫做出栈。出数据也在栈顶。</p>
<p>来看图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200917417.png"></p>
<p>栈底就是我们栈的开头位置，我们放数据进去，就是以一种往上叠的感觉， 现在栈底已经有a1、a2两个数据，我们现在放a3进去，它就应该在a2的上面。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918395.png"></p>
<p>现在a3已经进栈，我们现在要拿出数据，就从栈顶开始拿，也就是说a3回先被我们拿出来</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918113.png"></p>
<p>然后出栈的顺序就是：a3、a2、a1 </p>
<p>这就是栈的后进先出，但是我们没必要全部放进去了再一起拿出来，我们也可以放一个数据拿一个数据出来，比方说；</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918245.png"></p>
<p>现在有a3、a4要进栈，我们让a3先进栈：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918825.png"></p>
<p>但是我们又要用到a3这个数据，我们就直接把a3拿出去，然后把a4压进去：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918103.png"></p>
<p>那我们出数据的时候，就变成了：</p>
<p>a3、a4、a2、a1</p>
<hr>
<p>我们来看两道题加深理解：</p>
<p>1.一个栈的初始状态为空。现将元素1、2、3、4、5、A、B、C、D、E依次入栈，然后再依次出栈，则元素出栈的顺序是（ ）。 </p>
<p>A 12345ABCDE </p>
<p>B EDCBA54321 </p>
<p>C ABCDE12345 </p>
<p>D 54321EDCBA </p>
<hr>
<p>想到答案了吗？</p>
<p>B，题目里面说依次进栈，那它出栈的时候也就是以此出栈，我们把放进去的数据反一遍就好。</p>
<p>下一题：</p>
<p>2.若进栈序列为 1,2,3,4 ，进栈过程中可以出栈，则下列不可能的一个出栈序列是（）</p>
<p>A 1,4,3,2</p>
<p>B 2,3,4,1</p>
<p>C 3,1,4,2</p>
<p>D 3,4,2,1</p>
<hr>
<p>我们一个一个看，依次分析；</p>
<p>A，如果是1、4、3、2，那它的进栈顺序就是：</p>
<p>先放1，再把1拿出来，然后依次放进2、3、4，最后出栈的时候反着出</p>
<p>–</p>
<p>B、2、3、4、1</p>
<p>先压一个1，然后2、3、4直接拿出来，最后拿1</p>
<p>–</p>
<p>C，我们看这里最先出来的是3，也就是说底下压着的是 2 1，如果是出栈的话那2后进栈应该显出，但这里3后面是1，很明显错误</p>
<p>–</p>
<p>D就不分析了，有兴趣可以自己分析一下。</p>
<hr>
<h2 id="栈的实现"><a href="#栈的实现" class="headerlink" title="栈的实现"></a>栈的实现</h2><p>我们之前学过数组、链表，这两种都可以用在栈上，栈只需要在栈顶进行操作，我们来看看两种分别怎么实现：</p>
<p>数组：可以把数组首元素地址看作栈底，而数组之后的位置看作栈顶，这样数组的栈压栈即是在尾补插入数据，也就是top的位置</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918447.png"></p>
<p>链表：</p>
<p>我们如果把数组第一个节点认为是栈底，那压栈就是在它后面尾插，尾插不难，但是我们要如何出栈呢？</p>
<p>如果是双向的链表就可以解决这个问题，但未免有些麻烦</p>
<p>如果是单链表，我们首插就简单一点。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918325.png"></p>
<p>这里我们就来实现一下数组的栈：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 初始化栈</span><br><span class="line">void StackInit(Stack* ps); </span><br><span class="line">// 入栈</span><br><span class="line">void StackPush(Stack* ps, STDataType data); </span><br><span class="line">// 出栈</span><br><span class="line">void StackPop(Stack* ps); </span><br><span class="line">// 获取栈顶元素</span><br><span class="line">STDataType StackTop(Stack* ps); </span><br><span class="line">// 获取栈中有效元素个数</span><br><span class="line">int StackSize(Stack* ps); </span><br><span class="line">// 检测栈是否为空，如果为空返回非零结果，如果不为空返回0 </span><br><span class="line">int StackEmpty(Stack* ps); </span><br><span class="line">// 销毁栈</span><br><span class="line">void StackDestroy(Stack* ps);</span><br></pre></td></tr></table></figure>

<p>在实现之前，我们要做几件事：</p>
<p>头文件，就不多说了</p>
<p>类型最好定义一下，用typedef，这样我们之后修改存储类型直接在这里修改就好</p>
<p>定义一个结构体</p>
<p>声明我们要实现的函数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line">typedef int STDataType;</span><br><span class="line"></span><br><span class="line">typedef struct Stack</span><br><span class="line">&#123;</span><br><span class="line">	STDataType* arr;</span><br><span class="line">	int top;</span><br><span class="line">	int capacity;</span><br><span class="line">&#125;Stack;</span><br><span class="line"></span><br><span class="line">// 初始化栈</span><br><span class="line">void StackInit(Stack* ps);</span><br><span class="line">// 入栈</span><br><span class="line">void StackPush(Stack* ps, STDataType data);</span><br><span class="line">// 出栈</span><br><span class="line">void StackPop(Stack* ps);</span><br><span class="line">// 获取栈顶元素</span><br><span class="line">STDataType StackTop(Stack* ps);</span><br><span class="line">// 获取栈中有效元素个数</span><br><span class="line">int StackSize(Stack* ps);</span><br><span class="line">// 检测栈是否为空，如果为空返回非零结果，如果不为空返回0 </span><br><span class="line">int StackEmpty(Stack* ps);</span><br><span class="line">// 销毁栈</span><br><span class="line">void StackDestroy(Stack* ps);</span><br></pre></td></tr></table></figure>

<p>然后我们一个一个来实现：</p>
<h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void StackInit(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	ps-&gt;arr = NULL;</span><br><span class="line">	ps-&gt;capacity = 0;</span><br><span class="line">	ps-&gt;top = 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>很简单，我们让指针指向NULL，然后数据为0即可，这里要注意的是：</p>
<p>top位置，如果top的位置是0开始的话，也就是说以下标位置为栈顶</p>
<p>当放 1 2 3 4 5数据的时候，top的下标就是5，也就是下一个数据的位置</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918569.png"></p>
<p>当top为-1开始的时候，放1 2 3 4 5的时候，top的下标为4，也就是当前数据的位置</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918844.png"></p>
<h2 id="入栈"><a href="#入栈" class="headerlink" title="入栈"></a>入栈</h2><p>入栈也就是我们数组top的位置放上要存储的数据，看代码实现：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void StackPush(Stack* ps, STDataType data)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	if (ps-&gt;top == ps-&gt;capacity)</span><br><span class="line">	&#123;</span><br><span class="line">		int newcapacity = ps-&gt;capacity == 0 ? 4 : (ps-&gt;capacity) * 2;</span><br><span class="line">		ps-&gt;arr = (Stack*)realloc(ps-&gt;arr, sizeof(Stack)* newcapacity);</span><br><span class="line">		if (ps-&gt;arr == NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			printf(&quot;realloc fail\n&quot;);</span><br><span class="line">			exit(-1);</span><br><span class="line">		&#125;</span><br><span class="line">		ps-&gt;capacity = newcapacity;</span><br><span class="line">	&#125;</span><br><span class="line">	ps-&gt;arr[ps-&gt;top++] = data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>一进函数，先判断ps是否空指针，其次判断是否还有空间，如果没有空间我们就开辟一个新空间，开辟失败就退出并打印错误信息，如果开辟成功并有空间，我们就放数据进去。</p>
<h2 id="出栈"><a href="#出栈" class="headerlink" title="出栈"></a>出栈</h2><p>就跟顺序表一样，如果出栈，我们就直接将top–即可，不需要销毁什么空间</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void StackPop(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	assert(ps-&gt;top &gt; 0);</span><br><span class="line">	ps-&gt;top--;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="获取栈顶元素"><a href="#获取栈顶元素" class="headerlink" title="获取栈顶元素"></a>获取栈顶元素</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">STDataType StackTop(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	return ps-&gt;arr[ps-&gt;top-1];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>我们直接返回即可，不过要注意这里top，top是0和top是-1的返回值课不一样，别搞错了。</p>
<h2 id="获取栈的有效数据个数"><a href="#获取栈的有效数据个数" class="headerlink" title="获取栈的有效数据个数"></a>获取栈的有效数据个数</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int StackSize(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	return ps-&gt;top;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>直接返回top即可。</p>
<h2 id="看看栈是否为空"><a href="#看看栈是否为空" class="headerlink" title="看看栈是否为空"></a>看看栈是否为空</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int StackEmpty(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	return ps-&gt;top == 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>直接在返回里面判断即可</p>
<p>销毁栈</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void StackDestroy(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	free(ps-&gt;arr);</span><br><span class="line">	ps-&gt;arr = NULL;</span><br><span class="line">	ps-&gt;top = 0;</span><br><span class="line">	ps-&gt;capacity = 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>跟初始化一样，主要是注销里面的指针，然后将其他置零。</p>
<hr>
<h2 id="栈的练习"><a href="#栈的练习" class="headerlink" title="栈的练习"></a>栈的练习</h2><p>我们知道并了解了栈，做一道题看看：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200918756.png"></p>
<p><a class="link"   href="https://leetcode-cn.com/problems/valid-parentheses/"  title="20. 有效的括号 - 力扣（LeetCode） (leetcode-cn.com)">20. 有效的括号 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>这里就可以用到我们刚刚学会的栈。</p>
<p>先说思路：</p>
<p>我们将得到的字符进行判断，如果是左半边，就压栈，如果就右半边就取出来然后配对，配对错就返回false。</p>
<p>看代码实现：</p>
<p>我们先把上面的函数都拿过来用，先复制过来，下面的是主程序</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool isValid(char * s)</span><br><span class="line">&#123;</span><br><span class="line">    Stack arr;</span><br><span class="line">    StackInit(&amp;arr);</span><br><span class="line">    while(*s)</span><br><span class="line">    &#123;</span><br><span class="line">        //压栈</span><br><span class="line">        if(*s == &#x27;(&#x27;||*s == &#x27;[&#x27;||*s == &#x27;&#123;&#x27;)</span><br><span class="line">        &#123;</span><br><span class="line">            StackPush(&amp;arr,*s);</span><br><span class="line">            ++s;</span><br><span class="line">        &#125;</span><br><span class="line">        //*s是右半边，我们开始配对</span><br><span class="line">        else</span><br><span class="line">        &#123;</span><br><span class="line">            //如果取不出来直接就返回false</span><br><span class="line">            if(StackEmpty(&amp;arr))</span><br><span class="line">            &#123;</span><br><span class="line">                return false;</span><br><span class="line">            &#125;</span><br><span class="line">            //取出来开始判断</span><br><span class="line">            char data = StackTop(&amp;arr);</span><br><span class="line">            //取出来顶上一个我们就删一个，方便下一次配对</span><br><span class="line">            StackPop(&amp;arr);</span><br><span class="line">            if((*s ==&#x27;&#125;&#x27;  &amp;&amp; data != &#x27;&#123;&#x27;)</span><br><span class="line">              || (*s == &#x27;)&#x27; &amp;&amp; data != &#x27;(&#x27;)</span><br><span class="line">              || (*s == &#x27;]&#x27; &amp;&amp; data != &#x27;[&#x27;))</span><br><span class="line">               &#123;</span><br><span class="line">                   StackDestroy(&amp;arr);</span><br><span class="line">                   return false;</span><br><span class="line">               &#125;</span><br><span class="line">            //配对成功继续走</span><br><span class="line">            else</span><br><span class="line">            &#123;</span><br><span class="line">                ++s;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    //看栈里面还有没有括号</span><br><span class="line">    int ret = StackEmpty(&amp;arr);</span><br><span class="line">    StackDestroy(&amp;arr);</span><br><span class="line">    return ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>感谢您的观看，我们下次再见</p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>栈和队列的互相实现</title>
    <url>/2022/03/28/%E6%A0%88%E5%92%8C%E9%98%9F%E5%88%97%E7%9A%84%E4%BA%92%E7%9B%B8%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200922988.png"></p>
<p><a class="link"   href="https://leetcode-cn.com/problems/implement-stack-using-queues/"  title="225. 用队列实现栈 - 力扣（LeetCode） (leetcode-cn.com)">225. 用队列实现栈 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>先看题目要求，用两个队列实现栈，我们先把之前写过的队列相关的一些函数拿来：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef int QDataType;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">typedef struct QNode</span><br><span class="line">&#123;</span><br><span class="line">	QDataType data;</span><br><span class="line">	struct queue* next;</span><br><span class="line">&#125;QNode;</span><br><span class="line"></span><br><span class="line">typedef struct Queue</span><br><span class="line">&#123;</span><br><span class="line">	QNode* head;</span><br><span class="line">	QNode* tail;</span><br><span class="line">&#125;Queue;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void QueueInit(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	pq-&gt;head = pq-&gt;tail = NULL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void QueueDestory(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	QNode* cur = pq-&gt;head;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		QNode* next = cur-&gt;next;</span><br><span class="line">		free(cur);</span><br><span class="line">		cur = next;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	pq-&gt;head = pq-&gt;tail = NULL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void QueuePush(Queue* pq, QDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	QNode* newnode = (QNode*)malloc(sizeof(QNode));</span><br><span class="line">	assert(newnode);</span><br><span class="line"></span><br><span class="line">	newnode-&gt;data = x;</span><br><span class="line">	newnode-&gt;next = NULL;</span><br><span class="line"></span><br><span class="line">	if (pq-&gt;tail == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		assert(pq-&gt;head == NULL);</span><br><span class="line">		pq-&gt;head = pq-&gt;tail = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		pq-&gt;tail-&gt;next = newnode;</span><br><span class="line">		pq-&gt;tail = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void QueuePop(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	assert(pq-&gt;head &amp;&amp; pq-&gt;tail);</span><br><span class="line"></span><br><span class="line">	if (pq-&gt;head-&gt;next == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		free(pq-&gt;head);</span><br><span class="line">		pq-&gt;head = pq-&gt;tail = NULL;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		QNode* next = pq-&gt;head-&gt;next;</span><br><span class="line">		free(pq-&gt;head);</span><br><span class="line">		pq-&gt;head = next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bool QueueEmpty(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line"></span><br><span class="line">	return q-&gt;head == NULL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">size_t QueueSize(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	QNode* cur = pq-&gt;head;</span><br><span class="line">	size_t size = 0;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		size++;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	return size;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">QDataType QueueFront(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	assert(pq-&gt;head);</span><br><span class="line"></span><br><span class="line">	return pq-&gt;head-&gt;data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">QDataType QueueBack(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	assert(pq-&gt;tail);</span><br><span class="line"></span><br><span class="line">	return pq-&gt;tail-&gt;data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这里用两个队列实现，所以我们创建一个结构体：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef struct MYStack</span><br><span class="line">&#123;</span><br><span class="line">	Queue q1;</span><br><span class="line">	Queue q2;</span><br><span class="line">&#125;MyStack;</span><br></pre></td></tr></table></figure>



<p>结构体里面两个指针，每一个指针对应一个队列，然后我们正式开始写这道题</p>
<hr>
<h2 id="先从初始化函数开始："><a href="#先从初始化函数开始：" class="headerlink" title="先从初始化函数开始："></a>先从初始化函数开始：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MyStack* myStackCreate() &#123;</span><br><span class="line">	</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>单看函数，它需要返回一致MyStack* 的指针，既然要返回指针，那我们就得用malloc开辟空间，然后开辟好的MyStack结构体其中的Queue结构体，我们可以直接调用其初始化函数，然后即可。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MyStack* myStackCreate() &#123;</span><br><span class="line">    //开辟空间</span><br><span class="line">	MYStack* obj = (MYStack*)malloc(sizeof(MyStack));</span><br><span class="line">    //最好判断一下开辟成功没    </span><br><span class="line">    assert(obj);</span><br><span class="line"></span><br><span class="line">    //初始化obj结构体里面的Queue结构体</span><br><span class="line">    QueueInit(&amp;obj-&gt;q1);</span><br><span class="line">    QueueInit(&amp;obj-&gt;q2);</span><br><span class="line">    </span><br><span class="line">    //返回初始化好的结构体</span><br><span class="line">    return obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="将元素X压入栈顶："><a href="#将元素X压入栈顶：" class="headerlink" title="将元素X压入栈顶："></a>将元素X压入栈顶：</h2><p>先说说思路：</p>
<p>我们现在有两个队列，两个队列分别有头尾指针，假设现在要将 1 2压入栈，那我们找其中一个不是空的队列压进去：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200922769.png"></p>
<p>这样方便我们之后的一系列操作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void myStackPush(MyStack* obj, int x) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line"></span><br><span class="line">    //判断obj-&gt;q1是否为空，不为空压q1</span><br><span class="line">    if(!QueueEmpty(&amp;obj-&gt;q1))</span><br><span class="line">    &#123;</span><br><span class="line">        QueuePush(&amp;obj-&gt;q1,x);</span><br><span class="line">    &#125;</span><br><span class="line">    //其他情况压q2</span><br><span class="line">    else</span><br><span class="line">    &#123;</span><br><span class="line">        QueuePush(&amp;obj-&gt;q2,x);</span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="移除并返回栈顶元素："><a href="#移除并返回栈顶元素：" class="headerlink" title="移除并返回栈顶元素："></a>移除并返回栈顶元素：</h2><p>还是先看思路，我们要移除并且返回栈顶的元素，因为栈是先后后出，所以我们刚刚压进来的 1 2出的时候就应该是 2 1。</p>
<p>按照之前玩单链表的思路，我们就应该是先找到最后一个节点，保存里面的值，然后释放掉最后节点，但是这题要用到第二个队列，所以这样做就有点跑题，这里我们换一种办法。</p>
<p>已知 q1 里面有 1 2，我们要返回 2</p>
<p>我们拿q2来接收q1里面的值，直到q1 里面只剩一个值位置，然后我们保存这个值再将其释放掉，下一次压栈压的就是q2的位置，如果还要移除的话就把q2的值移到q1里面。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200922605.png"></p>
<p>将q1的1拿过来：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200922041.png"></p>
<p>然后返回q1里面的值，再将其置为空：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200922114.png"></p>
<p>假设现在要尾插 3 4 5：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200922722.png"></p>
<p>然后再出来就把1 3 4 移到q1里面：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200922642.png"></p>
<p>重复即可，然后我们就来实现代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int myStackPop(MyStack* obj) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line">    Queue* empty = &amp;obj-&gt;q1;</span><br><span class="line">    Queue* noempty = &amp;obj-&gt;q2;</span><br><span class="line"></span><br><span class="line">    if(!QueueEmpty(&amp;obj-&gt;q1))</span><br><span class="line">    &#123;</span><br><span class="line">        noempty = &amp;obj-&gt;q1;</span><br><span class="line">        empty = &amp;obj-&gt;q2;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    while(QueueSize(noempty) &gt; 1)</span><br><span class="line">    &#123;</span><br><span class="line">        QueuePush(empty,QueueFront(noempty));</span><br><span class="line">        QueuePop(noempty);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    int data = QueueFront(noempty);</span><br><span class="line">    QueuePop(noempty);</span><br><span class="line">    return data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>一行一行看：</p>
<p>先判断obj不为空指针，然后假设空的那个队列是q1，非空的是q2，然后判断，如果q1是非空的，那就让非空的指向q1，空的指向q2</p>
<p>如果非空链表里面留下的节点数量大于1，说明我们还要移动</p>
<p>我们在空链表的后面尾插，尾插的是非空链表的元素</p>
<p>比方说刚刚的 1 2</p>
<p>那我们就把1移过去</p>
<p>每次移完记得删掉原本节点，不然程序会死循环</p>
<p>当循环结束，就意味着非空队列里面只有一个元素了，我们获取到这个元素之后再将这个节点也删掉，返回我们获得的元素。</p>
<h2 id="返回栈顶元素："><a href="#返回栈顶元素：" class="headerlink" title="返回栈顶元素："></a>返回栈顶元素：</h2><p>返回栈顶元素，也就是返回我们最后压进去的元素，正因为我们每次压栈都是在非空队列尾插，所以我们可以直接返回非空节点的最后一个元素即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int myStackTop(MyStack* obj) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line">    if(!QueueEmpty(&amp;obj-&gt;q1))</span><br><span class="line">    &#123;</span><br><span class="line">        return QueueBack(&amp;obj-&gt;q1);</span><br><span class="line">    &#125;</span><br><span class="line">    return QueueBack(&amp;obj-&gt;q2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="判断栈是否为空："><a href="#判断栈是否为空：" class="headerlink" title="判断栈是否为空："></a>判断栈是否为空：</h2><p>我们只需要判断两个队列即可，调用一下我们之前写的判断队列的函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ibool myStackEmpty(MyStack* obj) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line">    return QueueEmpty(&amp;obj-&gt;q1)&amp;&amp; QueueEmpty(&amp;obj-&gt;q2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="销毁栈："><a href="#销毁栈：" class="headerlink" title="销毁栈："></a>销毁栈：</h2><p>这个更是简单，我们还是直接调用销毁队列的两个函数，最后释放掉我们的obj即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void myStackFree(MyStack* obj) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line">    QueueDestory(&amp;obj-&gt;q1);</span><br><span class="line">    QueueDestory(&amp;obj-&gt;q2);</span><br><span class="line">    free(obj);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这里放上全部代码，有需要可以复制：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef int QDataType;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">typedef struct QNode</span><br><span class="line">&#123;</span><br><span class="line">	QDataType data;</span><br><span class="line">	struct queue* next;</span><br><span class="line">&#125;QNode;</span><br><span class="line"></span><br><span class="line">typedef struct Queue</span><br><span class="line">&#123;</span><br><span class="line">	QNode* head;</span><br><span class="line">	QNode* tail;</span><br><span class="line">&#125;Queue;</span><br><span class="line"></span><br><span class="line">typedef struct MYStack</span><br><span class="line">&#123;</span><br><span class="line">	Queue q1;</span><br><span class="line">	Queue q2;</span><br><span class="line">&#125;MyStack;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void QueueInit(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	pq-&gt;head = pq-&gt;tail = NULL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void QueueDestory(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	QNode* cur = pq-&gt;head;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		QNode* next = cur-&gt;next;</span><br><span class="line">		free(cur);</span><br><span class="line">		cur = next;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	pq-&gt;head = pq-&gt;tail = NULL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void QueuePush(Queue* pq, QDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	QNode* newnode = (QNode*)malloc(sizeof(QNode));</span><br><span class="line">	assert(newnode);</span><br><span class="line"></span><br><span class="line">	newnode-&gt;data = x;</span><br><span class="line">	newnode-&gt;next = NULL;</span><br><span class="line"></span><br><span class="line">	if (pq-&gt;tail == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		assert(pq-&gt;head == NULL);</span><br><span class="line">		pq-&gt;head = pq-&gt;tail = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		pq-&gt;tail-&gt;next = newnode;</span><br><span class="line">		pq-&gt;tail = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void QueuePop(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	assert(pq-&gt;head &amp;&amp; pq-&gt;tail);</span><br><span class="line"></span><br><span class="line">	if (pq-&gt;head-&gt;next == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		free(pq-&gt;head);</span><br><span class="line">		pq-&gt;head = pq-&gt;tail = NULL;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		QNode* next = pq-&gt;head-&gt;next;</span><br><span class="line">		free(pq-&gt;head);</span><br><span class="line">		pq-&gt;head = next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bool QueueEmpty(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line"></span><br><span class="line">	return q-&gt;head == NULL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">size_t QueueSize(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	QNode* cur = pq-&gt;head;</span><br><span class="line">	size_t size = 0;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		size++;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	return size;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">QDataType QueueFront(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	assert(pq-&gt;head);</span><br><span class="line"></span><br><span class="line">	return pq-&gt;head-&gt;data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">QDataType QueueBack(Queue* pq)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pq);</span><br><span class="line">	assert(pq-&gt;tail);</span><br><span class="line"></span><br><span class="line">	return pq-&gt;tail-&gt;data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">MyStack* myStackCreate() &#123;</span><br><span class="line">	MyStack* obj = (MyStack*)malloc(sizeof(MyStack));</span><br><span class="line">    assert(obj);</span><br><span class="line"></span><br><span class="line">    QueueInit(&amp;obj-&gt;q1);</span><br><span class="line">    QueueInit(&amp;obj-&gt;q2);</span><br><span class="line"></span><br><span class="line">    return obj;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void myStackPush(MyStack* obj, int x) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line"></span><br><span class="line">    if(!QueueEmpty(&amp;obj-&gt;q1))</span><br><span class="line">    &#123;</span><br><span class="line">        QueuePush(&amp;obj-&gt;q1,x);</span><br><span class="line">    &#125;</span><br><span class="line">    else</span><br><span class="line">    &#123;</span><br><span class="line">        QueuePush(&amp;obj-&gt;q2,x);</span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int myStackPop(MyStack* obj) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line">    Queue* empty = &amp;obj-&gt;q1;</span><br><span class="line">    Queue* noempty = &amp;obj-&gt;q2;</span><br><span class="line"></span><br><span class="line">    if(!QueueEmpty(&amp;obj-&gt;q1))</span><br><span class="line">    &#123;</span><br><span class="line">        noempty = &amp;obj-&gt;q1;</span><br><span class="line">        empty = &amp;obj-&gt;q2;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    while(QueueSize(noempty) &gt; 1)</span><br><span class="line">    &#123;</span><br><span class="line">        QueuePush(empty,QueueFront(noempty));</span><br><span class="line">        QueuePop(noempty);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    int data = QueueFront(noempty);</span><br><span class="line">    QueuePop(noempty);</span><br><span class="line">    return data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int myStackTop(MyStack* obj) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line">    if(!QueueEmpty(&amp;obj-&gt;q1))</span><br><span class="line">    &#123;</span><br><span class="line">        return QueueBack(&amp;obj-&gt;q1);</span><br><span class="line">    &#125;</span><br><span class="line">    return QueueBack(&amp;obj-&gt;q2);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bool myStackEmpty(MyStack* obj) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line">    return QueueEmpty(&amp;obj-&gt;q1)&amp;&amp; QueueEmpty(&amp;obj-&gt;q2);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void myStackFree(MyStack* obj) &#123;</span><br><span class="line">    assert(obj);</span><br><span class="line">    QueueDestory(&amp;obj-&gt;q1);</span><br><span class="line">    QueueDestory(&amp;obj-&gt;q2);</span><br><span class="line">    free(obj);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="用栈实现队列"><a href="#用栈实现队列" class="headerlink" title="用栈实现队列"></a>用栈实现队列</h1><p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200924631.png"></p>
<p><a class="link"   href="https://leetcode-cn.com/problems/implement-queue-using-stacks/"  title="232. 用栈实现队列 - 力扣（LeetCode） (leetcode-cn.com)">232. 用栈实现队列 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>在答题之前，我们先把之前写的栈函数都拿过来用，我看C++代码几十行写完我写了一大堆，泪目</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef int STDataType;</span><br><span class="line"> </span><br><span class="line">typedef struct Stack</span><br><span class="line">&#123;</span><br><span class="line">	STDataType* arr;</span><br><span class="line">	int top;</span><br><span class="line">	int capacity;</span><br><span class="line">&#125;Stack;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void StackInit(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	ps-&gt;arr = NULL;</span><br><span class="line">	ps-&gt;capacity = 0;</span><br><span class="line">	ps-&gt;top = 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void StackPush(Stack* ps, STDataType data)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	if (ps-&gt;top == ps-&gt;capacity)</span><br><span class="line">	&#123;</span><br><span class="line">		int newcapacity = ps-&gt;capacity == 0 ? 4 : (ps-&gt;capacity) * 2;</span><br><span class="line">		ps-&gt;arr = (Stack*)realloc(ps-&gt;arr, sizeof(Stack)* newcapacity);</span><br><span class="line">		if (ps-&gt;arr == NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			printf(&quot;realloc fail\n&quot;);</span><br><span class="line">			exit(-1);</span><br><span class="line">		&#125;</span><br><span class="line">		ps-&gt;capacity = newcapacity;</span><br><span class="line">	&#125;</span><br><span class="line">	ps-&gt;arr[ps-&gt;top++] = data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void StackPop(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	assert(ps-&gt;top &gt; 0);</span><br><span class="line">	ps-&gt;top--;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">STDataType StackTop(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	return ps-&gt;arr[ps-&gt;top-1];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int StackSize(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	return ps-&gt;top;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int StackEmpty(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	return ps-&gt;top == 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void StackDestroy(Stack* ps)</span><br><span class="line">&#123;</span><br><span class="line">	assert(ps);</span><br><span class="line">	free(ps-&gt;arr);</span><br><span class="line">	ps-&gt;arr = NULL;</span><br><span class="line">	ps-&gt;top = 0;</span><br><span class="line">	ps-&gt;capacity = 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>正式开始答题；</p>
<h2 id="初始化并返回创建好的指针"><a href="#初始化并返回创建好的指针" class="headerlink" title="初始化并返回创建好的指针"></a>初始化并返回创建好的指针</h2><p>我们先创建好结构体，结构体里面有两个栈：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef struct &#123;</span><br><span class="line">    Stack s1;</span><br><span class="line">    Stack s2;</span><br><span class="line">&#125; MyQueue;</span><br></pre></td></tr></table></figure>



<p>然后我们还是判断一下是否为空</p>
<p>初始化还是用我们的栈初始化函数即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MyQueue* myQueueCreate()</span><br><span class="line">&#123;</span><br><span class="line">	MyQueue* obj = (MyQueue*)malloc(sizeof(MyQueue));</span><br><span class="line">	assert(obj);</span><br><span class="line">    </span><br><span class="line">	StackInit(&amp;obj-&gt;s1);</span><br><span class="line">	StackInit(&amp;obj-&gt;s2);</span><br><span class="line"></span><br><span class="line">	return obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="将元素X推入队列的末尾"><a href="#将元素X推入队列的末尾" class="headerlink" title="将元素X推入队列的末尾"></a>将元素X推入队列的末尾</h2><p>我们知道栈是先进后出，而队列是先进先出，假设现在有 1 2 3 4 四个元素要进队列，我们将其全部尾插到 s1 里面：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void myQueuePush(MyQueue* obj, int x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(obj);</span><br><span class="line">	StackPush(&amp;obj-&gt;s1, x);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="返回队列开头元素并将其移除"><a href="#返回队列开头元素并将其移除" class="headerlink" title="返回队列开头元素并将其移除"></a>返回队列开头元素并将其移除</h2><p>我们知道现在全部元素都在s1里面，我们最好判断一下：</p>
<p>第一种情况：</p>
<p>s1有元素，s2为空，我们将s1的元素压到s2里面，因为是栈，所以每次操作都是从尾部（图里从右向左）</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200924102.png"></p>
<p>压s2就变成了：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200924487.png"></p>
<p>我们最后再把s1里面的删掉，返回s2最后的值即可；</p>
<p>第二种情况：</p>
<p>如果s2里面已经有元素，那我们直接返回即可；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int myQueuePop(MyQueue* obj)</span><br><span class="line">&#123;</span><br><span class="line">	assert(obj);</span><br><span class="line"></span><br><span class="line">	if (StackEmpty(&amp;obj-&gt;s2))</span><br><span class="line">	&#123;</span><br><span class="line">		while (!StackEmpty(&amp;obj-&gt;s1))</span><br><span class="line">		&#123;</span><br><span class="line">			StackPush(&amp;obj-&gt;s2, StackTop(&amp;obj-&gt;s1));</span><br><span class="line">			StackPop(&amp;obj-&gt;s1);</span><br><span class="line">		&#125;</span><br><span class="line">		int data = StackTop(&amp;obj-&gt;s2);</span><br><span class="line">		StackPop(&amp;obj-&gt;s2);</span><br><span class="line">		return data;</span><br><span class="line">	&#125;</span><br><span class="line">	int data = StackTop(&amp;obj-&gt;s2);</span><br><span class="line">	StackPop(&amp;obj-&gt;s2);</span><br><span class="line">	return data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="返回队列开头的元素；"><a href="#返回队列开头的元素；" class="headerlink" title="返回队列开头的元素；"></a>返回队列开头的元素；</h2><p>同样，如果s2里面有元素，我们就返回s2尾部，但如果s2为空。那就返回s1，这里要注意：</p>
<p>s2的尾部是s1的头，如果s2为空，那我们返回s1的位置就应该是s1数组下标为0的位置</p>
<p>比方说：压进去 1  2   3  4</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200924922.png"></p>
<p>上面我们的操作是把s1的值压到s2里面，根据图示，1的位置是s2的尾部和s1的头，一定注意 </p>
<h2 id="看栈是否为空；"><a href="#看栈是否为空；" class="headerlink" title="看栈是否为空；"></a>看栈是否为空；</h2><p>判断obj里面的s1 和 s2 即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool myQueueEmpty(MyQueue* obj)</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	return StackEmpty(&amp;obj-&gt;s1) &amp;&amp; StackEmpty(&amp;obj-&gt;s2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="释放栈："><a href="#释放栈：" class="headerlink" title="释放栈："></a>释放栈：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void myQueueFree(MyQueue* obj)</span><br><span class="line">&#123;</span><br><span class="line">	assert(obj);</span><br><span class="line">	StackDestroy(&amp;obj-&gt;s1);</span><br><span class="line">	StackDestroy(&amp;obj-&gt;s2);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>模板</title>
    <url>/2022/05/31/%E6%A8%A1%E6%9D%BF/</url>
    <content><![CDATA[<hr>
<h1 id="模板"><a href="#模板" class="headerlink" title="模板"></a>模板</h1><p>在讲模板之前，我们先想在写程序的时候，有没有碰见这样的常见：</p>
<p>假设现在有一个交换函数我们用的很频繁，在C里面它是这么写的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201106718.png"></p>
<p>假设这里要传过去的是double或者是其他类型，就不太适合用这个交换函数了，所以在C++里面出现了一个概念叫：</p>
<p>函数重载，函数重载就可以解决这个问题，但是函数重载仅仅只是类型不同，但实际上代码里面几乎是一样的，而且每当使用不同类型的时候就需要手动添加重载函数。</p>
<p>反正就，不好用。</p>
<p>有没有这么一种可能，就是，既然里面内容都是差不多的，只有类型不一样，我们能不能写一个模板出来，然后编译去自己去看这是个什么类型然后将这个类型放到模板里面去？</p>
<p>当然有，这里就引出标题：模板。</p>
<hr>
<h2 id="函数模板"><a href="#函数模板" class="headerlink" title="函数模板"></a>函数模板</h2><p>先看看概念：</p>
<blockquote>
<p>函数模板代表了一个函数家族，该函数模板与类型无关，在使用时被参数化，根据实参类型产生函数的特定类型版本。 </p>
</blockquote>
<p>用通俗易懂的话来说，就是你传过来什么类型它就是什么类型，不再需要自己写。</p>
<p>这里直接看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;typename T&gt;</span><br><span class="line">void Swap(T&amp; a, T&amp; b)</span><br><span class="line">&#123;</span><br><span class="line">	T c = a;</span><br><span class="line">	a = b;</span><br><span class="line">	b = c;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201107714.png"></p>
<p>首先，要声明，这是一个模板：</p>
<p>template<typename T></p>
<p>这里的字母不是一定要用T，其他的字母都可以，然后在实现函数的时候需用用到这个类型直接拿T代替。</p>
<p>这里还可以用class代替：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201107106.png"></p>
<p>那这个模板是怎么实现的呢？</p>
<h3 id="函数模板的原理"><a href="#函数模板的原理" class="headerlink" title="函数模板的原理"></a>函数模板的原理</h3><p>函数模板是一个框架，是一个模板，函数里面大体思路是我们实现的，但其中还有一部分待完成的就可以交给编译器，如果缺什么就由编译器帮我们补全。</p>
<p>具体思路如下表：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201107915.png"></p>
<p>在 编译阶段 ，对于模板函数的使用，编译器会根据传入的实参类型来生成对应类型的函数随后调用。</p>
<hr>
<h3 id="函数模板的实例化"><a href="#函数模板的实例化" class="headerlink" title="函数模板的实例化"></a>函数模板的实例化</h3><p>用不同类似西的参数使用函数模板的时候，称之为函数模板的实例化，实例化又分为：隐式实例化和显式实例化。</p>
<p>这两种也很好区分。</p>
<p>如果我们传过去的时候没有指明类型，让编译器推出我们的类型，这就是隐式实例化，如下代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T&gt;</span><br><span class="line">T Add(const T&amp; left, const T&amp; right) </span><br><span class="line">&#123;</span><br><span class="line">	return left + right;</span><br><span class="line">&#125;</span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	int a1 = 10, a2 = 20;</span><br><span class="line">	double d1 = 10.0, d2 = 20.0;</span><br><span class="line">	Add(a1, a2);</span><br><span class="line">	Add(d1, d2);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这里肯定是没问题的，编译器会自动生成int类型和double类型的加法函数，但如果这个时候我们要让一个int和一个double相加呢？</p>
<p>就会发生如下情况：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201107089.png"></p>
<p>因为上面指定的只有一个T，而这里传过去的两个参数类型是不一样的，所以会报错，这个时候就有两种解决办法：</p>
<p>第一种，我们在传过去之前就将其中一个类型强转为另一个类型：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Add(a1, (int)d1);</span><br></pre></td></tr></table></figure>



<p>第二种，显式实例化，在函数名后面加上&lt;类型&gt;</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Add&lt;int&gt;(a1, d1);</span><br></pre></td></tr></table></figure>



<p>这样传过去的话会把d1也认为是int类型，虽然double转到int可能会有数据丢失，但这就能跑过去。</p>
<hr>
<h3 id="声明和定义分离"><a href="#声明和定义分离" class="headerlink" title="声明和定义分离"></a>声明和定义分离</h3><p>我们知道函数是可以声明在头文件里面，定义在.c文件的，这种声明和定义的分离模板可以做到吗？</p>
<p>先看在.cpp文件里面声明定义分离，它的格式跟函数有点不一样：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T &gt;</span><br><span class="line">T Add(const T&amp; left, const T&amp; right);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	int a1 = 10,a2 = 20;</span><br><span class="line">	Add(a1, a2);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">template&lt;class T&gt;</span><br><span class="line">T Add(const T&amp; left, const T&amp; right)</span><br><span class="line">&#123;</span><br><span class="line">	return left + right;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>它得这么些，在定义函数前面还得再次声明template<class T>表示这是个模板，之后实现跟函数一样，算是比较麻烦的。</p>
<p>那将它声明放到.h文件里面会怎么样？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201107992.png"></p>
<p>这里就将声明放到了.h里面，在.c里面实现，随后在test.c里面调用，运行程序，会发现以下错误：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201107845.png"></p>
<p>没有找到，这就是出现了链接错误，这里要记住：</p>
<blockquote>
<p>模板的声明和定义不支持分离</p>
</blockquote>
<p>为什么会出现这个问题？</p>
<p>可以这么理解，模板本身只是一个蓝图，它跟函数相比会多执行一步：将T变为对应类型，但就是这一步出现错误，因为编译的时候调用了模板，但是这个时候类型还没有确定，这只是一个空的模板。</p>
<p>如何解决？</p>
<p>给模板实例化就好了</p>
<p>具体实现跟显示实例化差不多：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template</span><br><span class="line">int Add&lt;int&gt;(const int&amp; left, const int&amp; right);</span><br></pre></td></tr></table></figure>



<p>这就可以解决报错的问题。</p>
<p>但假如我们再用double类型调用呢？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	double a1 = 10,a2 = 20;</span><br><span class="line">	Add(a1, a2);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201107799.png"></p>
<p>它又报错了，所以这种办法只能解决一个类型，如果要用double就要再次实例化一个double。</p>
<p>不好用。</p>
<p>有的程序里面会将.h文件写成.hpp，这就是将模板的定义也写到头文件里面。</p>
<p>–</p>
<h3 id="匹配原则"><a href="#匹配原则" class="headerlink" title="匹配原则"></a>匹配原则</h3><p>有没有这样一种可能，就是已经写了一个int的加法函数之后，又写了一个加法的模板，那这个时候如果我们调用这个加法，编译器会调用哪个？</p>
<p>假设我们现在的代码是这样的；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int Add(int a, int b)</span><br><span class="line">&#123;</span><br><span class="line">	return a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">template&lt;class T&gt;</span><br><span class="line">T Add(const T&amp; left, const T&amp; right) </span><br><span class="line">&#123;</span><br><span class="line">	return left + right;</span><br><span class="line">&#125;</span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	int a1 = 10, a2 = 20;</span><br><span class="line">	Add(a1, a2);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这个时候我们调试代码：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201107610.png"></p>
<p>可以看到编译器是跳转到了int类型的哪里</p>
<p>这里记住结论就好了：</p>
<blockquote>
<p>1、一个非模板函数可以和一个同名的函数模板同时存在，而且该函数模板还可以被实例化为这个非模板函数 </p>
<ol start="2">
<li>对于非模板函数和同名函数模板，如果其他条件都相同，在调动时会优先调用非模板函数而不会从该模板产生出一个实例。如果模板可以产生一个具有更好匹配的函数， 那么将选择模板</li>
</ol>
</blockquote>
<p>一个模板只能有一个类型、</p>
<p>不，可以有很多个，类似以下代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T ,class X&gt;</span><br><span class="line">T Add(const T&amp; left, const X&amp; right) </span><br><span class="line">&#123;</span><br><span class="line">	return left + right;</span><br><span class="line">&#125;</span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	int a1 = 10;</span><br><span class="line">	double a2 = 20.0;</span><br><span class="line">	Add(a1, a2);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这就是：</p>
<h2 id="类模板"><a href="#类模板" class="headerlink" title="类模板"></a>类模板</h2><h3 id="类模板的定义格式"><a href="#类模板的定义格式" class="headerlink" title="类模板的定义格式"></a>类模板的定义格式</h3><p>跟类很想，只有在细节里面不一样，这里先看一个大体模板：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T1, class T2, ..., class Tn&gt;</span><br><span class="line">class 类模板名</span><br><span class="line">&#123;</span><br><span class="line">// 类内成员定义</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>如果类型很多，class后面可以跟很多个T（type），中间用逗号分隔。</p>
<p>传来的类型用T1代替就好了，其他的具体细节还是没变</p>
<hr>
<p>后期有更新</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>类和对象</title>
    <url>/2022/05/23/%E7%B1%BB%E5%92%8C%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<hr>
<h2 id="面向对象和面向过程"><a href="#面向对象和面向过程" class="headerlink" title="面向对象和面向过程"></a>面向对象和面向过程</h2><p>举个例子：</p>
<p>淘宝这个软件，我要买一个东西，就应该有以下几步：</p>
<p>上架商品-》下单-》送快递-》收快递-》评价</p>
<p>C语言关注的就是这个过程</p>
<p>而C++关注的更多是对象：</p>
<p>平台-》快递-》用户</p>
<p>这就是面向对象，将上面繁琐的过程总结为三个对象</p>
<p>这样再来看解释是不是会更好理解：</p>
<blockquote>
<p>C语言是面向过程的，关注的是过程，分析出求解问题的步骤，通过函数调用逐步解决问题。</p>
<p>C++是基于面向对象的，关注的是对象，将一件事情拆分成不同的对象，靠对象之间的交互完成。</p>
</blockquote>
<h2 id="类的引用"><a href="#类的引用" class="headerlink" title="类的引用"></a>类的引用</h2><p>既然C++是靠对象交互实现的功能，那类，就是必须要掌握的知识，在C语言里面，结构体一般是这样的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201054716.png"></p>
<p>里面定义的都是变量，但是在C++里面就不是这样的，C++不仅可以定义变量还可以定义函数，就好像下面：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201054988.png"></p>
<p>这个结构体不仅有对象，还有函数，那它的使用就是这样：<img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201054550.png"></p>
<p>先定义一个对象，随后调用这个对象函数。</p>
<blockquote>
<p>这个结构体也可以用class来定义，区别下面会提到。</p>
</blockquote>
<h2 id="类的定义"><a href="#类的定义" class="headerlink" title="类的定义"></a>类的定义</h2><p>类的定义跟结构体很像，它的格式是这样的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201054949.png"></p>
<blockquote>
<p>类中的元素称为类的成员：类中的数据称为类的属性或者成员变量; 类中的函数称为类的方法或者成员函数。</p>
</blockquote>
<p>类有两种定义，第一种是先在头文件里面声明，再在.c文件里面实现，另一种是全部写上，这种全部写在类里面的函数会被当成内联函数，要注意 </p>
<p>第一种：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201054501.png"></p>
<p>在头文件里面声明show函数</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201054338.png"></p>
<p>在.c里面定义函数，这里要注意，在函数名前面一定要加上：：表面它属于这个类，否则它会找不到参数。</p>
<p>下面就是第二种全部写上：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201054454.png"></p>
<p>那这里多出来一个public，这是什么意思？</p>
<h2 id="类的访问限定符及封装"><a href="#类的访问限定符及封装" class="headerlink" title="类的访问限定符及封装"></a>类的访问限定符及封装</h2><h3 id="访问限定符"><a href="#访问限定符" class="headerlink" title="访问限定符"></a>访问限定符</h3><p>这里要说一个概念就是访问限定符，当定义出来一个类之后，它会默认有三种权限：</p>
<p>public（公有）</p>
<p>protecte（保护）</p>
<p>private（隐私）</p>
<hr>
<p>公有的意思就是在类的外面也可以使用，保护和隐私相反。</p>
<p>访问限定符它会持续到下一个访问限定符的出现或者是类结束。</p>
<p>class默认是protected，而struct则是public。</p>
<p>这些了解一下就可以了，不用想太深。</p>
<p>这里还有个问题：</p>
<blockquote>
<p>C++中struct和class的区别是什么？</p>
</blockquote>
<p>因为C++兼容C，所以struct在C++中同样可以创建类，class也可以，而struct的默认权限是public，而class则是protected。</p>
<hr>
<h3 id="封装"><a href="#封装" class="headerlink" title="封装"></a>封装</h3><p>面向对象的三大特性：封装、继承、多态</p>
<p>但是在这个阶段，先认识一下封装，封装的本质，是将一些对象的实现方式和参数隐藏，只对外面实现接口调用。</p>
<p>在创建类的时候那个class就包含这一点，因为class创建的类它里面成员的默认属性是私有，因为是私有所以在类外面是访问不到的，而struct则是公开。</p>
<h2 id="类的作用域"><a href="#类的作用域" class="headerlink" title="类的作用域"></a>类的作用域</h2><p>当写C++的时候，总会看到这么一句话：</p>
<p>假设是这样，我在一个头文件里面新建了一个类，这个类里面有一个函数，这个函数代码很长，可能有十几行，那我就可以这么写：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201054473.png"></p>
<p>我在头文件里面声明，但是在.c里面实现，不过要在实现的函数名前面带上作用域，表示这个函数实现的是这个对象里面的</p>
<h2 id="类的实例化、计算大小、存储方式"><a href="#类的实例化、计算大小、存储方式" class="headerlink" title="类的实例化、计算大小、存储方式"></a>类的实例化、计算大小、存储方式</h2><p>在说之前，先看一下正式的解释：</p>
<blockquote>
<ol>
<li>类只是一个模型一样的东西，限定了类有哪些成员，定义出一个类并没有分配实际的内存空间来存储它</li>
<li>一个类可以实例化出多个对象，实例化出的对象 占用实际的物理空间，存储类成员变量 </li>
<li>做个比方。类实例化出对象就像现实中使用建筑设计图建造出房子，类就像是设计图，只设计出需要什么东西，但是并没有实体的建筑存在，同样类也只是一个设计，实例化出的对象才能实际存储数据，占用物理空间</li>
</ol>
</blockquote>
<p>举个例子，我这里创建了一个日期类，里面有这样的几个参数：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055862.png"></p>
<p>那这里提一个问，这三个对象有没有占用空间？</p>
<p>没有，因为它只是声明有这样的三个参数，还没有真的给初始化出来，所以不占用内存空间。</p>
<p>但如果我们要计算一个类要占用的内存空间，我们该如何计算？</p>
<p>就好像在C语言里面的，算一个结构体的大小一样，别忘了内存对齐，这里就是</p>
<p>4*3 &#x3D; 12个字节。</p>
<p>类里面的函数占不占空间？</p>
<p>不占，举这样的一个例子，我这里创建了两个对象，一个d1，一个d2：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055548.png"></p>
<p>它们肯定是两个不同的对象，里面的参数也不一样，但是它们调用的函数，是不是这个类里面的同一个函数？</p>
<p>是的，所以为了优化，类里面的函数都会放到单独的一块空间，这块空间是公用的，所以计算的时候不算这一块。</p>
<p>假设这个类里面什么都没有，占用多少个字节？ </p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055324.png"></p>
<p>一个字节，虽然这个类里面什么都没有 ，但编译器在运行的过程中还是会象征的给它一个字节表示这是一个类。</p>
<h2 id="this指针"><a href="#this指针" class="headerlink" title="this指针"></a>this指针</h2><p>这个，其实不是很好讲，这里我用个例子来说：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055788.png"></p>
<p>首先，我定义了一个对象，这个对象很简单，一个构造函数一个打印函数，随后，我在主函数里面初始化了a并将其赋值为2，随后我调用打印函数，在屏幕上打印了一个2.</p>
<p>为什么这里编译器会打印a的2，而不是一些随机值？</p>
<p>这里就要说到this指针，当写出如上代码的时候，看上其PrintA里面什么参数也没有，实际上，它隐藏的传了一个指针过去，这个指针称为this，*this就是a的地址，所以当我们看到上面的代码的时候，实际上它是这样的：</p>
<p>这样它在找的时候就能知道该打印哪个，我们还可以通过别的方法看到this指针：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055053.png"></p>
<p>可以看到，我在类里面打印了this指针的地址，然后在外面打印了a的地址，发现它们两个的地址一样，可以说明确实是传了过去。</p>
<hr>
<h2 id="默认成员函数"><a href="#默认成员函数" class="headerlink" title="默认成员函数"></a>默认成员函数</h2><blockquote>
<p>如果一个类中什么成员都没有，简称为空类。空类中什么都没有吗？并不是的，任何一个类在我们不写的情况下，都会自动生成下面6个默认成员函数。</p>
</blockquote>
<p>以我要写的一个对象为例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Data</span><br><span class="line">&#123;</span><br><span class="line">&#125;；</span><br></pre></td></tr></table></figure>



<p>这个里面看起来是空的，但实际上，它里面会包含以下几个成员函数：</p>
<p>负责初始化和清理的：构造函数、析构函数</p>
<p>负责拷贝赋值的：拷贝构造、赋值重载</p>
<p>负责取地址重载的：普通对象和const对象取地址，但这两个很少用，这里就不考虑</p>
<p>一个一个来，从构造函数开始：</p>
<h3 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h3><p>假设我现在写一个简单的对象，日期类：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;iostream&gt;</span><br><span class="line">using namespace std;</span><br><span class="line"></span><br><span class="line">class Date</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	void InitDate(int year, int month, int day)</span><br><span class="line">	&#123;</span><br><span class="line">		_year = year;</span><br><span class="line">		_month = month;</span><br><span class="line">		_day = day;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	void Display()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; _year &lt;&lt; &quot;-&quot; &lt;&lt; _month &lt;&lt; &quot;-&quot; &lt;&lt; _day &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">private:</span><br><span class="line">	int _year;</span><br><span class="line">	int _month;</span><br><span class="line">	int _day;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>这是很简单的一个对象，里面有三个隐私变量的声明，而在变量之上有两个函数。</p>
<p>那根据上面的对象，我们定义对象应该是这样的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055664.png"></p>
<p>不太好用，对吧？</p>
<p>我们知道整形可以 int a &#x3D; 10；这样定义的时候就给赋值，那Date这样的类能不能也这样呢？</p>
<p>当然可以，这里就要用到构造函数，这里先看以下构造函数的定义：</p>
<blockquote>
<p>构造函数是一个特殊的成员函数，名字与类名相同,创建类类型对象时由编译器自动调用，保证每个数据成员都有 一个合适的初始值，并且在对象的生命周期内只调用一次。</p>
</blockquote>
<p>再看一下构造函数的特性：</p>
<blockquote>
<p>构造函数是特殊的成员函数，需要注意的是，构造函数的虽然名称叫构造，但是需要注意的是构造函数的主要任务并不是开空间创建对象，而是初始化对象。</p>
<p>其特征如下： </p>
<ol>
<li>函数名与类名相同。</li>
<li>无返回值。</li>
<li>对象实例化时编译器自动调用对应的构造函数。</li>
<li>构造函数可以重载。</li>
</ol>
</blockquote>
<p>看起来有点麻烦，但实际用起来还是比较简单的，首先，它的函数名跟类名一样，不需要返回值，类里面可以进行初始化的赋值。</p>
<p>这里就示范一个：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055566.png"></p>
<p>可以看到，这个跟函数有点像的就是构造，它有三个参数，分别对应我们类里面的三个参数，我们在定义类的时候编译器会自己调用这个构造：</p>
<p>这里再运行看看：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055128.png"></p>
<p>对象就可以这么写了，初始化的赋值也没问题。</p>
<p>除此之外，还有两种：</p>
<p>不给参数，无参的</p>
<p>编译器自己生成的</p>
<p>顺着来看看，先看无参的：</p>
<p>它就可以这么写：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055030.png"></p>
<p>不给参数，直接赋值，如果需要直接赋值的话，那还是不要用这种：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055818.png"></p>
<p>再来看看编译器自己生成的，如果我们不写：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055878.png"></p>
<p>直接就随机值了，这里要说的就是，编译器不会对内置类型处理，比方说int，double这种，它默认构造之后还是一个随机值。</p>
<p>还是别用，这里我们主要了解是是第一个，给参数的，但是我们可以这么写：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055403.png"></p>
<p>调用的时候不给参数，它就会在这里的缺省值。</p>
<p>注意一点，构造函数优先写全缺省的，别写了一个缺省之后再写一个不带参的，会报错</p>
<p>如果遇到了自定义类型，编译器会调用自定义类型的改造函数，有点类似于套娃，这里看下面的代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Time</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	Time()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;Time()&quot; &lt;&lt; endl;</span><br><span class="line">        _hour = 0;</span><br><span class="line">		_minute = 0;</span><br><span class="line">		_second = 0;</span><br><span class="line">	&#125;</span><br><span class="line">private:</span><br><span class="line">	int _hour;</span><br><span class="line">	int _minute;</span><br><span class="line">	int _second;</span><br><span class="line">&#125;;</span><br><span class="line">class Date</span><br><span class="line">&#123;</span><br><span class="line">private:</span><br><span class="line">	// 基本类型(内置类型)</span><br><span class="line">	int _year;</span><br><span class="line">	int _month;</span><br><span class="line">	int _day;</span><br><span class="line">	// 自定义类型</span><br><span class="line">	Time _t;</span><br><span class="line">&#125;;</span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	Date d;</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>Date里面有一个自定义类型Time，而当我们定义d这个对象的时候，编译器去找默认构造，发现Date里面的前三个是内置类型，所以它不会做处理，而_t则是自定义类型，那它就会在Time里面去找默认构造，看上面的代码是可以看到Time的默认构造是这个：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Time()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;Time()&quot; &lt;&lt; endl;</span><br><span class="line">        _hour = 0;</span><br><span class="line">		_minute = 0;</span><br><span class="line">		_second = 0;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>



<p>在默认构造里面，打印了一行话，然后我们运行：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055659.png"></p>
<p>它也确实调用了这个默认构造 。</p>
<hr>
<h3 id="析构函数"><a href="#析构函数" class="headerlink" title="析构函数"></a>析构函数</h3><p>这么说呢，这个其实理解起来跟构造函数是很像的，同样，我们先来看看它的盖帘和特性：</p>
<blockquote>
<p>概念 </p>
<p>与构造函数功能相反，析构函数不是完成对象的销毁，局部对象销毁工作是由编译器完成的。</p>
<p>而对象在销毁时会自动调用析构函数，完成类的一些资源清理工作。</p>
</blockquote>
<blockquote>
<p>特征 </p>
<p>其特征如下：</p>
<ol>
<li>析构函数名是在类名前加上字符 ~。</li>
<li>无参数无返回值。</li>
<li>一个类有且只有一个析构函数。若未显式定义，系统会自动生成默认的析构函数。</li>
<li>对象生命周期结束时，C++编译系统系统自动调用析构函数。</li>
</ol>
</blockquote>
<p>这个理解起来可以理解为是默认构造的翻版，它在这个对象生命周期到了的时候会自动调用，这里要注意的是它跟默认构造一样，它的析构同样不会对内置类型处理，而对自定义类型调用对应的析构函数。</p>
<p>如果，在这个对象里面有指针，那编译器也是不会处理的，所以如果写了指针，这个析构函数也是需要自己写的。</p>
<p>同时，如果一个类里面要自己写析构函数，那拷贝构造和拷贝赋值也是需要自己写的，原因之后再说，这里了解一下就好了。</p>
<hr>
<h3 id="拷贝构造函数"><a href="#拷贝构造函数" class="headerlink" title="拷贝构造函数"></a>拷贝构造函数</h3><p>当我们用构造函数的时候，有没有可能，我们有的时候需要拿一个对象初始化另一个对象，那我们就需要用到拷贝构造函数。</p>
<blockquote>
<p>构造函数：只有单个形参，该形参是对本类类型对象的引用(一般常用const修饰)，在用已存在的类类型对象创建新对象时由编译器自动调用。</p>
</blockquote>
<blockquote>
<p>拷贝构造函数也是特殊的成员函数，其特征如下：</p>
<ol>
<li>拷贝构造函数是构造函数的一个重载形式。</li>
<li>拷贝构造函数的参数只有一个且必须使用引用传参，使用传值方式会引发无穷递归调用。</li>
</ol>
</blockquote>
<p>这个说来也很简单，看看下面的例子：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201055851.png"></p>
<p>这看是一个很简单的拷贝构造，需要的参数是一个同类型的对象，同样不需要返回值，只需要很简答的把对方的值给自己就好了。</p>
<p>不过注意的是特征的第二条，至于为什么会无穷递归，是因为形参是实参的临时拷贝，拷贝就要用到拷贝构造，拷贝构造又传过去参数…</p>
<p>死循环，注意一下就好了。</p>
<p>拷贝同样有两种，第一种：</p>
<p>浅拷贝，一个字节一个字节的拷贝，对于一些简单的int或者是double，很简单，但是如果有指针类型，浅拷贝也会把这个地址拷过去，也就是说，这两个指针指向的是同一块空间，都学到这个地步了，这个后果我就不多说了。</p>
<p>深拷贝：为了解决指针的这种类似问题，就出现了深拷贝，但我们现在也用不到。</p>
<p>这里还有一点要注意：</p>
<p>如果没有写拷贝函数，那编译器自动生成的拷贝构造是浅拷贝。</p>
<h2 id="———–"><a href="#———–" class="headerlink" title="———–"></a>———–</h2><p>运算符的重载</p>
<p>我们知道，同一种类型的对象是可以赋值的，那类也不例外，但是我们总不可能直接写：</p>
<p>int a &#x3D; 10；</p>
<p>Data.a &#x3D; a；</p>
<p>首先，类的成员我们是私有的，其次这两类型都不一样，所以要写也是这么写：</p>
<p>Data a（xxx）；</p>
<p>Data b；</p>
<p>b &#x3D; a；</p>
<p>为了实现这样的操作（指直接用等于），我们就需要这个，运算符的重载：</p>
<p>我先写一个示范：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201056591.png"></p>
<p>operator就是关键，其后面跟着的&#x3D;号是你要实现的运算符，然后后面是需要的参数，单目操作符就不需要传参，双目就要一个参数…</p>
<p>其次，还要考虑的是有连续赋值的可能，所以返回值可以考虑用类型的引用。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201056592.png"></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201056568.png"></p>
<p>其他的类似实现的操作符在下面的日期类会依次实现。</p>
<hr>
<h2 id="const成员"><a href="#const成员" class="headerlink" title="const成员"></a>const成员</h2><p>这个说起来比较麻烦，所以先看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Date</span><br><span class="line">&#123; </span><br><span class="line">public :</span><br><span class="line"> void Display ()</span><br><span class="line"> &#123;</span><br><span class="line"> cout&lt;&lt;&quot;Display ()&quot; &lt;&lt;endl;</span><br><span class="line"> cout&lt;&lt;&quot;year:&quot; &lt;&lt;_year&lt;&lt; endl;</span><br><span class="line"> cout&lt;&lt;&quot;month:&quot; &lt;&lt;_month&lt;&lt; endl;</span><br><span class="line"> cout&lt;&lt;&quot;day:&quot; &lt;&lt;_day&lt;&lt; endl&lt;&lt;endl ;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">private :</span><br><span class="line"> int _year ; // 年</span><br><span class="line"> int _month ; // 月</span><br><span class="line"> int _day ; // 日</span><br><span class="line">&#125;;</span><br><span class="line">void Test ()</span><br><span class="line">&#123;</span><br><span class="line"> Date d1 ;</span><br><span class="line"> d1.Display ();</span><br><span class="line"> </span><br><span class="line"> const Date d2;</span><br><span class="line"> d2.Display ();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>d1调用d1.Display肯定是没有问题的，但是d2呢？</p>
<p>如果运行程序，就会出现这样的错误：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201056934.png"></p>
<p>因为d2是被const修饰的对象，但如果调用Dsiplay的话，函数隐藏的接受的对象类型是不会附带const的，这就设计到之前C++的基础知识。</p>
<p>const的对象只有const对象才能接收，如果是普通类型就会设计到一个权限放大的错误。</p>
<p>为了解决这个问题，所以C++就会有这样的规定：</p>
<p>在函数的括号后面加一个const：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201056867.png"></p>
<p>这样不管是普通对象类型还是被const修饰的都可以调用这个函数了。</p>
<hr>
<h2 id="取地址及const取地址操作符重载"><a href="#取地址及const取地址操作符重载" class="headerlink" title="取地址及const取地址操作符重载"></a>取地址及const取地址操作符重载</h2><p>这么说呢，这个操作也是类似的，这里简单写一下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Date</span><br><span class="line">&#123; </span><br><span class="line">public :</span><br><span class="line"> Date* operator&amp;()</span><br><span class="line"> &#123;</span><br><span class="line"> return this ;</span><br><span class="line"> &#125;</span><br><span class="line"> </span><br><span class="line"> const Date* operator&amp;()const</span><br><span class="line"> &#123;</span><br><span class="line"> return this ;</span><br><span class="line"> &#125;</span><br><span class="line">private :</span><br><span class="line"> int _year ; // 年</span><br><span class="line"> int _month ; // 月</span><br><span class="line"> int _day ; // 日</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>大概就是这样类似的操作…</p>
<hr>
<h2 id="日期类的实现"><a href="#日期类的实现" class="headerlink" title="日期类的实现"></a>日期类的实现</h2><p><a class="link"   href="https://blog.csdn.net/qq_60306111/article/details/124845089?spm=1001.2014.3001.5502"  title="【C++】日期类_ClaudineKnous的博客-CSDN博客">【C++】日期类_ClaudineKnous的博客-CSDN博客<i class="fas fa-external-link-alt"></i></a></p>
<p>看完了这个就可以去看看这个了，日期类的实现几乎概括了上面的所有知识。</p>
<hr>
<p>这是后期的更新，一些补充的知识：</p>
<h2 id="构造函数体的赋值"><a href="#构造函数体的赋值" class="headerlink" title="构造函数体的赋值"></a>构造函数体的赋值</h2><p>我们知道构造函数赋值的时候应该是这样：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Date</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line"> Date(int year, int month, int day)</span><br><span class="line"> &#123;</span><br><span class="line"> _year = year;</span><br><span class="line"> _month = month;</span><br><span class="line"> _day = day;</span><br><span class="line"> &#125;</span><br><span class="line"> </span><br><span class="line">private:</span><br><span class="line"> int _year;</span><br><span class="line"> int _month;</span><br><span class="line"> int _day;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>虽然调用之后_year _month _day里面都有了一个初始值，但这个操作却并不能称之为初始化，因为里面的式子是：_year &#x3D; year，&#x3D;我们都知道是赋值的意思，也就是说在进入花括号之前_year就已经初始化了。</p>
<p>初始化只能初始化一次。但却可以多次赋值</p>
<p>假设有一个变量，它是const修饰的，我们知道const修饰的词是不能在初始化之后再赋值的，所以它唯一的办法就是在初始化的时候赋值，但如果调用上面的构造函数，那const的值无法修改就会出现错误。</p>
<p>为了解决这种变量必须在初始的时候赋值的问题，这里就有一种办法，叫做：初始化列表</p>
<p>它是这么写的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Date</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line"> Date(int year, int month, int day)</span><br><span class="line"> : _year(year)</span><br><span class="line"> , _month(month)</span><br><span class="line"> , _day(day)</span><br><span class="line"> &#123;&#125;</span><br><span class="line"> </span><br><span class="line">private:</span><br><span class="line"> int _year;</span><br><span class="line"> int _month;</span><br><span class="line"> int _day;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>可以看到在构造函数的花括号之前就已经写了一串代码，这里的意思就是在初始化的时候将括号里面的值给到前面的对象，注意中间是逗号，最后也不要加分号，如果加上了就说明这行语句结束了，会报错。</p>
<blockquote>
<p>这里还有几点要注意：</p>
<ol>
<li>每个成员变量在初始化列表中只能出现一次(初始化只能初始化一次)</li>
<li>类中包含以下成员，必须放在初始化列表位置进行初始化：</li>
</ol>
<p>引用成员变量</p>
<p>const成员变量</p>
<p>自定义类型成员(该类没有默认构造函数)</p>
</blockquote>
<p>前面都很好理解，但最后一句：自定义类型成员（该类没有默认构造函数）怎么理解，这里看代码：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201057758.png"></p>
<p>可以看到这里A里面明明有一个构造函数，但却提醒说没有构造函数，这是一个在进入花括号里面之后_aobj是已经初始化完的状态，在里面的操作仅仅是赋值，所以这里在初始化会报错，正确办法应该是在进入花括号之前就把值给它初始化：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201057094.png"></p>
<p>总之，就是能用初始化队列的就用初始化队列，包括自定义类型。</p>
<p>看下面代码：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201057439.png"></p>
<p>可以看到对于_t它是完成了初始化的，并且它也执行了花括号里面的内容。</p>
<hr>
<p>成员变量在类中声明次序就是其在初始化列表中的初始化顺序，与其在初始化列表中的先后次序无关</p>
<p>看下面的代码，</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class A &#123;</span><br><span class="line">public:</span><br><span class="line">	A(int a)</span><br><span class="line">		:_a1(a)</span><br><span class="line">		, _a2(_a1)</span><br><span class="line">	&#123;&#125;</span><br><span class="line"></span><br><span class="line">	void Print() &#123;</span><br><span class="line">		cout &lt;&lt; _a1 &lt;&lt; &quot; &quot; &lt;&lt; _a2 &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">private:</span><br><span class="line">	int _a2;</span><br><span class="line">	int _a1;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	A a(1);</span><br><span class="line">	a.Print();</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>这里会发生什么？</p>
<p>先捋一遍思路，首先传过去了一个1，在调用构造函数的时候这个2先给到了_a1,然后_a1的值再给了_a2，按理来说，两个打印的值都应该是1，但实际上呢？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201057315.png"></p>
<p>可以看到，_a1的值是1，但是_a2的值却是随机值，这就是上面所说的，会按照声明顺序进行初始化。</p>
<p>因为_a2声明是_a1前面，所以在初始化的时候_a2会先初始化，_a2的初始化是_a1的拷贝，但此时_a1还没有完成初始化的赋值，所以就把一个随机值拷贝了过去，然后_a1才被初始化为1.</p>
<hr>
<h2 id="static成员"><a href="#static成员" class="headerlink" title="static成员"></a>static成员</h2><p>在C语言里面我们就知道static是静态变量，作用域是全局，在C++里面同样也可以使用，但却有一点不一样。</p>
<p>先看看概念：</p>
<blockquote>
<p>声明为static的类成员称为类的静态成员，用static修饰的成员变量，称之为静态成员变量；用static修饰的成员函数，称之为静态成员函数。静态的成员变量一定要在类外进行初始化</p>
</blockquote>
<p>简单写个代码方便理解：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201057617.png"></p>
<p>这里，我的_a2是被static修饰的，如果在构造函数初始化队列里面进行初始化会出现如上图一样的错误，正确方式是：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201057360.png"></p>
<p>在类外面 是，首先声明这是个什么类型，然后是这个对象属于哪个类，其次再赋值。</p>
<p>这里要注意对象的几点特性：</p>
<blockquote>
<ol>
<li>静态成员为所有类对象所共享，不属于某个具体的实例</li>
<li>静态成员变量必须在类外定义，定义时不添加static关键字</li>
<li>类静态成员即可用类名::静态成员或者对象.静态成员来访问</li>
<li>静态成员函数没有隐藏的this指针，不能访问任何非静态成员</li>
<li>静态成员和类的普通成员一样，也有public、protected、private3种访问级别，也可以具有返回值</li>
</ol>
</blockquote>
<p>这里再提出两个问题：</p>
<p>静态成员函数可不可以调用非静态成员函数，反之呢？</p>
<p>答：</p>
<p>静态的不能调用非静态的，但是非静态的可以调用静态的。</p>
<hr>
<h2 id="友元"><a href="#友元" class="headerlink" title="友元"></a>友元</h2><p>友元分为两种：友元函数和友元类</p>
<p>友元提供了一种突破封装的方式，但也正是因为突破了封装，导致代码的耦合性变差了。</p>
<h3 id="友元类"><a href="#友元类" class="headerlink" title="友元类"></a>友元类</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Time;</span><br><span class="line"></span><br><span class="line">class Date</span><br><span class="line">&#123;</span><br><span class="line">	friend Time;</span><br><span class="line">public:</span><br><span class="line">	Date(int year = 1, int month = 1, int day = 1)</span><br><span class="line">		:_year(year),</span><br><span class="line">		_month(month),</span><br><span class="line">		_day(day)</span><br><span class="line">	&#123;&#125;</span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line">	int _year;</span><br><span class="line">	int _month;</span><br><span class="line">	int _day;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">class Time</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	Time(int h = 1,int min = 1,int s = 1)</span><br><span class="line">		:_h(h),</span><br><span class="line">		_min(min),</span><br><span class="line">		_s(s)</span><br><span class="line">	&#123;&#125;</span><br><span class="line"></span><br><span class="line">	void printf(Date&amp; d) const</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; d._year &lt;&lt; &quot;-&quot; &lt;&lt; d._month &lt;&lt; &quot;-&quot; &lt;&lt; d._day &lt;&lt; &quot;-&quot; &lt;&lt; this-&gt;_h &lt;&lt; &quot;-&quot; &lt;&lt; this-&gt;_min &lt;&lt; &quot;-&quot; &lt;&lt; this-&gt;_s &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">private:</span><br><span class="line">	int _h;</span><br><span class="line">	int _min;</span><br><span class="line">	int _s;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>这就是一个友元类。</p>
<p>首先有两个类，第一个是日期，第二个是时间，我想在时间类里面顺带着一起打印日期，那我就要在时间类里面加上 friend Time，表示Time是Date的友元，那Time就可以访问到Date里面的私有数据。</p>
<p>但是反过来，Date不是Time的友元，所以Date无法访问到Time里面的对象。</p>
<p>友元是单向性的</p>
<p>友元没有传递性 ，如果B是A的友元，C是B的友元，则不能说明C时A的友元</p>
<p>–</p>
<h3 id="友元函数"><a href="#友元函数" class="headerlink" title="友元函数"></a>友元函数</h3><p>如果知道了友元类，那友元函数就轻松多了。</p>
<p>这里还是看代码演示比较好：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Date</span><br><span class="line">&#123;</span><br><span class="line">	friend void Printf(const Date&amp; d);</span><br><span class="line">public:</span><br><span class="line">	Date(int year = 1, int month = 1, int day = 1)</span><br><span class="line">		:_year(year),</span><br><span class="line">		_month(month),</span><br><span class="line">		_day(day)</span><br><span class="line">	&#123;&#125;</span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line">	int _year;</span><br><span class="line">	int _month;</span><br><span class="line">	int _day;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">void Printf(const Date&amp; d)</span><br><span class="line">&#123;</span><br><span class="line">	cout &lt;&lt; d._year &lt;&lt; &quot;-&quot; &lt;&lt; d._month &lt;&lt; &#x27;-&#x27; &lt;&lt; d._day &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>在Dtae里面声明有Printf这个函数，格式是：返回值 函数名（函数参数）</p>
<p>这样就可以在类外面访问类的私有成员。</p>
<p>这里要注意几点：</p>
<blockquote>
<p>友元函数可以访问类的私有和保护乘员，但是不是类的成员函数</p>
<p>友元函数不能用const修饰</p>
<p>友元函数可以定义在类里面的任何地方，不受访问限定符的限制，但最好定义在类的最前面</p>
<p>一个函数可以是多个类的友元函数</p>
<p>友元函数的调用和普通函数的调用原理相同</p>
</blockquote>
<hr>
<h2 id="内部类"><a href="#内部类" class="headerlink" title="内部类"></a>内部类</h2><p>这个比较简单，先看概念和特征：</p>
<blockquote>
<p>概念：如果一个类定义在另一个类的内部，这个内部类就叫做内部类。注意此时这个内部类是一个独立的类，它不属于外部类，更不能通过外部类的对象去调用内部类。外部类对内部类没有任何优越的访问权限。</p>
<p>注意：内部类就是外部类的友元类。注意友元类的定义，内部类可以通过外部类的对象参数来访问外部类中的所有成员。但是外部类不是内部类的友元。</p>
<hr>
<p>特性：</p>
<ol>
<li>内部类可以定义在外部类的public、protected、private都是可以的。</li>
<li>注意内部类可以直接访问外部类中的static、枚举成员，不需要外部类的对象&#x2F;类名。</li>
<li>sizeof(外部类)&#x3D;外部类，和内部类没有任何关系</li>
</ol>
</blockquote>
<p>然后，我们看一段代码演示：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class A &#123;</span><br><span class="line">private:</span><br><span class="line">	static int k;</span><br><span class="line">	int h;</span><br><span class="line">public:</span><br><span class="line">	class B</span><br><span class="line">	&#123;</span><br><span class="line">	public:</span><br><span class="line">		void foo(const A&amp; a)</span><br><span class="line">		&#123;</span><br><span class="line">			cout &lt;&lt; k &lt;&lt; endl;//OK</span><br><span class="line">			cout &lt;&lt; a.h &lt;&lt; endl;//OK</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>这个给对象的名字是A，但是在A的里面有有一个对象叫B，我在B里面的函数要访问A的私有成员，运行结果如图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201057693.png"></p>
<p>可以直接访问,不受权限的限制 。</p>
<hr>
<p>thank watch</p>
<p>因为最近有考试，所以更新放慢</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>红黑树</title>
    <url>/2022/08/04/%E7%BA%A2%E9%BB%91%E6%A0%91/</url>
    <content><![CDATA[<hr>
<h1 id="什么是红黑树"><a href="#什么是红黑树" class="headerlink" title="什么是红黑树"></a>什么是红黑树</h1><p>从概念上来讲，它是一种二叉搜索树，但每一个节点不是红色就是黑色，它保证最长路径不会比其他路径长出两倍</p>
<p>为什么能保证，就要看下面几条性质：</p>
<blockquote>
<p><strong>1. 每个结点不是红色就是黑色<br> \2. 根节点是黑色的<br> \3. 如果一个节点是红色的，则它的两个孩子结点是黑色的<br> \4. 对于每个结点，从该结点到其所有后代叶结点的简单路径上，均 包含相同数目的黑色结点<br> \5. 每个叶子结点都是黑色的(此处的叶子结点指的是空结点)</strong></p>
</blockquote>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201201297.png" alt="img"></p>
<blockquote>
<p><strong>为什么满足了上面的性质就可以保证最长的不超过最短的两倍？</strong></p>
<p><strong>因为最短路径肯定是全黑，但是最长路径是黑红黑红，因为其保证了每条路径黑色节点数量要一致，所以最长的黑红最多也就等于两倍的黑</strong></p>
</blockquote>
<h1 id="二叉树节点的定义"><a href="#二叉树节点的定义" class="headerlink" title="二叉树节点的定义"></a>二叉树节点的定义</h1><p>这里会跟AVL树有所不同，因为这里之后要复用到map，所以会稍作修改</p>
<p>首先为了区分颜色，我们就可以用到枚举：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">enum Color</span><br><span class="line">&#123;</span><br><span class="line">	Red,</span><br><span class="line">	Black</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>然后创建节点的结构体，这里跟AVLTree差不多</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class K,class V&gt;</span><br><span class="line">struct RBTreeNode</span><br><span class="line">&#123;</span><br><span class="line">	pair&lt;K, V&gt; _kv;</span><br><span class="line">	RBTreeNode&lt;K, V&gt;* _parent;</span><br><span class="line">	RBTreeNode&lt;K, V&gt;* _left;</span><br><span class="line">	RBTreeNode&lt;K, V&gt;* _right;</span><br><span class="line"></span><br><span class="line">	Color _color;</span><br><span class="line"></span><br><span class="line">	RBTreeNode(const pair&lt;K,V&gt;&amp; kv)</span><br><span class="line">		:_kv(kv)</span><br><span class="line">		, _parent(nullptr)</span><br><span class="line">		, _left(nullptr)</span><br><span class="line">		, _right(nullptr)</span><br><span class="line">		, _color(Red)</span><br><span class="line">	&#123;&#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>顺便写一个构造函数，也很简单，把指针置为空，将里面的值用外面的拷贝一份，这里要注意的是，为什么插入节点的颜色要是红色。</p>
<p>这里我们插入节点是一定要给颜色的，给颜色就会出现两种情况：</p>
<blockquote>
<p><strong>3. 如果一个节点是红色的，则它的两个孩子结点是黑色的<br> \4. 对于每个结点，从该结点到其所有后代叶结点的简单路径上，均 包含相同数目的黑色结点</strong></p>
</blockquote>
<p><strong>如果我们插入红色，有可能会违反3，因为它的父亲节点可能是红色的，但如果我们插入黑色节点，那就一定会违反4，因为你在一条支路里面插入黑色节点，如何确保其他路上的黑色节点数量一样就是个问题</strong> </p>
<h1 id="红黑树的插入"><a href="#红黑树的插入" class="headerlink" title="红黑树的插入"></a>红黑树的插入</h1><p>先简单写一个红黑树的大框架出来</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class K, class V&gt;</span><br><span class="line">class RBTree</span><br><span class="line">&#123;</span><br><span class="line">	typedef RBTreeNode&lt;K, V&gt; Node;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line">	Node* _root = nullptr;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>然后就可以考虑写插入了，这里需要提醒一下，是否还记得AVL树的旋转，这里红黑树也需要用到，如果不记得，这里放上另一篇博客的链接可供参考：</p>
<p><a class="link"   href="https://blog.csdn.net/qq_60306111/article/details/126117035?csdn_share_tail=%7B%22type%22:%22blog%22,%22rType%22:%22article%22,%22rId%22:%22126117035%22,%22source%22:%22qq_60306111%22%7D&ctrtid=jnIxW" >【C++】AVL_Knous的博客-CSDN博客<i class="fas fa-external-link-alt"></i></a></p>
<p>ok，最基本的先写上，如果这是一个空树，那最开始插入的就是根节点：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//判断这是否为空树，是就直接插入</span><br><span class="line">if (_root == nullptr)</span><br><span class="line">&#123;</span><br><span class="line">	_root = new Node(kv);</span><br><span class="line">	//根节点一定要是黑色</span><br><span class="line">	_root-&gt;_color = Black;</span><br><span class="line">	return true;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>然后，就可以开始插入了，插入部分根AVL基本一致</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Node* cur = _root;</span><br><span class="line">Node* parent = cur-&gt;_parent;</span><br><span class="line">while (cur)</span><br><span class="line">&#123;</span><br><span class="line">	if (cur-&gt;_kv.first &gt; kv.first)</span><br><span class="line">	&#123;</span><br><span class="line">		parent = cur;</span><br><span class="line">		cur = cur-&gt;_left;</span><br><span class="line">	&#125;</span><br><span class="line">	else if (cur-&gt;_kv.first &lt; kv.first)</span><br><span class="line">	&#123;</span><br><span class="line">		parent = cur;</span><br><span class="line">		cur = cur-&gt;_right;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">		return false;</span><br><span class="line"></span><br><span class="line">	cur = new Node(kv);</span><br><span class="line">	cur-&gt;_color = Red;</span><br><span class="line">	if (cur = parent-&gt;_left)</span><br><span class="line">		parent-&gt;_left = cur;</span><br><span class="line">	else</span><br><span class="line">		parent-&gt;_right = cur;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>然后就是我们需要处理的地方，我们就开始判断：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201201704.png" alt="img"></p>
<p>假设，我们在这个位置插入，肯定是没问题的，但是如果我们再插入一个：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201201611.png" alt="img"></p>
<p>此时，就会出现错误：连续的红色节点</p>
<p><strong>这种情况我们就要处理一下：当cur为红且parent为红</strong></p>
<p>以上图举例：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201201563.png" alt="img"></p>
<p>然后我们再继续往上，因为gardefather也可能是子树，将它变为红色也可能出现连续红色的情况</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">while (parent &amp;&amp; parent-&gt;_color == Red)</span><br><span class="line">&#123;</span><br><span class="line">	Node* grandfather = parent-&gt;_parent;</span><br><span class="line"></span><br><span class="line">	if (grandfather-&gt;_left == parent)</span><br><span class="line">	&#123;</span><br><span class="line">		//找叔叔，如果它在右边</span><br><span class="line">		Node* uncle = grandfather-&gt;_right;</span><br><span class="line">		//如果叔叔的颜色为红</span><br><span class="line">		if (uncle &amp;&amp; uncle-&gt;_color == Red)</span><br><span class="line">		&#123;</span><br><span class="line">			grandfather-&gt;_color = Red;</span><br><span class="line">			parent-&gt;_color = uncle-&gt;_color = Black;</span><br><span class="line">			cur = grandfather;</span><br><span class="line">			parent = cur-&gt;_parent;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	//grandfather-&gt;right = parent</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		//如果叔叔在左边</span><br><span class="line">		Node* uncle = grandfather-&gt;_left;</span><br><span class="line">		//如果叔叔的颜色为红</span><br><span class="line">		if (uncle &amp;&amp; uncle-&gt;_color == Red)</span><br><span class="line">		&#123;</span><br><span class="line">			grandfather-&gt;_color = Red;</span><br><span class="line">			parent-&gt;_color = uncle-&gt;_color = Black;</span><br><span class="line">			cur = grandfather;</span><br><span class="line">			parent = cur-&gt;_parent;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>ok，第一种情况处理完，来看第二种情况：</p>
<p>有没有可能，叔叔是黑色或者是不存在？</p>
<p>当然有可能，这里就拿叔叔存在并且为黑色举个例子，不存在也是这样处理</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201201559.png" alt="img"></p>
<p>我们将这几个节点取名方便观看，将它进行一个旋转，这里旋转的代码就直接拿之前的用就可以了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void RotateL(Node* parent)</span><br><span class="line">&#123;</span><br><span class="line">	Node* subR = parent-&gt;_right;</span><br><span class="line">	Node* subRL = subR-&gt;_left;</span><br><span class="line"></span><br><span class="line">	parent-&gt;_right = subRL;</span><br><span class="line">	if (subRL)</span><br><span class="line">		subRL-&gt;_parent = parent;</span><br><span class="line"></span><br><span class="line">	Node* ppNode = parent-&gt;_parent;</span><br><span class="line"></span><br><span class="line">	subR-&gt;_left = parent;</span><br><span class="line">	parent-&gt;_parent = subR;</span><br><span class="line"></span><br><span class="line">	if (parent == _root)</span><br><span class="line">	&#123;</span><br><span class="line">		_root = subR;</span><br><span class="line">		_root-&gt;_parent = nullptr;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		if (parent == ppNode-&gt;_left)</span><br><span class="line">			ppNode-&gt;_left = subR;</span><br><span class="line">		else</span><br><span class="line">			ppNode-&gt;_right = subR;</span><br><span class="line"></span><br><span class="line">		subR-&gt;_parent = ppNode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void RotateR(Node* parent)</span><br><span class="line">&#123;</span><br><span class="line">	Node* subL = parent-&gt;_left;</span><br><span class="line">	Node* subLR = subL-&gt;_right;</span><br><span class="line"></span><br><span class="line">	parent-&gt;_left = subLR;</span><br><span class="line">	if (subLR)</span><br><span class="line">		subLR-&gt;_parent = parent;</span><br><span class="line"></span><br><span class="line">	Node* ppNode = parent-&gt;_parent;</span><br><span class="line"></span><br><span class="line">	subL-&gt;_right = parent;</span><br><span class="line">	parent-&gt;_parent = subL;</span><br><span class="line"></span><br><span class="line">	if (parent == _root)</span><br><span class="line">	&#123;</span><br><span class="line">		_root = subL;</span><br><span class="line">		_root-&gt;_parent = nullptr;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		if (ppNode-&gt;_left == parent)</span><br><span class="line">			ppNode-&gt;_left = subL;</span><br><span class="line">		else</span><br><span class="line">			ppNode-&gt;_right = subL;</span><br><span class="line">		subL-&gt;_parent = ppNode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>然后来看看如何旋转：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201201250.png" alt="img"></p>
<p>很明显是应该右单旋，变完了我们就要考虑变色，在旋转之前右边有两个黑色节点，左边有一个，那我们就要将原本parent的红色变为黑色，将grandfather的黑色变为红色</p>
<p>这里，如果parent是grandfather的右孩子，那就应该是一个左单旋，可以将上面的图做一个镜像版本，这里就不画了。</p>
<p>看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//如果叔叔的颜色为黑或者不存在</span><br><span class="line">else</span><br><span class="line">&#123;</span><br><span class="line">	//如果是g</span><br><span class="line">	//   p    u</span><br><span class="line">	// c</span><br><span class="line">	if (cur == parent-&gt;_left)</span><br><span class="line">	&#123;</span><br><span class="line">		RotateR(grandfather);</span><br><span class="line">		parent-&gt;_color = Black;</span><br><span class="line">		grandfather-&gt;_color = Red;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line">               ------</span><br><span class="line">//叔叔为黑或者不存在</span><br><span class="line">else</span><br><span class="line">&#123;</span><br><span class="line">	//		g</span><br><span class="line">	//  u       p</span><br><span class="line">	//              c</span><br><span class="line">	if (cur = parent-&gt;_right)</span><br><span class="line">	&#123;</span><br><span class="line">		RotateL(grandfather);</span><br><span class="line">		parent-&gt;_color = Black;</span><br><span class="line">		grandfather-&gt;_color = Red;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>第三种情况，cur跟parent不在一条直线上</p>
<p>这里参考下图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201201067.png" alt="img"></p>
<p>此时，单旋已经解决不了问题，因为它对在一条线上的cur和parent才会有效果，此时它们不在一条线上，该怎么处理？</p>
<p>思考一下—</p>
<p>揭晓答案</p>
<p>先将parent左旋，再对grande右旋，如下图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201202943.png" alt="img"></p>
<p>再右旋：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201202339.png" alt="img"></p>
<p>在第二步的时候就可以发现它已经变成了情况2…</p>
<p>这里同样是parent在grand左边的情况，如果在右边，则镜像即可 </p>
<p>这里我放上全部代码，每次放剧本属实不太好看</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool Insert(const pair&lt;K, V&gt;&amp; kv)</span><br><span class="line">&#123;</span><br><span class="line">	//判断这是否为空树，是就直接插入</span><br><span class="line">	if (_root == nullptr)</span><br><span class="line">	&#123;</span><br><span class="line">		_root = new Node(kv);</span><br><span class="line">		//根节点一定要是黑色</span><br><span class="line">		_root-&gt;_color = Black;</span><br><span class="line">		return true;</span><br><span class="line">	&#125;</span><br><span class="line">	Node* cur = _root;</span><br><span class="line">	Node* parent = nullptr;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		if (cur-&gt;_kv.first &gt; kv.first)</span><br><span class="line">		&#123;</span><br><span class="line">			parent = cur;</span><br><span class="line">			cur = cur-&gt;_left;</span><br><span class="line">		&#125;</span><br><span class="line">		else if (cur-&gt;_kv.first &lt; kv.first)</span><br><span class="line">		&#123;</span><br><span class="line">			parent = cur;</span><br><span class="line">			cur = cur-&gt;_right;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">			return false;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	cur = new Node(kv);</span><br><span class="line">	cur-&gt;_color = Red;</span><br><span class="line">	if (parent-&gt;_kv.first &lt; kv.first)</span><br><span class="line">		parent-&gt;_right = cur;</span><br><span class="line">	else</span><br><span class="line">		parent-&gt;_left = cur;</span><br><span class="line">	cur-&gt;_parent = parent;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	while (parent &amp;&amp; parent-&gt;_color == Red)</span><br><span class="line">	&#123;</span><br><span class="line">		Node* grandfather = parent-&gt;_parent;</span><br><span class="line">		assert(grandfather);</span><br><span class="line"></span><br><span class="line">		if (grandfather-&gt;_left == parent)</span><br><span class="line">		&#123;</span><br><span class="line">			//找叔叔，如果它在右边</span><br><span class="line">			Node* uncle = grandfather-&gt;_right;</span><br><span class="line">			//如果叔叔的颜色为红</span><br><span class="line">			if (uncle &amp;&amp; uncle-&gt;_color == Red)</span><br><span class="line">			&#123;</span><br><span class="line">				grandfather-&gt;_color = Red;</span><br><span class="line">				parent-&gt;_color = uncle-&gt;_color = Black;</span><br><span class="line">				cur = grandfather;</span><br><span class="line">				parent = cur-&gt;_parent;</span><br><span class="line">			&#125;</span><br><span class="line">			//如果叔叔的颜色为黑</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				//如果是g</span><br><span class="line">				//   p    u</span><br><span class="line">				// c</span><br><span class="line">				if (cur == parent-&gt;_left)</span><br><span class="line">				&#123;</span><br><span class="line">					RotateR(grandfather);</span><br><span class="line">					parent-&gt;_color = Black;</span><br><span class="line">					grandfather-&gt;_color = Red;</span><br><span class="line">				&#125;</span><br><span class="line">				//如果是g</span><br><span class="line">				//   p    u</span><br><span class="line">				//     c</span><br><span class="line">				else</span><br><span class="line">				&#123;</span><br><span class="line">					RotateL(parent);</span><br><span class="line">					RotateR(grandfather);</span><br><span class="line">					cur-&gt;_color = Black;</span><br><span class="line">					grandfather-&gt;_color = Red;</span><br><span class="line">				&#125;</span><br><span class="line">				break;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		//grandfather-&gt;right = parent</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			//如果叔叔在左边</span><br><span class="line">			Node* uncle = grandfather-&gt;_left;</span><br><span class="line">			//如果叔叔的颜色为红</span><br><span class="line">			if (uncle &amp;&amp; uncle-&gt;_color == Red)</span><br><span class="line">			&#123;</span><br><span class="line">				grandfather-&gt;_color = Red;</span><br><span class="line">				parent-&gt;_color = uncle-&gt;_color = Black;</span><br><span class="line">				cur = grandfather;</span><br><span class="line">				parent = cur-&gt;_parent;</span><br><span class="line">			&#125;</span><br><span class="line">			//叔叔为黑或者不存在</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				//		g</span><br><span class="line">				//  u       p</span><br><span class="line">				//              c</span><br><span class="line">				if (cur == parent-&gt;_right)</span><br><span class="line">				&#123;</span><br><span class="line">					RotateL(grandfather);</span><br><span class="line">					parent-&gt;_color = Black;</span><br><span class="line">					grandfather-&gt;_color = Red;</span><br><span class="line">				&#125;</span><br><span class="line">				//		g</span><br><span class="line">				//  u       p</span><br><span class="line">				//        c</span><br><span class="line">				else</span><br><span class="line">				&#123;</span><br><span class="line">					RotateR(parent);</span><br><span class="line">					RotateL(grandfather);</span><br><span class="line">					cur-&gt;_color = Black;</span><br><span class="line">					grandfather-&gt;_color = Red;</span><br><span class="line">				&#125;</span><br><span class="line"></span><br><span class="line">				break;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">		_root-&gt;_color = Black;</span><br><span class="line">		return true;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>删除就不打算讲了，理解插入就足够了</p>
<h1 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h1><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208232032423.png" alt="img"></p>
<p> 假设这是我们的红黑树，它会拿来实现map&#x2F;set，我们知道这两个容器如果用迭代器访问的话，它会是一个有序的一串数据：5 6 7 8 9 10 11 </p>
<p>那么，我们如何来完成这样想法？</p>
<p>先从最简单的获取begin和end开始，这两个其实不算难，begin肯定是指向最小的那一个，而最小的那一个就是树的最左节点，而end就跟它相反，指的是最有节点，我们肯定不能直接返回这个节点，所以我们封装一下一个迭代器出来。</p>
<p>这一步跟list其实很像，这里简单说一下，通过我们传过去的参数不同来实现非const 和 const的版本，迭代器其本质就是一个节点，不过我们通过重载来让它达成++，–这样的功能。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208232032861.png" alt="img"></p>
<p>那简单写一个迭代器出来，不过在此之前，我们要稍微堆RBTree修改一下。</p>
<blockquote>
<p>template&lt;class K, class T&gt;</p>
</blockquote>
<p> 改成这样，为什么？</p>
<p>后面的map、set会用到红黑树进行修改，而我们如何让红黑树适配map、set的不同？</p>
<p>map是key、val对</p>
<p>set是key、key对</p>
<p>这怎么办？</p>
<p>既然都不好办，那就折中一下，我们第一个类型照样接收，但是第二个类型val用来区分。</p>
<p>如果是set，那val也是key</p>
<p>如果是map，那val就是pair&lt;k,v&gt;</p>
<p>到时候我们就可以统一的用第二个参数，这样也比较好写我们的迭代器。</p>
<p>-–</p>
<p>正式开始写我们的迭代器</p>
<h2 id="typedef部分"><a href="#typedef部分" class="headerlink" title="typedef部分"></a>typedef部分</h2><p>我们知道迭代器应该有两个版本，一个是const，另一个是非const，在之前list我们就是通过重定义来区分，这里也一样：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef RBTree_iterator&lt;T, T&amp;, T*&gt; iterator;</span><br><span class="line">typedef RBTree_iterator&lt;T, const T&amp;, const T*&gt; const_iterator;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>这样，它就区分了两个迭代器的区别。</p>
<h2 id="获取begin和end"><a href="#获取begin和end" class="headerlink" title="获取begin和end"></a>获取begin和end</h2><p>很简单，直接找最左和最右即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">iterator begin()</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = _root;</span><br><span class="line"></span><br><span class="line">	while (cur &amp;&amp; cur-&gt;left)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;_left;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	return iterator(cur);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">iterator end()</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = _root;</span><br><span class="line"></span><br><span class="line">	while (cur &amp;&amp; cur-&gt;_right)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;_right;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	return iterator(cur);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const_iterator begin() const</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = _root;</span><br><span class="line"></span><br><span class="line">	while (cur &amp;&amp; cur-&gt;left)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;_left;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	return iterator(cur);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const_iterator end() const</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = _root;</span><br><span class="line"></span><br><span class="line">	while (cur &amp;&amp; cur-&gt;_right)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;_right;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	return iterator(cur);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>目前为止还没什么好说的，至于为什么返回值是个iterator，直接返回cur行不行？</p>
<p>从本质上来讲，可以，因为这个迭代器也是一个指针而已，但从使用角度来说，不推荐，因为其是一个迭代器，迭代器直接跟指针比较，多少有点不合适。</p>
<p>封装一下，好看一点嘛。</p>
<p>开始写迭代器的类</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template&lt;class T, class Ref ,class Ptr&gt;</span><br><span class="line">struct RBTree_iterator</span><br><span class="line">&#123;</span><br><span class="line">	typedef RBTreeNode&lt;T&gt; Node;</span><br><span class="line">	tyoedef RBTree_iterator&lt;T, Ref, Ptr&gt; self;</span><br><span class="line">	Node* _node;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">private:</span><br><span class="line">	Node* _node;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h2 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h2><p>迭代器的构造函数也还算节点，我们其本质也是一个节点的指针，我们把指针的值赋值过去即可。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">RBTree_iterator(Node&amp; node)</span><br><span class="line">	:_node(node)</span><br><span class="line">&#123;&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h2 id=""><a href="#" class="headerlink" title=""></a></h2><h2 id="-1"><a href="#-1" class="headerlink" title="++"></a>++</h2><p>++有两种，前置和后置，我们只需要写一个前置即可，后置可以复用前置。</p>
<p>如何写？</p>
<p>说来也简单，我们知道红黑树的有序要走中序，那我们++就是这个走中序的过程，先是左子树，再是根，再是右子树。</p>
<p>首先我们判断，右子树走完没有。</p>
<p>以下图举例：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208232032230.png" alt="img"></p>
<p>此时，begin是5，5已经打印出来，下一个是6，我们判断一个节点的右边走完没有，如何判断走完没有？</p>
<p>我们看这个节点的右子树。</p>
<p>如果这个节点右子树不为空，那我们的cur就要往右子树走，同时找到最左节点。</p>
<p>如果这个节点的右子树为空，那我们的cur就已经是叶子节点了，都叶子节点了，那它的下一个肯定是它的父亲，那我们就让begin&#x3D;begin-&gt;fater，然后等待下次判断：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208232032162.png" alt="img"></p>
<p>此时走到了6，6的右子树不为空，走右子树的最左节点，但是7就是叶子节点，所以来到7的位置。</p>
<p>假设7已经走完，那如何回到6的位置？</p>
<p>我们发现，6是8的左子树，那我们就可以找，孩子是父亲左边的那个。</p>
<p>举个例子，现在是7，父亲是6，7不是6的左子树，那7和6都往上走。</p>
<p>6是不是8的左子树，是。</p>
<p>但是6是已经访问完成的状态，所以我们要让begin指向父亲，也就是8的位置。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">self operator++()</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = _node;</span><br><span class="line"></span><br><span class="line">	if (cur-&gt;_right)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;_right;</span><br><span class="line"></span><br><span class="line">		while (cur-&gt;_left)</span><br><span class="line">		&#123;</span><br><span class="line">			cur = cur-&gt;_left;</span><br><span class="line">		&#125;</span><br><span class="line">		_node = cur;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		Node* parent = cur-&gt;_parent;</span><br><span class="line"></span><br><span class="line">		if (parent &amp;&amp; cur != parent-&gt;_left)</span><br><span class="line">		&#123;</span><br><span class="line">			cur = parent;</span><br><span class="line">			parent = parent-&gt;_parent;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		_node = parent;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	return *this;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>后置版本的就是复用前置</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">self operator++(int)</span><br><span class="line">&#123;</span><br><span class="line">	self tmp(*this);</span><br><span class="line">	++(*this);</span><br><span class="line">	return tmp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h2 id="–"><a href="#–" class="headerlink" title="–"></a>–</h2><p>这个跟++完全就是反过来，为什么这么说，看下图</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208232032191.png" alt="img"></p>
<p>此时 it是8，它的上一个是7，那我们怎么找到7？</p>
<p>是不是把之前的逻辑反过来即可？</p>
<p>7是6的右边。</p>
<p>也就是说，我们要找的是左子树的最右节点。</p>
<p>但是如果it是9，它的左子树为空，那怎么办？</p>
<p>我们就找孩子是父亲右边的那个。</p>
<p>为什么？</p>
<p>因为9是8通过走右子树然后找最左节点来的，也就是说，如果我父亲是我祖父的右节点，那基本就是一个原路返回。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">self operator--()</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = _node;</span><br><span class="line">	if (cur-&gt;left)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;_left;</span><br><span class="line">		while (cur-&gt;_right)</span><br><span class="line">		&#123;</span><br><span class="line">			cur = cur-&gt;_right;</span><br><span class="line">		&#125;</span><br><span class="line">		_node = cur;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		Node* parent = cur-&gt;_parent;</span><br><span class="line"></span><br><span class="line">		while (parent &amp;&amp; parent-&gt;_right != cur)</span><br><span class="line">		&#123;</span><br><span class="line">			cur = parent;</span><br><span class="line">			parent = parent-&gt;_parent;</span><br><span class="line">		&#125;</span><br><span class="line">		_node = cur;</span><br><span class="line">	&#125;</span><br><span class="line">	return *this;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">self operator--(int)</span><br><span class="line">&#123;</span><br><span class="line">	self tmp(*this);</span><br><span class="line">	--(*this);</span><br><span class="line">	return tmp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h2 id="、-gt"><a href="#、-gt" class="headerlink" title="*、-&gt;"></a>*、-&gt;</h2><p>*其实就是将这个地址数据返回过去即可。</p>
<p>-&gt;也只是找到这个地址所指向的数据即可。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Ref operator*()</span><br><span class="line">&#123;</span><br><span class="line">	return _node-&gt;_kv;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Ptr operator-&gt;()</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	return &amp;_node-&gt;_kv;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h2 id="！-x3D-、-x3D-x3D"><a href="#！-x3D-、-x3D-x3D" class="headerlink" title="！&#x3D;、&#x3D;&#x3D;"></a>！&#x3D;、&#x3D;&#x3D;</h2><p>迭代器本质是指针，直接比较</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool operator!=(const self&amp; s) const</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	return _node != s._node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bool operator==(const self&amp; s) const</span><br><span class="line">&#123;</span><br><span class="line">	return _node == s._node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>至此迭代器的大部分功能写完，希望对你的学习有所帮助。</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>继承</title>
    <url>/2022/07/18/%E7%BB%A7%E6%89%BF/</url>
    <content><![CDATA[<hr>
<h1 id="继承的概念及定义"><a href="#继承的概念及定义" class="headerlink" title="继承的概念及定义"></a>继承的概念及定义</h1><h2 id="继承的概念"><a href="#继承的概念" class="headerlink" title="继承的概念"></a>继承的概念</h2><blockquote>
<p>继承(inheritance)机制是面向对象程序设计使代码可以复用的最重要的手段，它允许程序员在保持原有类特性的基础上进行扩展，增加功能，这样产生新的类，称派生类。继承呈现了面向对象程序设计的层次结构，体现了由简单到复杂的认知过程。以前我们接触的复用都是函数复用，继承是类设计层次的复用。</p>
</blockquote>
<p>其实也很好理解，拿下图举例</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201151344.png" alt="img"></p>
<p>假设现在是两个类，但我们可以发现其中大部分类容是可以一样的，我们就可以把这部分提取出来，取名新类，然后让老师和学生分别继承这个类</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201153623.png" alt="img"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Person</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	void Print()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;name:&quot; &lt;&lt; _name &lt;&lt; endl;</span><br><span class="line">		cout &lt;&lt; &quot;age:&quot; &lt;&lt; _age &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">protected:</span><br><span class="line">	string _name = &quot;knous&quot;;</span><br><span class="line">	int _age = 18; </span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">class Student : public Person</span><br><span class="line">&#123;</span><br><span class="line">protected:</span><br><span class="line">	int _stuid; </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>这就是一个继承，现在Student就有了Person的参数，比如一个打印函数，两个被保护起来的对象，一个_name，一个_age，我们可以按照平时调用类里面函数一样调用这个打印函数</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201151862.png" alt="img"></p>
<p>-—- </p>
<h2 id="继承的定义"><a href="#继承的定义" class="headerlink" title="继承的定义"></a>继承的定义</h2><h3 id="定义格式"><a href="#定义格式" class="headerlink" title="定义格式"></a>定义格式</h3><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201153860.png" alt="img"><img src="/" alt="点击并拖拽以移动"></p>
<h3 id="继承关系与访问限定符"><a href="#继承关系与访问限定符" class="headerlink" title="继承关系与访问限定符"></a>继承关系与访问限定符</h3><p>我们知道访问限定符有三种：</p>
<p>public（公开）</p>
<p>protected（保护）</p>
<p>private（私人）</p>
<p>同样，继承方式也有三种：</p>
<p>public（公开）</p>
<p>protected（保护）</p>
<p>private（私人）</p>
<h3 id="继承基类成员访问方式的变化"><a href="#继承基类成员访问方式的变化" class="headerlink" title="继承基类成员访问方式的变化"></a>继承基类成员访问方式的变化</h3><table>
<thead>
<tr>
<th>类成员&#x2F;继承方式</th>
<th>public继承</th>
<th>protected继承</th>
<th>private继承</th>
</tr>
</thead>
<tbody><tr>
<td>基类的public成员</td>
<td>派生类的public成员</td>
<td>派生类的protected  		成员</td>
<td>派生类的private  		成员</td>
</tr>
<tr>
<td>基类的protected  		成员</td>
<td>派生类的protected  		成员</td>
<td>派生类的protected  		成员</td>
<td>派生类的private  		成员</td>
</tr>
<tr>
<td>基类的private成  		员</td>
<td>在派生类中不可见</td>
<td>在派生类中不可见</td>
<td>在派生类中不可</td>
</tr>
</tbody></table>
<blockquote>
<p>总结：</p>
<p>基类的不可见不是指的没有被继承，本质是继承了，但是不让用，不管是类里面还是外面。</p>
<p>如果是想在类外面不让使用，类里面可以用，可以用protected继承</p>
<p>class默认是private，struct默认是public，跟类访问限定符那里是一样的。</p>
<p>一般都是public继承，protected和private继承都比较少</p>
<p>基类的成员在被继承的时候取权限小为准，如果是公开继承原本公开的，那不变，如果是公开保护继承，那就是保护成员</p>
</blockquote>
<h1 id="基类和派生类对象赋值转换"><a href="#基类和派生类对象赋值转换" class="headerlink" title="基类和派生类对象赋值转换"></a>基类和派生类对象赋值转换</h1><blockquote>
<p>派生类对象 可以赋值给 基类的对象 &#x2F; 基类的指针 &#x2F; 基类的引用。这里有个形象的说法叫切片<br> 或者切割。寓意把派生类中父类那部分切来赋值过去。<br> 基类对象不能赋值给派生类对象。<br> 基类的指针或者引用可以通过强制类型转换赋值给派生类的指针或者引用。但是必须是基类<br> 的指针是指向派生类对象时才是安全的。这里基类如果是多态类型，可以使用RTTI(RunTime Type Information)的dynamic_cast 来进行识别后进行安全转换。</p>
</blockquote>
<p>怎么理解这话呢，看下面的图：</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201151453.png" alt="img"></p>
<p>现在要将学生赋值给人，学生里面四个参数，但是人里面只有两个，那编译器在处理的时候会将学生里面多的两个不要，这就叫切片（切割），将多的部分舍弃。 </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Person</span><br><span class="line">&#123;</span><br><span class="line">public:</span><br><span class="line">	void Print()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; &quot;name:&quot; &lt;&lt; _name &lt;&lt; endl;</span><br><span class="line">		cout &lt;&lt; &quot;age:&quot; &lt;&lt; _age &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">protected:</span><br><span class="line">	string _name = &quot;knous&quot;;</span><br><span class="line">	int _age = 18; // </span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">class Student : public Person</span><br><span class="line">&#123;</span><br><span class="line">protected:</span><br><span class="line">	int _stuid; // 学号</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    Student s;</span><br><span class="line">	Person p = s;	//将s对象直接赋值给p</span><br><span class="line">	Person&amp; rp = s; //将s对象取个别名叫rp</span><br><span class="line">	Person* ptrp = &amp;s; //让s对象的地址给p的指针</span><br><span class="line"></span><br><span class="line">	s = p;				//这种会报错，基类不能赋值给派生类</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h1 id="继承的作用域"><a href="#继承的作用域" class="headerlink" title="继承的作用域"></a>继承的作用域</h1><blockquote>
<p>\1. 在继承体系中基类和派生类都有独立的作用域。<br> \2. 子类和父类中有同名成员，子类成员将屏蔽父类对同名成员的直接访问，这种情况叫隐藏，也叫重定义。（在子类成员函数中，可以使用 基类::基类成员 显示访问）<br> \3. 需要注意的是如果是成员函数的隐藏，只需要函数名相同就构成隐藏。<br> \4. 注意在实际中在继承体系里面最好不要定义同名的成员。</p>
</blockquote>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201151691.png" alt="img"></p>
<p>从图里面可以看到，我创建了b，然后调用B里面的fun，B又继承的A，按理来说里面应该有两个fun，但直接调用的是B里面自身的fun，假设我一定要调用A的fun呢？</p>
<p>指定类，像下图：</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201151536.png" alt="img"></p>
<p>这样就可以了</p>
<h1 id="派生类的默认成员函数"><a href="#派生类的默认成员函数" class="headerlink" title="派生类的默认成员函数"></a>派生类的默认成员函数</h1><p> 这里更是重量级，头疼的地方来了</p>
<p>派生类也是类，里面也有六个默认函数，这里就要了解一下这几个成员函数怎么生成的：</p>
<h2 id="构造"><a href="#构造" class="headerlink" title="构造"></a>构造</h2><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201151132.png" alt="img"><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>初始化对象s的时候，它先调用了Person的初始化构造，然后才是自己，可以这么理解，子类继承的父类被当成了一个自定义类型，会调用自定义类型的构造函数，然后才是自己，如果没有合适的默认构造，就要在初始化列表里面显示调用：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201151879.png" alt="img"></p>
<h2 id="拷贝构造"><a href="#拷贝构造" class="headerlink" title="拷贝构造"></a>拷贝构造</h2><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201151722.png" alt="img"></p>
<p>如果写出了这种代码，就要注意了，这里的问题是之前说的，operator &#x3D;的运算符重载问题，因为person里面有一个，student也有一个，这里s里面有基类成员，所以必须用基类的拷贝构造，但是直接写的话就会出现死循环，因为studet里面也有一个拷贝构造，就会出现自己调用自己：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152621.png" alt="img"></p>
<p>直接报错，位了解决问题，我们必须指定用的是基类的拷贝构造：</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152833.png" alt="img"></p>
<p>这样就没问题了。 </p>
<h2 id="析构"><a href="#析构" class="headerlink" title="析构"></a>析构</h2><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152980.png" alt="img"><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>可以看到，基类先构造，但后析构，这里说一句，哪怕是手动先析构基类，但后面还是会再析构一次，因为编译器一定会保持先子后父的顺序</p>
<h1 id="继承与友元"><a href="#继承与友元" class="headerlink" title="继承与友元"></a>继承与友元</h1><p>友元不继承，基类友元不能访问子类私有和保护</p>
<h1 id="继承与静态成员"><a href="#继承与静态成员" class="headerlink" title="继承与静态成员"></a>继承与静态成员</h1><p>基类定义的static成员，则继承的整个体系里面都只会有一个这样的成员，无论派生了多少，都只有一个</p>
<h1 id="复杂的菱形继承及菱形虚拟继承"><a href="#复杂的菱形继承及菱形虚拟继承" class="headerlink" title="复杂的菱形继承及菱形虚拟继承"></a>复杂的菱形继承及菱形虚拟继承</h1><p>继承又分为两种：</p>
<p>单继承：每次只继承一个</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152096.png" alt="img"></p>
<p>多继承：一个子类有多个父类</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152272.png" alt="img"></p>
<p>菱形继承：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152179.png" alt="img"></p>
<p>当出现菱形继承，会发现什么、</p>
<p>类4里面有两个类1 </p>
<p> 因为类2里面有类1，类3里面有类1，但类4又继承了类2和类3，假设类1很小，十几个字节还好，但是如果类1很大，几万、甚至几十万字节呢？</p>
<p>出大问题。</p>
<p>这句是菱形继承的问题：形继承有数据冗余和二义性。</p>
<p>如何解决？</p>
<h2 id="虚拟继承"><a href="#虚拟继承" class="headerlink" title="虚拟继承"></a>虚拟继承</h2><p>这里举个例子</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152705.png" alt="img"></p>
<p> 这就是很明显的菱形继承，Person被Student和Teacher继承，Assistant又继承了Student和Teacher</p>
<p>虚拟进程是这样的，在继承方式的前面多加上关键词：virtual（虚拟）</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152112.png" alt="img"></p>
<p>这就算虚拟继承了，那虚拟继承是如何解决数据冗余和二义性的呢？</p>
<p>这里就直接说结论吧，过程较为复杂。</p>
<p>这里看图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152264.png" alt="img"></p>
<p>假设现在是普通继承，那此时的D里面就是这样；</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152445.png" alt="img"></p>
<p>但如果是虚拟继承，就会是这样：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201152468.png" alt="img"></p>
<p>此时的B、C里面就不再是直接放A的地址，而是放一个指针，这个指针称之为虚基表指针，虚基表指针指向的这块空间称之为虚基表，虚基表里面存放的是偏移量，通过偏移量就可以找到下面的A。</p>
<p>-–</p>
<p>感谢您的观看，希望对您学习继承有帮助</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>进程地址空间与虚拟存储空间</title>
    <url>/2022/08/14/%E8%BF%9B%E7%A8%8B%E5%9C%B0%E5%9D%80%E7%A9%BA%E9%97%B4%E4%B8%8E%E8%99%9A%E6%8B%9F%E5%AD%98%E5%82%A8%E7%A9%BA%E9%97%B4/</url>
    <content><![CDATA[<hr>
<p>前言：</p>
<p>在之前学编程的时候，我们就知道了数据的几个存储位置，大概是这样的一个图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205866.png" alt="img"></p>
<p> 然后我们运行程序：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205145.png" alt="img"></p>
<p> 我们可以看到内存分布确实是按照左边的表来分配的</p>
<h1 id="———–-感知地址空间"><a href="#———–-感知地址空间" class="headerlink" title="———–  感知地址空间"></a>———–  感知地址空间</h1><p> 现在，我们写了这样一段代码：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205068.png" alt="img"></p>
<p>很简单的代码，创建一个子进程，然后打印pid\ppid，同时再打印父进程的pid\ppid</p>
<p>看看效果：</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205425.png" alt="img"></p>
<p>这些都没问题，但是刚刚代码里面我又多定义了一个变量，假设现在我要看看这个变量，我就直接打印嘛，如下图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205575.png" alt="img"></p>
<p>运行：</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205871.png" alt="img"></p>
<p>还是没问题嘛，那你演示这些有什么用吗？</p>
<p>现在val的值是100，没变，父子进程读到的值都是一样的，地址也是一样的，但是如果在进程里面修改了值呢？</p>
<p>再看：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205285.png" alt="img"></p>
<p> 运行：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205885.png" alt="img"></p>
<p> 这就是一个问题了，我们学过编程，知道一个地址只会有一个确定的值，但是上面父子进程打印的val怎么不一样呢？</p>
<p>很明显是地址的问题，C&#x2F;C++里面，我们使用的地址，绝对不是物理地址，就是真实地址，如果是真实地址，这种一个地址两个值是不可能出现的。</p>
<p>那这是什么？</p>
<p>可以理解为，虚拟地址。</p>
<p>-———-</p>
<p>那为什么操作系统不让直接看到物理地址呢？</p>
<p>因为内存其本身是一个硬件，只能被动的写入读取，这里我举个例子：</p>
<p>这里有一块地，你跟邻居说好一人分一半，然后你在地上种玉米什么的植物，你跟邻居划分的很明确，不会出现什么我种你那边去了之类的情况，但是有一天，又来一个人，他也要种地，然后你跟邻居一商量，行，分了一块地给他。</p>
<p>但是这个人不会种，也不知道什么界限，他就在地上随便种，你去看自己的玉米的时候发现玉米已经被拔掉了，邻居肯定不会做这种事，但是那个新来的会不会做呢？</p>
<p>系统也是如此，早期的系统分配空间是直接分配的，但是这就很依靠程序员自身掌控能力，如果程序员写错了一个地方，修改了一块本不该被修改的空间，那就会出现错误。</p>
<p>为了解决这种问题，就有人想了个办法，这样，你要空间，可以，我给你开，但是这块空间的真实地址跟你看到的地址可能会有偏差，可以看下图：</p>
<p><img src="https://img-blog.csdnimg.cn/39a25343ab784dad80105c3ac4b4b513.png" alt="img"><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动">编辑</p>
<p> 这么看地址空间可能有点抽象，那我们是这样子呢：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205474.png" alt="img"></p>
<p>每一个虚拟地址都映射了一个在内存中真实存在的物理地址，而这个，我们叫做页表 。</p>
<p>系统先将程序加载到内存，然后将其变为进程，每一个进程，操作系统都会为其构建一个页表结构。</p>
<p>通过这个页表的映射，就可以通过虚拟地址找到真实的物理地址。</p>
<p>那为什么这里的val会有两个值？</p>
<p>不知道你还记不记得，进程的几大特效里面，有一条叫做：独立性</p>
<p>这个独立性是怎么确保的呢？</p>
<p>页表，通过这个，虚拟地址映射的物理地址不一样，这里可以看下面的图。</p>
<p>原本不修改时，他们指向的同一块空间：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205805.png" alt="img"></p>
<p>但是如果修改了，那就要出写时拷贝，怎么理解？</p>
<p>就是将修改的那个值重新分配一块空间，然后将原本指向val的指针指向新的空间：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208201205712.png" alt="img"></p>
<p>所以这里就回答一下fork的两个返回值，pid_t id同一个变量为什么会有两个值。</p>
<p>pid_t本质是父进程定义的变量，fork内部，return会被执行两次，return的本质是将值写到接收返回值的变量中。</p>
<p>所以当id &#x3D; fork(）的时候，谁先返回，谁就要发生写时拷贝，就跟上面一样，这就说明了为什么地址是一样的，但是值不一样。</p>
<p>因为其本质是一个虚拟地址，真实对应的物理地址是不一样的。</p>
<p>-—–</p>
<p>为什么要有虚拟地址空间，就有一下几个原因：</p>
<p>1、保护了内存，就好像上面说的，当你的程序出现了错误，非法访问之类的，它就可以审核之后将其拦截。</p>
<p>2、进程管理，你的进程运行到哪一部分，我加载哪一部分，就不会出现你占了一大块内存，但是又不用的情况。</p>
<p>3、当程序运行起来，变成进程之后，操作系统是随机分配的一块空间，这样就会出现程序运行的地址是不确定的情况，如果用了页表，进行统一管理，就可以直接找到对应的地址来编译加载所有程序，简化了进程的设计和实现。</p>
<p>-——-</p>
<p>感谢观看，希望对你有所帮助</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>链表</title>
    <url>/2022/03/13/%E9%93%BE%E8%A1%A8/</url>
    <content><![CDATA[<h2 id="单链表"><a href="#单链表" class="headerlink" title="单链表"></a>单链表</h2><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200655205.png" alt="img"></p>
<p>这张图是我们待会要实现的功能，我会尽可能的将每一步都说的很详细，方便理解。</p>
<p><strong>链表的概念及结构</strong> </p>
<p>概念：链表是一种<strong>物理存储结构上非连续</strong>、非顺序的存储结构，数据元素的<strong>逻辑顺序</strong>是通过链表中的<strong>指针链接</strong>次序实现的 。 </p>
<p>这是较为书面的解释，这里我画个图解释一下：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200656825.png" alt="img"></p>
<p>1的位置是当前链表的起始位置，我们称之为表头，它里面放着的是第一个数据的地址（2），而2里面不仅有数据还有p2，p2指向的是第二个数据的位置，第二个数据里面不仅有2，同时里面放着p3，p3指向下一个数据的位置，而4这里最后的指针指向的是NULL，也就是说链表就到这里结束。</p>
<p>就像一条火车，里面的数据就是我们的车厢，而指针就是将数据链接起来的链条，所以我们称之为链表。</p>
<p>我们如何实现链表？</p>
<p>跟顺序表一样，我们要用到跟顺序表（我的专栏也写过结构体）类似的结构体，但是我们顺序表里面是这么写的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200656963.png" alt="img"></p>
<p>我们拿一个指针指向一块连续的空间，然后有一个size记录当前有效数据，capicity记录容量，这样好处是我们可以通过下标来访问其中每一个数据，但缺点是当我们开辟数组的时候，第一个数组容量可能是10，然后我们放11个元素进去，这个时候空间不够，我们就重新开辟空间，一般我们开辟空间都是直接将原空间翻倍，但这里20个空间只放11个元素，空间就有些浪费了。</p>
<p>那我们链表是怎么写的呢？</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200658387.png" alt="img"></p>
<p>同样，我们先来一个结构体，data就是我们除非囊的数据，而第二个参数是next，也就是下一个结构体的地址，当我们需要放11个数据的时候，顺序表我们可能直接创建了一整块数组然后往里面放，但链表则是一次放一个数据到里面，然后根据需求再开辟一空间放下一个数据。</p>
<h3 id="尾插"><a href="#尾插" class="headerlink" title="尾插"></a>尾插</h3><p>因为打印需要先有内容，所以我们先冲尾插开始写：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200657416.png" alt="img"></p>
<p>尾插还没写完，我们就发现一个问题，就是当我们每次插入的时候都需要开辟一块空间，但开辟空间的代码又比较长，每次写起来会很麻烦，所以我们可以将其封装为一个函数 ：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SListNode* BuySLisenode(SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	SListNode*newnode = (SListNode*)malloc(sizeof(SListNode));</span><br><span class="line">	if (newnode == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;malloc fail\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		newnode-&gt;data = x;</span><br><span class="line">		newnode-&gt;next = NULL;</span><br><span class="line">	&#125;</span><br><span class="line">	return newnode;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>我们拿到要插入的数据，先计算空间大小，然后进行判断，如果指针为空指针我们就打印开括失败，如果成功，我们就将数据放到新开括空间的里面，然后将其下一个指针指向空，并返回这个这一块空间的地址。</p>
<p> 然后我们直接调用这个函数即可，尾插我们看代码分析：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SListPushBack(SListNode** pphead, SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	SLTDateType* newnode = BuySLisenode(x);</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		*pphead = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		SListNode*cur=*pphead;</span><br><span class="line">		while (cur-&gt;next != NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			cur = cur-&gt;next;</span><br><span class="line">		&#125;</span><br><span class="line">		cur-&gt;next = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>尾插分两种情况，第一种，目前什么都没有，我们直接将新创建好的结构体地址给原本指针即可。</p>
<p>第二种，是目前里面已经有了结构体，我们就得找到数组的最后一个结构体，而最后的一个结构体它的指针肯定为空，然后将这个结构体的指针指向变成我们新创建的结构体地址。</p>
<p>为了看效果，我们再写一个打印函数，这个函数实现也很简单，我们进行判断，如果结构体指针不为空，我们就打印里面的内容，如果为空我们就停下：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200657852.png" alt="img"></p>
<p><strong>头插</strong></p>
<p> 既然实现了尾插，那我们再实现一个头插，相较于顺序表的头插还要移动数据，链表就简单一点看图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200658118.png" alt="img"></p>
<p>原本我的1指向2，现在我中间插入一个5，我只需要将1指向的位置变成5，然后5的指针指向2即可：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200658823.png" alt="img"></p>
<p> 看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SListPushFront(SListNode** pphead, SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	SListNode* newnode = BuySLisenode(x);</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		*pphead = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		newnode-&gt;next = *pphead;</span><br><span class="line">		*pphead = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>同样，我们先创建一个新的结构体，然后给它赋值，随后我们还是先判断当前链表有没有数据，没有就直接赋值，如果有，那就将我们新创建的结构体的下一个地址赋值为之前的第一个数据的地址，也就是我们传进来的那个地址解引用，然后我们再将原本的指向第一个元素的地址指向我们新创建的节点。</p>
<p>实现效果如下：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200658810.png" alt="img"></p>
<h3 id="指定位置插入"><a href="#指定位置插入" class="headerlink" title="指定位置插入"></a><strong>指定位置插入</strong></h3><p>然后我们来一个难一点的，在指定位置插入，这里说难一点是因为我们要先找到指定的位置，然后再插入数据，但如果熟练适用以上两个插入，那这里其实也没那么难：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200658428.png" alt="img"></p>
<p>我原本是这样的结构，我现在要在2的位置插入一个结构，是不是要先找到2数据的位置，然后将它原本指向3的地址赋给我的新节点，然后将新节点的地址给它：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200658766.png" alt="img"></p>
<p> 然后我们就可以开始写代码了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListInsert(SListNode** pphead, size_t pos, SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	SListNode* newnode = BuySLisenode(x);</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		*pphead = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		SListNode*cur = *pphead;</span><br><span class="line">		while (pos--)</span><br><span class="line">		&#123;</span><br><span class="line">			if (cur == NULL)</span><br><span class="line">			&#123;</span><br><span class="line">				printf(&quot;位置错误\n&quot;);</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				cur = cur-&gt;next;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		newnode-&gt;next = cur-&gt;next;</span><br><span class="line">		cur-&gt;next = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>这里是拿while循环来找的位置，实现的效果是这样的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200659839.png" alt="img"></p>
<p> ——————</p>
<p>解决了插入，那我们来解决一下删除</p>
<h3 id="尾删"><a href="#尾删" class="headerlink" title="尾删"></a>尾删</h3><p>尾删就是将倒数第二个结构体的指针置为空指针<img src="https://img-blog.csdnimg.cn/b53afcd35c654d1ab3dce4130a12c4d7.png?x-oss-process=image/watermark,type_d3F5LXplbmhlaQ,shadow_50,text_Q1NETiBAS25vdXM=,size_20,color_FFFFFF,t_70,g_se,x_16" alt="img"></p>
<p>我们直接进行判断语句即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SListPopBack(SListNode** pphead)</span><br><span class="line">&#123;</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;链表为空\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	SListNode*cur = (*pphead)-&gt;next;</span><br><span class="line">	SListNode*perv = *pphead;</span><br><span class="line">	if (cur == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		free(*pphead);</span><br><span class="line">		*pphead = NULL;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		while (cur-&gt;next)</span><br><span class="line">		&#123;</span><br><span class="line">			if (cur-&gt;next != NULL)</span><br><span class="line">			&#123;</span><br><span class="line">				cur = cur-&gt;next;</span><br><span class="line">				perv = perv-&gt;next;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		perv-&gt;next = NULL;</span><br><span class="line">		free(cur);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p> 同样，一进来我们先判断传进来的地址是否有内容，如果没有内容我们就可以直接打印链表为空，随后我们拿两个指针，第一个从第二个结构体开始，第二个从起始位置，因为我们要靠第一个来判断结构体指针是否为空，如果结构体不为空，我们就让第一个和第二个分别指向下一个结构体，如果第一个结构体走到指针为空的结构体，那第二个正好是它的前一个，我们将第二个的指针置为空，然后释放第一个指针的位置即可，演示效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200659692.png" alt="img"></p>
<h3 id="头删"><a href="#头删" class="headerlink" title="头删"></a>头删</h3><p>头删说来也很简单，原本传进来的地址放着的是第一个结构体的地址，我们可以直接将第二个结构体的位置传给起始地址即可：</p>
<p><img src="https://img-blog.csdnimg.cn/500daa0c8608407cbecaa671786a43a8.png?x-oss-process=image/watermark,type_d3F5LXplbmhlaQ,shadow_50,text_Q1NETiBAS25vdXM=,size_20,color_FFFFFF,t_70,g_se,x_16" alt="img"><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动">编辑</p>
<p>代码实现如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SListPopFront(SListNode** pphead)</span><br><span class="line">&#123;</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;链表为空\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		SListNode*cur = (*pphead)-&gt;next;</span><br><span class="line">		free(*pphead);</span><br><span class="line">		*pphead = cur;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p> 我们直接拿到第二个结构体的地址，然后销毁第一个结构体，随后将地址重新赋值即可，看效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200659935.png" alt="img"></p>
<h3 id="删除指定位置的值"><a href="#删除指定位置的值" class="headerlink" title="删除指定位置的值"></a>删除指定位置的值</h3><p>这个实现其实就是一个复合，我们先找到位置，这里可以参考指定位置插入，然后删除结构体，删除我们也可以参考尾删，我们将其结合：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SListEraseAfter(SListNode**pphead, size_t pos)</span><br><span class="line">&#123;</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;链表为空\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	SListNode*cur = *pphead;</span><br><span class="line">	if (pos)</span><br><span class="line">	&#123;</span><br><span class="line">		while (pos--)</span><br><span class="line">		&#123;</span><br><span class="line">			if (cur-&gt;next == NULL)</span><br><span class="line">			&#123;</span><br><span class="line">				printf(&quot;位置错误\n&quot;);</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				cur = cur-&gt;next;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		SListNode*nex = cur-&gt;next;</span><br><span class="line">		cur-&gt;next = nex-&gt;next;</span><br><span class="line">		free(nex);</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		*pphead = (*pphead)-&gt;next;</span><br><span class="line">		free(cur);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>同样，一进来我们就可以判断是否为空指针，如果为空我们打印错误，如果不为空，我们就创建一个结构体变量来帮助我们找指定位置的数据，找到了数据之后我们在删除之前要先借助它找到它之后结构体的地址，所以我们才有</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SListNode*nex = cur-&gt;next;</span><br><span class="line">cur-&gt;next = nex-&gt;next;</span><br><span class="line">free(nex);</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>因为一旦你先删除，那这之后的所有结构体就再也找不到了，一定要先留下地址，再删除。</p>
<p>如果要删除0地址，那我们直接将第二个结构体的地址给地起始地址即可，别忘了释放创建的临时结构体。</p>
<h3 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h3><p>查找可能是最简单的，我们直接遍历，如果找到了数据我们就返回，没找到就返回-1，看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SListNode* SListFind(SListNode* phead, SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	if (phead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;空链表\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	SListNode* cur = phead;</span><br><span class="line">	while (cur != NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		if (cur-&gt;data == x)</span><br><span class="line">		&#123;</span><br><span class="line">			return cur;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			cur = cur-&gt;next;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	return -1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<h3 id="销毁"><a href="#销毁" class="headerlink" title="销毁"></a>销毁</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SListDestory(SListNode* phead)</span><br><span class="line">&#123;</span><br><span class="line">	SListNode*cur = phead;</span><br><span class="line">	SListNode*nex = phead;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">		free(nex);</span><br><span class="line">		nex = cur;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>我们先创建两个结构体，第一个指向第二个数据，第二个指向第一个数据，我们先删除第一个数据，然后将第一个的值赋给第二个，再销毁第二个，循环到最后为空指针就可以结束了。</p>
<p>-——————</p>
<p>这里放上所有代码，有需要可以直接复制：</p>
<p>SList.h</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line">#define SLTDateType int</span><br><span class="line"></span><br><span class="line">typedef struct SListNode</span><br><span class="line">&#123;</span><br><span class="line">	int data;</span><br><span class="line">	struct SListNode* next;</span><br><span class="line">&#125; SListNode;</span><br><span class="line"></span><br><span class="line">void SListPrint(SListNode* phead); //打印</span><br><span class="line">void SListPushBack(SListNode** pphead, SLTDateType x);  //尾插</span><br><span class="line">void SListPushFront(SListNode** pphead, SLTDateType x); //头插</span><br><span class="line">void SeqListInsert(SListNode** psl, size_t pos, SLTDateType x);//在指定位置插入</span><br><span class="line">void SListPopBack(SListNode** pphead);// 单链表的尾删</span><br><span class="line">void SListPopFront(SListNode** pphead);// 单链表头删</span><br><span class="line">void SListEraseAfter(SListNode**pphead, size_t pos);//删除pos之后的值</span><br><span class="line">SListNode* SListFind(SListNode* phead, SLTDateType x);//单链表的查找</span><br><span class="line">void SListDestory(SListNode* phead);//销毁链表</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>SList.c</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&quot;SList.h&quot;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void SListPrint(SListNode* plist)</span><br><span class="line">&#123;</span><br><span class="line">	if (plist == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;单链表已空!\n&quot;);</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line">	SListNode*cur = plist;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d -&gt; &quot;, cur-&gt;data);</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">	printf(&quot;NULL\n&quot;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">SListNode* BuySLisenode(SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	SListNode*newnode = (SListNode*)malloc(sizeof(SListNode));</span><br><span class="line">	if (newnode == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;malloc fail\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">		newnode-&gt;data = x;</span><br><span class="line">		newnode-&gt;next = NULL;</span><br><span class="line">	return newnode;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SListPushBack(SListNode** pphead, SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	SLTDateType* newnode = BuySLisenode(x);</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		*pphead = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		SListNode*cur = *pphead;</span><br><span class="line">		while (cur-&gt;next)</span><br><span class="line">		&#123;</span><br><span class="line">			cur = cur-&gt;next;</span><br><span class="line">		&#125;</span><br><span class="line">		cur-&gt;next = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SListPushFront(SListNode** pphead, SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	SListNode* newnode = BuySLisenode(x);</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		*pphead = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		newnode-&gt;next = *pphead;</span><br><span class="line">		*pphead = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line">void SeqListInsert(SListNode** pphead, size_t pos, SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	SListNode* newnode = BuySLisenode(x);</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		*pphead = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		SListNode*cur = *pphead;</span><br><span class="line">		while (pos--)</span><br><span class="line">		&#123;</span><br><span class="line">			if (cur == NULL)</span><br><span class="line">			&#123;</span><br><span class="line">				printf(&quot;位置错误\n&quot;);</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				cur = cur-&gt;next;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		newnode-&gt;next = cur-&gt;next;</span><br><span class="line">		cur-&gt;next = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SListPopBack(SListNode** pphead)</span><br><span class="line">&#123;</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;链表为空\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	SListNode*cur = (*pphead)-&gt;next;</span><br><span class="line">	SListNode*perv = *pphead;</span><br><span class="line">	if (cur == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		free(*pphead);</span><br><span class="line">		*pphead = NULL;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		while (cur-&gt;next)</span><br><span class="line">		&#123;</span><br><span class="line">			if (cur-&gt;next != NULL)</span><br><span class="line">			&#123;</span><br><span class="line">				cur = cur-&gt;next;</span><br><span class="line">				perv = perv-&gt;next;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		perv-&gt;next = NULL;</span><br><span class="line">		free(cur);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SListPopFront(SListNode** pphead)</span><br><span class="line">&#123;</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;链表为空\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		SListNode*cur = (*pphead)-&gt;next;</span><br><span class="line">		free(*pphead);</span><br><span class="line">		*pphead = cur;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SListEraseAfter(SListNode**pphead, size_t pos)</span><br><span class="line">&#123;</span><br><span class="line">	if (*pphead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;链表为空\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	SListNode*cur = *pphead;</span><br><span class="line">	if (pos)</span><br><span class="line">	&#123;</span><br><span class="line">		while (pos--)</span><br><span class="line">		&#123;</span><br><span class="line">			if (cur-&gt;next == NULL)</span><br><span class="line">			&#123;</span><br><span class="line">				printf(&quot;位置错误\n&quot;);</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				cur = cur-&gt;next;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		SListNode*nex = cur-&gt;next;</span><br><span class="line">		cur-&gt;next = nex-&gt;next;</span><br><span class="line">		free(nex);</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		*pphead = (*pphead)-&gt;next;</span><br><span class="line">		free(cur);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">SListNode* SListFind(SListNode* phead, SLTDateType x)</span><br><span class="line">&#123;</span><br><span class="line">	if (phead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;空链表\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	SListNode* cur = phead;</span><br><span class="line">	while (cur != NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		if (cur-&gt;data == x)</span><br><span class="line">		&#123;</span><br><span class="line">			return cur;</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			cur = cur-&gt;next;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	return -1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SListDestory(SListNode* phead)</span><br><span class="line">&#123;</span><br><span class="line">	SListNode*cur = phead;</span><br><span class="line">	SListNode*nex = phead;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">		free(nex);</span><br><span class="line">		nex = cur;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>主函数我就不放了，因为操作函数都在上面，也需要直接调用即可。</p>
<p>-———-</p>
<h3 id="修改"><a href="#修改" class="headerlink" title="修改"></a>修改</h3><p>更新，多一个修改函数，能将指定位置的值修改，原理就是找到位置然后修改，原理没变：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SListrevise(SListNode* phead, size_t pos, int x)</span><br><span class="line">&#123;</span><br><span class="line">	if (phead == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;链表为空\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		SListNode*cur = phead;</span><br><span class="line">		while (--pos)</span><br><span class="line">		&#123;</span><br><span class="line">			if (cur == NULL)</span><br><span class="line">			&#123;</span><br><span class="line">				printf(&quot;位置错误\n&quot;);</span><br><span class="line">			&#125;</span><br><span class="line">			else</span><br><span class="line">			&#123;</span><br><span class="line">				cur = cur-&gt;next;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		cur-&gt;data = x;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>演示效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200722614.png" alt="img"></p>
<p>如果是pos–，那就会先运行一次再修改，也就是说位置会不一样，这里看图：</p>
<p>这里是pos–，可以看到修改的是第二个值</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200722994.png" alt="img"></p>
<p> 同样的代码，这里是–pos，修改的是第一个值，注意一下就好<img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200700016.png" alt="img"></p>
<p>-————</p>
<h2 id="带头双向循环链表"><a href="#带头双向循环链表" class="headerlink" title="带头双向循环链表"></a>带头双向循环链表</h2><p>1、如何开辟新的节点和初始化链表</p>
<p>2、双向链表的打印</p>
<p>3、双向链表尾插</p>
<p>4、双向链表尾删</p>
<p>5、双向链表头插</p>
<p>6、双向链表头删</p>
<p>7、双向链表查找</p>
<p>8、双向链表在pos的前面进行插入</p>
<p>9、双向链表删除pos位置的节点</p>
<p>10、双向链表的销毁</p>
<p>-——-</p>
<p>事前准备：</p>
<p>我们需要一个创建一个结构体类型，并将里面的一些类型重定义名字，如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef int LTDataType;</span><br><span class="line">typedef struct ListNode</span><br><span class="line">&#123;</span><br><span class="line">	LTDataType _data;</span><br><span class="line">	struct ListNode* next;</span><br><span class="line">	struct ListNode* prev;</span><br><span class="line">&#125;ListNode;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>我们还要知道双向带头循环链表是什么样的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200700848.png" alt="img"></p>
<p>-——–</p>
<h3 id="如何开辟新的节点和初始化链表"><a href="#如何开辟新的节点和初始化链表" class="headerlink" title="如何开辟新的节点和初始化链表"></a>如何开辟新的节点和初始化链表</h3><p>我们知道，在链表中尾插，或者头插，你都必须先创建一个结构体，因为是双向带头循环的链表，所以这个链表必须有以下几条：</p>
<p><strong>1、我们存放的内容</strong></p>
<p><strong>2、下一个结构体的地址</strong></p>
<p><strong>3、上一个结构体的地址</strong></p>
<p><strong>4、如果链表仅有表头，那么表头的下一个元素和上一个元素必须指向自己</strong> </p>
<p>了解了条件，我们就来初始化我们链表：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void ListInit(ListNode** pphead)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pphead);</span><br><span class="line">	*pphead = BuyLTNode(0);</span><br><span class="line">	(*pphead)-&gt;next = *pphead;</span><br><span class="line">	(*pphead)-&gt;prev = *pphead;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p> -———-</p>
<p>然后我们来看看如何创建新的节点：</p>
<p>跟单链表一样，我们开辟一块空间，将里面的值改成我们需要插入的值，然后再将其指针置为NULL，返回即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ListNode* BuyListNode(int x)</span><br><span class="line">&#123;</span><br><span class="line">	ListNode* newnode = (ListNode*)malloc(sizeof(ListNode));</span><br><span class="line">	if (newnode == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;malloc false\n&quot;);</span><br><span class="line">		exit(-1);</span><br><span class="line">	&#125;</span><br><span class="line">		newnode-&gt;_data = x;</span><br><span class="line">		newnode-&gt;prev = NULL;</span><br><span class="line">		newnode-&gt;next = NULL;</span><br><span class="line">		return newnode;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>-———–</p>
<p>但是问题来，在初始化函数的时候，我们需不需要传入二级指针呢？</p>
<p>在单向不循环的链表时，我们是传入的链表的地址，因为如果我们要头插或者尾插，有可能会改变我们链表的表头位置</p>
<p>比方说：</p>
<p>ListNode* list &#x3D; NULL;</p>
<p>尾插;</p>
<p>如果是单链表，这里list原本指向的空指针就应该被改成尾插的元素，但是如果我们传一级指针，</p>
<p>形参的改变不会影响形参，list出了尾插的函数它任然还是一个空指针，我们要改变的是链表头的指向的地址，所以我们传二级指针过去。</p>
<p>这里呢？</p>
<p><strong>要改变什么，我们就传什么。</strong></p>
<p>这里需不需要传二级指针过去呢？</p>
<p>我们会不会修改到这个头节点呢？</p>
<p>不会，我们只需要修改的是头节点里面指向下一个元素和上一个元素的地址，我们返回的还是头节点的位置，所以我们可以不用传二级指针过去。</p>
<p>所以我们可以将初始化函数这么写：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ListNode* ListCreate()</span><br><span class="line">&#123;</span><br><span class="line">	ListNode* newnode = BuyListNode(0);</span><br><span class="line">	newnode-&gt;prev = newnode;</span><br><span class="line">	newnode-&gt;next = newnode;</span><br><span class="line">	return newnode;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>我们直接创建一个节点，将里面的数据记为0即可。</p>
<p>-———–</p>
<h3 id="双向链表的打印"><a href="#双向链表的打印" class="headerlink" title="双向链表的打印"></a>双向链表的打印</h3><p>我们如何打印这个链表？</p>
<p>跟单链表一样，单链表我们什么时候停下来？</p>
<p>当遇到空，也就是说链表走完了一遍就可以停下来，那双向带头循环链表呢？</p>
<p>我们从表头的下一个元素走，比方说：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200700941.png" alt="img"></p>
<p>我们让一个指针cur开从表头往后走，每遇到一个值就打印，正因为循环，所以它到 5 的时候会回到表头，也就是 0 的位置，然后我们判断，如果 cur &#x3D;&#x3D; 表头，那我们就可以停下来。</p>
<p> <img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200700559.png" alt="img"></p>
<p> 我们的代码就这么写：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void ListPrint(ListNode* pHead)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	ListNode* cur = pHead-&gt;next;</span><br><span class="line">	while (cur!= pHead)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d-&gt;&quot;, cur-&gt;_data);</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">	printf(&quot;end\n&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>-———</p>
<h3 id="双向链表尾插"><a href="#双向链表尾插" class="headerlink" title="双向链表尾插"></a>双向链表尾插</h3><p>我们知道这是一个双向循环链表，我们尾插的时候还需要注意几点；</p>
<p>1、最后的结构体的下一个指针应该指向表头</p>
<p>2、表头的上一个结构体指针应该指向最后一个结构体</p>
<p>我们尾插的时候，先要找到没修改前的最后一个结构体，然后得到它的地址，再让它的下一个元素指向新的结构体地址，然后再将新结构体的上一个结构体指针指向它，再修改新节点的next，头节点的prev。</p>
<p>代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void ListPushBack(ListNode* pHead, LTDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	ListNode* newnode = BuyListNode(x);</span><br><span class="line">	ListNode* prev = pHead-&gt;prev;</span><br><span class="line">	prev-&gt;next = newnode;</span><br><span class="line">	newnode-&gt;prev = prev;</span><br><span class="line"></span><br><span class="line">	newnode-&gt;next = pHead;</span><br><span class="line">	pHead-&gt;prev = newnode;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>别忘了断言。</p>
<p><strong>具体文字操作解释：</strong></p>
<p><strong>我们先让prev指向头节点的上一个元素，这里正是我们尾节点的位置，然后我们原本尾节点的下一个指针指向我们新节点，我们新节点的上一个指针指向我们原本的尾节点。</strong></p>
<p><strong>我们新节点的下一个指针应该指向表头，也就是pHead，而pHead表头的上一个结构体应该是尾节点，我们同样赋值过去。</strong></p>
<p>-—————</p>
<h3 id="双向链表尾删"><a href="#双向链表尾删" class="headerlink" title="双向链表尾删"></a>双向链表尾删</h3><p>单链表的尾删我们拿两个指针遍历数组找最后一个元素，双向链表就不需要遍历了，因为表头的上一个结构体正是我们的尾节点，我们拿一个指针记录尾戒点的上一个结构体，然后释放尾节点，再将尾节点释放，代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void ListPopBack(ListNode* pHead)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	ListNode* prev = pHead-&gt;prev-&gt;prev;</span><br><span class="line">	ListNode* cur = pHead-&gt;prev;</span><br><span class="line">	if (pHead-&gt;next == cur)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;NULL&quot;);</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		prev-&gt;next = pHead;</span><br><span class="line">		pHead-&gt;prev = prev;</span><br><span class="line">		free(cur);</span><br><span class="line">		cur = NULL;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p><strong>具体文字解释：</strong></p>
<p><strong>两个指针，一个指向尾节点，一个指向尾节点的上一个结构体，因为靠尾节点我们才找得到倒数第二个。</strong></p>
<p><strong>先判断链表有没有值，没有就返回，有就继续，我们让倒数第二个节点的下一个指针指向我们的表头， next &#x3D; pHead，再将表头的上一个结构体指针指向倒数第二个元素，也就是pHead-&gt;prev &#x3D; prev，不需要返回值。</strong></p>
<p><strong>————–</strong></p>
<h3 id="双向链表头插"><a href="#双向链表头插" class="headerlink" title="双向链表头插"></a>双向链表头插</h3><p>就跟单链表的头插一样，只不过我们多加了个prev（链表的上一个结构体指针）而已，多赋一个值即可，但是要注意顺序，别死循环了。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void ListPushFront(ListNode* pHead, LTDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	ListNode* newnode = BuyListNode(x);</span><br><span class="line">	ListNode* next = pHead-&gt;next;</span><br><span class="line">	pHead-&gt;next = newnode;</span><br><span class="line">	newnode-&gt;prev = pHead;</span><br><span class="line">	newnode-&gt;next = next;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p><strong>具体文字解释：</strong></p>
<p><strong>先创建一个指针指向原本第一个结构体，因为要在它之前插入新的结构体，所以地址先存一下</strong></p>
<p><strong>新结构体的下一个结构体指针指向原本的第一个结构体</strong></p>
<p><strong>表头的下一个结构体指针指向新开辟的节点</strong></p>
<p><strong>新开辟的节点的下一个结构体指针指向原本的第一个结构体</strong></p>
<p><strong>—————</strong></p>
<h3 id="双向链表头删"><a href="#双向链表头删" class="headerlink" title="双向链表头删"></a>双向链表头删</h3><p>既然你知道头插，也知道尾删，那头删就已经掌握了大半，看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void ListPopFront(ListNode* pHead)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	if (pHead-&gt;next == pHead)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;NULL\n&quot;);</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line">	ListNode* cur = pHead-&gt;next;</span><br><span class="line">	ListNode* next = cur-&gt;next;</span><br><span class="line">	free(cur);</span><br><span class="line">	cur = NULL;</span><br><span class="line">	pHead-&gt;next = next;</span><br><span class="line">	next-&gt;prev = pHead;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p><strong>具体文字解释：</strong></p>
<p><strong>新创建的结构体指针cur指向原本的第一个结构体，</strong></p>
<p><strong>next指向第二个结构体</strong></p>
<p><strong>释放cur（第一个结构体）</strong></p>
<p><strong>头节点的下一个结构体指针指向next（原本的第二个元素）</strong></p>
<p><strong>next的上一个结构体指针指向头节点</strong></p>
<p><strong>————–</strong></p>
<h3 id="双向链表查找"><a href="#双向链表查找" class="headerlink" title="双向链表查找"></a>双向链表查找</h3><p>跟单链表一样，我们找链表里面的值判断，如果不是就继续走，如果走到了表头，我们就停下。看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ListNode* ListFind(ListNode* pHead, LTDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	if (pHead-&gt;next == pHead)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;NULL\n&quot;);</span><br><span class="line">		return NULL;</span><br><span class="line">	&#125;</span><br><span class="line">	ListNode* cur = pHead-&gt;next;</span><br><span class="line">	while (cur != pHead)</span><br><span class="line">	&#123;</span><br><span class="line">		if (cur-&gt;_data == x)</span><br><span class="line">		&#123;</span><br><span class="line">			return cur;</span><br><span class="line">		&#125;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">	return NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p><strong>具体文字解释：</strong></p>
<p><strong>新创建的节点cur指向表头的下一个结构体</strong></p>
<p><strong>遍历整个链表判断，如果cur不等于pHead，也就是说链表还有元素，就执行</strong></p>
<p><strong>我们比较cur里面的值，是就返回当前结构体的地址，不是就指向下一个结构体</strong></p>
<p><strong>如果遍历完还没找到，那就返回空指针</strong></p>
<p><strong>————-</strong></p>
<h3 id="双向链表在pos的前面进行插入"><a href="#双向链表在pos的前面进行插入" class="headerlink" title="双向链表在pos的前面进行插入"></a><strong>双向链表在pos的前面进行插入</strong></h3><p>创建新的节点，再将这个节点与之前的节点链接起来即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void ListInsert(ListNode* pos, LTDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pos);</span><br><span class="line">	ListNode* prev = pos-&gt;prev;</span><br><span class="line">	ListNode* newnode = BuyListNode(x);</span><br><span class="line"></span><br><span class="line">	newnode-&gt;prev = prev;</span><br><span class="line">	pos-&gt;prev = newnode;</span><br><span class="line">	newnode-&gt;next = pos;</span><br><span class="line">	prev-&gt;next = newnode;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p><strong>具体文字解释：</strong></p>
<p><strong>新创建的节点prev指向我们插入节点的前一个元素</strong></p>
<p><strong>我们先让新节点的上一个元素指向prev</strong></p>
<p><strong>再将插入节点位置的pos的上一个指针指向新节点</strong></p>
<p><strong>新节点的下一个指针指向pos</strong></p>
<p><strong>prev的下一个指针指向新节点</strong></p>
<p><strong>————</strong></p>
<h3 id="删除指定节点"><a href="#删除指定节点" class="headerlink" title="删除指定节点"></a><strong>删除指定节点</strong></h3><p><strong>如果说上面的都能理解，那这是很简单的代码：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void ListErase(ListNode* pos)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pos);</span><br><span class="line">	ListNode* prev = pos-&gt;prev;</span><br><span class="line">	ListNode* next = pos-&gt;next;</span><br><span class="line">	free(pos);</span><br><span class="line">	pos = NULL;</span><br><span class="line">	prev-&gt;next = next;</span><br><span class="line">	next-&gt;prev = prev;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p><strong>文字详细解释：</strong></p>
<p><strong>prev指向删除节点的前一个元素</strong></p>
<p><strong>next指向删除节点的后一个元素</strong></p>
<p><strong>释放删除节点</strong></p>
<p><strong>链接prev和next</strong></p>
<p><strong>————-</strong></p>
<h3 id="销毁链表"><a href="#销毁链表" class="headerlink" title="销毁链表"></a><strong>销毁链表</strong></h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void ListDestory(ListNode* pHead)</span><br><span class="line">&#123;</span><br><span class="line">	ListNode*cur = pHead;</span><br><span class="line">	ListNode*nex = pHead;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">		free(nex);</span><br><span class="line">		nex = cur;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p><strong>跟单链表一样，每次删除一个并保留下一个的地址，如果为空就删完了。</strong></p>
<p><strong>————</strong></p>
<p><strong>这里放上所有代码，有需要可复制：</strong></p>
<p><strong>List.h</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line">typedef int LTDataType;</span><br><span class="line">typedef struct ListNode</span><br><span class="line">&#123;</span><br><span class="line">	LTDataType _data;</span><br><span class="line">	struct ListNode* next;</span><br><span class="line">	struct ListNode* prev;</span><br><span class="line">&#125;ListNode;</span><br><span class="line"></span><br><span class="line">//开辟一个新的节点并返回</span><br><span class="line">ListNode* BuyListNode(int x);</span><br><span class="line"></span><br><span class="line">// 创建返回链表的头结点.</span><br><span class="line">ListNode* ListCreate();</span><br><span class="line">// 双向链表销毁</span><br><span class="line">void ListDestory(ListNode* pHead);</span><br><span class="line">// 双向链表打印</span><br><span class="line">void ListPrint(ListNode* pHead);</span><br><span class="line">// 双向链表尾插</span><br><span class="line">void ListPushBack(ListNode* pHead, LTDataType x);</span><br><span class="line">// 双向链表尾删</span><br><span class="line">void ListPopBack(ListNode* pHead);</span><br><span class="line">// 双向链表头插</span><br><span class="line">void ListPushFront(ListNode* pHead, LTDataType x);</span><br><span class="line">// 双向链表头删</span><br><span class="line">void ListPopFront(ListNode* pHead);</span><br><span class="line">// 双向链表查找</span><br><span class="line">ListNode* ListFind(ListNode* pHead, LTDataType x);</span><br><span class="line">// 双向链表在pos的前面进行插入</span><br><span class="line">void ListInsert(ListNode* pos, LTDataType x);</span><br><span class="line">// 双向链表删除pos位置的节点</span><br><span class="line">void ListErase(ListNode* pos);</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p>List.c</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&quot;Lt.h&quot;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">ListNode* BuyListNode(int x)</span><br><span class="line">&#123;</span><br><span class="line">	ListNode* newnode = (ListNode*)malloc(sizeof(ListNode));</span><br><span class="line">	if (newnode == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;malloc false\n&quot;);</span><br><span class="line">		exit(-1);</span><br><span class="line">	&#125;</span><br><span class="line">		newnode-&gt;_data = x;</span><br><span class="line">		newnode-&gt;prev = NULL;</span><br><span class="line">		newnode-&gt;next = NULL;</span><br><span class="line">		return newnode;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ListNode* ListCreate()</span><br><span class="line">&#123;</span><br><span class="line">	ListNode* newnode = BuyListNode(0);</span><br><span class="line">	newnode-&gt;prev = newnode;</span><br><span class="line">	newnode-&gt;next = newnode;</span><br><span class="line">	return newnode;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void ListPrint(ListNode* pHead)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	ListNode* cur = pHead-&gt;next;</span><br><span class="line">	while (cur!= pHead)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d-&gt;&quot;, cur-&gt;_data);</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">	printf(&quot;end\n&quot;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void ListPushBack(ListNode* pHead, LTDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	ListNode* newnode = BuyListNode(x);</span><br><span class="line">	ListNode* prev = pHead-&gt;prev;</span><br><span class="line">	prev-&gt;next = newnode;</span><br><span class="line">	newnode-&gt;prev = prev;</span><br><span class="line"></span><br><span class="line">	newnode-&gt;next = pHead;</span><br><span class="line">	pHead-&gt;prev = newnode;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void ListPopBack(ListNode* pHead)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	ListNode* prev = pHead-&gt;prev-&gt;prev;</span><br><span class="line">	ListNode* cur = pHead-&gt;prev;</span><br><span class="line">	if (pHead-&gt;next == cur)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;NULL&quot;);</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		prev-&gt;next = pHead;</span><br><span class="line">		pHead-&gt;prev = prev;</span><br><span class="line">		free(cur);</span><br><span class="line">		cur = NULL;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void ListPushFront(ListNode* pHead, LTDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	ListNode* newnode = BuyListNode(x);</span><br><span class="line">	ListNode* next = pHead-&gt;next;</span><br><span class="line">	pHead-&gt;next = newnode;</span><br><span class="line">	newnode-&gt;prev = pHead;</span><br><span class="line">	newnode-&gt;next = next;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line">void ListPopFront(ListNode* pHead)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	if (pHead-&gt;next == pHead)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;NULL\n&quot;);</span><br><span class="line">		return;</span><br><span class="line">	&#125;</span><br><span class="line">	ListNode* cur = pHead-&gt;next;</span><br><span class="line">	ListNode* next = cur-&gt;next;</span><br><span class="line">	free(cur);</span><br><span class="line">	cur = NULL;</span><br><span class="line">	pHead-&gt;next = next;</span><br><span class="line">	next-&gt;prev = pHead;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ListNode* ListFind(ListNode* pHead, LTDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pHead);</span><br><span class="line">	if (pHead-&gt;next == pHead)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;NULL\n&quot;);</span><br><span class="line">		return NULL;</span><br><span class="line">	&#125;</span><br><span class="line">	ListNode* cur = pHead-&gt;next;</span><br><span class="line">	while (cur != pHead)</span><br><span class="line">	&#123;</span><br><span class="line">		if (cur-&gt;_data == x)</span><br><span class="line">		&#123;</span><br><span class="line">			return cur;</span><br><span class="line">		&#125;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">	return NULL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void ListInsert(ListNode* pos, LTDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pos);</span><br><span class="line">	ListNode* prev = pos-&gt;prev;</span><br><span class="line">	ListNode* newnode = BuyListNode(x);</span><br><span class="line"></span><br><span class="line">	newnode-&gt;prev = prev;</span><br><span class="line">	pos-&gt;prev = newnode;</span><br><span class="line">	newnode-&gt;next = pos;</span><br><span class="line">	prev-&gt;next = newnode;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void ListErase(ListNode* pos)</span><br><span class="line">&#123;</span><br><span class="line">	assert(pos);</span><br><span class="line">	ListNode* prev = pos-&gt;prev;</span><br><span class="line">	ListNode* next = pos-&gt;next;</span><br><span class="line">	free(pos);</span><br><span class="line">	pos = NULL;</span><br><span class="line">	prev-&gt;next = next;</span><br><span class="line">	next-&gt;prev = prev;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void ListDestory(ListNode* pHead)</span><br><span class="line">&#123;</span><br><span class="line">	ListNode*cur = pHead;</span><br><span class="line">	ListNode*nex = pHead;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">		free(nex);</span><br><span class="line">		nex = cur;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p><strong>—————</strong> </p>
<p><strong>练手</strong></p>
<p>1.构建单向链表并打印</p>
<p>2.交换链表的首两个元素，尾两个元素</p>
<p>3.单链表求和</p>
<p>4.双链表求和</p>
<h2 id="练手"><a href="#练手" class="headerlink" title="练手"></a>练手</h2><h3 id="1-构建单向链表并打印"><a href="#1-构建单向链表并打印" class="headerlink" title="1.构建单向链表并打印"></a>1.构建单向链表并打印</h3><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200709562.png"></p>
<p><a class="link"   href="https://www.nowcoder.com/practice/95559da7e19c4241b6fa52d997a008c4?tpId=291&tags=&title=&difficulty=0&judgeStatus=0&rp=0&sourceUrl=/exam/oj?tab=%25E8%25AF%25AD%25E6%25B3%2595%25E7%25AF%2587&topicId=291&fromPut=pc_zh_n_yuanlai45_c"  title="牛牛的单向链表_牛客题霸_牛客网 (nowcoder.com)">牛牛的单向链表_牛客题霸_牛客网 (nowcoder.com)<i class="fas fa-external-link-alt"></i></a></p>
<p>非常简单，先创建数组接收数据，然后构建链表开始尾插，最后打印</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line">typedef struct Node</span><br><span class="line">&#123;</span><br><span class="line">	int data;</span><br><span class="line">	struct Node* next;</span><br><span class="line">&#125;Node;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//开辟节点并返回</span><br><span class="line">Node* BuyNode(int x)</span><br><span class="line">&#123;</span><br><span class="line">	Node* node = (Node*)malloc(sizeof(Node));</span><br><span class="line">	node-&gt;data = x;</span><br><span class="line">	node-&gt;next = NULL;</span><br><span class="line"></span><br><span class="line">	return node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//尾插</span><br><span class="line">void PushBack(Node** head, int x)</span><br><span class="line">&#123;</span><br><span class="line">	Node* node = BuyNode(x);</span><br><span class="line">	Node* cur = *head;</span><br><span class="line"></span><br><span class="line">		while (cur-&gt;next)</span><br><span class="line">			cur = cur-&gt;next;</span><br><span class="line">		cur-&gt;next = node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">//打印</span><br><span class="line">void NodePrintf(Node* head)</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = head;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d &quot;, cur-&gt;data);</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	int n;</span><br><span class="line">	scanf(&quot;%d&quot;, &amp;n);</span><br><span class="line">	int* a = (int*)malloc(sizeof(int)*n);</span><br><span class="line">	for (int i = 0; i&lt;n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		scanf(&quot;%d&quot;, &amp;a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	Node* node = (Node*)malloc(sizeof(Node));</span><br><span class="line"></span><br><span class="line">    //初始化...</span><br><span class="line">	node-&gt;data = a[0];</span><br><span class="line">	node-&gt;next = NULL;</span><br><span class="line"></span><br><span class="line">    //依次尾插</span><br><span class="line">	for (int i = 1; i &lt; n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		PushBack(&amp;node, a[i]);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    //打印</span><br><span class="line">	NodePrintf(node);</span><br><span class="line">    free(a);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="2-交换链表的首两个元素，尾两个元素"><a href="#2-交换链表的首两个元素，尾两个元素" class="headerlink" title="2.交换链表的首两个元素，尾两个元素"></a>2.交换链表的首两个元素，尾两个元素</h3><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200709706.png"></p>
<p>代码几乎没变，上来判断有几个元素，如果有两个，就先交换，然后再找尾交换</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line">typedef struct Node</span><br><span class="line">&#123;</span><br><span class="line">	int data;</span><br><span class="line">	struct Node* next;</span><br><span class="line">&#125;Node;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Node* BuyNode(int x)</span><br><span class="line">&#123;</span><br><span class="line">	Node* node = (Node*)malloc(sizeof(Node));</span><br><span class="line">	node-&gt;data = x;</span><br><span class="line">	node-&gt;next = NULL;</span><br><span class="line"></span><br><span class="line">	return node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void PushBack(Node** head, int x)</span><br><span class="line">&#123;</span><br><span class="line">	Node* node = BuyNode(x);</span><br><span class="line">	Node* cur = *head;</span><br><span class="line"></span><br><span class="line">		while (cur-&gt;next)</span><br><span class="line">			cur = cur-&gt;next;</span><br><span class="line">		cur-&gt;next = node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void NodePrintf(Node* head)</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = head;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d &quot;, cur-&gt;data);</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void Reverse(Node* head)</span><br><span class="line">&#123;</span><br><span class="line">    Node* cur = head;</span><br><span class="line">    Node* prev = NULL;</span><br><span class="line"></span><br><span class="line">    //如果cur-&gt;不为空，说明至少有两个元素，交换以下</span><br><span class="line">    if(cur-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        prev = cur;</span><br><span class="line">        cur = cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    int tmp = cur-&gt;data;</span><br><span class="line">    cur-&gt;data = prev-&gt;data;</span><br><span class="line">    prev-&gt;data = tmp;</span><br><span class="line">    </span><br><span class="line">    //如果cur-&gt;next为空，说明它就是最后一个元素</span><br><span class="line">    while(cur-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        prev = cur;</span><br><span class="line">        cur = cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    //交换</span><br><span class="line">    tmp = cur-&gt;data;</span><br><span class="line">    cur-&gt;data = prev-&gt;data;</span><br><span class="line">    prev-&gt;data = tmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	int n;</span><br><span class="line">	scanf(&quot;%d&quot;, &amp;n);</span><br><span class="line">	int* a = (int*)malloc(sizeof(int)*n);</span><br><span class="line">	for (int i = 0; i&lt;n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		scanf(&quot;%d&quot;, &amp;a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	Node* node = (Node*)malloc(sizeof(Node));</span><br><span class="line">	node-&gt;data = a[0];</span><br><span class="line">	node-&gt;next = NULL;</span><br><span class="line">	for (int i = 1; i &lt; n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		PushBack(&amp;node, a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">    Reverse(node);</span><br><span class="line">	NodePrintf(node);</span><br><span class="line">    free(a);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="3-单链表求和"><a href="#3-单链表求和" class="headerlink" title="3.单链表求和"></a>3.单链表求和</h3><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200709066.png"><strong>编辑</strong></p>
<p>代码几乎一样，遍历求和即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line">typedef struct Node</span><br><span class="line">&#123;</span><br><span class="line">	int data;</span><br><span class="line">	struct Node* next;</span><br><span class="line">&#125;Node;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Node* BuyNode(int x)</span><br><span class="line">&#123;</span><br><span class="line">	Node* node = (Node*)malloc(sizeof(Node));</span><br><span class="line">	node-&gt;data = x;</span><br><span class="line">	node-&gt;next = NULL;</span><br><span class="line"></span><br><span class="line">	return node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void PushBack(Node** head, int x)</span><br><span class="line">&#123;</span><br><span class="line">	Node* node = BuyNode(x);</span><br><span class="line">	Node* cur = *head;</span><br><span class="line"></span><br><span class="line">		while (cur-&gt;next)</span><br><span class="line">			cur = cur-&gt;next;</span><br><span class="line">		cur-&gt;next = node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void NodePrintf(Node* head)</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = head;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d &quot;, cur-&gt;data);</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void Reverse(Node* head)</span><br><span class="line">&#123;</span><br><span class="line">    Node* cur = head;</span><br><span class="line">    Node* prev = NULL;</span><br><span class="line">    if(cur-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        prev = cur;</span><br><span class="line">        cur = cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    int tmp = cur-&gt;data;</span><br><span class="line">    cur-&gt;data = prev-&gt;data;</span><br><span class="line">    prev-&gt;data = tmp;</span><br><span class="line">    </span><br><span class="line">    while(cur-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        prev = cur;</span><br><span class="line">        cur = cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    tmp = cur-&gt;data;</span><br><span class="line">    cur-&gt;data = prev-&gt;data;</span><br><span class="line">    prev-&gt;data = tmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	int n;</span><br><span class="line">	scanf(&quot;%d&quot;, &amp;n);</span><br><span class="line">	int* a = (int*)malloc(sizeof(int)*n);</span><br><span class="line">	for (int i = 0; i&lt;n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		scanf(&quot;%d&quot;, &amp;a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	Node* node = (Node*)malloc(sizeof(Node));</span><br><span class="line">	node-&gt;data = a[0];</span><br><span class="line">	node-&gt;next = NULL;</span><br><span class="line">	for (int i = 1; i &lt; n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		PushBack(&amp;node, a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	int sum = 0;</span><br><span class="line">    while(node)</span><br><span class="line">    &#123;</span><br><span class="line">        sum+=node-&gt;data;</span><br><span class="line">        node=node-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    printf(&quot;%d &quot;,sum);</span><br><span class="line">    free(a);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="4-双链表求和"><a href="#4-双链表求和" class="headerlink" title="4.双链表求和"></a>4.双链表求和</h3><p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200709378.png"></p>
<p>先将数组里面加起来，然后插入节点遍历打印即可</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line">typedef struct Node</span><br><span class="line">&#123;</span><br><span class="line">	int data;</span><br><span class="line">	struct Node* next;</span><br><span class="line">&#125;Node;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Node* BuyNode(int x)</span><br><span class="line">&#123;</span><br><span class="line">	Node* node = (Node*)malloc(sizeof(Node));</span><br><span class="line">	node-&gt;data = x;</span><br><span class="line">	node-&gt;next = NULL;</span><br><span class="line"></span><br><span class="line">	return node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void PushBack(Node** head, int x)</span><br><span class="line">&#123;</span><br><span class="line">	Node* node = BuyNode(x);</span><br><span class="line">	Node* cur = *head;</span><br><span class="line"></span><br><span class="line">		while (cur-&gt;next)</span><br><span class="line">			cur = cur-&gt;next;</span><br><span class="line">		cur-&gt;next = node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void NodePrintf(Node* head)</span><br><span class="line">&#123;</span><br><span class="line">	Node* cur = head;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d &quot;, cur-&gt;data);</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void Reverse(Node* head)</span><br><span class="line">&#123;</span><br><span class="line">    Node* cur = head;</span><br><span class="line">    Node* prev = NULL;</span><br><span class="line">    if(cur-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        prev = cur;</span><br><span class="line">        cur = cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    int tmp = cur-&gt;data;</span><br><span class="line">    cur-&gt;data = prev-&gt;data;</span><br><span class="line">    prev-&gt;data = tmp;</span><br><span class="line">    </span><br><span class="line">    while(cur-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        prev = cur;</span><br><span class="line">        cur = cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    tmp = cur-&gt;data;</span><br><span class="line">    cur-&gt;data = prev-&gt;data;</span><br><span class="line">    prev-&gt;data = tmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">	int n;</span><br><span class="line">	scanf(&quot;%d&quot;, &amp;n);</span><br><span class="line">	int* a = (int*)malloc(sizeof(int)*n);</span><br><span class="line">    int i;</span><br><span class="line">	for (i = 0; i&lt;n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		scanf(&quot;%d&quot;, &amp;a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">    for(i = 0;i&lt;n;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        int tmp;</span><br><span class="line">        scanf(&quot;%d&quot;,&amp;tmp);</span><br><span class="line">        a[i] += tmp;</span><br><span class="line">    &#125;</span><br><span class="line">	Node* node = (Node*)malloc(sizeof(Node));</span><br><span class="line">	node-&gt;data = a[0];</span><br><span class="line">	node-&gt;next = NULL;</span><br><span class="line">	for (int i = 1; i &lt; n; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		PushBack(&amp;node, a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	NodePrintf(node);</span><br><span class="line">    free(a);</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>正式</strong></p>
<p>**1. 删除链表中等于给定值 val 的所有节点。 **</p>
<p><strong>2. 反转一个单链表。</strong></p>
<p><strong>3. 给定一个带有头结点 head 的非空单链表，返回链表的中间结点。如果有两个中间结点，则返回第二个 中间结点。</strong></p>
<p>**4. 输入一个链表，输出该链表中倒数第k个结点。 **</p>
<p><strong>5. 将两个有序链表合并为一个新的有序链表并返回。新链表是通过拼接给定的两个链表的所有节点组成 的。</strong></p>
<p><strong>6. 编写代码，以给定值x为基准将链表分割成两部分，所有小于x的结点排在大于或等于x的结点之前 。</strong></p>
<p><strong>7. 链表的回文结构。</strong></p>
<p><strong>8. 输入两个链表，找出它们的第一个公共结点。</strong></p>
<p><strong>9. 给定一个链表，判断链表中是否有环。</strong></p>
<p><strong>10. 给定一个链表，返回链表开始入环的第一个节点。 如果链表无环，则返回 NULL。</strong></p>
<p><strong>11.链表相加</strong></p>
<p><strong>————</strong></p>
<h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a><strong>正文</strong></h2><h3 id="1-删除链表中等于给定值-val-的所有节点。"><a href="#1-删除链表中等于给定值-val-的所有节点。" class="headerlink" title="1. 删除链表中等于给定值 val 的所有节点。"></a>1. 删除链表中等于给定值 val 的所有节点。</h3><p><a class="link"   href="https://leetcode-cn.com/problems/remove-linked-list-elements/description/"  title="203. 移除链表元素 - 力扣（LeetCode） (leetcode-cn.com)">203. 移除链表元素 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200709573.png"></p>
<p>这题有两种思路：</p>
<p>第一种，我们再开辟一个数组，如果原数组的值不等于我们要找的值，我们就开始链接下去，如果等于要找的值，我们就跳过找下一个元素，最后将最后一个元素的next &#x3D; NULL。</p>
<p>这种思路较为简单，但是因为需要再开辟一个链表，所以它会有额外的空间消耗。</p>
<p>第二种，我们拿两个指针，一个快一步，一个慢一点，比如示例1中，我的快指针指向了6，而我的慢指针还指向的是2，那我们就直接让2的next指向3，然后释放6的空间，这样的好处就是不用在开辟空间，下面我们看代码实现：</p>
<p>第一种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode* removeElements(struct ListNode* head, int val)</span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode* newhead,*newtail;</span><br><span class="line">    newhead = newtail = (struct ListNode*)malloc(sizeof(struct ListNode));</span><br><span class="line">    struct ListNode* cur = head;</span><br><span class="line">    while(cur)</span><br><span class="line">    &#123;</span><br><span class="line">        if(cur-&gt;val != val)</span><br><span class="line">        &#123;</span><br><span class="line">            newtail-&gt;next = cur;</span><br><span class="line">            newtail = newtail-&gt;next;</span><br><span class="line">            cur = cur-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        else</span><br><span class="line">        &#123;</span><br><span class="line">            cur = cur-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    newtail-&gt;next = NULL;</span><br><span class="line">    return newhead-&gt;next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第二种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode* removeElements(struct ListNode* head, int val)</span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode* cur = head;</span><br><span class="line">    struct ListNode* hhead = NULL;</span><br><span class="line">        while(cur)</span><br><span class="line">    &#123;</span><br><span class="line">        if(cur-&gt;val != val)</span><br><span class="line">        &#123;</span><br><span class="line">            hhead= cur;</span><br><span class="line">            cur= cur-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        else</span><br><span class="line">        &#123;</span><br><span class="line">                struct ListNode* next = cur-&gt;next;</span><br><span class="line">                if(hhead == NULL)</span><br><span class="line">                &#123;</span><br><span class="line">                    free(cur);</span><br><span class="line">                    head = next;</span><br><span class="line">                    cur= next;</span><br><span class="line">                &#125;</span><br><span class="line">                else</span><br><span class="line">                &#123;</span><br><span class="line">                free(cur);</span><br><span class="line">                hhead-&gt;next = next;</span><br><span class="line">                cur=next;</span><br><span class="line">                &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="2-反转一个单链表。"><a href="#2-反转一个单链表。" class="headerlink" title="2. 反转一个单链表。"></a><strong>2. 反转一个单链表。</strong></h3><p><a class="link"   href="https://leetcode-cn.com/problems/reverse-linked-list/description/"  title="206. 反转链表 - 力扣（LeetCode） (leetcode-cn.com)">206. 反转链表 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200710852.png"></p>
<p><strong>同样两种方法：</strong></p>
<p><strong>第一种，我们用三个指针，第一个指针指向空，第二个指针指向第一个指针，第三个指针指向第二个指针的下一个元素，以图举例，第一个指针为NULL，第二个指针是1，第三个指针是2，我们让1指向NULL,然后2指向1，然后我们将2的值给1，3的值给2，让它们指向下一个元素。</strong></p>
<p>第二种，类似头插，我们让一个指针为NULL，然后我们让数组的第一个元素头插进去，第二个也头插进去，第三个也头插进去…返回最后一个头插的地址即可，看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode* reverseList(struct ListNode* head)</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    struct ListNode* n1,*n2,*n3;</span><br><span class="line">    if(head == NULL)</span><br><span class="line">    &#123;</span><br><span class="line">        return NULL;</span><br><span class="line">    &#125;</span><br><span class="line">    n1 = NULL;</span><br><span class="line">    n2 = head;</span><br><span class="line">    n3 = head-&gt;next;</span><br><span class="line">    while(n2)</span><br><span class="line">    &#123;</span><br><span class="line">        n2-&gt;next = n1;</span><br><span class="line">        n1 = n2;</span><br><span class="line">        n2 = n3;</span><br><span class="line">        if(n3)</span><br><span class="line">        &#123;</span><br><span class="line">            n3 = n3-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return n1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第二种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode* reverseList(struct ListNode* head)</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    struct ListNode* cur = head;</span><br><span class="line">    struct ListNode* newhead =NULL;</span><br><span class="line">    while(cur)</span><br><span class="line">    &#123;</span><br><span class="line">            struct ListNode* next = cur-&gt;next;</span><br><span class="line">            cur-&gt;next = newhead;</span><br><span class="line">            newhead = cur;</span><br><span class="line">            cur=cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    return newhead;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="3-给定一个带有头结点-head-的非空单链表，返回链表的中间结点。如果有两个中间结点，则返回第二个-中间结点。"><a href="#3-给定一个带有头结点-head-的非空单链表，返回链表的中间结点。如果有两个中间结点，则返回第二个-中间结点。" class="headerlink" title="3. 给定一个带有头结点 head 的非空单链表，返回链表的中间结点。如果有两个中间结点，则返回第二个 中间结点。"></a><strong>3. 给定一个带有头结点 head 的非空单链表，返回链表的中间结点。如果有两个中间结点，则返回第二个 中间结点。</strong></h3><p><a class="link"   href="https://leetcode-cn.com/problems/middle-of-the-linked-list/description/"  title="876. 链表的中间结点 - 力扣（LeetCode） (leetcode-cn.com)">876. 链表的中间结点 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200710508.png"></p>
<p>同样，两种办法：</p>
<p>第一种，比较简单粗暴，我们先计算链表长度，然后除2，让头指针走这么多步再返回。</p>
<p>第二种，我们两个指针，一个快一个慢，快的一次走两步，慢的一次走一步，当快的指针走向尾部的时候慢的指针才走一半，我们返回慢的指针即可。</p>
<p>看代码实现：</p>
<p>第一种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode* middleNode(struct ListNode* head)</span><br><span class="line">&#123;</span><br><span class="line">    int count = 0;</span><br><span class="line">    struct ListNode* cur = head;</span><br><span class="line">    while(cur)</span><br><span class="line">    &#123;</span><br><span class="line">        count++;</span><br><span class="line">        cur = cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    count /= 2;</span><br><span class="line">    cur = head;</span><br><span class="line">    while(count--)</span><br><span class="line">    &#123;</span><br><span class="line">        cur = cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    return cur;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第二种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode* middleNode(struct ListNode* head)</span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode* show = head;</span><br><span class="line">    struct ListNode* fast = head;</span><br><span class="line">    while(fast&amp;&amp;fast-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        show = show-&gt;next;</span><br><span class="line">        fast = fast-&gt;next-&gt;next;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    return show;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="4、输入一个链表，输出该链表中倒数第k个结点。"><a href="#4、输入一个链表，输出该链表中倒数第k个结点。" class="headerlink" title="4、输入一个链表，输出该链表中倒数第k个结点。"></a>4、输入一个链表，输出该链表中倒数第k个结点。</h3><p><a class="link"   href="https://leetcode-cn.com/problems/lian-biao-zhong-dao-shu-di-kge-jie-dian-lcof/"  title="剑指 Offer 22. 链表中倒数第k个节点 - 力扣（LeetCode） (leetcode-cn.com)">剑指 Offer 22. 链表中倒数第k个节点 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200710397.png"></p>
<p>这题解法就比较单一，我们还是用双指针的思路，倒数第k个节点，我们就让快的指针先走k次，然后慢的指针再开始走，最后返回慢指针即可，看代码实现：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode* FindKthToTail(struct ListNode* pListHead, int k ) </span><br><span class="line">&#123;</span><br><span class="line">    // write code here</span><br><span class="line">    struct ListNode* cur = pListHead;</span><br><span class="line">    while(k-- != 0)</span><br><span class="line">    &#123;</span><br><span class="line">        if(cur == NULL)</span><br><span class="line">        &#123;</span><br><span class="line">            return NULL;</span><br><span class="line">        &#125;</span><br><span class="line">        cur=cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    struct ListNode* prev = pListHead;</span><br><span class="line">    while(cur)</span><br><span class="line">    &#123;</span><br><span class="line">        cur = cur-&gt;next;</span><br><span class="line">        prev = prev-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    return prev;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<ol start="5">
<li>将两个有序链表合并为一个新的有序链表并返回。新链表是通过拼接给定的两个链表的所有节点组成的。</li>
</ol>
<p><a class="link"   href="https://leetcode-cn.com/problems/merge-two-sorted-lists/description/"  title="21. 合并两个有序链表 - 力扣（LeetCode） (leetcode-cn.com)">21. 合并两个有序链表 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200710100.png"></p>
<p>如果常见有做过一道合并两个数组的题的话，那这里就可以用到其中的思路，就是我们先比较两个的值，把值小的先放进去，然后再让那个往下走一步，这里也可以用这种思路：</p>
<p>第一个指针指向list1 - 1，第二个指向list2 - 1，我们比较，一样大，我们就随便放一个进去，比如我这里放list2，然后list 2 &#x3D; list2-&gt;next;然后我们在再比一次，如果其中一个为空了，我们就可以停下，然后判断，如果list1还有，我们就让最后的指针指向list1，如果list2还有，就指向list2，如果都没有，就置为空。</p>
<p>看代码实现：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode* mergeTwoLists(struct ListNode* list1, struct ListNode* list2)</span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode cur = &#123;0&#125;;</span><br><span class="line">    struct ListNode*  prev = &amp;cur;</span><br><span class="line">    while(list1&amp;&amp;list2)</span><br><span class="line">    &#123;</span><br><span class="line">        if(list1-&gt;val&gt;list2-&gt;val)</span><br><span class="line">        &#123;</span><br><span class="line">            prev-&gt;next = list2;</span><br><span class="line">            list2 = list2-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        else</span><br><span class="line">        &#123;</span><br><span class="line">            prev-&gt;next = list1;</span><br><span class="line">            list1=list1-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        prev=prev-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    if(list1)</span><br><span class="line">    &#123;</span><br><span class="line">        prev-&gt;next = list1;</span><br><span class="line">    &#125;</span><br><span class="line">    else if(list2)</span><br><span class="line">    &#123;</span><br><span class="line">        prev-&gt;next=list2;</span><br><span class="line">    &#125;</span><br><span class="line">    else</span><br><span class="line">    &#123;</span><br><span class="line">        prev-&gt;next = NULL;</span><br><span class="line">    &#125;</span><br><span class="line">    return cur.next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="6-编写代码，以给定值x为基准将链表分割成两部分，所有小于x的结点排在大于或等于x的结点之前-。"><a href="#6-编写代码，以给定值x为基准将链表分割成两部分，所有小于x的结点排在大于或等于x的结点之前-。" class="headerlink" title="6. 编写代码，以给定值x为基准将链表分割成两部分，所有小于x的结点排在大于或等于x的结点之前 。"></a>6. 编写代码，以给定值x为基准将链表分割成两部分，所有小于x的结点排在大于或等于x的结点之前 。</h3><p><a class="link"   href="https://www.nowcoder.com/practice/0e27e0b064de4eacac178676ef9c9d70?tpId=8&&tqId=11004&rp=2&ru=/activity/oj&qru=/ta/cracking-the-coding-interview/question-ranking"  title="链表分割_牛客题霸_牛客网 (nowcoder.com)">链表分割_牛客题霸_牛客网 (nowcoder.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200710488.png"></p>
<p>这题就比较阴间，为什么这么说，不仅因为它不支持c，而且它还没有给图，全靠我们想，这里给大家画图理解一下：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200711837.png"></p>
<p>这是它提供给我们的链表，我们要返回的是这样的：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200711258.png"></p>
<p>这里就说思路，我们拿两个指针开始遍历数组，第一个指针放的都是小于x的，第二个放的都是大于x的，然后我们再把它们连起来，具体看代码实现：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class Partition &#123;</span><br><span class="line">public:</span><br><span class="line">    ListNode* partition(ListNode* pHead, int x) &#123;</span><br><span class="line">        // write code here</span><br><span class="line">         ListNode*lessHead,*lessEnd,*greatHead,*greatEnd;</span><br><span class="line">        lessHead = lessEnd = (ListNode*)malloc(sizeof(ListNode));</span><br><span class="line">        greatHead = greatEnd = (ListNode*)malloc(sizeof(ListNode));</span><br><span class="line">        lessEnd-&gt;next = greatEnd-&gt;next=NULL;</span><br><span class="line">        ListNode*cur = pHead;</span><br><span class="line">        while(cur)</span><br><span class="line">        &#123;</span><br><span class="line">                if(cur-&gt;val &lt; x)</span><br><span class="line">                &#123;</span><br><span class="line">                    lessEnd-&gt;next  = cur;</span><br><span class="line">                    lessEnd = lessEnd-&gt;next;</span><br><span class="line">                &#125;</span><br><span class="line">                else</span><br><span class="line">                &#123;</span><br><span class="line">                    greatEnd-&gt;next  = cur;</span><br><span class="line">                    greatEnd = greatEnd-&gt;next;</span><br><span class="line">                &#125;</span><br><span class="line">            cur = cur-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        lessEnd-&gt;next = greatHead-&gt;next;</span><br><span class="line">                greatEnd-&gt;next = NULL;</span><br><span class="line">        ListNode*list = lessHead-&gt;next;</span><br><span class="line">        free(lessHead);</span><br><span class="line">        free(greatHead);</span><br><span class="line">        return list;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>上来我们开辟四个指针，其中两个是我们头，两个是我们的尾，我们需要尾去走，然后记住头来链接。</p>
<hr>
<h3 id="7-链表的回文结构。"><a href="#7-链表的回文结构。" class="headerlink" title="7. 链表的回文结构。"></a>7. 链表的回文结构。</h3><p><a class="link"   href="https://www.nowcoder.com/practice/d281619e4b3e4a60a2cc66ea32855bfa?tpId=49&&tqId=29370&rp=1&ru=/activity/oj&qru=/ta/2016test/question-ranking"  title="链表的回文结构_牛客题霸_牛客网 (nowcoder.com)">链表的回文结构_牛客题霸_牛客网 (nowcoder.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200711083.png"></p>
<p>两种办法：</p>
<p>第一种比较搓，我们开辟一个数组，数组里面是我们链表的val，然后比较数组的值再返回，因为靠开辟的是数组，所以实现起来相当简单,但是实际上我们是取巧了的，不建议适用这种办法，但是如果实在做不出来，还是可以用的，不管多搓，能做出来就算成功。</p>
<p>第二种，我们先找到链表中间的位置，然后我们再将链表后面的位置反转，然后再一个一个比较，因为反转链表我们之前写过，所以我直接拿来用了，具体实现看代码：</p>
<p>第一种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class PalindromeList &#123;</span><br><span class="line">public:</span><br><span class="line">    bool chkPalindrome(ListNode* A) &#123;</span><br><span class="line">        // write code here</span><br><span class="line">        int arr[900];</span><br><span class="line">        int count = 0;</span><br><span class="line">        ListNode* cur = A;</span><br><span class="line">        while(cur)</span><br><span class="line">        &#123;</span><br><span class="line">            arr[count++] = cur-&gt;val;</span><br><span class="line">            cur = cur-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        int left=0,right=count-1;</span><br><span class="line">        while(left&lt;right)</span><br><span class="line">        &#123;</span><br><span class="line">            if(arr[left]== arr[right])</span><br><span class="line">            &#123;</span><br><span class="line">                left++;</span><br><span class="line">                right--;</span><br><span class="line">            &#125;</span><br><span class="line">            else</span><br><span class="line">            &#123;</span><br><span class="line">                return false;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>第二种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class PalindromeList &#123;</span><br><span class="line">public:</span><br><span class="line">    bool chkPalindrome(ListNode* A) &#123;</span><br><span class="line">        // write code here</span><br><span class="line">        ListNode* show,*fast;</span><br><span class="line">        show = fast = A;</span><br><span class="line">        while(fast&amp;&amp;fast-&gt;next)</span><br><span class="line">        &#123;</span><br><span class="line">            show = show-&gt;next;</span><br><span class="line">            fast = fast-&gt;next-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        show = reverseList(show);</span><br><span class="line">        ListNode* head = A;</span><br><span class="line">        while(show&amp;&amp;show-&gt;next)</span><br><span class="line">        &#123;</span><br><span class="line">            if(head-&gt;val == show-&gt;val)</span><br><span class="line">            &#123;</span><br><span class="line">                head = head-&gt;next;</span><br><span class="line">                show = show-&gt;next;</span><br><span class="line">            &#125;</span><br><span class="line">            else</span><br><span class="line">            &#123;</span><br><span class="line">                return false;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这里得提一下，就是反转链表只能用三指针，因为你用第二种相当于在函数内部开辟了一个新节点返回节点的头，但出了函数节点头就找不到了，所以会报错，注意下就好。</p>
<hr>
<h3 id="8-输入两个链表，找出它们的第一个公共结点。"><a href="#8-输入两个链表，找出它们的第一个公共结点。" class="headerlink" title="8.输入两个链表，找出它们的第一个公共结点。"></a>8.输入两个链表，找出它们的第一个公共结点。</h3><p><a class="link"   href="https://leetcode-cn.com/problems/intersection-of-two-linked-lists/description/"  title="160. 相交链表 - 力扣（LeetCode） (leetcode-cn.com)">160. 相交链表 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200711497.png"></p>
<p>两种思路：</p>
<p>第一种时间复杂度是O（n^2），大家看到这个时间复杂度就应该知道是什么办法了，就是拿a的每一个元素都跟b比，知道链表结束。</p>
<p>第二种：我们先看两个链表长度，我们让长的先走，这里我们看图，b很明显长一些，我们就让b先走，让它从b2开始比，然后同时往后走，碰到相交就停下返回，看代码实现：</p>
<p>第一种：</p>
<p>不推荐使用，看着简单，但链表一长太耗时</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode *getIntersectionNode(struct ListNode *headA, struct ListNode *headB)</span><br><span class="line">&#123;</span><br><span class="line">    for(struct ListNode* a = headA;a!= NULL;a = a-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        for(struct ListNode* b = headB;b!= NULL;b = b-&gt;next)</span><br><span class="line">        &#123;</span><br><span class="line">            if(a == b)</span><br><span class="line">            &#123;</span><br><span class="line">                return a;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第二种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode *getIntersectionNode(struct ListNode *headA, struct ListNode *headB)</span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode* cura = headA;</span><br><span class="line">    struct ListNode* curb = headB;</span><br><span class="line">    int count=0,count1=0,count2=0;</span><br><span class="line">    while(cura)</span><br><span class="line">    &#123;</span><br><span class="line">        count1++;</span><br><span class="line">        cura = cura-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    while(curb)</span><br><span class="line">    &#123;</span><br><span class="line">        count2++;</span><br><span class="line">        curb=curb-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    if(count1&lt;count2)</span><br><span class="line">    &#123;</span><br><span class="line">        curb = headA;</span><br><span class="line">        cura = headB; </span><br><span class="line">    &#125;</span><br><span class="line">    else</span><br><span class="line">    &#123;</span><br><span class="line">        cura = headA;</span><br><span class="line">        curb = headB;</span><br><span class="line">    &#125;</span><br><span class="line">    count = abs(count1-count2);</span><br><span class="line">    while(count--)</span><br><span class="line">    &#123;</span><br><span class="line">        if(cura)</span><br><span class="line">        &#123;</span><br><span class="line">            cura = cura-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    while(cura&amp;&amp;curb)</span><br><span class="line">    &#123;</span><br><span class="line">        if(cura == curb)</span><br><span class="line">        &#123;</span><br><span class="line">            return cura;</span><br><span class="line">        &#125;</span><br><span class="line">        else</span><br><span class="line">        &#123;</span><br><span class="line">            cura = cura-&gt;next;</span><br><span class="line">            curb = curb-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="9-给定一个链表，判断链表中是否有环。"><a href="#9-给定一个链表，判断链表中是否有环。" class="headerlink" title="9. 给定一个链表，判断链表中是否有环。"></a><strong>9. 给定一个链表，判断链表中是否有环。</strong></h3><p><a class="link"   href="https://leetcode-cn.com/problems/linked-list-cycle/description/"  title="141. 环形链表 - 力扣（LeetCode） (leetcode-cn.com)">141. 环形链表 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200711098.png"></p>
<p>很简单的一题，我们两个指针遍历链表，慢的一次一步，快的一次两步，什么时候慢的等于快的，就说明有环，如果没有环就直接返回，看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool hasCycle(struct ListNode *head) </span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode* show,*fast;</span><br><span class="line">    fast = show = head;</span><br><span class="line">    while(fast &amp;&amp;fast-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        show=show-&gt;next;</span><br><span class="line">        fast = fast-&gt;next-&gt;next;</span><br><span class="line">        if(fast == show)</span><br><span class="line">        &#123;</span><br><span class="line">            return true;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;    </span><br><span class="line">    return false;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="10-给定一个链表，返回链表开始入环的第一个节点。-如果链表无环，则返回-NULL"><a href="#10-给定一个链表，返回链表开始入环的第一个节点。-如果链表无环，则返回-NULL" class="headerlink" title="10. 给定一个链表，返回链表开始入环的第一个节点。 如果链表无环，则返回 NULL"></a>10. 给定一个链表，返回链表开始入环的第一个节点。 如果链表无环，则返回 NULL</h3><p><a class="link"   href="https://leetcode-cn.com/problems/linked-list-cycle-ii/description/"  title="142. 环形链表 II - 力扣（LeetCode） (leetcode-cn.com)">142. 环形链表 II - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200711590.png"></p>
<p>这题更是重量级，这里说两种思路：</p>
<p>第一种，看图：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200711647.png"></p>
<p>这是我们已有的，我们拿两个快慢指针：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200712251.png"></p>
<p>同时从这里开始走，当show和fast相遇的时候，它们肯定在环里面，我们假设是这里：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200712827.png"></p>
<p>我们让这个地方断开，让这里指向空，然后我们从下一个位置开始走，然后head开始走，当它们重合的时候，就是环的入口，看图；</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200712658.png"></p>
<p>第二种：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200712191.png"></p>
<p>我们可以看到，show走的路程是L+X，而fast走的路程是 L + NC+C-X</p>
<p>N是圈数，简答来说，就是，当一个指针从fast和show相遇的地方走，另一个指针从head开始走，它们一定会相遇在环的入口处。</p>
<p>我推荐使用第一种，因为简单易懂，第二种理解起来比较麻烦，但如果问起，这也是一种解决办法。</p>
<p>看代码实现：</p>
<p>第一种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode *getIntersectionNode(struct ListNode *headA, struct ListNode *headB)</span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode* cura = headA;</span><br><span class="line">    struct ListNode* curb = headB;</span><br><span class="line">    int count=0,count1=0,count2=0;</span><br><span class="line">    while(cura)</span><br><span class="line">    &#123;</span><br><span class="line">        count1++;</span><br><span class="line">        cura = cura-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    while(curb)</span><br><span class="line">    &#123;</span><br><span class="line">        count2++;</span><br><span class="line">        curb=curb-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    if(count1&lt;count2)</span><br><span class="line">    &#123;</span><br><span class="line">        curb = headA;</span><br><span class="line">        cura = headB; </span><br><span class="line">    &#125;</span><br><span class="line">    else</span><br><span class="line">    &#123;</span><br><span class="line">        cura = headA;</span><br><span class="line">        curb = headB;</span><br><span class="line">    &#125;</span><br><span class="line">    count = abs(count1-count2);</span><br><span class="line">    while(count--)</span><br><span class="line">    &#123;</span><br><span class="line">        if(cura)</span><br><span class="line">        &#123;</span><br><span class="line">            cura = cura-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    while(cura&amp;&amp;curb)</span><br><span class="line">    &#123;</span><br><span class="line">        if(cura == curb)</span><br><span class="line">        &#123;</span><br><span class="line">            return cura;</span><br><span class="line">        &#125;</span><br><span class="line">        else</span><br><span class="line">        &#123;</span><br><span class="line">            cura = cura-&gt;next;</span><br><span class="line">            curb = curb-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return NULL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">struct ListNode *detectCycle(struct ListNode* head) </span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode* show,*fast;</span><br><span class="line">    struct ListNode* cur = NULL;</span><br><span class="line">    show = fast = head;</span><br><span class="line">    while(fast &amp;&amp;fast-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        show = show-&gt;next;</span><br><span class="line">        fast = fast-&gt;next-&gt;next;</span><br><span class="line">        if(fast == show)</span><br><span class="line">        &#123;</span><br><span class="line">            cur = fast-&gt;next;</span><br><span class="line">            fast-&gt;next = NULL;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    struct ListNode* prev = head;</span><br><span class="line">   struct ListNode*pos =  getIntersectionNode(prev,cur);</span><br><span class="line">   if(pos)</span><br><span class="line">   &#123;</span><br><span class="line">       return pos;</span><br><span class="line">   &#125;</span><br><span class="line">   else</span><br><span class="line">   return NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>第二种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct ListNode *detectCycle(struct ListNode* head) </span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode* show,*fast;</span><br><span class="line">    struct ListNode* cur = NULL;</span><br><span class="line">    show = fast = head;</span><br><span class="line">    while(fast &amp;&amp;fast-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        show = show-&gt;next;</span><br><span class="line">        fast = fast-&gt;next-&gt;next;</span><br><span class="line">        if(show == fast)</span><br><span class="line">        &#123;</span><br><span class="line">            struct ListNode* meet = show;</span><br><span class="line">            struct ListNode* start = head;</span><br><span class="line">            while(meet != start)</span><br><span class="line">            &#123;</span><br><span class="line">                start = start-&gt;next;</span><br><span class="line">                meet = meet-&gt;next;</span><br><span class="line">            &#125;</span><br><span class="line">            return meet;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id=""><a href="#" class="headerlink" title=""></a></h3><p>11.两数相加</p>
<p><a class="link"   href="https://leetcode-cn.com/problems/add-two-numbers/"  title="2. 两数相加 - 力扣（LeetCode） (leetcode-cn.com)">2. 两数相加 - 力扣（LeetCode） (leetcode-cn.com)<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200712181.png"></p>
<p>先说思路，我们拿两个指针分别开始走这两个链表，再拿两个值记录这里面的值，再拿一个值记录进位，把这两个链表的值相加，如果这个值大于9，我们就放0，然后让计数+1，下一次计算相加的时候就加1，如果下一次的值不大于9，那加完就让计数变为0</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//插入节点函数</span><br><span class="line">struct ListNode* BuyNoude(int x)</span><br><span class="line">&#123;</span><br><span class="line">    struct ListNode* newnode = (struct ListNode*)malloc(sizeof(struct ListNode));</span><br><span class="line">    newnode-&gt;val = x;</span><br><span class="line">    newnode-&gt;next = NULL;</span><br><span class="line">    return newnode;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">struct ListNode* addTwoNumbers(struct ListNode* l1, struct ListNode* l2)</span><br><span class="line">&#123;</span><br><span class="line">    //两个指针开始对应两个链表</span><br><span class="line">    struct ListNode* cur1 = l1;</span><br><span class="line">    struct ListNode* cur2 = l2;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    //要返回的新链表</span><br><span class="line">    struct ListNode* cur,*head;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    //计算进位</span><br><span class="line">    int count = 0;</span><br><span class="line"></span><br><span class="line">    //带哨兵位，其中一个往后走，另一个在头节点不变，等会就返回这个</span><br><span class="line">    cur = head = (struct ListNode*)malloc(sizeof(struct ListNode));</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    //判断循环条件，如果两个链表都为空才停下</span><br><span class="line">    while(cur1 || cur2)</span><br><span class="line">    &#123;</span><br><span class="line">        //计算里面的值，如果是空链表就算0</span><br><span class="line">        int c1 = cur1==NULL?0:cur1-&gt;val;</span><br><span class="line">        int c2 = cur2==NULL?0:cur2-&gt;val;</span><br><span class="line"></span><br><span class="line">        //相加，count不进位情况为0，所以不影响</span><br><span class="line">        int sum = c1+c2+count;</span><br><span class="line">        //比较</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        if(sum&lt;10)</span><br><span class="line">        &#123;</span><br><span class="line">            //尾插一个</span><br><span class="line">            cur-&gt;next = BuyNoude(sum%10);</span><br><span class="line">            cur = cur-&gt;next;</span><br><span class="line">            //将计数变为0</span><br><span class="line">            count = 0;</span><br><span class="line">        &#125;</span><br><span class="line">        else</span><br><span class="line">        &#123;</span><br><span class="line">            //如果大于9，我们就尾插一个0进去，表示进位，下一次计算就多加count/10，也就是1</span><br><span class="line">            cur-&gt;next = BuyNoude(sum-10);</span><br><span class="line">            cur = cur-&gt;next;</span><br><span class="line">            count = sum/10;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        //让两个链表往下走</span><br><span class="line">        if(cur1!=NULL)//l1不为空，链接到下一节点</span><br><span class="line">            cur1=cur1-&gt;next;</span><br><span class="line">        if(cur2!=NULL)//l2不为空，链接到下一节点</span><br><span class="line">            cur2=cur2-&gt;next;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    //判断，此时count如果还有，就意味着没进位，所以再链接一下</span><br><span class="line">    if(count != 0)</span><br><span class="line">    &#123;</span><br><span class="line">        cur-&gt;next = BuyNoude(count);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    return head-&gt;next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<p>希望以上的题能对你学习起到帮助，如果有兴趣可以点开我的分类看更多，谢谢您的观看。</p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>队列</title>
    <url>/2022/03/26/%E9%98%9F%E5%88%97/</url>
    <content><![CDATA[<p>在学习队列前，我们还是先来看看什么是队列：</p>
<h2 id="队列的概念及结构"><a href="#队列的概念及结构" class="headerlink" title="**队列的概念及结构 **"></a>**队列的概念及结构 **</h2><p>队列：只允许在一端进行插入数据操作，在另一端进行删除数据操作的特殊线性表，队列具有先进先出FIFO(First In First Out)</p>
<p>入队列：进行插入操作的一端称为**队尾 **</p>
<p>出队列：进行删除操作的一端称为**队头 **</p>
<p>队列就好像去做核酸排队，先去排队先做完。</p>
<h2 id="队列的实现"><a href="#队列的实现" class="headerlink" title="队列的实现"></a><strong>队列的实现</strong></h2><p><strong>头文件</strong></p>
<p>队列也可以数组和链表的结构实现，使用链表的结构实现更优一些，因为如果使用数组的结构，出队列在数组头上出数据，效率会比较低。</p>
<p>这里我们就用链表来实现一下队列。</p>
<p>先看看我们要实现的几个功能：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 初始化队列</span><br><span class="line">void QueueInit(Queue* q); </span><br><span class="line">// 队尾入队列</span><br><span class="line">void QueuePush(Queue* q, QDataType data); </span><br><span class="line">// 队头出队列</span><br><span class="line">void QueuePop(Queue* q); </span><br><span class="line">// 获取队列头部元素</span><br><span class="line">QDataType QueueFront(Queue* q); </span><br><span class="line">// 获取队列队尾元素</span><br><span class="line">QDataType QueueBack(Queue* q); </span><br><span class="line">// 获取队列中有效元素个数</span><br><span class="line">int QueueSize(Queue* q); </span><br><span class="line">// 检测队列是否为空，如果为空返回非零结果，如果非空返回0 </span><br><span class="line">int QueueEmpty(Queue* q); </span><br><span class="line">// 销毁队列</span><br><span class="line">void QueueDestroy(Queue* q);</span><br></pre></td></tr></table></figure>



<p>我们先创建头文件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">#include&lt;stdbool.h&gt;</span><br></pre></td></tr></table></figure>



<p>既然是用链表实现，那我们就要先创建一个链表，就用单链表就好了：</p>
<p>这里的数据类型是自定义的，因为如果定义为整型那之后修改也会比较麻烦，所以我们拿tepedef重定义，以后如果要修改的话，直接在这里修改就好：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef int QDataType;</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef struct QNode</span><br><span class="line">&#123;</span><br><span class="line">	QDataType data;</span><br><span class="line">	struct queue* next;</span><br><span class="line">&#125;QNode;</span><br></pre></td></tr></table></figure>

<p><img src="/%22%E7%82%B9%E5%87%BB%E5%B9%B6%E6%8B%96%E6%8B%BD%E4%BB%A5%E7%A7%BB%E5%8A%A8%22"></p>
<p>为了方便我们的插入和删除，我们最好再另外建立一个结构体：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef struct Queue</span><br><span class="line">&#123;</span><br><span class="line">	QNode* head;</span><br><span class="line">	QNode* tail;</span><br><span class="line">&#125;Queue;</span><br></pre></td></tr></table></figure>



<p>这两个指针帮我们找到队列的头和尾，方便我们之后的操作，然后我们声明一下我们要实现的函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 初始化队列</span><br><span class="line">void QueueInit(Queue* q);</span><br><span class="line">// 队尾入队列</span><br><span class="line">void QueuePush(Queue* q, QDataType data);</span><br><span class="line">// 队头出队列</span><br><span class="line">void QueuePop(Queue* q);</span><br><span class="line">// 获取队列头部元素</span><br><span class="line">QDataType QueueFront(Queue* q);</span><br><span class="line">// 获取队列队尾元素</span><br><span class="line">QDataType QueueBack(Queue* q);</span><br><span class="line">// 获取队列中有效元素个数</span><br><span class="line">int QueueSize(Queue* q);</span><br><span class="line">// 检测队列是否为空，如果为空返回非零结果，如果非空返回0 </span><br><span class="line">int QueueEmpty(Queue* q);</span><br><span class="line">// 销毁队列</span><br><span class="line">void QueueDestroy(Queue* q);</span><br></pre></td></tr></table></figure>



<p>然后我们就可以开始实现我们的代码了，我们来到.c文件。</p>
<hr>
<p><strong>.c文件</strong></p>
<p>在使用前，我们先包含一下我们刚刚声明的头文件</p>
<h2 id="初始化："><a href="#初始化：" class="headerlink" title="初始化："></a>初始化：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void QueueInit(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">    //都置为空指针即可</span><br><span class="line">	q-&gt;head = q-&gt;tail = NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="队尾入队列："><a href="#队尾入队列：" class="headerlink" title="队尾入队列："></a><strong>队尾入队列：</strong></h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void QueuePush(Queue* q, QDataType data)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">    //创建新的节点</span><br><span class="line">	QNode* newnode = (QNode*)malloc(sizeof(QNode));</span><br><span class="line">    //判断开辟成功</span><br><span class="line">	if (newnode == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;malloc fail\n&quot;);</span><br><span class="line">		exit(-1);</span><br><span class="line">	&#125;</span><br><span class="line">    //放数据到里面</span><br><span class="line">	newnode-&gt;data = data;</span><br><span class="line">	newnode-&gt;next = NULL;</span><br><span class="line">    //判断队列是否为刚初始化为空的状态</span><br><span class="line">	if (NULL == q-&gt;tail)</span><br><span class="line">	&#123;</span><br><span class="line">		q-&gt;head = q-&gt;tail = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">    //不是就尾部链接起来</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		q-&gt;tail-&gt;next = newnode;</span><br><span class="line">		q-&gt;tail = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="队头出队列："><a href="#队头出队列：" class="headerlink" title="队头出队列："></a>队头出队列：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void QueuePop(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">    //判断队列里面是否还有数据</span><br><span class="line">	assert(q-&gt;head&amp;&amp;q-&gt;tail);</span><br><span class="line">    //如果只有一个数据，那释放这一个节点，然后将头尾都置为空指针</span><br><span class="line">	if (q-&gt;head-&gt;next == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		free(q-&gt;head);</span><br><span class="line">		q-&gt;head = q-&gt;tail = NULL;</span><br><span class="line">	&#125;</span><br><span class="line">    //如果有多个数据，那先用next保存头节点的下一个位置，然后释放头节点，再让头节点等于next</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		QNode* next = q-&gt;head-&gt;next;</span><br><span class="line">		free(q-&gt;head);</span><br><span class="line">		q-&gt;head = next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="获取头、尾数据："><a href="#获取头、尾数据：" class="headerlink" title="获取头、尾数据："></a>获取头、尾数据：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">QDataType QueueFront(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">    //判断队列有数据</span><br><span class="line">	assert(q-&gt;head);</span><br><span class="line">    //直接返回即可</span><br><span class="line">	return q-&gt;head-&gt;data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">    //同理</span><br><span class="line">QDataType QueueBack(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	assert(q-&gt;tail);</span><br><span class="line">	return q-&gt;tail-&gt;data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="看队列的有效数据个数："><a href="#看队列的有效数据个数：" class="headerlink" title="看队列的有效数据个数："></a>看队列的有效数据个数：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int QueueSize(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">    //从头开始遍历</span><br><span class="line">	QNode* cur = q-&gt;head;</span><br><span class="line">	int count = 0;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		count++;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">	return count;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="看队列是否为空："><a href="#看队列是否为空：" class="headerlink" title="看队列是否为空："></a>看队列是否为空：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bool QueueEmpty(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">    //直接拿判断值返回即可</span><br><span class="line">	return q-&gt;head == NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="销毁队列："><a href="#销毁队列：" class="headerlink" title="销毁队列："></a>销毁队列：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void QueueDestroy(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	QNode* cur = q-&gt;head;</span><br><span class="line">    //每次删一个，保留下一个的位置直到删完</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		QNode* next = cur-&gt;next;</span><br><span class="line">		free(cur);</span><br><span class="line">		cur = next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>我们来看看效果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200920597.png"></p>
<p>这里把全部代码都放出来方便复制使用：</p>
<p>Queue.h</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">#include&lt;stdbool.h&gt;</span><br><span class="line"></span><br><span class="line">typedef int QDataType;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">typedef struct QNode</span><br><span class="line">&#123;</span><br><span class="line">	QDataType data;</span><br><span class="line">	struct queue* next;</span><br><span class="line">&#125;QNode;</span><br><span class="line"></span><br><span class="line">typedef struct Queue</span><br><span class="line">&#123;</span><br><span class="line">	QNode* head;</span><br><span class="line">	QNode* tail;</span><br><span class="line">&#125;Queue;</span><br><span class="line"></span><br><span class="line">// 初始化队列</span><br><span class="line">void QueueInit(Queue* q);</span><br><span class="line">// 队尾入队列</span><br><span class="line">void QueuePush(Queue* q, QDataType data);</span><br><span class="line">// 队头出队列</span><br><span class="line">void QueuePop(Queue* q);</span><br><span class="line">// 获取队列头部元素</span><br><span class="line">QDataType QueueFront(Queue* q);</span><br><span class="line">// 获取队列队尾元素</span><br><span class="line">QDataType QueueBack(Queue* q);</span><br><span class="line">// 获取队列中有效元素个数</span><br><span class="line">int QueueSize(Queue* q);</span><br><span class="line">// 检测队列是否为空，如果为空返回非零结果，如果非空返回0 </span><br><span class="line">bool QueueEmpty(Queue* q);</span><br><span class="line">// 销毁队列</span><br><span class="line">void QueueDestroy(Queue* q);</span><br></pre></td></tr></table></figure>



<p>Queue.c</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&quot;queque.h&quot;</span><br><span class="line"></span><br><span class="line">void QueueInit(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	q-&gt;head = q-&gt;tail = NULL;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void QueuePush(Queue* q, QDataType data)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	QNode* newnode = (QNode*)malloc(sizeof(QNode));</span><br><span class="line">	if (newnode == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;malloc fail\n&quot;);</span><br><span class="line">		exit(-1);</span><br><span class="line">	&#125;</span><br><span class="line">	newnode-&gt;data = data;</span><br><span class="line">	newnode-&gt;next = NULL;</span><br><span class="line">	if (NULL == q-&gt;tail)</span><br><span class="line">	&#123;</span><br><span class="line">		q-&gt;head = q-&gt;tail = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		q-&gt;tail-&gt;next = newnode;</span><br><span class="line">		q-&gt;tail = newnode;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void QueuePop(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	assert(q-&gt;head&amp;&amp;q-&gt;tail);</span><br><span class="line">	if (q-&gt;head-&gt;next == NULL)</span><br><span class="line">	&#123;</span><br><span class="line">		free(q-&gt;head);</span><br><span class="line">		q-&gt;head = q-&gt;tail = NULL;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;</span><br><span class="line">		QNode* next = q-&gt;head-&gt;next;</span><br><span class="line">		free(q-&gt;head);</span><br><span class="line">		q-&gt;head = next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">QDataType QueueFront(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	assert(q-&gt;head);</span><br><span class="line">	return q-&gt;head-&gt;data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">QDataType QueueBack(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	assert(q-&gt;tail);</span><br><span class="line">	return q-&gt;tail-&gt;data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int QueueSize(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	QNode* cur = q-&gt;head;</span><br><span class="line">	int count = 0;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		count++;</span><br><span class="line">		cur = cur-&gt;next;</span><br><span class="line">	&#125;</span><br><span class="line">	return count;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bool QueueEmpty(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line"></span><br><span class="line">	return q-&gt;head == NULL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void QueueDestroy(Queue* q)</span><br><span class="line">&#123;</span><br><span class="line">	assert(q);</span><br><span class="line">	QNode* cur = q-&gt;head;</span><br><span class="line">	while (cur)</span><br><span class="line">	&#123;</span><br><span class="line">		QNode* next = cur-&gt;next;</span><br><span class="line">		free(cur);</span><br><span class="line">		cur = next;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>主程序就不放出来了，函数都在上面，有需要直接调用即可，</p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>顺序表</title>
    <url>/2022/03/10/%E9%A1%BA%E5%BA%8F%E8%A1%A8/</url>
    <content><![CDATA[<h2 id="顺序表"><a href="#顺序表" class="headerlink" title="顺序表"></a>顺序表</h2><p>我们先来看看顺序表的概念和结构：</p>
<p>顺序表是用一段物理地址连续的存储单元依次存储数据元素的线性结构，一般情况下采用数组存储。在数组上完成数据的增删查改。</p>
<p>顺序表一般可以分为：</p>
<ol>
<li>静态顺序表：使用定长数组存储元素。</li>
</ol>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200650973.png"></p>
<p>这里我们就直接开辟了一个数组，但因为是一次性开辟的，就会有几种情况：</p>
<p>第一种，数组开小了，要放的数据放不下，比如我们要放100个数据，而数组只有10个位置。</p>
<p>第二种，数组开大了，要放的数组没那么多，我只要放10个数据，但你有100个位置，那不就浪费了吗。</p>
<p>所以这里就有第二种顺序表，要用到realloc函数。</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200650762.png"></p>
<p>我们先malloc一个数组，这个数组不用很大，我们先用着，然后我们那size记录要放的数据，然后capicty记录还能放多少进去，如果size &#x3D; capicity，那就说明我们放满了，放满了那就要扩容。</p>
<p>接口实现</p>
<p>静态顺序表只适用于确定知道需要存多少数据的场景。静态顺序表的定长数组导致N定大了，空间开多了浪费，开少了不够用。所以现实中基本都是使用动态顺序表，根据需要动态的分配空间大小，所以下面我们实现动态顺序表。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define MAX 100</span><br><span class="line">//静态</span><br><span class="line">//struct SeqList        </span><br><span class="line">//&#123;</span><br><span class="line">//	int arr[MAX];</span><br><span class="line">//	int size;// 记录存储个数</span><br><span class="line">//&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">struct SeqList</span><br><span class="line">&#123;</span><br><span class="line">	int *a;</span><br><span class="line">	int size;</span><br><span class="line">	int capaity;//数组容量</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>那我们学数据结构，就是为了对这些顺序表进行管理，那管理，通常就是增删查改，那我们就来写函数来模拟实现一下。</p>
<p>在这些操作前，我们得先初始化，在操作完，我们也要销毁，所以我们写两个函数：</p>
<p>初始化：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListInit(SeqList s)</span><br><span class="line">&#123;</span><br><span class="line">	s.a = NULL;</span><br><span class="line">	s.size = 0;</span><br><span class="line">	s.capicity = 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当我们写出这样一个函数的时候，我们就要注意这里是传了一个形参过去，我们知道，对形参的操作不会影响到实参，所以我们要传址过去：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListInit(SeqList* psl)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	psl-&gt;a = NULL;</span><br><span class="line">	psl-&gt;size = 0;</span><br><span class="line">	psl-&gt;capicity = 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>程序调试：<img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200651069.png"></p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200651965.png"></p>
<p>可以看到我们成功将s初始化，那我们下一步就先不销毁，先写另外一个函数：</p>
<p>尾插</p>
<p>当我们想实现一个尾插的时候，就是在开辟空间的最后一个位置放进我们要输入的值，而我们的size就可以帮我们实现这个功能，当初始化完成的时候，我们size &#x3D; 0，这个时候尾插就相当于在size 的位置放进一个数字，然后size++，第二次尾插同样是在size的位置放进数字，所以我们直接找到空间里面下标为size的那个地址将数字放进去即可。</p>
<p>但是还有一个问题是，如果我们size &#x3D;&#x3D; capicity，那就说明我们没位置了，要扩容了，这里我们还得写一个扩容的函数，我们先判断capicity &#x3D;&#x3D; 0，如果它等于0，说明这里一点空间没有，我们给它一个常数值好方便下一次扩容，如果它是一个常数值，那我们可以直接乘2，然后我们开辟新capicity*sizeof（SeqList）的空间再赋给tmp，这里是为了判断开辟成功与否，如果开辟失败，它会返回一个空指针，那我们就可以判断如果为空指针就打印开辟失败，如果开辟成功我们再给a赋值，具体代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListPushBack(SeqList* psl, int x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	if (psl-&gt;capicity == psl-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		size_t newcapicity = psl-&gt;capicity == 0 ? 4 : psl-&gt;capicity * 2;</span><br><span class="line">		SLDataType* tmp = (SLDataType*)realloc(psl-&gt;a, sizeof(SLDataType)*newcapicity);</span><br><span class="line">		if (tmp == NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			printf(&quot;realloc fail\n&quot;);</span><br><span class="line">			exit(-1);</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			psl-&gt;a = tmp;</span><br><span class="line">			psl-&gt;capicity = newcapicity;</span><br><span class="line">		&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	psl-&gt;a[psl-&gt;size] = x;</span><br><span class="line">	psl-&gt;size++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>同时，为了方便我们看效果，我们可以写一个打印函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListPrint(SeqList* psl)</span><br><span class="line">&#123;</span><br><span class="line">	for (int i = 0; i &lt; psl-&gt;size; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d &quot;, psl-&gt;a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>调试结果：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200651486.png"></p>
<p>那我们再来写一个尾删：</p>
<p>当我们插入数据成功的时候，size++指向的是下一个空间的位置，我们尾删是不是将最后一个元素的位置重新赋值，然后size–就可以，但如果我们重新赋的那个值就是原本的值呢？那不就相当于没有赋值吗，所以我们可以直接将size–。</p>
<p>代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListPopBack(SeqList *psl)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	psl-&gt;size -= 1;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200651263.png"></p>
<p>但是我们还有一个要注意的问题是，如果我们尾删删多了，size减到负数去了，那就出现问题了，所以我们最好加一个判断条件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListPopBack(SeqList *psl)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	if (psl-&gt;size &gt; 0)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;size -= 1;</span><br><span class="line">	&#125;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200651086.png"></p>
<p>然后我们就来看看头插和头删：</p>
<p>我们这里发现如果头插也需要检查一下数组容量，但如果将刚刚检查容量的代码也复制一遍就不太美观，所以我们可以封装一个函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListCheckCapacity(SeqList* psl)</span><br><span class="line">&#123;</span><br><span class="line">	if (psl-&gt;capicity == psl-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		size_t newcapicity = psl-&gt;capicity == 0 ? 4 : psl-&gt;capicity * 2;</span><br><span class="line">		SLDataType* tmp = (SLDataType*)realloc(psl-&gt;a, sizeof(SLDataType)*newcapicity);</span><br><span class="line">		if (tmp == NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			printf(&quot;realloc fail\n&quot;);</span><br><span class="line">			exit(-1);</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			psl-&gt;a = tmp;</span><br><span class="line">			psl-&gt;capicity = newcapicity;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200651962.png"></p>
<p>我们让end指向数组的最后一个数字，然后我们在end+1的位置放上end数据，将数组整体右移一位，然后在原本1的位置放上数字：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202208200651053.png">当end指向1的时候，就说明它已经将数组移动完了，我们就只用放进数字即可：</p>
<p>代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListPushFront(SeqList* psl, int x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	SeqListCheckCapacity(psl);</span><br><span class="line">	int end = psl-&gt;size - 1;</span><br><span class="line">	while (end &gt;= 0)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;a[end + 1] = psl-&gt;a[end];</span><br><span class="line">		end--;</span><br><span class="line">	&#125;</span><br><span class="line">	psl-&gt;a[0] = x;</span><br><span class="line">	psl-&gt;size++;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>头删我们只需要将头插右移的数字再移回来，我们用begin指向第一个元素，然后begin &#x3D; begin+1，直到begin &gt;size就停下，代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListPopFront(SeqList *psl)</span><br><span class="line">&#123;</span><br><span class="line">	int begin = 0;</span><br><span class="line">	while (begin &lt;= psl-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;a[begin] = psl-&gt;a[begin + 1];</span><br><span class="line">		begin++;</span><br><span class="line">	&#125;</span><br><span class="line">	psl-&gt;size--;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>到这一步，头插头删，尾插尾删都已经解决了，那我们就来考虑一下中间入和中间删除的情况。</p>
<p>中间插入跟头插很像，只不过我们头插是将数组全部内容右移，而中间插入只需要将目标后面的数字右移，这里我们看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListInsert(SeqList* psl, int pos, SLDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	if (pos &gt; psl-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;error，crossed\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	SeqListCheckCapacity(psl);</span><br><span class="line">	int end = psl-&gt;size;</span><br><span class="line">	while (end &gt; pos)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;a[end + 1] = psl-&gt;a[end];</span><br><span class="line">		end--;</span><br><span class="line">	&#125;</span><br><span class="line">	psl-&gt;a[pos] = x;</span><br><span class="line">	psl-&gt;size++;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>至于中间删除，那也跟头删没什么区别，还是看代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void SeqListErase(SeqList* psl, size_t pos)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	int begin = pos+1;</span><br><span class="line">	while (begin &lt;=psl-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;a[begin- 1] = psl-&gt;a[begin];</span><br><span class="line">		begin++;</span><br><span class="line">	&#125;</span><br><span class="line">	psl-&gt;size--;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>至于查找，那就更简单了，我们遍历整个数组，然后找到的返回下标即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int SeqListFind(SeqList* psl, SLDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	for (int i = 0; i &lt; psl-&gt;size; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		if (psl-&gt;a[i] == x)</span><br><span class="line">		&#123;</span><br><span class="line">			return i;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	return -1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>还有一个是修改功能，这里有两种：</p>
<p>第一，是直接在数组里面找下标，然后通过下标修改</p>
<p>第二，是在数组里面找数字，然后修改数字</p>
<p>这两种都挺好实现的，这里放上代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//通过下标修改</span><br><span class="line">void SeqListModfy(SeqList* psl, int pos, int x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	assert(pos &lt; psl-&gt;size);</span><br><span class="line">	psl-&gt;a[pos] = x;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//通过找数字修改</span><br><span class="line">void SeqListModfy(SeqList* psl, int pos, int x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	assert(pos &lt; psl-&gt;size);</span><br><span class="line">	int poss = SeqListFind(psl, pos);</span><br><span class="line">	psl-&gt;a[poss] = x;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后，给大家放上全部代码：</p>
<p>SeqList.h</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#pragma once</span><br><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//顺序表要求数据从0开始，依次存储</span><br><span class="line">#define MAX 100</span><br><span class="line">typedef int SLDataType;</span><br><span class="line">//静态</span><br><span class="line">//struct SeqList        </span><br><span class="line">//&#123;</span><br><span class="line">//	int arr[MAX];</span><br><span class="line">//	int size;// 记录存储个数</span><br><span class="line">//&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">typedef struct SeqList</span><br><span class="line">&#123;</span><br><span class="line">	SLDataType *a;</span><br><span class="line">	int size;</span><br><span class="line">	int capicity;</span><br><span class="line">&#125;SeqList;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void SeqListPrint(SeqList* psl);</span><br><span class="line">void SeqListCheckCapacity(SeqList* psl);</span><br><span class="line"></span><br><span class="line">void SeqListInit(SeqList *psl);</span><br><span class="line">void SeqListDestroy(SeqList *psl);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void SeqListPushBack(SeqList* psl,int x);//尾插</span><br><span class="line">void SeqListPopBack(SeqList *psl);  // 尾删</span><br><span class="line"></span><br><span class="line">void SeqListPushFront(SeqList* psl, int x);//头插</span><br><span class="line">void SeqListPopFront(SeqList *psl);  // 头删</span><br><span class="line"></span><br><span class="line">void SeqListInsert(SeqList* psl, int pos, SLDataType x);//在指定位置插入</span><br><span class="line">void SeqListErase(SeqList* psl, size_t pos);//删除指定位置元素</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">int SeqListFind(SeqList* psl, SLDataType x);//查找元素</span><br><span class="line">void SeqListModfy(SeqList* psl, int pos, SLDataType x);//修改元素```-</span><br></pre></td></tr></table></figure>

<hr>
<p>SeqList.c</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&quot;SeqList.h&quot;</span><br><span class="line"></span><br><span class="line">void SeqListPrint(SeqList* psl)</span><br><span class="line">&#123;</span><br><span class="line">	for (int i = 0; i &lt; psl-&gt;size; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;%d &quot;, psl-&gt;a[i]);</span><br><span class="line">	&#125;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SeqListCheckCapacity(SeqList* psl)</span><br><span class="line">&#123;</span><br><span class="line">	if (psl-&gt;capicity == psl-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		size_t newcapicity = psl-&gt;capicity == 0 ? 4 : psl-&gt;capicity * 2;</span><br><span class="line">		SLDataType* tmp = (SLDataType*)realloc(psl-&gt;a, sizeof(SLDataType)*newcapicity);</span><br><span class="line">		if (tmp == NULL)</span><br><span class="line">		&#123;</span><br><span class="line">			printf(&quot;realloc fail\n&quot;);</span><br><span class="line">			exit(-1);</span><br><span class="line">		&#125;</span><br><span class="line">		else</span><br><span class="line">		&#123;</span><br><span class="line">			psl-&gt;a = tmp;</span><br><span class="line">			psl-&gt;capicity = newcapicity;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void SeqListInit(SeqList* psl)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	psl-&gt;a = NULL;</span><br><span class="line">	psl-&gt;size = 0;</span><br><span class="line">	psl-&gt;capicity = 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SeqListPushBack(SeqList* psl, int x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	SeqListCheckCapacity(psl);</span><br><span class="line">	psl-&gt;a[psl-&gt;size] = x;</span><br><span class="line">	psl-&gt;size++;</span><br><span class="line">&#125;</span><br><span class="line">	</span><br><span class="line">void SeqListPopBack(SeqList *psl)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	if (psl-&gt;size &gt; 0)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;size -= 1;</span><br><span class="line">	&#125;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SeqListPushFront(SeqList* psl, int x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	SeqListCheckCapacity(psl);</span><br><span class="line">	int end = psl-&gt;size - 1;</span><br><span class="line">	while (end &gt;= 0)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;a[end + 1] = psl-&gt;a[end];</span><br><span class="line">		end--;</span><br><span class="line">	&#125;</span><br><span class="line">	psl-&gt;a[0] = x;</span><br><span class="line">	psl-&gt;size++;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SeqListPopFront(SeqList *psl)</span><br><span class="line">&#123;</span><br><span class="line">	int begin = 0;</span><br><span class="line">	while (begin &lt;= psl-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;a[begin] = psl-&gt;a[begin + 1];</span><br><span class="line">		begin++;</span><br><span class="line">	&#125;</span><br><span class="line">	psl-&gt;size--;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SeqListInsert(SeqList* psl, int pos, SLDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	if (pos &gt; psl-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		printf(&quot;error，crossed\n&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">	SeqListCheckCapacity(psl);</span><br><span class="line">	int end = psl-&gt;size;</span><br><span class="line">	while (end &gt; pos)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;a[end + 1] = psl-&gt;a[end];</span><br><span class="line">		end--;</span><br><span class="line">	&#125;</span><br><span class="line">	psl-&gt;a[pos] = x;</span><br><span class="line">	psl-&gt;size++;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SeqListErase(SeqList* psl, size_t pos)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	int begin = pos+1;</span><br><span class="line">	while (begin &lt;=psl-&gt;size)</span><br><span class="line">	&#123;</span><br><span class="line">		psl-&gt;a[begin- 1] = psl-&gt;a[begin];</span><br><span class="line">		begin++;</span><br><span class="line">	&#125;</span><br><span class="line">	psl-&gt;size--;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int SeqListFind(SeqList* psl, SLDataType x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	for (int i = 0; i &lt; psl-&gt;size; i++)</span><br><span class="line">	&#123;</span><br><span class="line">		if (psl-&gt;a[i] == x)</span><br><span class="line">		&#123;</span><br><span class="line">			return i;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	return -1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void SeqListModfy(SeqList* psl, int pos, int x)</span><br><span class="line">&#123;</span><br><span class="line">	assert(psl);</span><br><span class="line">	assert(pos &lt; psl-&gt;size);</span><br><span class="line">	int poss = SeqListFind(psl, pos);</span><br><span class="line">	psl-&gt;a[poss] = x;</span><br><span class="line">	return;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>主程序就不放出来了，因为没什么好说的，所有用到的函数都在上面，要用直接调用即可，感谢您的观看，我们下次链表再见。</p>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
  </entry>
  <entry>
    <title>【甜点】重新排列数组</title>
    <url>/2022/09/02/LeetCode1470/</url>
    <content><![CDATA[<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209022113446.png" alt="image-20220902211340161"></p>
<p>简单到我不知道该说什么。</p>
<hr>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">shuffle</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; nums, <span class="type">int</span> n)</span> </span>&#123;</span><br><span class="line">        vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line">        <span class="type">int</span> p1 = <span class="number">0</span>,p2 = n;</span><br><span class="line">        <span class="keyword">while</span>(p1&lt;n)</span><br><span class="line">        &#123;</span><br><span class="line">            v.<span class="built_in">push_back</span>(nums[p1++]);</span><br><span class="line">            v.<span class="built_in">push_back</span>(nums[p2++]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> v;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>每日练习</category>
      </categories>
  </entry>
  <entry>
    <title>【剑指 Offer II】 和为 k 的子数组</title>
    <url>/2022/09/03/%E3%80%90%E5%89%91%E6%8C%87%20Offer%20II%E3%80%91%E5%92%8C%E4%B8%BA%20k%20%E7%9A%84%E5%AD%90%E6%95%B0%E7%BB%84/</url>
    <content><![CDATA[<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209030918242.png" alt="image-20220903091818313"></p>
<hr>
<p>两种办法，第一种是典中典的暴力，要我说这种题如果暴力能做出来那才叫离谱，这里给一个示例：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209030920416.png" alt="image-20220903092047531"></p>
<p>全是 0，1，简直阴间。</p>
<h1 id="暴力"><a href="#暴力" class="headerlink" title="暴力"></a>暴力</h1><p>这里还是把暴力的办法写一下，每找到一个位置就开始往前或者往后 +，一直判断值</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">subarraySum</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; nums, <span class="type">int</span> k)</span> </span>&#123;</span><br><span class="line">        <span class="type">int</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>;i&lt;nums.<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> sum = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j = i;j&lt;nums.<span class="built_in">size</span>();j++)</span><br><span class="line">            &#123;</span><br><span class="line">                sum+=nums[j];</span><br><span class="line">                <span class="keyword">if</span>(sum == k)</span><br><span class="line">                &#123;</span><br><span class="line">                    count++;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>别用，超时。</p>
<hr>
<h1 id="前缀和-map"><a href="#前缀和-map" class="headerlink" title="前缀和+map"></a>前缀和+map</h1><p>这个办法很有意思，先来看看 LeetCode 官方的解释：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209030923484.png" alt="image-20220903092348669"></p>
<p>它说的可能很复杂，我解释一下。</p>
<p>假设数组是：1，2，3</p>
<p>此时我们逻辑走起来，map&lt;int,int&gt; 里面要存放一个值，&lt;0,1&gt;，为什么要存这个，因为我们找的值是前缀和 - k</p>
<p>举个例子，这里我要找的是 3 ，那我的前缀和就是，0，1，3，6。</p>
<p>此时逻辑开始走，现在在前缀和里面开始走，0 - 3 &#x3D; -3，那我就要在 map 里面找 -3 有没有，如果没有，说明这个位置肯定不会是符合 k 的区间，于是我在 map 里面加一个 &lt;-3，1&gt;。</p>
<p>继续往下走，当我拿到这个 3 的时候，我就在 map 里面找 3 - 3 &#x3D; 0，这里就说明为什么了要有 &lt;0，1&gt;，因为只有这样你之后找的时候这里才不会出现明明等于 k 但是没算上。</p>
<p>此时计数就要 ++。</p>
<p>然后我们把 3 加到 map 里面 &lt;3，1&gt;。</p>
<p>继续往后走，6 - 3 &#x3D; 3，此时再去 map 里面找 3 ，找到了就继续++。</p>
<p>如果上面逻辑听懂了，那之后的代码你也能看懂。</p>
<p> 不过这里要注意的是，计数不是单纯的每次 ++，而是 + 你要找的值后面的那个计数</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209030946189.png" alt="image-20220903094632187"></p>
<p>比方说这样，一开始的 map 是 &lt;0，1&gt;。</p>
<p>1 - 0 &#x3D; 1，没找到，就是在 map 后面多加一个 &lt;1,1&gt;。</p>
<p>然后重点就来了，现在它到了 -1，-1+1 &#x3D; 0，然后它去找，找到了这个 &lt;0，1&gt;，然后 count ++，但是下一个又是 0，0 - 0 &#x3D; 0，它再去找 0 的时候，&lt;0，1&gt;，它再++，变成 2 ，因为当前缀和 等于 tar 的时候，说明上一个位置到这一个位置（1，-，0）是满足条件的，而这个位置本身也是满足条件的，因为有&lt;0，2&gt;说明当 tar 为 0 的时候有两个位置都满足条件，所以不能只加 1，而是要加 对于的位置数。</p>
<p>看代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">subarraySum</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; nums, <span class="type">int</span> k)</span> </span>&#123;</span><br><span class="line">        map&lt;<span class="type">int</span>,<span class="type">int</span>&gt; m;</span><br><span class="line">        m[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">        <span class="type">int</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> n = nums.<span class="built_in">size</span>();</span><br><span class="line">        <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">v</span><span class="params">(n+<span class="number">1</span>,<span class="number">0</span>)</span></span>;  <span class="comment">//前缀和</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">1</span>;i&lt;=n;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            v[i] = v[i<span class="number">-1</span>]+nums[i<span class="number">-1</span>];</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">1</span>;i&lt;=n;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(m.<span class="built_in">find</span>(v[i]-k) != m.<span class="built_in">end</span>())</span><br><span class="line">            &#123;</span><br><span class="line">                count+=m[v[i]] ;</span><br><span class="line">            &#125;</span><br><span class="line">            m[v[i]]++;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>每日练习</category>
      </categories>
  </entry>
  <entry>
    <title>【剑指office】数组中和为 0 的三个数</title>
    <url>/2022/09/03/%E3%80%90%E5%89%91%E6%8C%87office%E3%80%91%20%E6%95%B0%E7%BB%84%E4%B8%AD%E5%92%8C%E4%B8%BA%200%20%E7%9A%84%E4%B8%89%E4%B8%AA%E6%95%B0/</url>
    <content><![CDATA[<hr>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209031022123.png" alt="image-20220903102229044"></p>
<hr>
<p>首先，一看到这个，暴力肯定是不行的，因为这里要找的可不是一个两个数，是三个，三个就代表着，时间复杂度（N^3），就是十个数字都要找 1000 次，这里就不试了。</p>
<p>但是要用的办法其实也只能不那么暴力，采用排序+双指针的办法。</p>
<p>首先我们针对给过来的数组进行排序：</p>
<p><img src="https://knous.oss-cn-nanjing.aliyuncs.com/PicGo/202209031025288.png" alt="image-20220903102553292"></p>
<p>然后我们先找找第一个数字 -4，下一个数字要不等于 -4，那就在 -4 右边找一个 -1，最后一个数字不能等于 -1，那我们就不要再在 -1 右边找了，我们直接从数组最后找 2 。</p>
<p>为什么？</p>
<p>i （-4） 肯定是最小的，j （-1）是中间的，而 k（2）是最大的，这三个加起来我结果就会有三种情况。</p>
<p>sum &gt;0 ，说明这个值大了，那我们就让 K –，然后重新计算</p>
<p>sum&lt;0，说明这个值小了，那我们就让 J ++，然后计算。</p>
<p>为什么要动的是 J，K，且 J++呢？J 已经不能再小了，因为它再小就是 i ，条件说不能等于 i ，所以直接pass，当小于 0 的时候， K已经是最大值了，最大值怎么还能变大呢？ 那就只能让中间值继续 ++。</p>
<p>K 已经是最大的了，当结果大于的时候 J 已经是不能动了，那就只能动 K 了。</p>
<p>每一次确定 i ，j，k 组合成功之后，就要让 j，k分别移动，这次移动要确保 j，k 不能是已经选好的数字，它要重新找到两个新数字。</p>
<p>那每一个数字我们要找多少次？</p>
<p>每一个数字我们都要找完 i 右边的所有数字，J 会一直往右走，而 K 会一直往左走，当 J 不再小于 K 的时候，就说明已经找完了，后面再找也只是重复一遍。</p>
<p>为了确保这个大逻辑可以走下去，数组里面最起码要有三个元素，同时 i 不能到数组倒数第二或者第一个位置，那样的话 j，k 就无法选出来。</p>
<p>明白了逻辑之后，我们正式开始写。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">threeSum</span>(vector&lt;<span class="type">int</span>&gt;&amp; nums) &#123;</span><br><span class="line">        <span class="built_in">sort</span>(nums.<span class="built_in">begin</span>(),nums.<span class="built_in">end</span>());</span><br><span class="line"></span><br><span class="line">        vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; vv;</span><br><span class="line">        <span class="type">int</span> n = nums.<span class="built_in">size</span>();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>;i&lt;n<span class="number">-2</span>;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(i &gt; <span class="number">0</span> &amp;&amp;nums[i] == nums[i<span class="number">-1</span>])</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">            <span class="type">int</span> j = i+<span class="number">1</span>;</span><br><span class="line">            <span class="type">int</span> k = n<span class="number">-1</span>;</span><br><span class="line">            <span class="keyword">while</span>(j&lt;k)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="type">int</span> sum = nums[i]+nums[j]+nums[k];</span><br><span class="line">                    <span class="keyword">if</span>(sum == <span class="number">0</span>)</span><br><span class="line">                        &#123;</span><br><span class="line">                            vv.<span class="built_in">push_back</span>(&#123;nums[i],nums[j],nums[k]&#125;);</span><br><span class="line">                            <span class="keyword">while</span>(j&lt;k &amp;&amp; nums[j] == nums[j+<span class="number">1</span>])</span><br><span class="line">                            &#123;</span><br><span class="line">                                j++;</span><br><span class="line">                            &#125;</span><br><span class="line">                            j++;</span><br><span class="line">                            <span class="keyword">while</span>(j&lt;k &amp;&amp; nums[k] == nums[k<span class="number">-1</span>])</span><br><span class="line">                            &#123;</span><br><span class="line">                                k--;</span><br><span class="line">                            &#125;</span><br><span class="line">                            k--;</span><br><span class="line">                        &#125;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span>(sum &gt; <span class="number">0</span>)</span><br><span class="line">                        k--;</span><br><span class="line">                    <span class="keyword">else</span></span><br><span class="line">                        j++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> vv;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>别忘了 j，k 要跟上一次不一样，i 也是，当 i 跟上一次一样的时候我们就直接跳过这次查找，直到找出不一样的。</p>
]]></content>
      <categories>
        <category>每日练习</category>
      </categories>
  </entry>
</search>
